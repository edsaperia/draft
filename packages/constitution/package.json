{
  "name": "@draft/constitution",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json",
    "bundle": "node scripts/bundle.mjs",
    "golden:freeze": "tsx test/golden/freeze.ts"
  }
}
