# Plan — the fixes the convention found (nh2026, 2026-09-20)

Written 2026-09-21 for builders with none of the planning session's context. Deleted once every stage is folded and its questions are closed in `QUESTIONS.md`.

**Precedence.** SPEC wins over SURFACE, SURFACE over this plan, and the `QUESTIONS.md` block of each finding is the diagnosis of record: this plan **cites** those blocks and does not restate them. Where a block and this plan disagree about a file or a line, re-read the code; both were true when written and the tree moves.

**What happened.** The first real membership — twelve people, the Newspeak House convention — ran on docs.vote at `5929a6e` on 2026-09-20 and closed itself at 17:10. It worked, and it found defects in three ways: Ed's sightings in the room (Q1482–Q1486), the wrong-line hunt beside them (Q1487–Q1489, and Q1477 seen again), and the host's error log read the day after (Q1491–Q1493, and Q1477's first evidence). Q1490, the quorum scale, was ruled the same day and is built separately.

**The evidence is on this machine and nowhere in git**: `data/nh2026/errors.jsonl`, `log.jsonl`, `engine.jsonl` (gitignored; real people's words — never commit, never paste into a report). `node scripts/repro/nh2026-refusals.mjs` rebuilds all 98 versions of the text from them.

## Rules for every stage

1. **One builder at a time, in the main tree, stages in order.** Each stage leaves the tree green and is its own run of commits, one commit per finding, message in the house style (`git log --oneline -8`), via `git commit -F <file>`. **Never push** — a push to `main` is a deploy, and it is Ed's call. Never `git stash`.
2. **Files are CRLF.** Use the Edit tool; never `sed -i` or `perl -pi`. `git diff --ignore-cr-at-eol --stat` tells a real change from ending churn.
3. **Every fix lands with a named guard** that is red on the pre-fix tree: a unit test for `packages/`, a walk step or a `scripts/repro/` script promoted into a walk for the page. A fix with no guard is not done. Where a repro script already exists it is named below — make it assert, then make it green.
4. **Every string a member can read lives in `design/copy.js`** and passes `design/STYLE.md`. A copy change reddens `copy-check` until `npm run copy-freeze`; read the freeze's diff and confirm only intended strings moved.
5. **The seven gates before a stage is called green**: `npm test`, `npm run lint`, `npm run typecheck`, `npm run spec-check`, `npm run copy-check`, `npm run clock-check`, `npm run build`. A change to `cards.js` re-runs `design/tools/session-probe.js`; a change to the setup page re-runs `design/tools/setup-probe.js`; page changes run `npm run journey` against a dev server on a **fresh port and a fresh data dir**, killed by port afterwards.
6. **Nothing renders under a caret, a focused field or a press** (CLAUDE.md *Gotchas*: *Nothing rebuilds under a press*, *A data swap under a caret takes it*, *A drag is a press held down*). Several of these fixes live exactly there.
7. **A finding that turns out to need a decision stops and is reported**, with the readings as options. Do not implement a reading on spec.
8. Each stage's report: what changed by `file:line`, each guard and that it was red before, each gate's result with counts, walks that could not run and why, anything found on the way.

## Stage 0 — land Q1490 (done 2026-09-21)

The 👥 scale opens to 1–100% with two conditional sentences on the card; R-126 reversed as R-139; SPEC v0.139. Built in five commits ending `20d9c95`. Reviewed, the seven gates green (`febaeb0` cleared a lint red the repro scripts carried, `7bbf742` a test-runner heartbeat timeout in `memo-differential`); the builder left the probe references unfrozen on purpose, the session-probe carrying 43 rail diffs that predate it. **Stage 1 does not start until Stage 0 is committed**, because both touch `packages/engine-core` and `design/setup.js`.

## Stage 0b — two things owed before the page stages — DONE 2026-09-21

**Built 2026-09-21 evening; seven gates green.** (1) The bracket: `shareFloor` beside `shareCount` in `design/setup.js`, the module's own `adoptionFloor`, read by `shareTail` and `shareWords`; `shareCount` and `quorumNote` untouched. **Two sites went round the writers and were brought in**: the page's own constitution clause (`session-view.html`, `quorum:` in the rule table) and the spectator feed's rule sentence (`design/feed.js`), which was also the last site on the quotient-before-product rounding issue #24 cured. No golden moved. Guard: `npm run slider-walk`, red on the pre-fix page at *(1 of 2)* and *(1 of 12)*, and asserting the *minimum quorum is 2* sentence still stands beside *(2 of 12)*. Not touched: the topbar's *quorum n of m*, which reads the served floor live and the bare count only in the fixture. (2) The drift: **not environmental and not drift — a race on the page, seen twice** (2026-09-16 straight after a fresh freeze, 2026-09-21), exactly 43 each time: every charter rail entry one text line (24.6px) above its clause on one side, the clauses agreeing. The rail is re-laid after the font swap's reflow by `session.js` handing itself a resize on `loadingdone`, **which fires only when the last pending face is in**; Regular landing is what moves the text, so with Bold behind it the rail stood stale for the gap — 706ms with Bold held 700ms, 3.1s with Bold and Italic withheld. Cured on the page (each face's own `loaded` re-lays; `npm run rail-font-walk`, in CI's `probe` job, red before at *stale for 706ms*, 5ms after) and in the runner (`scripts/probe.mjs` waits for the rail at rest, not 250ms against a 220ms glide). **The probe's own flake did not reproduce** in fifty-four cold runs, quiet or under load, nor on the tree it was seen on, nor with the clock pinned across a day; the runner's comment says so. Nothing was re-frozen: the session-probe reads IDENTICAL, and the setup-probe's 19 are Q1490's and wait for Stage 6's `qa:freeze`. What follows is the stage as it was specified.

- **The 👥 bracket prints the real floor** (Ed, 2026-09-21 evening, Q1490's block: *print the real floor*). The `(x of y)` after a share is the floor the engine applies, `max(⌈n·E/100⌉, min(2, E))`, never the share's bare arithmetic — a 1% share in a membership of twelve reads *(2 of 12)*. One helper computes it, `shareCount` (`design/setup.js`, beside `SHARE`), and the same helper feeds `quorumNote`'s test for *The minimum quorum is 2…* — **so do not raise `shareCount` itself, which would switch that sentence off**: give `shareTail` and `shareWords` a floor figure of their own and leave the note reading the bare share. Every site that prints a settled share goes through those two writers (the constitution's clause, the strip's taken line, the composer's lane), so all of them follow. Moves the card-copy golden where a fixture's share comes to one; read the diff. Guard: a `slider-walk` or `card-audit` assertion that at E ≥ 2 no 👥 bracket reads *(1 of*.
- **Read the session-probe's drift before anything is re-frozen.** On 2026-09-21 `design/tools/session-probe.js` showed 43 rail differences, every right-hand rail entry 24–25 px low against `design/reference/`, **with no surface file changed since the last freeze (`88a1136`)** — so the cause is outside the code: the browser the probe drives, a font, the window, or the date. Run the probe, find which element above the rail's entries gained the height, and say which it is. If it is environmental, make the probe robust to it or re-freeze with the reason written in the commit; never let `qa:freeze` swallow it unread. The setup-probe's own 19 differences are Q1490's 👥 card and are intended.

## Stage 1 — server and engine, no ruling needed (the full lane) — DONE 2026-09-21

**Built as `2b8d76e` (Q1491), `03d6de0` (Q1482), `78ee689` (Q1488); reviewed; seven gates green (1,426 tests); `journey` and `npm run crlf-paste` green; the real nh2026 engine log replays to version 98 with its 15 CR lines intact and every one of them now takes a proposal.** What the build settled beyond the text below: (1) only a hunk's `lines` are normalised at the doors, **never `was` or `after`** — a claim about the document is whatever it is, SPEC §2.1's *the comparison is exact* stays true, and that is what lets a text already holding returns be proposed on; (2) the refusing check the page actually met was the server's own (`commands.ts` `attestedOf`), fixed beside `participant-api.ts`, both reading the new `linesAt(v)`; (3) Q1482's road was `uninvite` (and `resign`) as well as `remove`; (4) a record's *Previous text* is the whole field's span carried to one version, and **its place `at` is the winner's** — which also takes *This clause has changed again since* off records that had not. **For Ed, unanswered:** whether SPEC §2.1 gains the sentence *A line holds no line ending: a hunk's replacement lines are split at every line ending where the act enters, so the document is always an array of lines*; and that `normalizeLines` splits an embedded newline for a non-page client where the server's door refuses one. What follows is the stage as it was specified.

**1a. Q1491 — a line ending in a carriage return can never be proposed on again.** Highest priority in the plan: it silently destroys members' work and any paste from a Windows editor triggers it. Diagnosis: the Q1491 block. The rule to build, reading (a): **a carriage return never enters the text.**
- At the door: every hunk's `lines` (and `was`, and `after`) are normalised on submission — a trailing `\r` stripped, an embedded `\r\n` or lone `\r` inside one "line" refused or split the way `splitLines` (`packages/engine-core/src/text/diff.ts:22`) already does for a whole text. Do it once, at the engine's submit boundary, so the participant API, the bridge and the Founder's ✒️ road all pass through it. **Normalise at the command, never in the fold**: replay of an existing log must stay bit-identical (the golden-log tests are the assertion).
- One line array: `participant-api.ts:286` checks an attestation against `splitLines(this.session.document())` while `session.ts:1276`, `:1380`, `:1573` read `currentLines()`. Make them read the same thing, so the two checks cannot disagree again whatever a text holds.
- The page: the composer's paste path strips `\r` before it reaches a lane (find the paste handler in `design/composer.js` / `edit-mode.js`), so a draft never holds one.
- Guards: an engine test that submits a hunk whose lines end `\r`, adopts it, and then proposes on that line through the participant API (red today: *not what this proposal replaces*); a test that a log already holding CR lines still replays identically **and** that its CR lines can be proposed on (the one-array change is what cures an old text); a journey or repro step that pastes CRLF text into a lane and proposes twice on the result.
- No repair tool: nh2026 is closed and alpha documents are disposable (Ed, 2026-09-17).

**1b. Q1482 — a delegated question that has every answer and never resolves.** Diagnosis and cure in the block: `remove` (in `@draft/constitution`'s session) calls `afterRosterChange` only `if (wasInE)`, so withdrawing an unopened invitation lifts the hold and re-asks nobody. Add the missing `maybeResolveAll` on that road. Guard: a case in `begin.test.ts` — delegate 👥, invite two, have every arrived member answer, withdraw the unopened invitation, assert the question resolves and `readiness()` is ready.

**1c. Q1488 — a sealed record's *Previous text* off by a line after an early-closed rival.** Diagnosis and cure in the block (`packages/server/src/views.ts:492–496`): carry each field member's span to the record's version by `spanNow`, as `stillRacing` does a few lines above, or take the span and the displaced text from the winner alone. Guard: a server test beside `close-stranded.test.ts`; `scripts/repro/wrong-line-room.mjs`'s *record* case goes green.

## Stage 2 — two reproductions before any fix — DONE 2026-09-21

**Both reproduced, both cured, seven gates green (1,428 tests), `journey` green with a new *head insertion* step, `stale-key` and `focus-steal` green.** Built as `e8054820` (Q1477), `c3146c52` (Q1492) and `740c9279` (a hole in the two new repros' own assertions). What the build settled beyond the text below:

- **2a did not reproduce as the plan framed it, and the error log said why.** The head insertion is not the cause: `--case=insertion` drives all twelve variants the plan names and is green. The cause is the **cork**. `textVersion` reads 0 on both sides of 🍾 — over the founder's unversioned text before it, over the engine's document at version 0 after — and the slim view (2026-09-11) leaves the text out when the page's `tv` matches. So the first answer after 🍾 left the text out of every page that had polled through it, and each went on drawing the founder's text against the engine's line numbers until the first adoption moved the version. 🍾 confirms whatever the column holds (R-081) and the column draws no paragraph for a blank line, so nh2026's engine opened on seventy lines where every page held seventy-two, blanks at 2 and 4: `log.jsonl` carries `starting-text-confirmed` at 09:43 with 72 lines and again at 10:05:54 with 70, in the same millisecond as `constituted`. Every line beneath the blanks was two out, which is the five refusals exactly. Cured at the wire (`routes-member.ts`: a part is left out only for a page that has been served it under a version, and the page's engine seq is 0 until there is an engine) and caught on the page (`live.js`, `askFull`: a slim answer naming a part this page has never held is not completed from its own copy).
- **`standDown` could not have caught it**, which is a finding rather than an omission: `misaimed` compares the draft against the text the command is about to name a version of, and that text was itself the wrong one. The page held one self-consistent line space that was not the document's, and nothing in its possession told the two apart except whether it had ever been served a versioned text — so the page's half of the cure lives at the view seam.
- **2b's road was a selection dragged across an open card.** `selectedBlocks` reads the column's editable blocks and an open card takes the ones it covers out of the list, so the ends come back as one "run" spanning them; `startDraftFromRun` checked neither adjacency nor whether the draft already held those keys. Cured at the draft model, with `misaimed` refusing an overlapping press as the backstop and a `copy.js` sentence on the card. The plan's other two roads were already right.
- **For Ed, unanswered:** what a selection across an open card *should* do, beyond being refused with a sentence. Absorbing the covered site into the run would keep the gesture, but the run's text is built from the blocks the column could see and the site's own drafted words are not among them, so a merge would have to invent what the joined lane holds. Refusing loses no work and needs no ruling; it is a keystroke that does nothing but says why.

What follows is the stage as it was specified.

**2a. Q1477 — a proposal aimed two lines low.** Three hunts did not reproduce it; the error log now gives the recipe. Evidence (Q1491–Q1493 block, last sentences): at version 0, with **one rival's one-line insertion live at `[0, 0)`**, two members' proposals each attested wording **exactly two lines above** the span they aimed at (line 5 for line 3; line 70 for line 68 of 70); two of the five came up to sixteen seconds **after** the insertion was withdrawn. The server's R-136 guard refused all five; the page's own `standDown` (`LIVE_HOOKS.misaimed`) let all five through.
- Build `scripts/repro/head-insertion-aim.mjs` on a multi-seat dev server: seat A proposes a one-line insertion at the head of the text; seat B, with the page already open, then with a fresh load, then after A withdraws, begins a draft on line 3 and on the last line by click-and-type and by ✏️ *propose edit*, and presses Propose. Record the hunk sent (`start`, `end`, `was`) against the served text. The suspects, from reading: a live insertion standing at a gap site (`siteOfSpan`, M19) counted as blocks by `blocksOf`-keyed readers, so keys beneath it shift by the anchor and the line; and `followSites` re-keying against a column that still holds it after the withdrawal.
- When red: fix the aim at its source, **and** make `standDown` catch what the server caught — it compares against the served text and should have refused these on the page without spending the flight. Guard: the repro promoted to a `journey` step (*head insertion*).
- If it does not reproduce in a day's honest hunting, stop and report what was driven; do not guess a fix.

**2b. Q1492 — the composer sent a patch whose two sites overlap.** Evidence: `validateHunks: hunks 0 and 1 overlap: [73, 86) and [74, 80)` (base v19, 15:49) and `[85, 95) and [86, 95)` (base v33, 16:05), one member rewriting a long section each time; the args are in `errors.jsonl`, truncated at 2,000 characters, and `engine.jsonl` gives the text at both versions. Reproduce in `scripts/repro/overlapping-sites.mjs`: a draft site over a long run, then a second site begun inside or at the edge of the first (Enter inside a multi-line site; ✏️ *propose edit* on a card whose span the draft already covers; a site grown by `followSites` over its neighbour). The rule (SURFACE K14–K16): one site is a run of adjacent blocks and two sites never share a line — a second site inside the first **is** the first. Fix at the draft model (`design/composer.js`), and let the row's ✏️ refuse on the page rather than the server if it can still happen. Guard: the repro, asserting.

## Stage 3 — the page, plain defects (the surface lane, no ruling needed) — DONE 2026-09-22

**All seven built, one commit each; the seven gates green (1,428 tests, 97 files); `journey` green with two new *run edit* steps; `wrong-line-room` green on `shapes`, `patches` and `deadlock`; `rate-motion` green on chromium **and webkit**, sixteen scenarios; `drawer-walk`, `powers-walk`, `head-insertion-aim`, `overlapping-sites`, `stale-key` and `focus-steal` green; the session-probe reads IDENTICAL and the setup-probe's 19 👥 diffs are Q1490's, unmoved.** Commits: `c05d2b19` (3a, completing the unfinished `c42fcea4`), `649dea30` (3b), `24270b1b` (3c), `6e653646` (3d), `a5158f6a` (3e), `8286a093` (3f), `d49054c6` (3g). What the build settled beyond the text below:

- **3b was two defects, not one.** Reading the composers on `input` lights ✏️ as you type, and on its own that still took two presses: `change` fires on the blur the press itself causes and it *rendered*, so the very button the pencil's flight is aimed at was replaced under it and the flight's synthetic click landed on a node no longer in the document. Nothing renders there now (plan rule 6, exactly).
- **3a's cure is one move at the draft model**: the seeded wording goes onto the site and the origin is what the *document* holds — so `misaimed`, `followSites` and `hunksOf`'s own changed-place test are correct with no change at all in those three files.
- **Three walks carried the defect in their own second derivations** and could never have found what they were pointed at: `wrong-line-room`'s `readings` spliced hunks in the same wrong order as `applyIn`; its ⚔️ probe looked for an item keyed to one block, which is the symptom, so cured it silently stopped running; `member-rate-motion`'s `grant-cap` asserted the shape Ed has since ruled against.
- **`LIVE_HOOKS` is closed over inside `session-view.html` and no walk can reach it**, so journey's new steps read the hunk off the wire instead — which is the better reading anyway.
- **For Ed, unanswered: Q1496 and Q1497** — the Founder's refusal sentence, which is composed server-side and written into the engine's log rather than living in `copy.js`; and the lay-down on a valueless setting, whose lock is built and whose *road* no walk can reach.

What follows is the stage as it was specified.

- **3a. Q1483, both halves** — ✏️ *propose edit* on a rival's wording can never be proposed, and on a multi-line card it seeds the first line alone. Cure as the block states: the draft opens over the item's whole run (`addDraftRun` over `s.keys`), one origin per block, and a seeded origin keeps two wordings apart — what the diff is measured against, and what the document holds — with `misaimed` and `followSites` reading the second. Guard: the journey step the block specifies (✏️ on both lanes of a two-line run's card, each sent); `proposer-feedback.mjs --case=press/seeded` and `wrong-line-room.mjs --case=shapes` green.
- **3b. Q1486 (A) (B) (C) (G)** — the field composers (⏱️ 👥 💤 🪶 📍): read on `input` as well as `change` so ✏️ wakes while typing; `renderKeep`/`renderRestore` (band.js) carry every composer field and the caret across a poll's render, or the render is deferred while one is focused (the same `pressInFlight` family the polls already honour); `PROPOSE.rate` reads `d.val` and never the display phrase (Q1289's cure on ⏰); a validator's raw text never reaches a card — the field enforces its `min`/`max` and the refusal is a `copy.js` sentence. Guard: **a new walk that composes a settings motion with real key presses** on WebKit as well as Chromium if the toolchain has it (`design/REPORT-xbrowser.md` says what is installed), promoted from `scripts/repro/member-rate-motion.mjs`; the block notes that no walk does this today.
- **3c. Q1487** — the ⚔️ card headed by one line of a several-line race: a deadlocked item takes the race's span for its site and the head reads `runTextFor`. Guard: `wrong-line-room.mjs`'s *deadlock* case.
- **3d. Q1489** — a reader's view of a candidate holding an insertion and a replacement at one start: `applyIn` (live.js) orders `[k, k)` before `[k, k+1)`. Guard: `wrong-line-room.mjs`'s *reading* case.
- **3e. Q1484 (c)** — the ↻ stranded line is missing from the phone's task drawer: `session.js:1226–1227` lists `urgent · propose · weigh · unread` where the wide rail's list (`:1360`) has `stranded` too. Guard: `npm run drawer-walk` gains a stranded entry.
- **3f. Q1485 (D)** — the two accidents, whichever way Q1485 is ruled: the un-animated snap one round trip after *Submitted* (the id handed over before the refresh — live.js:190, :1900; session.js:5239), and a rule proposal's card redrawing as if untouched (`return render()` at session-view.html:7987 where its neighbours `closeThen`). Build only what both rulings share; leave the rest to Stage 4.
- **3g. Q1493's offered controls** — the Founder's lay-down on a setting with no value (SPEC §9.7: *a power can only be laid down once the setting is set*) is not offered until the setting has one; the four member-readable strings outside `copy.js` that Q1485's block lists (live.js:1456, :1576; session.js:4310; engine-bridge.ts:377) move into it.

## Stage 4 — the page, as Ed ruled it (2026-09-21, by the question tool, one at a time)

- **4a. Q1485 — a card closes at Propose, and says so** (Ed: *Close, and say so*; reverses his 2026-08-17 *one lifecycle, not two screens* for text proposals). Text and rule proposals alike: at the landing the card closes with the ordinary closing animation onto its clause (`closeThen`, never a bare `render()`), edit mode ends where no other draft site is left, and the proposer's rail entry reads one sentence for a few seconds — draft: *Proposed — the members are deciding* — before it settles to its one-line *yours* form. SURFACE K17–K18 and §9's *mine (proposed)* row are amended to say so and motions are brought to L5; the *Submitted* face goes. Moves the copy golden, both probe references and journey's propose steps; read each diff. Guard: journey asserts, for a text proposal and for a rule proposal, that the card is closed after the command lands, that the rail sentence appeared, and that no refused command was logged.
- **4b. Q1484 — a stranded proposal's entry is red** (Ed, in his own words: *make the card a colour that suggests something needs to be done (red?)*, then *Red entry, words unchanged*). The stranded rail entry (`↻`, SURFACE §6's *stranded* row, `.mk-stranded`) and the head of the card it opens take the surface's one red, `--slash`, in place of `--lc-yours`: the entry's ground as a wash of it, the mark painted it. **Words unchanged** — no two-line entry, no news card, no *yours* on records; those remedies were offered and not taken. SURFACE §6's row and the `lifecycle palette` rule gain the sentence: red means *this needs you because something went wrong*, beside its two existing uses (the strike on a laid-down power, the phone drawer's count). Check contrast of the entry's text on the red wash in `npm run a11y-audit`; colour is the only signal by Ed's choice, and REPORT-a11y's next pass should record that. Stage 3e's drawer fix carries the same red at 390. Guard: `card-audit` asserts the stranded entry's computed ground on the fixture; `drawer-walk` the same at 390. **Not ruled and still open in Q1484's block:** (e), the refund — SPEC §2.6/§7 read *returned whole either way* and the engine returns a stranded ✏️ only on 🗑️ and never at the close; that is spec and code disagreeing, recorded, not built here.
- **4c. Q1486 (D) — a member's ⏱️ proposal stays minutes only** (Ed: *Minutes only, as now*; Q1160 stands). Nothing to build.
- **4d. Q1486 (E), widened by Ed — an empty wallet darkens every ✏️, with a countdown** (Ed: *dark, with ✏️ hh:mm countdown (for proposals as well as rule changes, the same anywhere you would want to press the button but you have no ✏️s)*). Everywhere a press would spend a ✏️ — the proposal row's ✏️ over a draft, ✏️ on a rule proposal's composer, ✏️ *propose edit* is NOT one (it spends nothing until Propose) — while the wallet holds none the button is dark, nothing is sent, and beside it stands *✏️ hh:mm*, the time until the next one arrives, in the shape and with the machinery of the `abstain-countdown` (*💤 abstain in hh:mm*: one 1 s timer patching text in place, never a render; `syncProposeCtls` is the propose row's patch point). The deadline is the wallet's own next-drip moment as the view serves it; if the view does not serve it, add the field to the member's view rather than recomputing the drip on the page. At the cap, or where the rate gives no further ✏️, no countdown is drawn and the tooltip says why. The refusal *insufficient ✏️ for the stake* must become unreachable from the page; its sentence still moves into `copy.js` without the word *stake*. Guard: a journey step that spends the wallet dry and asserts the dark button, the countdown's text moving, no command sent on a press, and the button waking when the ✏️ arrives.
- **4e. Q1493 (a) — the page answers a race with the poll itself** (Ed: *The page handles both*). A `judge-race` refused because the candidate or race is no longer live, or the card is stale: the page files that card as closed, as the next poll would have, shows no refusal, and fetches a fresh view. A `propose-text` refused **only** for *patch targets version n; current is n + 1*: the page fetches the view, lets `followSites` re-key the draft, and where every site's origin wording still stands unchanged sends the proposal again by itself, once; where a site is `lost` the existing stranded handling takes over and nothing is re-sent. At most one automatic re-send per press. Both cases are tallied rather than logged as refusals: the server counts them (a `races` counter beside `errors` in `/healthz`, by kind) and keeps them out of the error log. Guard: a multi-seat repro that adopts in the gap between a reader's poll and their press, for each case, asserting no refusal is shown and the proposal lands.
- **4f. A withdrawn invitation mails its invitee** (Ed: *An email when withdrawn*). Withdrawing an invitation that was sent — before the start by the Founder's hand, after it by whichever route withdrew it — queues one mail to that address saying the invitation to ‹title› is no longer open; a new `MAILS` template, STYLE.md §6, through the outbox like every other mail, never to a reserved TLD, a bot address filed in the bot outbox as usual. The old link then lands on the ordinary stranger's door (no error, no sentence about the withdrawal): `/auth/login`'s *unknown member* for a seat that was withdrawn redirects to `/d/:slug`. Full lane. Guard: a server test that withdraws and reads the outbox, and one that follows the dead link and gets the door; `invite-walk` gains the step.

## Stage 5 — so the next room leaves a better record (Ed chose two of four, 2026-09-21)

- **5a. The error log is kept in Postgres beside the documents**, so a deploy or restart no longer deletes it; the file store keeps `errors.jsonl`; `draft-tools errors` reads either store; docs/OPERATING.md §11 rewritten to match. `MaintainablePersistence` is the seam. Guard: the persistence tests over both stores.
- **5b. The page reports its own uncaught errors and rejections** to the same log: `kind: 'page'`, the message, the source file and line, the seat, the build, the address's path — rate-limited per seat (a few a minute), capped in length, nothing else sent, no text a member typed. The `?debug=1` strip stays. `docs/legal/PRIVACY.md`'s draft gains the sentence. Guard: a server test for the route's limits, and a journey step that throws on purpose and reads the log through the dev route.
- *Not taken:* a demo-day runbook step (unneeded once 5a lands) and a keyed web address for reading the log (the SSH grant of 2026-09-21 stands in its place).

## Stage 6 — green, rehearsed, pushed

1. The seven gates; both probes; `journey`, `applicants-walk` (three prices), `after-begin-walk`, `invite-walk`, `first-keys-walk`, `member-questions-walk`, `slug-walk`, `ladder`, `powers-walk`, `seat-matrix --hat=both`, `room-walk` on a cooldown-0 server — sequentially, not in parallel.
2. `npm run qa:freeze`, its three diffs read.
3. A bot room on a dev server as rehearsal — including a bot that pastes CRLF text and one that inserts at the head of the text while others draft beneath it.
4. `README.md`'s spec version and counts; CLAUDE.md gains a one-line gotcha for each finding whose guard now exists (the eviction rule: the failure, and the guard that catches it), post-mortems to `design/DECISIONS.md`; each closed question's block leaves `QUESTIONS.md` for DECISIONS.
5. **The push is Ed's**, one full-lane deploy, with `data/errors.jsonl` copied off the host first if any room has run since.
6. Then a second supervised sitting on the fixed build. PRODUCTION.md stage 19's criterion needs two in a row with no finding of the class *a control did the wrong thing*; nh2026 produced several, so the count stands at zero.
