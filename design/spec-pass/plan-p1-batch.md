# The P1 batch — ruled by Ed on 2026-09-22, PRODUCTION.md stage 19's fix batch

**What this is.** The twenty open P1 GitHub issues Ed ruled *build* on the
evening of 2026-09-22, one question at a time, plus the three plain
accessibility defects he sent to the same batch earlier that evening
(Q1394, Q1395, Q1397 — PRODUCTION.md stage 13). The GitHub milestone
*P1 batch, ruled 2026-09-22* lists the issues; each carries a comment stating
his ruling and the date. **This file is the contract**: the order, the lanes,
what each item builds and what it does not, and the rules the build runs
under. It is deleted once the batch is folded (its own rule, as the
convention plan's was), its stage notes lifted into `design/DECISIONS.md`.

**Precedence.** SURFACE and SPEC win where an issue's body and a rule
disagree; the issue's *Not in scope* stands unless Ed's comment says
otherwise; CLAUDE.md's conventions bind every commit. Where an issue's line
numbers have moved (every one filed 2026-09-19 has), the file and symbol are
the citation, never the number.

**How it is built** (Ed's standing instructions; memory
`fable-plans-opus-builds`, `draft-convention-fix-plan`):

- One builder at a time in the main tree, briefed to read this plan,
  CLAUDE.md, the issue and the cited QUESTIONS blocks. Best available model.
- **Every item sees its guard red first**: the issue's *Reproduce first*
  driven on a scratch server (fresh port, `mktemp` data dir — never 8140,
  never docs.vote) or a new test, red on the pre-fix tree, green after.
- The seven `ci` gates after every item: `npm test`, lint, `npm run
  typecheck`, `spec-check`, `copy-check`, `clock-check`, build. Copy edits
  touch `design/copy.js` only and are frozen (`npm run copy-freeze`) as they
  land.
- **Nothing is pushed by the builder.** One paused full deploy at the end,
  by Ed or on his standing word, never during a live room, with the walk
  sweep, `qa:freeze` read, and a bot-room rehearsal before it (the
  convention batch's Stage 6 is the model; `design/DECISIONS.md`
  2026-09-22).
- Each issue's commit names the issue and the finding; the issue is closed
  by the commit that finishes it, with the guard named.
- A finding that turns out to need a ruling not in the issue's comment stops
  and files a question in QUESTIONS.md (claim the number in the file).

## Stage 0 — the docs that are wrong today

Already done in the same commit as this plan: OPERATING.md's per-address
login cap reads 10 (#86 F1). **#86 F2/F3 (the runbook) are Stage 5**, after
the batch, so the runbook lists only the workarounds still needed.

## Stage 1 — page only (a surface upload could carry these alone)

Order by blast radius; each is independent.

| # | Build | Guard | Not built |
|---|---|---|---|
| **#78** | `readLane` (cards.js) keeps the trailing empty block an Enter at the lane's end makes; the caret lands in it | a repro driver beside `scripts/repro/` and a `journey` step (*lane keys*) | charters already carrying a join |
| **#75** | (F1) the ⏰ answer card: the rung chooses first, the box appears, as the Founder's own ⏰ does — `session-view.html`'s `[data-ans]` date arm and `setup.js`'s `on` test; (F2) the `ans-*` `[data-ansnum]`/`[data-ansdate]` `change` branches take the in-place repaint the motion fields got in Q1486 (A) | `founding-walk --delegate=ending` answering *At a set time* on a member seat; a first-press assertion in `member-questions-walk` | — |
| **#80** | the live `settingState` adapter (live.js) carries `previousValue` and `setWhy`, so `news()`'s ordinary-change arm fires | `journey`: a Founder pen on ⏱️ after 🍾 serves every member a news card whose OK clears `owedOks` | — |
| **#34** | (F1) `setSetting` in live.js sends `why`; (F2) 🗑️ on 🍾 restores the struck power cells (`REVERTKEYS` / `beginRows`); (F3) `REVERTKEYS` carries `setWhy` | `journey`: the member's card reads the reason; a walk step pressing 🗑️ on 🍾 | reasons already lost |
| **#88** | `consentBody` passes the mover, as `ordinaryBody` does, so the mover's 🏛️ card draws the inert two blocks and 🗑️ | `journey`'s *a keep ends* row: the mover's card has no pressable lane | **finding 2** — no module refusal (Ed: a mover who changes their mind withdraws) |
| **#37** | (F1) `LIVE_HOOKS.judge` reads the answer: a refusal un-files the pair (`resolved.delete`) and re-asks, with the sentence; (F2) `withdraw` likewise puts the proposal and the ✏️ back; (F3) `api.cmd`'s chain gets a timeout (`AbortSignal.timeout`) that reports and releases the chain | a `journey` step with a refusing server seam; a unit on the chain | — |
| **#76** | Begin shows while a delegated question is collecting, refused with its reason from `readiness().holds` — **and its rail entry is a ⏳ Begin task listing the members whose answers are still due, by name and avatar** (Ed's shape). `founder-answers` re-asserted against the live page | `founder-answers` red on the pre-fix page; `journey -- --delegate-all` | — |
| **#32** | the band's `adm:` and `mo:` cards gain the awaiting-crown branch (Refuse / Accept, SURFACE E12) for the Founder; every other seat reads *waiting on the Founder*; the Founder's rail entry is live, a clerk Founder gets one too | `applicants-walk` at every price with 🛡️ kept; a `powers-walk` step for a carried motion | — |
| **Q1500** | the judgment cards — quick · insert · race · patch · diagonal · the ⏳ judged pair — draw no 🗑️ (Ed, 2026-09-22, reading (b)); a chosen radio is undone by another choice or Indifferent, the card closes by a click outside; the deadlock ⚔️ keeps its bin for the desk's draft (Q613, assumed). SURFACE C4 and §9.3's commit column amended in the same commit; `spec-check` reads §9.3 against the cards, so the row moves with the code | `card-audit`: no discard control on those kinds; the session-probe re-frozen by `qa:freeze` | the settings, answer and editing cards' bins |
| **Q1501 + Q1502** | the five grant cards (💡 ⚖️ ✒️ 🛡️ 🏛️): a wash of their own until accepted — the *yours* hue, since a grant is addressed to you (C8) — on card, tab and rail entry, grey after; the commit reads *Accept* + the power's glyph (T44 amended for grants: *Accept ✏️* · *Accept ⚖️* · *Accept ✒️* · *Accept 🛡️* — and **🏛️'s reads *Activate 🏛️***, to match its title, Ed 20:14); the unaccepted rail entry **sparkles** (Ed's word — a small, slow, repeating shimmer, with a still form under `prefers-reduced-motion`, and never on an accepted entry). The 🏛️ grant is titled **Activate your membership** and its body is about 🏛️ alone: what a 🏛️ is, that you are already a member, what accepting it opens — a body of its own in copy.js, `proposalsClause` staying the constitution's. SURFACE §6 (the wash rules), §7's table, §9's grants row and STYLE T44 amended in the same commit | `card-audit` on the five cards' wash and commit label; the session- and setup-probes re-frozen by `qa:freeze`; `copy-freeze`; a `journey` line reading *Activate 🏛️* on a member's seat; `a11y-audit`'s reduced-motion probe | body text saying what the OK gates (Ed did not choose it) |
| **Q1503** — **DONE 2026-09-22 evening, before the batch** (`hatCurrent`, band.js; the ladder's session rung and journey's *hat* line) | 🎩's marked radio derived from the module (`iDraft()`) whenever `settled(c)`, in `BODY.hat` and the commit row's `cur` alike, never from `S.seen`; every seat but the founder's gets the founder's own locked view (the settled two-block grammar, the standing sentence marked and greyed) instead of `readBody`'s *Set to* line, which has no `VALUE.hat` to print | `npm run ladder`'s session rung asserts 🎩's marked radio after the reload past 🍾; a `journey` line opening 🎩 on a member's seat; the setup-probe re-frozen | the member's lockline (noted in the block, Ed's call) |
| **a11y** | Q1394 the `<html lang>` element; Q1397 focus stays on the card after an act (open, commit, OK); Q1395 each judgment lane's radio carries a distinct accessible name (the wording, or *first / second wording*) | `npm run a11y-audit` — A16 and the lane rows green; the audit re-run and its report re-dated | everything else in `design/REPORT-a11y.md` (the stage) |
| **Q1505** — **DONE 2026-09-23, 7bbb0410** (ruled by Ed 09:50, built as its own row) | warn before anybody acts: the `offline` event, or two view polls in a row failed or unanswered (the poll gains a 4 s limit), draw a red bar along the top — *Reconnecting…* plus *Your device is offline.* / *docs.vote is not answering.* where the cause is known — every commit held with that reason, typed work kept, lifted by the first good poll; the pause keeps its modal, the stalled flag stays; a vote refused at the press says Y25's *That was refused: …* under its card. SURFACE C17 (new), Y25 and §6's red amended | `scripts/repro/reconnecting.mjs` (offline · 502 · hang · pause · refused) and `journey`'s *refused ¶* line | a probe of anything but the host; the birth page (no view poll) |

## Stage 2 — server and engine

| # | Build | Guard | Not built |
|---|---|---|---|
| **#89** | the three `/auth/*` doors count the request before the body can throw: `readTokenBody` in a try/catch falling through to `tooMany` with an empty token | `door.test.ts`: a malformed body on `/auth/login` is counted; the busy case unedited | the cap numbers (#93); log rotation (#70) |
| **#79** | (F1) `commit` (write-path.ts) undoes the in-memory apply — or refuses before applying — when `persist` throws, and the refusal says so; (F2) `stalled` marks the document on any persistent write failure, not 23505 alone | two `pause.test.ts`-shaped cases: a permanent and a transient failure, the room's view asserted after each | rewriting anything stored; a document already carrying a doubled candidate |
| **#35** | `/auth/login` after the close: a condition before the spend, landing on the closed page; `/auth/apply` on a refused application: catch → a page that says so; the mail arm | four tests in `server.test.ts` / `mail-give-up.test.ts` | — |
| **#33** | `refuseTaken` takes a blind flag; the applicant's refusal names no holder; the status check runs first | `faces.test.ts`: an applicant's refusal carries no name | — |
| **#38** | one accessor (`Stash.pendingOf`) read by all three `routes-auth.ts` handlers: a superseded create link refused, the claimed stash readable by the stale tab, 📨 on a claimed stash answering what happened, no twin; **then** F5's nullable `email` column, migration 7 | `server.test.ts` cases per finding; `slug-walk`'s resend half | — |
| **#65** | the engine's author preference counts only while the author is in E (`RacesHost` closure for membership); a whole-room lapse retires nothing; the bridge's two fallbacks deleted | three engine tests; `memo-differential` still green; a golden replay unchanged | — |
| **#87** | `settingFeed` retracts a `proposed` entry at `motion-held`, `motion-withdrawn` and the close's keep; `feed.js` scroll hold on `grew !== 0`; the `decreed` branch skips an equal value; the stalled sentence said about the document; `toEqual` on all three rules entry kinds | `spectator.test.ts`'s failed-motion case (the issue's repro), `feed.test.ts` | `candidate-retired` on the feed (ruled, #68) |
| **#29 + Q1498** | `relay` gains `application-refused` on all three roads with its `MAILS` template; the applicant's page reads the outcome; **and** a member removed by a carried 🥾 motion gets E31's tells (the mail among them) — Q1498's block | `mail-give-up.test.ts` / `server.test.ts`; `applicants-walk` at *proposal* through a refusal; `seat-matrix`'s E40 row | refusals from before the deploy |
| **#36** | the Join card on an open-door document (`joinOpen`, Q509 (a)); `STRS.sent` set from the answer, never before it, each refusal in its own words | `applicants-walk`: a join at 🪪 ✒️ 🤝 yes; a refused knock reading refused | — |
| **#67** | the interstitial waits for a press (a `Continue` button, the script gone); a cut link diagnosed as cut, the remedy naming what the mail carries; the two stranger doors get a send queue; a non-ASCII address refused at the gate | the walks that follow links (`scripts/lib/walk.mjs`'s `followLink`) press the button; `door.test.ts`; then **a real Gmail and Outlook test by Ed** | — |
| **#10** | `DRAFT_ADMIN_KEY` guards pause, resume and surface; `DRAFT_BOT_KEY` guards the bot outbox alone; CI carries the admin key; OPERATING §3, §10 and PRODUCTION's Q1310 row rewritten; **both keys rotated by Ed at the deploy** (the transition-push trap in the issue's body: set the new variable before the push that reads it) | `bots.test.ts` and `pause.test.ts`: each route refuses the other key; `verify-deploy` asks both | — |
| **Q1504** — **DONE 2026-09-23 00:18, before the batch** (31e04c37; `records-uncapped.test.ts`) | the fifty-record cap leaves `raceView` (`views.ts`, `.slice(-50)` on `records`): every resolved race's record is served, live and closed, the slim view's `recordsKey` doing the withholding it already does | a server test beside `slim-text.test.ts`: a closed document with sixty resolved races serves sixty records; red on the pre-fix tree at fifty | a cap of any size; any change to what a record holds |
| **Q1506** (ruled 2026-09-23, (b)) | the `/auth/create` claimed branch #38 F1 built (routes-auth.ts) refuses a creation link sent to an address the founder has since changed with a page of its own naming the cause — *This link was sent to an address the document's founder has since changed.* (Ed's words, *changed* not *corrected*, through STYLE.md) — in the server's own `PAGE` copy, not `design/copy.js`; a genuinely spent link keeps `PAGE.used` | a `server.test.ts` case beside #38's: the wrong-address link reads the sentence, a spent link reads `PAGE.used` | any other dead-link page; a sentence naming the founder or the document's title |
| **Q1508** (ruled 2026-09-23 ~10:02) | `strangerView` (views.ts) carries a closed document's `records` wherever `canRead` (🌍 link or public), sealed as a member's are — the `authorVisible` names rule, no judgments, no standings; none on a closed 🌍, nothing changed on a live document; the door's page (`hydrateFromModule`, door.js / live.js) draws them through the member path's own record drawing (`closedBlocks`), not a copy of it | a server test — closed at link, closed at public, closed at closed, live; a walk step (a signed-out page on a closed link-readable document shows ✔ tabs) | records on a live document's door; anything a record does not already hold |
| **Q1509** (ruled 2026-09-23 ~10:10) | (a) each feed entry names its author by reference, the pictures travelling once in the payload's `members`, `design/feed.js` resolving them; (b) `FEED_LIMIT` (200, routes-feed.ts) holds only while the document is live — once closed the whole history is served | `feed.test.ts`: no entry carries a picture; a closed document past 200 entries serves them all; a live one 200; `scripts/repro/feed-scroll-hold.mjs` still green; an nh2026-sized payload measured before and after | a cap on the closed feed; paging |
| **answer caret** (found by the P1 sweep's fixer, PR #97; Q1486 (B)'s class, no new ruling) | the band's render keeper (`KEEP_ATTRS`, band.js) holds the answer fields `[data-ansdate]` and `[data-ansnum]` as it holds the motion and founder fields, so a poll re-rendering under a member typing an answer keeps the caret and the half-typed value | a `member-questions-walk` step: a partial answer typed, a re-render forced (another seat's act landing), value and focus asserted — red on the pre-fix page | `[data-ansunit]` (a select, no caret) |
| **Q1511** (ruled 2026-09-23 ~13:55, (a)) | #38 F5's unclaimed branch of `/auth/create` (routes-auth.ts) — the typo's creation link followed after the correction but before anything is founded — reads Q1506's `PAGE.changed`, one cause, one sentence; a genuinely spent link keeps `PAGE.used` | `server.test.ts`'s *one pending creation, read one way*: F5's link reads the sentence (the case that pinned `PAGE.used` there), a spent link still `PAGE.used` | any other dead-link page |
| **Q1512** (ruled 2026-09-23 ~13:56, (d)) | the door's closed page draws **the Signatures and the Amendments** as a member's closed page does: `strangerView` (views.ts) carries what the member's closed `record` carries for those two sections — signatures under the record's own `authorVisible`/anonymity reading, Amendments naming the office — only on a closed, 🌍 link- or public-readable document; the door draws them through the member path's `closedBlocks` / `amendmentBlocks`, not a copy, with no member-only control | `stranger-records.test.ts`: both present at link and public, absent at closed, names following the anonymity rung; `scripts/repro/stranger-records.mjs`: a signed-out page shows the Signatures and the Amendments | anything on a live document's door; anything the record does not already hold |
| **Q1513** (ruled 2026-09-23 ~13:57, (b)) | while an answer's or a motion's date box has focus (`[data-ansdate]`, `[data-mdate]`) the 4 s poll does not re-render its card — the `pressInFlight` seam the slider drag uses, capped at 30 s so a forgotten focus cannot freeze the page; the number fields' caret keeper stays as built | a `member-questions-walk` step: half a date typed, another seat's act landing, the half-typed fields surviving and the next keys completing the date — red on the pre-fix page | patching the card around the box (reading (c)) |
| **Q1514** (ruled at the claim, confirmed by Ed 2026-09-23 14:50) | a rule's filed history reads *Changed by the Founder: ‹value›* for a change on the pen route (a Founder's ✒️ decree) where it read *Passed: ‹value›*; a carried motion keeps *Passed:* — the lead chosen where the history line is drawn, by the route band.js's news card already tells apart; the new lead in `design/copy.js` | `scripts/repro/member-rate-motion.mjs`'s empty-wallet scenario asserts the decree's history line reads *Changed by the Founder:* (red on the pre-fix page, which prints *Passed:*); `copy-check --walk` frozen with the diff read | the news card's own sentences (already right) |
| **Q1516 (1)–(5)** (ruled by Ed one question at a time, confirmed 2026-09-23 14:50) | (1) the floating 📝 (`edit-door`) sparkles until a member first presses it — Q1501's sparkle and its still form under reduced motion — the first press persisted per seat as a grant's acceptance is, so it survives a reload; (2) the 📝 `riding-tab` no longer pins to the view: it rests at the text's heading and in edit mode is still the strip; (3) the floating 📝 in its own corner, bottom-right, never in the proposal row's place; (4) about 1.5× the row's circles; (5) the section heading *Constitution* reads **Rules** on the surface — heading, contents rail, every member-readable mention, through `design/copy.js`; code names unchanged. SURFACE K13, K31, §9's 📝 row and §8's band heading amended in the same commit; CLAUDE.md's `edit-door` and `riding-tab` entries | `card-audit`'s D1–D3 hold the new corner and size at 1600 and at 390 (`--baseline`), and a check that nothing overlaps the proposal row (red first); a `journey` line reading the sparkle before the first press and its absence after, across a reload; `a11y-audit`'s A17; `npm run drawer-walk` at 390; `copy-freeze` and `qa:freeze` read | (6) the page break and paper on a desk (a mockup first, another builder) |
| **Q1517** (ruled by Ed 2026-09-23, one question at a time) — **DONE 2026-09-23** (guard `card-audit` R3) | a passed ✔ green, a held ✖ grey: the Fluent `CHECK` and `MULTIPLY` pictures (cards.js) lose their baked purple (`#785DC8`) and take their fill from the palette — the unfiled `adopted` ✔ in `--ok`, the unfiled `retired` ✖ in `--muted`, the shapes kept; filed marks stay grey as now (`.mk-filedYes`, `.mk-filedNo`). SURFACE §6's palette and mark tables amended in the same commit | `card-audit`: the drawn fill of an unfiled ✔ and ✖ read against the tokens (red first); `qa:freeze` read (the marks' colour alone moves); `copy-check --walk` | any other Fluent picture's colour |
| **Q1516 (6)** (ruled by Ed 2026-09-23 after the mockup, branch `paper-mockup` c69589b5) — **DONE 2026-09-23** (guards `a11y-audit` A18, `card-audit` R3 read in context) | the paper look for everyone: `paper.css` / `paper.js` folded into the shared files the page loads, the `?paper=1` switch gone, `QUEUE_WASH_K` the only strength, the Rules/Text break, the mockup's collapsed duplicate title and edit-mode sheet end; card-audit's `--query=` kept; `design/mockups/paper-2026-09-23/` left as the record; **and the yellow wire's contrast** (1.63:1 on white, 1.43:1 on the desk) brought to 3:1 while matching its entry's colour. SURFACE §6 and the column/rails text, CLAUDE.md's `desk` / `sheet` entries | `card-audit` at 1600 and 390 (`--baseline`) nothing new, the clicked tab still; `a11y-audit` no worse; `drawer-walk` at 390; `qa:freeze` read (column, rails, wash alone); the founding golden only if it moves; `journey` | anything in the live page that behaves unlike the mockup — reported, not improvised |
| **Q1520** (ruled by Ed at the claim, 2026-09-23 15:58) | each contents-rail mark (`tocMarksHtml`, session.js) is a control that opens its entry's card and travels to it by the rail entry's own path (`bringIntoView`), named by the entry's title and state, no longer `aria-hidden`; the `+n` tally goes to the section as the heading does | a `toc-travel` step clicking a mark: its card opens, its clause in view (red first); `a11y-audit` reading the marks as named controls; `journey` | — |

## Stage 3 — the convention's own residue

Q1494–Q1497 carry readings for Ed and are **not in this batch until ruled**;
Q1499 (the seat-matrix step for E42) is built if the count bump left the
step itself owed — read the block. Q1498 rides #29 above.

## Stage 4 — the sweep and the deploy

The convention batch's Stage 6, repeated: every walk in CI's `walks` and
`probe` jobs green locally against a scratch server; `qa:freeze` read, never
run blind; a twelve-bot rehearsal room that closes itself; README's counts;
CLAUDE.md gotchas for anything that earned one, each naming its guard;
DECISIONS.md takes this file's stage notes. Then Ed's push — one paused full
deploy — with `#10`'s key rotation sequenced as its body says.

## Stage 5 — after the deploy

#86 F2/F3: `docs/runbooks/demo-day.md` rewritten against what now ships —
delegation described, the workarounds that survive listed, the rest gone.
Then the second supervised sitting (PRODUCTION.md stage 19).

## Stage notes

(Appended by the builder as each stage lands: commit, what was found on the
way, what was left and why.)

- **#78** (Stage 1, 2026-09-23): `readLane` (cards.js) keeps an empty last block of two or more as the lane's final newline, each block's own trailing break taken off first; `sentText` (cards.js, new) drops that line again from what goes out — `hunksOf` (live.js) and `draftRowState` (composer.js) read it, so Enter alone at a lane's end neither sends a blank line nor counts as a change. Guards: `scripts/repro/lane-enter.mjs` (fixture, four cases: end · bare · middle · twice — red at end, bare and twice on the pre-fix page) and `journey`'s *lane keys* (red on the pre-fix `readLane`: one line, glued). Found: live.js's comment promised `SESSION.LIVE_HOOKS.hunksOf` to walks and no walk could reach it — `SESSION.hunksOf` now exists. Left: `focus-steal --lane` (needs a bot room); charters already carrying a join (not built, the plan's column).
- **#75** (Stage 1, 2026-09-23): (F1) the `[data-ans]` click branch records a date rung's press as `myAns.<key>Kind = 'date'` beside the answer (clearing a *Never*, keeping a date already given), `setup.js`'s ⏰ `on` test reads it, `cardKeys` names it so 🗑️ puts it back; (F2) the `[data-ansnum]` and `[data-ansdate]` `change` branches repaint rail and commit in place and never render, and the `input` listener gained the `[data-ansdate]` arm. Guards: `member-questions-walk` (⏱️ answered by one real press; ⏰ *At a set time* chosen by its press, box shown, answered with the date by one press — red on the pre-fix page at both, *0 answer(s) sent* and `chosen:false, box:false`), `founding-walk --delegate=ending` (red on the pre-fix page: 🍾 unreachable, the question unanswerable). Found: `founding-walk`'s verdict read *answers it* over a commit step logged *no commit control*; it now asks for a commit that pressed. `[data-ansunit]` left rendering: the plan names the two field branches, and a select's `change` is not a blur under a press.
- **#80** (Stage 1, 2026-09-23): the live `settingState` adapter (live.js) carries `previousValue` and `setWhy`, so `changedFrom` reads a real *from* and `news()`'s ordinary arm fires; `window.__founding()` reads out `changedFrom` per setting. Guards: `journey`'s *title news* (the Founder's ✒️ on 🪶 served to the member as news with its reason, one OK clearing `owedOks`, and a second change news again — red on the pre-fix `live.js` at *railed: false* with `owedOks ['title']`), and F2's pin in `change-record.test.ts` (a second ✒️ change after 🍾, the first OK given, is owed again — green on both trees, being the module's side). Found: the plan's guard names ⏱️, but journey promises ⏱️'s ✒️ away before 🍾 (`beginRowsBeforeStart`), so the step drives 🪶, the ordinary rule whose pen the Founder still holds there. The news card reads *No reason given.* for a reason typed on the Founder's card until #34 F1, next.
- **#34** (Stage 1, 2026-09-23): (F1) `setSetting` (live.js) sends `why`; (F2) `takeSnap` copies `S.beginRows` as `__begin`, `cardKeys` names `beginRows` on a card with `.pwtoggle[data-bkey]`, `revertSnap` restores a copy; (F3) `takeSnap` copies `S.setWhy` as `__why`, `revertSnap` restores the open card's reason alone; `openCardDirty` compares both by content. Guards: `powers-walk`'s *⏰ reason* (✒️ with a typed reason, `setWhy` read off the view — red on the pre-fix `live.js` at `setWhy: null`), `journey`'s *begin bin* (two struck cells, 🗑️, 🍾 reopened as it opened, twice — red on the pre-fix page at both rounds). **Found on the way**: the band's render keeper (`renderKeep`/`renderRestore`, band.js) held `input` fields only, so a 4 s poll landing while the Founder typed a reason took the caret and ✒️ sent half the sentence (`setWhy: "The co"` on the first run); it now holds the `[data-setwhy]` lane by character offset — Q1486 (B)'s *every field on the card*, no new ruling — and the powers-walk step lets one poll land mid-reason on purpose (red without the band fix at `"The cohort"`). The member's news card reading the reason is covered by `journey`'s *title news* (set at the wire with a reason) plus this step (the Founder's card carrying it); no single walk drives both halves.
- **#88** (Stage 1, 2026-09-23): `consentBody` passes the mover (`moverOf`, session-view.html) as `ordinaryBody` does, so the mover's 🏛️ card draws `motionBlocks`' inert two blocks; band.js's two consent branches (a setting's motion and a power's) drop the 🏛️ *Give your answer* commit for the mover, leaving 🗑️ *Withdraw it* alone — the issue's *Done when*, and the ordinary route's row. Guard: `journey`'s *mover's 🏛️* line beside *a keep ends* (red on the pre-fix page: three lanes, three blocks, the 🏛️ commit drawn). Finding 2 not built (Ed); `motions.test.ts` pins the module taking the mover's own keep. `copy-freeze`: `card-copy.golden.json` moves on the fixture's `mo:mo-4` alone (the Founder's own 🏛️ motion — three options and the 🏛️ commit gone, *Proposed* marked), read before freezing. `card-audit --walk=settled` identical before and after (86 S1 sightings, no P-rule).
- **#37** (Stage 1, 2026-09-23): (F1) `LIVE_HOOKS.judge` reads the answer — a refusal or no answer, not a pair that closed under the press (Q1493 (a)'s `raceGone`), calls the new `SESSION.unjudge(id, what)` (session.js), which takes the pair out of `resolved`/`verdicts`/`picked`/`committed` only if the verdict on record is the one sent, and the entry asks again; `pendingJudge` holds a pair between press and filing so a double press sends one judgment; (F2) `LIVE_HOOKS.withdraw` on a refusal re-syncs the wallet and rebuilds the items from the view; (F3) `api.cmd` aborts the command after `CMD_TIMEOUT_MS` (20 s, `window.__cmdTimeoutMs` shortens it for a walk) and waits for the refresh no longer than that. Guards: `journey`'s *refused* line in the one-pair block (a 500 on `judge-race`, two polls, the entry asking — red on the pre-fix page at *deciding*), `scripts/repro/refused-acts.mjs` (judge · double · withdraw · hang, all four red on the pre-fix page). **Filed Q1505**: the plan's *with the sentence* against the issue's *Not in scope* — the refusal prints only the stagehand's `#errline`, no sentence on the judgment card; readings for Ed, (b) recommended. Not added to CI: `refused-acts` is a repro beside the others.
- **#76** (Stage 1, 2026-09-23): `beginCollecting()` and `dueMembers()` (begin.js) — every `readiness()` hold `collecting` and the founder's own `answered >= owed` — make `beginOffered` true, `mustAct` false and `waiting` true for 🍾, so it is a ⏳ entry; `summary` (session-view.html) draws each due member's face and name in it (`.qdues`/`.qdue`, system.css, the rail's 18px); the card opens refused with its existing readout. SURFACE F5 and §8's `ORDER` row 23 amended in the same commit (Ed's shape, the issue's comment). Guard: `journey -- --delegate-all`'s *who is due* (the invitee arrives, 🍾 must be `st-wait` naming them with a face — red on the pre-fix page: no 🍾 in the rail at all). **Found**: the live/fixture split was not the collecting state itself but ✋ 🖼️ — served from the save, never blocking (Q980) — counting as *something else being served*, so F18's last-resort door never opened for a live founder with no name; the fixture's founder has one. `founder-answers` stays the fixture's one-voice walk and its header no longer claims the fixture and the live path are identical, naming *who is due* as the live assertion (the issue's F2, its second reading); it cannot be red on the pre-fix page, its state being one-voice. No new copy: the names are data.
- **#32** (Stage 1, 2026-09-23): **already built** as Q1475 (77766042, 2026-09-19 — the admit branch's awaiting-crown card with `crownPairRow()` and E36's sentence for other seats, `crownAsksMe` in `mustAct`/`waiting` so the Founder's `adm:` and `mo:` entries ask, member or clerk). Driven on the current tree: the issue's repro inverts (founder `st-ask` with Refuse/Accept, m2 `st-wait` with nothing to press and *Awaiting assent from the Founder*, the clerk Founder's `mo:` entry `st-ask`). Guard added: `scripts/repro/crown-rail.mjs` (admission at ✏️ · member · clerk) — red with Q1475's two page halves stubbed out, at all four cases with the issue's own symptoms; `applicants-walk` green at all three prices and `invite-walk` green. **Deviation**: the plan named a `powers-walk` step for a carried motion; powers-walk seats the founder alone, so the carried-motion cases live in `crown-rail.mjs` instead. No code change.
- **Q1500** (Stage 1, 2026-09-23): `commitBarHtml` (cards.js) draws no 🗑️ — a `.binslot` holds the left of the row so the commit stays right — so quick · insert · race · ⏳ judged pair · diagonal and the patch's floating row carry none; the ⚔️ deadlock card (its own desk row), the Text's 👑 card and the park keep theirs. `grammar.commit.bin` left copy.js, unread. SURFACE C4, CP7, §9's five judgment rows (left and closes columns), the patch row's two mentions and CLAUDE.md's `proposal-row` entry amended in the same commit. Guard: `card-audit`'s CP7 taught the judgment card (`judgment` in the payload; *no 🗑️ on a judgment card*) — red on the pre-fix page at 26 charter cards, green after, at 1600 and at 390 against its baseline, no other rule moved at either width. `qa:freeze` run and read: the session-probe's 40 diffs were the commit row's HTML alone (no geometry), the copy-source golden lost the one string, the card-copy golden's charter feet and hints shifted by the bin; the reference copy also caught up with this batch's earlier page edits, whose probes had read IDENTICAL. `journey` green.
- **Q1501 + Q1502** (Stage 1, 2026-09-23): setup.js's `hueFor`/`grantCard` — the five (💡 ⚖️ ✒️ 🛡️ 🏛️) in `news` take the *yours* hue on the tab, the rail entry and the card's strip, grey once accepted; the unaccepted rail entry draws a `.sparkle` span (system.css `sparkleSweep`, 3.2 s, its phase written from the clock so a rebuilt rail carries on; under reduced motion a still inset ring in the yours hue); band.js's commit reads `COPY.page.gate.accept(glyph)` — *Accept ✏️* on 💡 — and 🏛️'s `gate.voice.accept`, *Activate 🏛️*, the accepted close staying *OK*; 🏛️ titled *Activate Your Membership* (Title Case, T1) with a body of its own, `gate.voice.why`, and no head (`headFor` null, like ✒️ 🛡️). SURFACE §6's setup-alphabet `news` row, §7.2's grant row, §9's gates and grants rows, E4 and E8's ask and close cells, and STYLE T18, T44 (amended for grants) and T45 (the 🏛️ body its one exception) amended in the same commit. Guards: `card-audit` GA1 (an unaccepted grant's commit word and tab hue — red on the pre-fix page at six sightings, *OK* and `changed`, green after; the unaccepted 💡 ⚖️ are reached by no card-audit walk, the founder's gates being self-acked), `journey`'s *accept* lines (the founder's ✒️ 🛡️ 🏛️), *activate* (the member's 🏛️), *gate hue* and *⚖️ accept* (the member's 💡 ⚖️), and `a11y-audit`'s new A17 (nothing repeats by itself under reduced motion; the sparkle's still form probed from the stylesheet — red with the still form taken out). **Found**: the first body ran 366 characters and card-audit's H4 holds a `.why` to 200 (Ed's budget), so it is one sentence of what 🏛️ is and one of what activating opens, 197 characters. The 🏛️ card's geometry moved with its head (17 px up, the paragraph taller) to match ✒️ 🛡️'s — the clicked tab does not move (no P rule in card-audit at either walk). `qa:freeze` read: the setup-probe's founding and motions diffs were the grant cards' HTML and the 🏛️ card's box; the copy goldens took the three new strings and the title.
- **a11y — Q1394, Q1397, Q1395** (Stage 1, 2026-09-23): **Q1394** already built (2026-09-18, `<html lang="en">`); A15 finds nothing. **Q1395** already built as Q1395 (a) (2026-09-18: lanes `role="radio"` in a `radiogroup`, named by the wording beside them) — Chrome's own tree reads two distinct names and *Indifferent*; **the audit's A6 was wrong twice** — names compared cut at sixty characters (rival wordings share that much) and read off `textContent`, which carries the hidden half of *Prefer this* / *Preferred* — corrected (`shownText`, whole-name comparison); A6 green, and red again (twenty cards, *Prefer this ×2*) with the lanes' `aria-labelledby` taken out. **Q1397** built: session.js's `focusOpenedCard` (C5 — the chosen lane, else the first; a record its strip tab), `focusTabOf` after a judgment's commit (the pile's front tab where the card's own is filed behind), and `focusKeep`/`focusRestore` round `renderAll`; session-view.html's `closeThen` hands a band card's keyboard press to its tab (`:focus-visible` on a button or radio only — with a text field included the setup-probe moved 1–2 px at thirty founding steps after a typed commit; the cause of that shift, a focused band tab, is **unexplained** and noted here). Guard: `a11y-audit` A16 (three opens and three commits with Enter — red on the pre-fix `session.js` at six sightings, *focus is on body*), A6 and the new A17 green; full run 23 distinct hand findings (670 sightings) against the first audit's 42 — the rest is stage 13's. **Not done**: the plan's *report re-dated* — the new dated report was refused as a report file written by a builder, so its content is these notes and the final report; the band hand-back is not driven by any walk (A16 drives the charter alone). Probes IDENTICAL, card-audit unchanged (534 before and after), `journey`, `first-keys-walk`, `focus-steal --gap`, `lane-enter`, `heading-marker`, `copy-check --walk` green.
- **Q1505** (Stage 1, 2026-09-23, 7bbb0410, ruled by Ed 09:50 — *warn before anybody acts, never after*): live.js `wire()` gains `CONN` beside `HOST`. `refresh()` aborts a view after `POLL_TIMEOUT_MS` (4 s; `window.__pollTimeoutMs` for a walk) and reads the status before the body — `noteReach('ok' | 'server' | 'none')`: a 2xx or any 4xx, and a 503 carrying the pause, are the host answering; any other 5xx (a bare 503 included) is *server*; an abort or a dropped fetch is *none*, *offline* only where `navigator.onLine` says so. Two failures in a row, or the `offline` event at once, draw `#reconnectbar` (role `status`, polite, fixed flush under the measured `.navbar`, full width, `--slash`, no motion) — *Reconnecting…* plus `COPY.page.host.offline` / `.notAnswering`, nothing where the cause is ambiguous; `online` drops the offline clause and polls at once; the first answered poll lifts it. Held while it stands: every `.btn-approve`, `.btn-propose` but 📝's `edit-door`, `[data-confirm]`, `[data-putmotion]`, `[data-holdmotion]`, `[data-withdrawmotion]`, `draft-withdraw`, `draft-remake`, `crown-refuse` — `aria-disabled` and the tooltip `host.held` set by a sweep that a `MutationObserver` (live only while the bar stands) re-runs on every render, greyed by `html.reconnecting [data-held]`, and their `pointerdown` / `mousedown` / `touchstart` / `click` / Enter-or-Space swallowed in the window's capture phase; released and the titles given back at the lift. The pause stands in front (`renderConn` hides the bar while `HOST.paused`); the stalled flag untouched. The charter half of Y25: `SESSION.unjudge(id, what, said)` stores the sentence (`refusedSay`), draws it as `.foot.refusal` above the commit row on the four judgment card sites, re-opens the card once its press's own close has run (unless another charter card is open), and `choose` retires it; `LIVE_HOOKS.judge` passes `env.refusedSentence(error)` (`PAGE_COPY.refused(plainRefusal(…))`). SURFACE C17 (new), Y25 and §6's red amended; CLAUDE.md's `reconnecting-bar`; four strings frozen. Guards: **`scripts/repro/reconnecting.mjs`** (new; offline · 502 · hang · pause · refused) — red on the pre-fix page at the offline bar, the held ✓, the held ✏️, the 502 bar, the hang bar and both refused lines (`pause` a pin, green on both); green after (bar at once offline, ≈6.7 s on two 502s, ≈10.7 s on a hang, the cut landing anywhere in a 4 s tick); **journey's *refused ¶*** line (the #37 step now finds its card re-opened and reads the sentence). Not wired into CI (`.github/`, `ci-walks.sh` left to the lead). Probes IDENTICAL, card-audit unchanged at 1600 and at 390 against its baseline (six findings, 535 sightings, as HEAD), a11y hand findings unchanged (23, 670), `journey` and `refused-acts` green.
- **#89** (Stage 2, 2026-09-23, 13dd740b): the three `/auth/*` doors read the token through `tokenOrEmpty` (routes-auth.ts), so a body `readTokenBody` cannot read — malformed JSON, past 10 KB — is an empty token, counted by `auth:<ip>` and answered by the spent-link page below the brake, never thrown to the error log. Guard: `door.test.ts`'s *counts a malformed or oversized body at all three doors* (the bucket filled to one short straight through the limiter; the unreadable request must fill it and the good arrival after it be the 429 — red on the pre-fix tree at `/auth/create · after malformed json`, 410 for 429); the busy case unedited and green. Left: the cap numbers (#93), log rotation (#70).
- **#79** (Stage 2, 2026-09-23, e373395a): (F1) a save the store refuses no longer leaves the command standing — `commit` (write-path.ts) rewinds the document to what the store holds (`DocStore.rewind`: the persisted prefix of the log re-folded, as a boot reads it; `rewindEngine` in engine-host.ts: the bridge resumed from the written prefix of the engine log and the bridge state as last written — nothing read back from a failing store) and throws `NotSavedError`, whose sentence reaches the wire (*that could not be saved, so nothing changed — please try again in a moment*, 500, counted by the store's own code in `/healthz`); a command applied on top while the failing save was in flight is refused too (`commit` compares the session it was applied to). (F2) `stalled` on **any** failed save, the 23505 reason kept in the comment. Guards: `pause.test.ts`'s *a save that fails* — the issue's case A (Cy dealt no failed proposal, the flag flying: red on the pre-fix tree at *Cy is not dealt a proposal that failed*, one race where none), case B (red at *nothing of Bo stands*, the proposal flushed by the next commit), and a queued case (red with the identity check taken out, 200 for 500). **Found**: a rewound document re-stamped presence on every 4 s poll, so each view would fail, re-fold the whole log and answer 500 — the one place the red flag is read; a stalled document's view no longer stamps presence (routes-member.ts). OPERATING §11 and CLAUDE.md's `stalled` entry rewritten. The test store's failure carries EACCES: a codeless throw reads as a 400 refusal on the pre-fix tree. Left: a save whose document log landed and whose engine write failed keeps the document half (#28's residue, as a boot); the pg suite was not run here (no local database).
- **#35** (Stage 2, 2026-09-23, 7a100c97): (F1) the login door's one `noSeat` condition (routes-auth.ts) — a removed seat, or an invitation never followed on a closed document — commits and redirects to the document seatless, never reaching `arrive`; `memberReturn` no longer writes into a closed log. (F2, F3) `/auth/apply`'s three module calls in one try/catch, `commit` outside it: a refusal is `spentPage`'s 410 led by Y25's sentence (`refusedSentence`, the `(§…)` dropped) with a `refused` row naming the document and slug. (F4) the closing mail carries a login link and its token hash to every arrived member and the convenor (by the convenor's own record), the bare address to an invitation never followed; write-path.ts's exile comment re-pointed. Guards: `server.test.ts`'s clock-closes case (Dee invited before 🍾, followed after the close — red on the pre-fix tree at 400 for 302; the closing links — red at *ada: expected …/d/… to contain /auth/login?token=*) and *review #1 hardening*'s kept link after `uninvite` (green on both trees since Q1493, pinned); `door.test.ts`'s *the apply door refuses with a page* (two knocks — red at 400 for 410; a door shut before the link — red the same). **Found**: Q1493 (4cde23ab, live) redirected a **clerk** founder's own login link seatless, a clerk having no row in `memberRecords`; fixed in the same condition, guard *a clerk founder’s login link seats them* (red on the pre-fix tree: no cookie). Server copy stays where the tree keeps it (`PAGE` beside the route, `MAILS` in mailer.ts, both headed as the server's own copy convention) — not `design/copy.js`, which the server never loads. Walks: `ladder`, `applicants-walk` at proposal · assembly · pen, `journey` green. Left: seating the second apply link (Ed's, not in scope).
- **#33** (Stage 2, 2026-09-23, 1ff1de88): `refuseTaken` (commands.ts) takes a `blind` flag, passed at the applicant's `submit-application` alone — *Taken — Somebody got there first.*, `faceTakenBy`'s own no-name word, so no new string; a member's `set-identity` still names the holder. The status is asked first (the plan's row, beyond the issue's *Not in scope*): an application not `verified` meets the module's own refusal before any face is compared. Guard: `faces.test.ts`'s *an applicant meets a taken face without the holder's name* (the issue's test, red on the pre-fix tree at *got 'Taken — Bo got there first.'*, plus a submitted applicant probing a taken face and reading the status refusal); the member case unedited and green. `applicants-walk` at proposal and assembly green. Left: greying taken faces in the applicant's grid (Y28, Ed's), rate-limiting `/cmd` for an applicant seat (not in scope).
- **#38** (Stage 2, 2026-09-23, 3d22c807 for F1–F4 and c5fcf87c for F5 — the issue's two deploys): `Stash.pendingOf` is the one read of a pending creation, `claimedBy` delegating. (F1) a creation link into a claimed creation whose founder's address is not the link's is refused as a used link; (F2) the founding reads the address the creation now reserves, not its token's; (F3) a send carrying a claimed creation's `pendingId` answers `{created, slug}`, no token, no mail, and `sendVerify` sets `S.birthMade`; (F4) the stash update on a claimed creation answers 409 `{created, slug}`, `birthBoot`'s listener reads it and stops, and the 📧 clause reads the issue's pinned sentence (`COPY.page.birthMade`, frozen); (F5) `StashRecord.email`, written by `open`, overwritten by `renew`, refusing a link to an address the founder has since corrected — **migration 7** (`stashes.email`, nullable; the issue said 6, which the error log took on 2026-09-22), in `putStash`, `getStash`, `dumpStashes`; the cutover runbook's table caught up (it read five). Guards: `server.test.ts`'s *one pending creation, read one way* (red on the pre-fix tree at F2's abandoned address, and at F5's typo-first link founding the document, 302 for 410), `unit.test.ts`'s *pendingOf*, `pg.test.ts`'s stash round-trip, and **`slug-walk`'s new third half** — the birth tab left open types and presses 📨 (red on the pre-fix page: the 404 and 409 at the wire, 📍 re-opened, no sentence). **Filed Q1506**: the issue's *Decision for Ed* — the wrong-address link's sentence — which the ruling left unanswered; built with the issue's own `PAGE.used`, readings for Ed. **The pg suite was run** against a throwaway `postgres:16-alpine` container on 55437 (`pg.test.ts` 19/19, `DRAFT_TEST_STORE=pg` server suite 216/216) — which covers #79's pg half too. Probes IDENTICAL, `founding-walk`, `journey`, `slug-walk` green. Left: stashes opened before migration 7 cannot be asked for seven days (the issue's *Rollout*); the page half has no walk beyond `slug-walk`'s.
- **#65** (Stage 2, 2026-09-23, ef6c257f): (F1) the engine host's `suspended` closure (engine-core session.ts, read by races.ts's floor count alone) reads *removed* as well as *lapsed*, so a departed author's derived preference stops counting — text races and motion races alike; (F2) `dominations` (races.ts) returns nothing while E is empty, and **SPEC §4.4 gains the clause** *nothing is dominated while E is empty* (v0.140, R-140 in SPEC-REASONING — Ed's *build, all three* taken as the sign-off the issue's *Decision for Ed* asked for; README's version line follows); (F3) the register (session-view.html `syncIdentity` and the `syncFromCs` push) invents no name from the address, so a member who chose none reads the same on every seat. **The plan's *the bridge's two fallbacks deleted*** has no referent in engine-bridge.ts; it can only mean the issue's *two deleted fallbacks*, finding 3's two page lines, and that is what was built. Guards: `approval-floor.test.ts` — a removed author (red on the pre-fix tree at 1 for 0), a whole-room lapse (red: one `{by: incumbent}` domination) and a removed mover's motion (the issue's *Verify*, red at 1 for 0); `memo-differential` and the golden log green unchanged; F3's guard is the issue's own grep (two hits before, none after). **A resignation can now retire its author's own live proposal** (§4.4 working, as the issue's *Rollout* says). Walks: `journey` green; **`seat-matrix --hat=both` was stopped by the system for low memory** partway through the member hat (no failure in the log to that point) — not re-run, owed. README's test counts left to Stage 4's sweep.
- **#87** (Stage 2, 2026-09-23, 3b751d20): (F1) `settingFeed` (constitution spectator.ts) takes a motion's own `proposed` entry back at `motion-held`, `motion-withdrawn` and `motion-kept-at-close` — the three the plan names; an ordinary motion the engine holds (`motion-adjudicated` held) is left, being the rules twin of the text's dominated and undecided candidates #68 ruled left as built; (F2) `feed.js`'s scroll hold on `grew !== 0`; (F3) the `decreed` branch skips a ✒️ that restates the standing value (`eqValue`); (F4) the feed's own stalled sentence, `COPY.feed.stalled` — *This document cannot save changes at the moment, so nothing here will change until it can.* (said about the document; true since #79 rewinds a failed save), frozen; (F5) `toEqual` on the `adopted` and `decreed` entries. Guards: `spectator.test.ts` — a vote against, a withdrawal, the close's keep, the restated ✒️ (all four red on the pre-fix tree) and the two key-set pins (green on both, being pins); **`scripts/repro/feed-scroll-hold.mjs`** (new, no server: `feed.html` served from disk with a fabricated feed) — red on the pre-fix `feed.js` at *an entry leaving above does not move the reader (370 → 131)*. Gates run with #29's uncommitted tests set aside. Left: `copy-check's` key guard still reads `session.js` alone, so `feed.js`'s `T.stalled` is unguarded (#94).
- **#29 + Q1498** (Stage 2, 2026-09-23, dda66a5c): (F1) `relay` (write-path.ts) gains the `application-refused` arm — one event for every road, so one template, `MAILS.applicationRefused` (mailer.ts: names no actor, no reason, no login, the document's address); (Q1498, reading (a)) the removal arm mails every removal but a resignation, a carried one in `MAILS.removedByMotion`'s words naming the members (E40); (F2) `hydrateApplicant` (door.js) reads `admitted` beside Q1473's `refused`, and band.js's 🪪 title, rail teaser and body say it (`appcards.admitted`, `admittedTitle`); (F3) the body's sentence by 🪪's price, `appcards.why` — 🏛️ at *assembly* (*only if every member agrees*), the ✏️ line at *proposal*, a free join at *pen* before submitting and nothing once submitted — all frozen. Guards: `server.test.ts's` *a refusal and a carried removal are mailed* — a 🏛️ vote against, the close at ✏️, and a carried 🥾 (all three red on the pre-fix tree: no mail); `applicants-walk` — the card names its route at its price, and at *assembly* the admitted applicant's own page reads *admitted* (both red on the pre-fix page), green at all three prices. **Not built**: the plan's *applicants-walk at proposal through a refusal* (the walk's room admits; the refused card is Q1473's, guarded by `scripts/repro/admit-keep.mjs`, and the refusal mail by the server test); **`seat-matrix's` E40 row not run** — the matrix was stopped for low memory during #65 and is not to be restarted unasked. Server copy (`MAILS`) stays in mailer.ts, the tree's convention. Left, per the issue: refusals from before the deploy; an admission the close keeps at 🏛️ (a module change); C15's row (#19).
- **#36** (Stage 2, 2026-09-23, 96b32374): (F1) `STRCARDS` (door.js) offers the 🪪 card wherever `applyOpen || joinOpen`, titled *Join* with its own sentence and sent line where the door is open and free (`COPY.page.strjoin`, three strings, frozen), the key kept `strapply` so it posts the same knock and `/auth/apply` admits; the Q509 *not built* comment gone. (F2) the door's 📧 posts first and marks *sent* only on a body carrying `ok`; anything else — a refusal, a 429, a host answering HTML, no answer — is said under the card (`setDoorErr` in Y25's *That was refused*) and on the stagehand's line (`showErrLine`), the four helpers handed into `DOOR.make`; the fixture keeps its synchronous flip, so the probes are IDENTICAL. The door posts by `fetch` rather than `api.post`, which drops the status the stagehand's line prints. (F3) `applyShutOnMe` (band.js) reads shut as 🤝 no — live, `applyOpen` and `joinOpen` both false — and the applicant rail's `now2` (session-view.html) no longer drops the five cards when 🪪 moves to ✒️. (F4) `applicants-walk` at *pen* presses the door's own 📧 on the Join card and follows the link to a member's seat, then stubs a 429 on the join and on Log In and asserts both read refused — red on the pre-fix page at all three (*an open door offers no Join card*, both refusals *Sent to*). **Filed Q1507**: the issue's *Decision for Ed* (a Join send from an address holding a verified application) — nothing changed; today it admits them. Walks: `applicants-walk` at pen · proposal · assembly, `journey`, both probes green; **`seat-matrix` not run** (stopped for low memory during #65; not restarted unasked). Left: F3 has no walk guard of its own (the plan's guard column names F1 and F2's); a verified applicant moved to ✒️ is by reading and the gates.
- **#67** (Stage 2, 2026-09-23, cdd16134): (F1) the interstitial (routes-auth.ts) is one sentence by door (`PAGE.proceed`) and a real Continue button, `busyDoor's` shape — no script and no `<noscript>`, so only a person's press spends a link; (F2 a) a token not the 32-character base64url shape every mint makes (`TOKEN_SHAPE`) reads `PAGE.cut` at the GET and spends nothing; (F2 b) the seven mails carrying a login or application link say the document's own address in prose (`ownAddress`, mailer.ts, read off the link's `d`); (F3) `sendNow` queues a door's mail the provider refused, with its token hash, and kicks the sender — the creation mail and the operator's notice (no document) stay synchronous; (F4) `emailOk` refuses any character outside printable ASCII where it is typed. Guard: `door.test.ts's` *the magic link waits for a press* — five cases, one per finding, all red on the pre-fix tree. **The walks** press through the new page: `landOn(page, url)` in `scripts/lib/walk.mjs` (a plain `goto` except on an interstitial, where it presses Continue and waits to leave `/auth/`) replaces 62 `goto` sites across 33 walks and repros; `seat-matrix` imports it as `openLink` beside its own older `landOn`. `followLink` (a fetch POST) is unchanged. **Walks owed**: the link-following batch (journey, ladder, first-keys, invite, member-questions, after-begin, powers, slug) was stopped by the system for low memory — `journey` got through the founding by a pressed creation link and the invitations, then its browser crashed opening a second seat (*Target crashed*); not re-run, not to be restarted unasked. CLAUDE.md's `alpha-flag` entry no longer says the interstitial submits itself. **Ed's own test, owed at the deploy**: follow one invitation to a Gmail address and one to an Outlook.com address (the issue's *Rollout*): the page must wait for Continue, one press must seat, the mail must name the document's address in its words, and `/healthz's` outbox counts stay flat. Server copy (`PAGE`, `MAILS`) stays beside the server, the tree's convention; neither copy golden moves.
- **#10** (Stage 2, 2026-09-23, this commit; option 1, Ed 2026-09-22): `DRAFT_ADMIN_KEY` (`config.ts` `adminKey`) guards `POST /api/admin/pause`, `/resume` and `/surface`; `DRAFT_BOT_KEY` guards the bot outbox alone; `bearerRefused(key)` (routes.ts) takes the key its route takes. Guards: `pause.test.ts` *refuses the bot key on pause and resume, and the admin key at the bot outbox* and `surface.test.ts` *the surface takes the admin key and no other* — both red on the pre-fix tree (200 for 401); the two boot helpers take the admin key with the bot key defaulting to it, so every older case stands unedited. `verify-deploy`: the admin check names the new key, and **where the bot key is in its environment** it offers that key to `/api/admin/surface` with a malformed sha — 401 on a split host, 400 on an unsplit one, which installs nothing either way. Docs: OPERATING §2 (a `DRAFT_ADMIN_KEY` row; `DRAFT_BOT_KEY`'s row now bots-only), §3 (the surface lane, the pause, the restart's curl), the verify-deploy paragraph, §10 (*nothing else rides it*); PRODUCTION.md a security row for the admin key and Q1310's guard cell; `render.yaml` the key under `draft` only, draft-dev's comment saying never; CLAUDE.md's `announced-pause` and `surface-reload` entries. **Not done, by the brief: `.github/workflows/ci.yml`** still bears `DRAFT_BOT_KEY` — the session must change the deploy step's env (`BOT_KEY: ${{ secrets.DRAFT_BOT_KEY }}`, :174) and the failure-path resume's (:408) to `ADMIN_KEY: ${{ secrets.DRAFT_ADMIN_KEY }}`, the three uses (:269 lane guard, :275 upload header, :307–308 pause) and the two messages (:298, :328) with them, and pass `DRAFT_BOT_KEY` to verify-deploy's step if the new check is wanted in CI. **Ed's steps at the deploy, in this order**: (1) `openssl rand -hex 32` for the admin key; (2) Render → service **draft** → Environment: add `DRAFT_ADMIN_KEY` with it (not on draft-dev; inert until the code lands); (3) GitHub → Settings → Secrets and variables → Actions: add `DRAFT_ADMIN_KEY`, the same value; (4) push only after both, and no room live — **the transition trap**: the push that first reads the admin key pauses the *old* host, which does not know it, so that one deploy is refused its pause and runs unpaused (green in CI); and if step 2 is skipped every later deploy runs unpaused and every surface-only push silently takes the full lane; (5) after a verified deploy (`node scripts/verify-deploy.mjs https://docs.vote`), rotate `DRAFT_BOT_KEY` on `draft` (and on draft-dev if bots run there), hand the new value to room-bots users, and delete the `DRAFT_BOT_KEY` Actions secret if nothing else reads it. Not touched: any real secret or Render setting.
- **Q1506** (Stage 2, 2026-09-23, this commit; ruled (b), Ed): the `/auth/create` claimed branch #38 F1 built (routes-auth.ts) refuses a link whose address is not the founded document's Founder with `PAGE.changed` — *This link was sent to an address the document’s Founder has since changed.* — Ed's sentence with *Founder* capitalised (STYLE.md's office, as every surface sentence names it) and the typographic apostrophe `PAGE` already uses; the rest of `spentPage` below it unchanged. A genuinely spent link keeps `PAGE.used`. Guard: `server.test.ts`'s *one pending creation, read one way* reads the sentence on F1's stray link (red on the pre-fix tree: `PAGE.used` read instead) and pins `PAGE.used` on F5's; the spent creation link's `PAGE.used` case above it unedited and green. **Filed Q1511**: F5's unclaimed branch — the typo's link followed before the corrected one founds anything — is the same cause, but the ruling named the founded document's page; it keeps `PAGE.used` until Ed rules. Server copy stays beside the server; neither copy golden moves.
- **Q1508** (Stage 2, 2026-09-23, this commit; ruled by Ed ~10:02): `strangerView` (views.ts) carries `records` on a closed document where `canRead` (🌍 link or public) — the member's own `raceView` rows, read for a seat that is nobody (`NOBODY`), so they are sealed exactly as a member's are: one `authorVisible` rule, no row anybody's `judgedByMe`, no author's early row, nothing live on a closed engine; none at 🌍 closed, nothing on a live document; the feed's own call skips them (`{ records: false }`). The page: live.js hands out `itemsFromView`, the page passes it and `closedBlocks` to `DOOR.make`, and `syncStrangerCharter` (door.js) files the records through the member path's drawing and appends the Backlog; `undecidedRecords` reads the undecided rows off `records` where no closed `record` is served, and `closedBlocks({ records: true })` is the backlog alone. Guards: `packages/server/test/stranger-records.test.ts` (closed at link and at public equal the member's records less their `judgedByMe`; closed at closed and live carry none — red on the pre-fix tree at link and public, *expected undefined to be defined*) and **`scripts/repro/stranger-records.mjs`** (new; the ladder's closed rung from `--seed=7272` until 🌍 is readable; a signed-out page files the very `rec:` tabs a member's closed page files, the Backlog stands, a tab opens its card, no Signatures block — red on the pre-fix tree at five checks: no records at the door, 0 tabs, no Backlog, the member comparison, nothing to open). **Found on the way**: the ✔s alone filed 3 of 17 records — the 14 undecided ones are keyed to the Backlog paragraphs `closedBlocks` builds from the closed `record`, which the door is not served — so the Backlog came with them, from the same records. **Filed Q1512**: the closed page's Signatures and Amendments blocks at the door, which the ruling (the ✔s) did not name; the door draws neither (never *Nobody has signed*). **Noted, not changed**: a record's card reads *you never voted on this* at the door as it does to a member who did not — the member path's own sentence, true of a stranger. Probes IDENTICAL; `applicants-walk --price=proposal` green. Not wired into CI (`.github/`, `ci-walks.sh` left to the lead).
- **Q1509** (Stage 2, 2026-09-23, this commit; ruled by Ed ~10:10): (a) `byReference` (routes-feed.ts) is the payload's last step — `members` becomes `{ arrived, list }`, the door's own shape for the same thing (`arrived` the size a passed entry's *n of E* is read against, which `members` was alone; `list` each author once), and an entry's `author` is its index there, null where sealed. **The reference is the person as shown** — name, picture, erasure — never the member id, so two entries share one exactly where they would have carried identical objects: nothing is disclosed that the repeats did not, and no member id reaches a spectator. `design/feed.js` reads `members.arrived` and resolves `author` through `members.list`; its own sample payload moved to the shape. (b) `feedEntries` caps at `FEED_LIMIT` only while `!cs.closed`. **Measured** (an in-process nh2026-sized room — 31 members with ~6.5 KB pictures, 150 adoptions; nh2026 itself measured 1,299,500 bytes): live **1,382,300 → 289,129** bytes (200 entries); closed **200 entries in 1,382,300 → all 300 in 332,380**. Guards: `feed.test.ts` — the first case's author is a number indexing `{ name: 'Bo Tanner', picture: 'e🦉' }`, no entry's JSON carries `picture`, the proposal and its passing share one reference (red on the pre-fix tree, *expected 'object' to be 'number'*); *the feed's cap* (in process, 110 rounds: live 200, closed 220 text entries down to *revision 1* — red on the pre-fix tree at 200 for 220); `scripts/repro/feed-scroll-hold.mjs` still green, and gained a named text entry whose author is read by reference (red on the pre-fix `feed.js`, *Anonymous*). `rehearsal.mjs` reads the feed's entries and never `author` or `members`, unchanged.
- **answer caret** (Stage 2, 2026-09-23, this commit; the P1 sweep's finding, Q1486 (B)'s class): `KEEP_ATTRS` (band.js) gains `data-ansnum` and `data-ansdate`, so the render keeper puts a member's focus, caret and value back after a render lands under an answer being typed; the field is found again by its place among the fields sharing its key (`nth`, `keptSel`), since 👥's two boxes share one; and a number box — which has no selection API, so focus put its caret at the start and the next digit went in front — is given its value again after focus, leaving the caret at the end (founder and motion number fields too). Guard: `member-questions-walk`'s new step before ⏱️ is answered — one digit typed with real keys, the founder's invitation landing (a full view, a render — the box asserted a new node, the step's own premise), then the value, the focus and the open card asserted, then the next key read as `10` — red on the pre-fix page (`{"value":"1","focused":false}`), green after; #75's first-press checks, 💤 and ⏰ unchanged and green. **Found**: the first cut restored focus and the next key landed in front (`"01"`) — hence the number-box line. **Filed Q1513**: a half-typed `datetime-local` holds no value until whole, so a render gives it back empty with the caret in it; no keeper can carry that (readings for Ed; the motion's `[data-mdate]` has the same gap). Probes IDENTICAL; `rate-motion` all sixteen scenarios OK (typing-under-poll, one-under-poll, others-under-poll among them); `member-questions-walk` is in CI's `walks` already, so the step rides it.
- **Q1511** (Stage 2, 2026-09-23, this commit; ruled (a), Ed ~13:55): #38 F5's unclaimed branch of `/auth/create` (routes-auth.ts) — the typo's creation link followed while the pending creation reserves the corrected address, nothing founded yet — is refused with `PAGE.changed`, Q1506's sentence, in place of `PAGE.used`; `PAGE.changed`'s comment names both branches. Guard: `server.test.ts`'s *one pending creation, read one way* reads the sentence on F5's link and not *already been used* (red on the pre-fix tree: `PAGE.used` received); the spent creation link's `PAGE.used` case above it unedited and green. No walk reads either sentence. Server copy stays beside the server; neither copy golden moves.
- **Q1512** (Stage 2, 2026-09-23, this commit; ruled (d), Ed ~13:56): `strangerView` (views.ts) carries, on a closed document where `canRead` (🌍 link or public), `closed.signatures` — the member view's own `closingSignatures()` rows (the record's reading, Q769: named wherever a name was chosen, at every ✍️ rung) less the member id — and `amendmentRecords`, the carried `set` and `text` motions `amendmentBlocks` files, each as route · payload · reason · date · from, never the mover, the motion's id or an answer (a text amendment's payload cut to its kind). Kept off `view.motions` on purpose, so the door's band grows no motion chips the ruling did not name. The page: `amendmentBlocks` (session-view.html) reads `cs.v.amendmentRecords` where the view carries it (`amendmentSource`), else the motion records as before; `closedBlocks`' `{ records: true }` option retires, and the door (`syncStrangerCharter`, door.js) draws the whole `closedBlocks()` wherever it is closed and readable, keyed on the signature and amendment counts so a new signature redraws. Guards: `stranger-records.test.ts`'s three new cases — link · public at ✍️ sealed, and link at ✍️ anonymous — the door's signatures and amendments equal the member's view less the ids, `['Bo Tanner', null]` at every rung, one ✒️ amendment by `pen` with its from and reason (red on the pre-fix tree at all three, *expected undefined*); the closed-🌍 and live cases now also assert neither rides; `scripts/repro/stranger-records.mjs` — the payload's counts against the room's, no id, no mover, both headings, and the Amendments and Signatures blocks read word for word as a member's closed page (red on the pre-fix tree at five checks: 0 of 4 signatures, 0 of 1 amendment, neither heading, 0 of 7 blocks; green after, the ladder's seed 7272). **Moved with the ruling**: `promise-chamber.test.ts`'s *the record reaches a member and never the door* pinned "not one word of who signed it" at 🌍 public, reading SPEC §9.3's *the record's distribution is the convenor's* literally; it now asserts the door's signature (name, comment, when; no member id, no `record`), and the file's redaction lists allow `"signatures"` and `"amendmentRecords"` only on a closed, readable door. SPEC §9.3's sentence is unchanged. Probes IDENTICAL; `applicants-walk --price=proposal` green.
- **Q1513** (Stage 2, 2026-09-23, this commit; ruled (b), Ed ~13:57): `pressInFlight` (session-view.html) gains `dateInFlight` — while an answer's or a motion's date box (`[data-ansdate]`, `[data-mdate]`) is the focused element, the 4 s poll skips its refresh, so nothing re-renders the card under a half-typed date; the cap is 30 s from the last `focusin` or key in the box, the slider drag's number, so a box left focused cannot park the page, and a render or a click away ends the hold without needing a `focusout`. The number fields' caret keeper (the *answer caret* row) is unchanged. Guard: `member-questions-walk`'s new step inside ⏰'s *At a set time* — the box clicked at its left edge, month and day typed, the Founder's invitation landing, one full poll, then the box asserted the same node, focused, the card still open, and the rest of the keys asserted to make `2030-01-01T12:00` — red on the pre-fix page (`{"same":false,…}`, the date `0001-03-20T12:00`, and #75's first-press check red behind it with the host refusing the year-0001 answer), green after. The cap checked once by hand, not in the walk (it costs half a minute): a throwaway copy idling 31 s in the box saw the next act's render land. Probes IDENTICAL; `rate-motion` all scenarios ended in a motion or a sentence. CLAUDE.md gains the gotcha, naming its guard.
- **Q1514** (Stage 2, 2026-09-23, this commit; ruled at the claim, confirmed by Ed 14:50): `recordCard` (session-view.html) — where a filed record's title is built, the one reader of `passedLead` — reads the record's own `route`: a carried motion on the pen route (a Founder's ✒️ decree) leads with `COPY.page.synth.founderChangedLead`, *Changed by the Founder: *, a carried vote keeps *Passed: *, a failed one *Rejected: *. Guard: `rate-motion`'s *empty-wallet · history* — the scenario now has the Founder decree ⏱️ to every 4 minutes itself (so `--only=empty-wallet` carries a decree too), then reads the member's card: red on the pre-fix page (*NO “Changed by the Founder:” line · and a decree still reads “Passed: a new proposal every 4 minutes”*), green after. `copy-freeze` read: the copy-source golden gains the one string; the card-copy golden's fifteen fixture titles for the 🌍 decree move *Passed: Anyone with the link* → *Changed by the Founder: Anyone with the link*, and the carried 👥 motion keeps *Passed: 38% …* — the split the ruling draws. Probes IDENTICAL, no re-freeze. Left: the news card's sentences (already right).
- **Q1516 (1)–(5)** (Stage 2, 2026-09-23, this commit; ruled by Ed, confirmed 14:50): (1) `renderEditDoor` (edit-mode.js) draws a `.sparkle` span in the door until the seat's first press, its phase the clock's as the rail's is; the press adds `DOOR_PRESSED` (`'editdoor'`, session-view.html) to `S.okd` and calls `saveGrants`, `ackPersists` naming it, so the grants' own per-seat store keeps it across a reload; system.css reuses `sparkleSweep` inside the disc with the band in the *yours* hue (white over the white disc showed nothing) and the reduced-motion still ring shared with the rail. (2) `.ridetab` is `position: relative`, not sticky — it rests at the text's first line in both modes; `syncRideTab` keeps no `detached` state, and its CSS rules went. (3), (4) `#editdoor`'s row is `position: fixed`, `--s5` off the window's right and foot, `--float-d` 4.5rem (the row's 3rem × 1.5), the glyph at B6's one size (the disc grew, not the picture — the 2026-09-16 precedent). (5) `COPY.page.rulesHeading` read by the band's group label and the contents rail's entry (both were page literals); *the constitution* in five member sentences, the feed's `§` place and the door's drafting sentence — moved from a door.js literal into `COPY.page.rulesDrafting`, and the server's own copy of it in `strangerView` (views.ts) with its `server.test.ts` pin. SURFACE K13, K31, §9's 📝 row, §8's band line and §5's head amended; STYLE §1 gains the **Rules** row; CLAUDE.md's `edit-door`, `riding-tab`, `constitution-section`. Guards: **card-audit D1** rewritten to the corner and the size, and **D4** new — the door's read-mode box meets none of the proposal row's buttons — both red on the pre-fix page at 1600 (the door 48px in the ✏️'s own box, meeting the row's commit), green after, the other seven findings and their sightings unchanged (10 → 7 distinct, 540 → 537); at 390 the door is not drawn (MOBILE.md), and the narrow run against the baseline is identical on HEAD and on the change (7, 537). **journey**: *door* (corner, 1.5×, clear of ✏️ and 🗑️ — red on the pre-fix page, all three false), the new *sparkle* line (before the press 1, after it 0, after a reload 0 — red on the pre-fix page, 0/0/0), and *strip ride* rewritten: the strip still rides, the 📝 tab stays at its heading (red by construction on the pre-fix page, whose tab rode level with the strip). **a11y-audit A17**'s still-form probe also asks the door: green (the session and band scenes meet the door's one sparkle, still), red with the door taken out of the reduced-motion rule (*sparkleSweep runs for ever*, session and band); hand findings unchanged (23, 670). `drawer-walk` at 390 all green; `first-keys-walk` green. `copy-freeze` read: the copy-source golden takes the eight strings and nothing else; the card-copy golden did not move (no walked card carries the heading or those sentences). **`qa:freeze` read**: the setup-probe's 154 diffs were the `toc` and `band` hashes of the 77 steps after the save, and the band and toc HTML equal the reference's once *Rules* reads *Constitution* (fixture live and closed); the session-probe was IDENTICAL before and after; the reference also caught up with the batch's earlier page edits, as at Q1500; the founding golden did not move. **Filed**: Q1518 (the corner covers the rail's lowest pinned entries at 1280 and 1000 wide — clear at 1600 and 1920) and Q1519 (with the tab not riding, whether the door stays drawn in edit mode as the way out); both built as the literal ruling until Ed rules. **Noted for Ed**: the door's drafting sentence reads *The rules are being drafted.* by the rename, where the kind it stands for (`!cs.textConfirmed`) is the text not yet confirmed. Left: (6) the page break — a mockup, another builder.
- **Q1517** (Stage 2, 2026-09-23, this commit; ruled by Ed one question at a time): the Fluent `CHECK` and `MULTIPLY` pictures (cards.js) fill with `currentColor` where they carried Fluent's `#785DC8`, shapes untouched; system.css paints `.mk-adopted` / `.mk-filedYes` `--ok` and `.mk-retired` / `.mk-filedNo` `--muted`, the filed twins still drained by the unchanged `grayscale(1)`. **The greys, measured** through Chromium's own grayscale filter on a canvas: the purple drained to 106; `--ok` drains to 108 (the filed ✔ unmoved to the eye), `--muted` to 115 — the filed ✖ nine levels lighter than it was, and exactly the luminance of the `--muted` ⏸ and ↻ beside it. SURFACE §6's paragraph on painted pictures, the ✔/✖/✔-grey hue cells and the exceptions row for ✖ (grey, wash and mark, though it still wants an OK — was *its mark stays in colour*) amended; `spec-check`'s painted-or-drained split now reads a picture as painted where its definition fills with `currentColor`. Guard: **`card-audit` R3** (new, in `walkRail`) — every `.mk-adopted` / `.mk-retired` path's computed fill against `--ok` / `--muted` resolved through a probe element, and a finding if no walk met one — red on the pre-fix tree (*12 of 12 .mk-adopted path(s) fill rgb(120, 93, 200)*, *3 of 3 .mk-retired*), green after, the rest of the audit unchanged at 1600 and at 390 (`--baseline`). `qa:freeze` read: the session-probe's 42 diffs were the `fill` attribute alone (every `htmlLen` step a multiple of five, the length of *currentColor* over *#785DC8*; no rect moved), the setup-probe's four were band hashes in the motions scenario from Cy's vote against onward (the filed ✖), no geometry; both copy goldens and the founding golden unmoved; `copy-check --walk` green.
- **Q1516 (6)** (Stage 2, 2026-09-23, this commit; ruled by Ed after three mockup rounds, *build it*): the paper look is the page's for everyone. `paper.css` folded into the end of `system.css` under `html.desk` — the session-view's `<html>` carries `desk`, so the feed page, which loads the same stylesheet and is dark for a projector, is untouched, and every rule keeps the mockup's specificity (only its order against `setup.css` changed, and no `setup.css` rule sets the same properties); `paper.js` loaded always, after `session.js`, reading `SESSION.NARROW_Q` (newly exported) instead of a third 900px literal, and added to the probe's `FROZEN`; `QUEUE_WASH_K` a constant 2 (`isQueueWash` the `q:` / `set:` keys). The fixture's collapsed repeated title and the edit-mode sheet end stand as the mockup had them — only the fixture draws that title, so the collapse never touches a live document. card-audit keeps the mockup's `--query=` (as `withQuery`); `design/mockups/paper-2026-09-23/` is in the tree with a README line saying the switch is gone. **The wire**: every cable, not only the yellow, stood under SC 1.4.11's 3∶1 — measured by the new **`a11y-audit` A18** on the fixture at 1600 (session and band scenes, 96 wires): yellow 1.13–1.56∶1 on the sheet and 1.0–1.36∶1 on the desk, grey 1.41 / 1.23, green 1.72 / 1.51, blue 1.77 / 1.55, red 2.16 / 1.89. The cable keeps its entry's exact colour and gains an edge (`wireEdge`, session.js): one pixel either side (`#wires .edge path`, 8px under the 6px cable), in the entry's hue at full strength taken toward black only as far as 3∶1 off both the sheet and the desk, fading with the cable — yellow's edge `rgb(167, 134, 16)`, grey's `rgb(132, 139, 146)`, blue, green and red undarkened; after, the lowest of 144 wires is 3.45∶1 on the sheet and 3.02∶1 on the desk (A18 green; red before at all 96). SURFACE M21 (new: the sheets, the break, the margins, the rails as gutters, the slips, the wire's edge) and M10's run ground amended; CLAUDE.md gains `desk` and `sheet`, `queue-wire` names its edge. **card-audit R3 corrected** on the way: on the desk the rails redefine `--muted` (`--desk-muted`, `--slip-muted`), so a ✖ there is painted that local grey; R3 now resolves the token where the mark stands (still red with the purple put back). Guards: card-audit at 1600 and at 390 against it — the same findings before and after (F6, H2, H4, S1 ×4 at 1600, ×3 at 390), no P rule at either width (the clicked tab still); `a11y-audit` no worse — 23 hand findings / 670 sightings and 4 axe rules / 125 nodes at 1600, 34 / 545 and 3 / 70 at 390, line for line; `drawer-walk` all green at 390; `toc-travel` green; `journey` green. **`qa:freeze` read**: the session-probe's 57 diffs were the rail washes (the doubling) and six pinned rail entries 28px up in the prose's own frame (they stay with the window while the column's top margin grew from 36 to 64px of padding, the 80px sheet margin); the setup-probe's 293 were every region's first box 28px lower and nothing else (no HTML hash, no width or height); both copy goldens and the founding golden unmoved. **Live against the mockup**: the live birth page draws the mockup's one sheet with no page error; nothing seen to differ.
