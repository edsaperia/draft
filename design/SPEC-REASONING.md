# SPEC-REASONING — why the rules are the way they are

The reasoning behind SPEC.md's mechanism sections: the dated rulings, what each replaced, and what was tried and rejected. SPEC.md states rules only and points here as `→ why: R-nnn`. This file is to the spec what `design/DECISIONS.md` is to CLAUDE.md's glossary — mechanism reasons here, surface reasons there (Q564). Born 2026-08-22 in spec pass 1, from the history stripped out of SPEC.md v0.64, which covered **§9 Sessions** alone; the pass of 2026-08-29 (v0.90, `R-061` onward) added **§3 The move**, **§4 Resolution** and **§8 Routing**. The full prose of every superseded paragraph is in git — at tag `spec-v0.64` and earlier for pass 1, and at `SPEC.md` v0.89 for the sections the later pass took.

Entries are condensed, not verbatim. Each carries the ruling's date and question number so the QUESTIONS.md fold can be found.

---

**R-001 Nothing arrives delegated** (Ed, 2026-08-21, Q511; SPEC v0.63). Replaced §9.7's *constitutional settings default to the roster, ordinary to the convenor* (2026-08-18) and, before it, §9.0a's *everything delegated by default* (2026-08-17). The roster-default argument was that a default you must argue your way out of is stronger than a ticked radio. Three things were wrong with it in practice: a default holder states an answer the convenor has not given (a document one second old asserted ten settings belonged to the room); the questions opened at creation, so the roster could be answering before the convenor had seen one; and no surface can tell a default from a decision. What the old rule protected survives elsewhere: anonymity rests on the consent rule (R-013), and the room's claim on a constitutional setting is the delegate/reserve asymmetry (R-008). The cost, stated: a convenor who never delegates holds a constitution the room only ever gets to refuse. The §9.7 paragraph survived the fold of Q511 because Q511 was folded into §9.0a alone (spec pass 1, finding 543).

**R-002 The hat is a card** (Ed, 2026-08-21; spec pass 1, finding 545). §9.6a had the question as a *pre-checked row in the membership list* (2026-08-18, after a morning as *a roster fact, not a role*). Two rulings since made that false: *nothing is pre-answered* removed the pre-check, and the single-file founding needed a place for 🎩 between the register and the convenor's own row. It had been hard-coded *member by default*, which is exactly why it never appeared as a task. Locked at the start since §9.6a (2026-08-18): the right to re-tick oneself dies with the start.

**R-003 The Text is a setting like any other** (Ed, 2026-08-21, Q440). §9.7 had said *the starting text sits outside all of this… holding it crowns nobody* and *the text is delegated as soon as proposals open* (2026-08-19). "Starting" named the moment we were looking at it from, not a property of the setting. The start lays both powers down, so a drafting engine's default is that adoptions stand by themselves; the road to a held Text is a post-start reserve motion, after which each adoption is a 👑 question — assent over the drafting mechanism itself, the largest single power in the design, kept available for consistency. Reserving the text from creation is out of scope (Q387).

**R-004 The pen is a route** (Ed, 2026-08-22, Q530). *A unilateral rule change by the founder is still just a kind of amendment.* `MotionRoute` gained `'pen'` — the four verbs' own ladder of who had to agree, minus 🪶, which is not an amendment because nothing exists yet. A pen motion is never raised: it opens and settles in the one act, so it is only ever seen carried; folded from the `setting-set` event the log already carried, hash chain untouched. It carries a reason and is owed an acknowledgement by everybody who had no say. §9.0's *never by anybody's unilateral hand* (2026-08-16) predated v0.54's separately-held powers and had been false since.

**R-005 The pen works on constitutional settings after the start** (spec pass 1, finding 549, Ed 2026-08-22: stands). §9.0a v0.63 had said *a convenor who keeps one still cannot change it after the start except by the unanimous route* — written the day roster-defaults were retired, as the consolation for losing them. It described a constraint the module never had: `setSetting` succeeds post-start whenever ✒️ is held, on any setting, `by: 'crown'`, on the reasoning that the assent was consented on the way in (Ed's 366). The room's claim is not a bar on the pen but the asymmetry of R-008: delegation is the convenor's free act, and the reserve motion lands without their assent.

**R-006 The verb is delegation, and it is never a button** (Ed, 2026-08-19, superseding v0.51's *unreserve*; Q349(b)). *The title is delegated to the members* is a constitutional sentence among the setting's own options, preferred and committed like any value. From the moment proposing opens the founder may delegate anything they hold (*members can propose about anything that can change, apart from names and pictures*). A delegable setting delegates into its founding question until the start; past that the same verb is a hand-over — the settled value stands, only the holder changes. The title and link take no founding question because a blind consent question over prose is nonsense (there is no most-protective title for maxima to find); their hand-over waits for the text to confirm, which is when the outside world can first knock (the quill line).

**R-007 Two powers, separately held** (Ed, 2026-08-19, v0.54; Q403, Q454). Reservation had been one bit. Splitting it into unilateral change and assent gave every held-able setting four states, and Ed's two examples fixed their meaning: the membership (invite-and-remove kept unilateral while carried applications need nobody's accept) and the threshold (presented for assent while the founder cannot re-set it to taste). *Delegation is the state of holding neither power* (Q403) retired the 2026-08-17 third-value modelling — *the members decide* is no longer a value in any list; giving up the second power is the delegation. Timing (corrected the same day): giving up assent alone is available from creation; giving up unilateral change alone waits for the start, because the assent-only state is inert before it — no motions to assent to, and §9.6a lets the founder set freely regardless. The glyphs were 🔧 and 👑 until the glyph batch of 2026-08-21 (Q454) made them ✒️ and 🛡️ — a sword and a shield, one writes, one only refuses — which also freed 👑 to mean only the crown mark (finding 544). The engine keeps `unilateral` / `assent` as field names. Assent is the more restrictive power because it stops members doing what they have decided (Q395), which is why the blind applications question once ordered the holder states that way; since Q506 the pair is 🤝's own and the value is the join policy alone.

**R-008 The road back lands without assent** (Ed, 2026-08-19, Q394). A member moves to return a power to the founder's reserve; it carries like any amendment, one refusal keeps it the members'; it lands without the founder's assent, willing or lapsed alike, because the release from an unwanted crown is already in their hands — delegating is always their own free act. The payload may name one power or both, defaulting to both; the surface offers only the whole crown (*simpler wins, revisit at QA*). Two consequences Ed drew: the zero-reservation founder keeps one standing property — the room can restore powers to them; and a reservation returned to a lapsed founder is a constitutional monarchy (R-011).

**R-009 👑 and 📯** (Ed, 2026-08-18, closing Q379 the wide way). The mark's job is to say *this founder still holds levers*, whichever they are, so a founder who reserves anything is a 👑 and one who reserves nothing is a 📯 (🏴 until the same day). The rule's own test, Ed's words: *a convenor who reserves nothing and is not a member has no role at all* — a name in the record, the mechanism owing them nothing and asking them nothing. The one residual proves it: they may still change their own name and picture, which is §9.0c's category — identity binds nobody, so it is not a role.

**R-010 Reserved is assent, not silence, at the end of either route** (Ed, 2026-08-18, superseding v0.48). A reserved setting may still be moved against by its own kind's route — reservation never alters the route — but a change the members pass goes to the convenor as a 👑 question, accept or reject. v0.48 had exempted the constitutional route on the reasoning that consent already includes the convenor; it does not include a clerk, and a member-convenor's accept in the vote is their answer as a member, not their assent as holder. Since v0.54 this is the assent power specifically. There is no timeout because, where the pen is held, none is needed: a convenor sitting on a passed change is simply quiet, and quiet is what the lapse clock measures. The 👑 question is a task of its own kind — news-pinned, Accept / Reject (Q351) — which is SURFACE.md's E12.

**R-011 The crown lapses like a member** (Ed, 2026-08-18, superseding v0.48's holder transfer; Q351(b)). §9.5a's clock runs on the convenor too, and lapse is automatic abstention: while the crown sleeps, assent on reserved matters is granted automatically, so a change the members pass applies as if accepted; nothing changes hands; revival is logging in, restoring the requirement from that moment. A pending 👑 question at crown lapse passes. At the close it fails closed instead (Q467): lapse is absence and the close is everybody's deadline. What keeps a monarchy mortal is that an absent king can refuse nothing. Whose clock — whether administering counts as activity — is Q349(a).

**R-012 A consent rule rather than a vote** (Ed, 2026-08-16/17). Taking the maximum of stated minimums dodges the constitutional bootstrap — *by what quorum do you decide the quorum?* — because the result satisfies every stated minimum by construction and nobody can say the rules were imposed on them. The cost: the most demanding member sets the bar, and asking for full quorum is a perfectly reasonable position — a document that cannot move without everyone is a legitimate thing to want. Answers are blind for the reason judgment is blind: otherwise the room anchors on whoever answers first. The distribution is published without names because the shape of what people asked for makes the resulting bar easier to live with; the identity of whoever needed most is not.

**R-013 Disclosure is consented, ordered by privacy** (Ed, 2026-08-17, §3.5a). The disclosure family is constitutional because a judgment was cast under a promise about who would see it. Ordered most-private-first (anonymous · sealed · public; nobody · each · everybody; never · after), each member states the most exposure they will accept and the document takes the most private — the same rule seen from the other end. That is what makes anonymous the strong default without anything being preselected: it holds unless every member is content with more, so a single person keeps the whole document unnamed. Where the convenor decides instead, that guarantee is not available — a convenor may open a document in which members are named without their having agreed to it; the price of the founding being optional, worth a convenor knowing. Elective signing is a real and costly option: in a small roster an unsigned candidate among signed ones says something about its author — a cost for the roster to weigh, not a reason to withhold it. Disclosure changes what the mechanism measures (a room that can see whose text it is can prefer the person), and in some rooms that is wanted — which is exactly why the answer belongs to the roster.

**R-014 Delegate the decision, not the field** (Ed, 2026-08-18, Q341). The threshold is two decisions of different kinds: what consent is about is *how sure the room must be before a change is permanent* — the bar at the close, one number, the only part a minimum can bind, since an early adoption is challengeable under the ramp's own logic; how the bar gets there is pacing, ordinary by §9.6's test because every adoption is recorded against the bar as it stood. Hence two cards (🌡️ constitutional, 🪜 ordinary) and one quorum card with two eyebrows. A delegated question must be phrased over the thing consent is about and collect exactly the binding scalar; the frame is fixed before the question is served, so a room always answers something well-posed; and a question whose meaning depends on another setting is not served until that settles (the drip's units depend on the window). Quorum's form: shares suit a windowed convention, counts a perpetual document where a drifting membership would silently re-rate every parked race.

**R-015 A blind question does not resolve while an invitation is outstanding, and never on one voice** (Ed, 2026-08-19, Q413; v0.61). The electorate is E, the arrived, which left one road to a rule settled on a single voice: a founder who delegates and answers before anybody opens their email. *A founder obviously does not intend to delegate to themselves, nor would they use this tool if they were writing a document on their own — more members are coming.* A consent rule computed over a single answer is that answer, which is not a delegation to anybody. A founder standing alone has both halves of the choice they already had: invite somebody, or take the setting back. The remedy for an email that will never be opened is §9.6a's free hand on the roster until the start.

**R-016 Membership begins at first arrival; inheritance is owed nothing** (Ed, 2026-08-18; Q257, Q339; amended 2026-08-25, Q842). An invitation is an invitation *to become* a member; until somebody opens the document they are *invited*, with no wallet, no answers owed and no place in any count — which is what keeps one unopened email from blocking judging. A late arrival inherits the constitution rather than reopening it (otherwise a long-lived document never settles). It **used** to be owed an OK on every constitutional setting it had no say in — the unacknowledged-decision rule covering inheritance with no new machinery — and that is what Ed reversed: *a setting that predates you is simply what the document says; a power handed to you is news addressed to you.* An acknowledgement is a piece of news, and a rule that was already there when you walked in is not news to you; nine of them stacked in a new member's rail on their first morning is the whole constitution demanding to be dismissed one card at a time before the document will speak. The ruling is a widening of the reason this entry already carried rather than a contradiction of it. Nothing is owed before the start (Q414): §9.6a lets the founder re-set freely until then, so an earlier OK would acknowledge a rule that may not be the rule; a gate opening is the exception, **being a thing that happened rather than a rule that was set** — and that is exactly the line the amendment generalises. What a member is owed is what *happened while they were here*: a rule set or changed after their arrival, and a power handed to them. A lapsed member is owed one too (2026-08-22): lapse is a stall, not a departure, and the person who was living under the old rule and went quiet is exactly the one a change ought to find — which is the same test read the other way, since they *were* here.

**R-017 The roster is the ground of every founding answer** (Ed, 2026-08-18). *How much of these particular people* — so a roster change while answers are collected is a ground shift: answers stand, their authors are told and may revise until the question settles; blindness is unaffected since nobody sees anybody's answer either way. How they are told was SURFACE.md E20 (Q572: a ↻ acknowledgement task on their own answer card, no mail) — **reversed by Ed, 2026-09-07 (Q1231, SPEC v0.103): nobody is told.** The ↻ acknowledgement was never built; an arrival shows where every arrival does, the answer stands, and the answer card stays revisable as it always is, so a ground shift during the founding asks nothing of anybody.

**R-018 A constitutional motion is a unanimous vote, not a re-asked founding** (Ed, 2026-08-18, revising the one-rule reading). The founding collects minima by construction because there is no constitution yet for a vote to govern. An amendment is a different act: a specific value is on the table, and *what will you accept?* is the wrong question about it — re-collecting minima can synthesise a third value nobody proposed, and a failed re-ask could still move the setting through fresh maxima. A unanimous vote escapes the bootstrap on its own terms: unanimity is the one decision rule with no quorum question inside it to govern, because the answer to *by what quorum do you amend the quorum?* is *everyone*. What the two rules share is the property that matters: no member is ever bound by a constitutional value they did not personally accept. Surface consequence: the card that asked the founding question is the card the amendment re-opens.

**R-019 An ordinary motion is the whole race mechanism** (Ed, 2026-08-19, Q390: *we've built a sophisticated system for handling proposals, and motions are just a kind of proposal and work the same way*). The standing value is the incumbent; a rival value joins the same race (propose-C over values); an entry stakes one edit and refunds by §7; withdrawal returns it whole; the floor and quorum gate the carry; deadlock detection applies; a standing-value change mid-race is a ground shift. Values are simpler than prose in one place: equality is decidable, so an identical value is the same proposal — proposing what stands is rejected, proposing a live rival's value is an invitation to co-sign. Moving the close never lowers the threshold (§4.3): it keeps its value and rises more slowly to the same ceiling.

**R-020 A constitutional motion is free, and limited to one per member** (Ed, 2026-08-18, Q327). An ordinary motion is a proposal and priced like one. A constitutional motion is a member asking everyone to decide again about a rule that binds them; charging for that would put a price on consent, the one thing that must stay free. What keeps it from abuse is a limit rather than a price, and the limit is the member, not the setting: one open at a time, returned on withdrawal or settlement, so the cost of raising one is the inability to raise another until the room has answered. The hold and the assembling circle are SURFACE.md's (the `assembly-press`).

**R-021 Live electorate, abstention never blocks, the mover stands at accept** (Ed, 2026-08-18). Abstention is an explicit answer; what blocks is a standing keep. There is no snapshot at open: the set whose answers are needed is the quorum base as it is now, re-checked on every answer and roster event — an arrival means one more answer is needed, a departure can complete a motion. Nobody active at the moment of carriage is bound by an amendment they did not accept or stand aside from, which is §9.6's property restated; no ground-shift machinery is required, because nothing about the question changed, only who must answer it. A standing keep blocks but does not kill; a blocked motion ends by withdrawal. **That sentence is superseded by R-138** (Ed, 2026-09-19, Q1473): a vote against ends the motion where it stands, because an application has no mover and so could never take the one way out this ruling gave it. Everything else here stands — the live electorate, the abstention, the arrival and the departure. The mover's own answer is accept from the open, as §3.3 counts an author's preference without asking.

**R-022 A carried amendment binds races in flight** (Ed, 2026-08-18, Q328: *historical constitutions don't bind races in flight*). A race is always evaluated against the constitution as it stands; nothing already decided is re-rated — every past adoption keeps its recorded bar-as-it-stood, which is what §9.6's test protects: the record, not the future. Where an amendment leaves a running race no longer making sense, that is a ground shift (§4.4). Engine-side, §4.3's anchor re-seating: a bar never jumps because timings changed.

**R-023 How a member is removed** (Ed, 2026-08-19, Q401(a); v0.60). Three rungs, most protective first, each mapping to real constitutional practice: everyone's consent, the subject's included (one refusal keeps them in, their own counted — you cannot be thrown out of a constitution you consented to be bound by; the pre-v0.60 rule); 🏛️ without the subject (partnerships expel by unanimity of the others — a decision class of its own, the live-electorate settle check with the subject filtered out, their answer refused); an ordinary proposal (clubs and charities expel by a supermajority — a one-candidate race, `remove:<id>`, the admit race's mirror). Whichever rung holds, the subject sees the motion running — nobody is removed in secret. Whether the subject may judge their own ordinary removal race is Q401(b). The pressure valves otherwise are sign-out, the freeze and the advisory-inactivity signal.

**R-024 Two membership settings; every application its own race** (Ed, 2026-08-18/19; Q348, Q397, Q506). Ed's split: *separate "Membership", the register of people who are members, from "Membership Policy", how people become members after the ceremony* — the policy card was titled *Membership Policy* until renamed *Applications* the same day. A submitted application becomes an ordinary motion to grant membership, deliberately a different door from invitation-by-consent: the policy is the constitutional act, each application its ordinary consequence. Q397 fixed the shape: a one-candidate race against the membership as it stands, and every application its own race — two applicants are never raced against each other, because admissions are not rivals (values on one setting share a race precisely because they exclude each other, and admitting one costs the other nothing). A second is a proposed application (Q348a); under *apply* the application text is the applicant's rationale. The rationale lane is always offered and a blank one is a real proposal (v0.57 relaxing v0.56). Q506 moved the register's power pair out of the applications value onto the setting's own powers, leaving the join policy as the whole value. *Superseded in part, 2026-08-26 (plan-queue entry 94, v0.74): one price of admission on 🪪 for every route in; 🤝 is a switch — may strangers apply — and an application is a stranger proposing their own invitation, paying 🪪's price, so the second is gone; the register's pair became the ✉️ door's, and ❌ got its own (§9.7 rule 9). Every application its own race stands.*

**R-025 The applicant's preference is not a mover** (spec pass 1, Q565, Ed 2026-08-22: (a)). §9.7½ said the applicant is *counted toward no quorum and no floor*; §3.3 and §8.2 count every author's derived preference as a mover. At a roster of four, F = 2, the applicant's own preference would be half their own floor and one member's judgment would admit them. The exception is stated in §9.7.3 (X11). Whether the engine-bridge already excludes the applicant's derived preference from the admit race's floor is recorded in QUESTIONS.md as a code check. **Checked 2026-08-22 (Q583): it did not** — the engine's derived-preference loop never looked at suspension, so the applicant was half their own floor at a roster of four. Fixed in engine-core as the general rule E already implies: a suspended author's derived preference is not a mover; their cast judgments still count.

**R-026 AI proposals are ordinary** (Ed, 2026-08-18, Q352). The coherence auditor is not a member — it judges nothing and counts toward no quorum — so switching it on or off re-rates nothing already decided. A member could put the document through an AI themselves; the tool is a convenience for the membership. Machine members left §9.6's roster bullet when they left the roster.

**R-027 Pacing is the founder's, not a founding question** (Ed, 2026-08-19, Q415; v0.61 — reverting his own 2026-08-18 override, made with *we can change this later* attached). Q341 drew the line: the bar at the close is consent, the ramp that reaches it is pacing. The catalogue had defaulted 🪜 to the members and marked it delegable, which meant giving up both powers pre-start would open a blind question no surface had a body for — there is no stated consent rule a room could answer for a shape-and-start pair. Delegating it is a hand-over once the text confirms, so 📯 stays reachable. Its consent order survived in the catalogue for a case that never performs a resolution; dropped in spec pass 1 (Q560).

**R-028 Proposing opens at the start** (spec pass 1, Q570, Ed 2026-08-22: (a)). §9.0b (2026-08-17) had three gates to three holders — reading to nobody, proposing to the convenor and then to *you* (a confirmed text and your own founding answers), judging to the room — so that a member who had answered could draft while the rest were still answering. The build took a different shape from the wallets and the server: ✏️ is granted at 🍾 (*no power arrives without acknowledgment*), and the engine bridge that holds candidates is born at constitute, so `propose-text` is refused before the start. Ed's ruling keeps the build: members answer delegated constitutional questions before the start, and cannot propose until they have their wallets. The three-holders design is recorded here as the one not built; its surviving half — the proposal rate applies prospectively when it lands, nothing clawed back (Q343) — still holds.

**R-029 Six judge-gates** (spec pass 1, finding 556; titled *Seven* until 2026-09-07 — the count is §9.0b's and the catalogue's six, tidy item 60). §9.0b said *judging needs the whole constitution*; the code gates on exactly the settings a judgment is recorded under or counted towards — threshold, quorum, authorship, signing, judgments, visibility, lapse — which is the reason §9.0b itself gave. Not gated: the ending, removal, applications, rate, machines, title, link. A delegated question on any setting still blocks the start while it collects.

**R-030 No observer role** (Ed, 2026-08-18; Q324 closed in spec pass 1, Q577). Observers are not members: no row, no name, no picture, because nothing about them is known; who may read is settled by 🌍 alone. §9.3's sentence promising an anonymized live metrics feed to observers is struck; the feed itself is parked with Q42 (spectator commentary, backlog) as a spectator-api note.

**R-031 The convenor holds no power over routing** (Q54, closed in spec pass 1, finding 547). §8.5's *The convenor's in-session powers: none* was about feeds — nobody, the convenor included, touches who is served which pair — and had been contradicted wholesale by §9.7 since reservation existed. Reworded to say the narrow thing.

**R-032 The freeze: the clock says so, and proposals still park** *(superseded by R-088, v0.99)* (Ed, 2026-08-22, Q573 (b)). No mail on a freeze: a member logging out is intentional on their part. Proposals made after the freeze still go into the record and are saved with the document when it closes, for the next session. Whether the freeze and the close are communicated any further is undesigned — Q463.

**R-033 The kinds are derived, not argued** *(⏰'s derivation — the ramp is the bar — superseded by R-119, v0.128; the test itself stands)* (Ed, 2026-08-18). The test — *a constitutional decision is one that would make past decisions mean something different* — produces the list, bullet by bullet: disclosure and visibility (a judgment was cast under a promise); quorum and the threshold (every adoption means *this cleared that bar with that many behind it*); the roster (quorum is a fraction of it); whether the document ends (the ramp is the bar). Everything else is ordinary because no past judgment means anything different when the title, the link, the date or the wallet changes. Q329: the route is read off the value at compose time, so ⏰'s card offers `routeOf` and the price flips as you type.

**R-034 Two independent axes at creation** (Ed, 2026-08-16, amended 2026-08-18). The window (an end datetime or perpetual) and the threshold shape (fixed or ramping) are independent; only the perpetual case is constrained, since a ramp needs an endpoint. Without a window the document ends by freeze, so a perpetual document is one whose ending is decided by its roster rather than its calendar. The proposal rate is two settings (the grant; the drip with its cap), not an economy and not tokens; the stake is not a third, being a flat 1 (§13). The drip runs on real minutes everywhere (Q353), so moving the close touches nobody's wallet. A perpetual document has no inter-session reset — Q252.

**R-035 The link is never broken** (Ed, 2026-08-18/19, Q460, Q462b). Nothing exists until the magic link is followed, so the link is the instantiation; the address is reserved on send for the pending stash's TTL, the one oracle on pending documents. Verification comes first because there are no passwords and the address is the only way back in. Text pasted before the save rides the pending creation (*copy the text, open docs.vote, paste it, then do the tasks* is a reasonable order to work in). Every link the document has ever had keeps working because members are invited by a link sent once, often weeks before they open it — a rename that broke old links would make the one ordinary setting with a real cost for being used as intended.

**R-036 Identity is not authorship; Anonymous is a name** (Ed, 2026-08-18/19). A name is how you appear in the room; authorship is whether a name is attached to a proposal, sealed unless every member consented to more. A document can show fourteen named people and not one named candidate, and that is the ordinary case. A blank name renders as Anonymous because an unnamed member is an ordinary member and the surface must not read their silence as an error; at the stranger's door an unnamed founder is *the founder*, which makes this a rule about how a member appears in the room.

**R-037 Lapse** (Ed, 2026-08-18, closing Q345 with the sunset option). Each member states the shortest quiet spell they would accept being lapsed after and the document takes the longest, so nobody is lapsed faster than they accepted. Sign-out applied by clock rather than by declaration; it passes §9.6's test because it re-rates nothing decided; on a perpetual document it is the answer to the member who will neither participate nor leave. Warnings by mail first; the package with the lapse; revival by logging in.

**R-038 Sign-out, holding and abstaining** *(superseded by R-088, v0.99)* (Ed, 2026-08-16). Holding keeps you in the quorum base (*I do not consent to you finishing without me*); abstaining leaves it (*I trust you to finish up*). The split makes a walkout visible: quorum-busting requires the explicit, logged act of signing out holding. The base is E minus abstainers, never the active remainder, or every departure would lower the bar. An abstainer is told when the base drops materially below where it stood when they left, so the choice can be revisited rather than inherited.

**R-040 Appendix A is engine tuning** (spec pass 1, Q562, Ed 2026-08-22: (a)). The appendix was titled *Constitution defaults* and since Q511 the constitution has none. What remained split cleanly: numbers the mechanism runs on and nobody is asked (cooldown, hot set, deadlock test, refund, rival gate, boost) and constitution settings whose "defaults" were sim/fixture values (⏩'s `SEED`). The first group stays as the published routing and calibration parameters §8.5 promises; the second lives only in §9.7.1, with the fixture values in sim-harness. The document-level half of Q335's split.

**R-042 Foundership carries a read** (Ed, 2026-08-22, v0.64). Without it a clerk under *members only* was shut out of the document they convened — a rule no product has implemented and the surface should not state, since the convenor holds the founding, the record's distribution and, wherever they reserve, the pen. The server always behaved this way; what was wrong was that the clause stated the opposite, so under *members only* — and only when the founder is a clerk — the clause names them: *Only members and the Founder can see the document.* It said this as a second sentence appended to the members-only one until Ed's QA of 2026-09-02 (Q1143, STYLE audit item 80 in `design/DECISIONS.md`) made it one sentence naming both audiences.

**R-043 Out of scope** (Ed, 2026-08-19). Reserving the text — every change needing the founder's assent to merge — is how suggested amendments work in many editing products and against the spirit of this one; parked as Q387. Delegating powers to individual members, transferring the foundership deliberately or on lapse, inheritance — the 👑 is a benevolent dictator for life rather than a literal monarch, and nobody inherits it; sequel material, Q388. The holder is the constitutional fact, consented once by everybody on the way in: nobody joins a crowned convenor's document unknowingly, since the 👑 stands beside their name.

**R-044 Laying a power down is news** (Ed, 2026-08-22, Q571 (b)). A relinquishment changes no rule's value, so Q530's pen machinery (keyed on `previousValue`) did not fire, and the members learned of it only by noticing a clause was shorter. But who holds a veto over the threshold is constitutional, and *a constitutional setting a member had no say in is a decision they are owed* — so it is a news entry owed an OK, like a changed rule. SURFACE.md E9. **Extended: one act, one entry** (Ed, 2026-08-27, backlog entry 162, Q1013). The rule was written in 2026-08-22 and built in 2026-08-29, and what happened in between is entry 158, which gives the 🍾 card a table of zones × ✒️/🛡️ switches — so one press can lay down about thirty-four powers at once. Unbatched that is thirty-four acknowledgements landing in every member's rail at the moment the document opens, which is the flood that makes members stop reading acknowledgements altogether; so *everything one act lays down is one news entry, stating its whole batch and taking one OK*, and the boundary of the group is the act — releases sharing one event, or one `t` and one actor. Not taken: batching by *setting*, which would put ✒️ and 🛡️ on one clause together and every other clause apart, grouping by where a power sits rather than by what was done; and batching by a window of time, which would join two deliberate acts a founder made minutes apart. The owed object is its own thing (`releasesOwed`) rather than an entry in `okOwed`: a release is news about a **power**, not about a value, and landing it in `okOwed` would fire the setting's own value-news card with the wrong copy. SURFACE.md C8a.

**R-045 A delegated question blocks the start, gate or not** (Ed, 2026-08-22, Q626). Since spec pass 1 (v0.65) the two halves of §9 disagreed: §9.7.1's note said a delegated question on **any** setting blocks the start, §9.0b said only a judge-gate does, and the code followed §9.0b. Ed ruled the §9.7.1 half wins. The reason is what the surviving state actually is: a question the founder hands over and then starts on top of cannot resolve (the consent rule refuses to settle on fewer than two voices), cannot be reclaimed (reclaiming is pre-start) and cannot be set (it is the members' now) — while the engine runs on a fallback nobody chose, ⏰ null reading perpetual and ⏱ null handing out a grant of 4, under a clause that says the members are still deciding it. That is not a delegation; it is a setting deleted from the document while the document claims otherwise. The clerk-founder case makes it terminal: delegating 🤝 takes the register's drafting power with it, so a clerk who begins with an empty room can neither invite nor let anybody apply, and four questions can never resolve — a document sealed for ever, by two ordinary presses. The blocking rule closes that for free, which is the argument for fixing the invariant rather than the three refusals downstream: each of those is individually right, and repairing any one of them would make the module less coherent. Only the ten delegable settings can ever collect, so the widening is exactly those ten. Superseded: nothing in the module. Narrowed: the reachable half of Q343 (*until the proposal rate is settled everyone drafts on the standard grant — prospective only, nothing clawed back*) — that rule now bites only when the room answers ⏱ **after** the start, which is still possible and still correct. Not taken: a dependency check on `setSetting` (Q627) — the contradiction it would have caught, a threshold ramping over a document whose ending is unresolved, is unreachable once no question survives 🍾.

**R-046 What a picture may be** (Ed, 2026-08-23, Q732–Q735). §9.0c said a member chooses a name and a picture and was silent on what a picture *is*, which left four answers live at once: a colour behind your initials, a drawn mark, an emoji, an uploaded image. Two of them were mockup devices from before either real answer existed — the grounds and the marks were how a surface with no uploader gave somebody a face without inventing one — and they survived as stored formats long after the picker stopped being the only way in. The rule now names two shapes and the absence of one, and the absence is the point: initials are not a third thing you pick, they are what the room shows when you have given no picture, so the card states that in a sentence rather than offering it in a row. Exclusivity is the same argument that made the surface's own marks reserved: a face is how somebody is recognised across a rail, and two members wearing one glyph is two people the eye cannot tell apart, exactly as a member wearing ✏️ turns every wallet into a possible mention of them. Nothing historical is tolerated — Ed, the same day: *we're still in alpha, you can mostly ignore backwards compatibility, there are no real documents yet* — so `c0`–`c5` and `m0`–`m2` are refused at the server rather than merely un-offered, and the picture cap comes down from 150,000 characters to what the page now encodes, since an uploaded image is scaled and re-encoded in the browser before it is ever stored. That re-encode is not only about size: it is what strips EXIF, and with it the GPS coordinates a phone photograph would otherwise have carried into a hash-chained append-only log that holds answers in plaintext. Superseded: the *no invented photographs in a mockup* rule, which was a good rule about fixtures and had already stopped being one about controls when the uploader landed. Not taken: keeping the grounds as a fallback for a room that wants colour without a picture — the initials disc already is that, and a second way to say it is a decision nobody has.

**R-047 ✍️ is about proposals, and a signature is always named** (Ed, 2026-08-23, closing Q634 and Q638 (i); Q768, Q769, v0.69). Q634 named three problems with ✍️ and no merge option was on its list. The third rung was the root of two of them. **everybody signs** is not an answer to *may an author choose to sign?* — it is the same fact as 👤 **public**, said a second time on a second card, which is how the module came to admit `authorship: anonymous` with `signing: everybody`: a document that names nobody and names everybody, reachable by two ordinary presses and coherent to neither the engine nor a reader. Deleting the rung removes that **structurally rather than guarding it**, which is the whole reason to prefer it to a cross-check: with `everybody` gone every surviving pair reads, and there is no invariant left for a later change to break. `each` + `anonymous` is *nobody is named unless they choose to be*; `each` + `sealed` is *named at the close, or earlier by choice*; and under `public` the opt-in is inert rather than contradictory, since every proposal already carries a name. The axes stay two, and that is Ed's own ruling that the opt-in resolves most-private-wins doing the work: a member who accepts `sealed` **without** elective signing must not be handed elective signing because somebody else asked for it, and an independently consented question is exactly what this module calls a setting. What merges is the card and the clause (Q767), not the axes.

**Superseded within the week by Ed's own ruling of 2026-08-25** (Q767): *I think five rungs is fine; there is definitely a ladder going from anonymous forever to open forever*. ✍️ is not a second axis at all — an opt-in is a **step**, and the step falls between never-named and named-at-the-close. So ✍️ leaves the catalogue and 👤 takes one total ladder of five, `anonymous · anonymousElective · sealed · sealedElective · public`, resolved by one consent. What the two-axis reasoning above got right is why the merge could not be done at the surface alone: an independently consented question is exactly what this module calls a setting, so *one card over two settings* was a contradiction in the other direction — the surface derives a card's composer, its power tabs, its owed-OK and its answer body from one key each — and the answer is to have one of each. Nobody is handed an exposure they did not accept, because the order is total: never-named-unless-you-ask is strictly more private than named-at-the-close, which is strictly more private than named-as-you-propose. The engine keeps its three values; the two elective rungs map to their base rung in `adapter.ts` until the per-proposal sign control exists (Q770), which is what they behave as.

The second half is Q634 (ii). `closingSignatures()` read ✍️ and anonymised the whole block under `nobody`, falling back to `each` when nothing had settled — a **middle-rung default** where every other privacy default here is the most private one, which on its own would have been a finding. But the setting should never have been read at all. ✍️ prices a name attached to something you wrote *while the room is still deciding*, which is what the blindness discipline (§3.5) exists to protect; signing the finished document is the opposite act, taken after every decision is made, and an unnamed signature is an anonymous comment rather than a signature. Superseded: the rule that made the closing signature block follow the ladder. Not taken: a separate *do signatures carry names* setting — that is a decision nobody has asked for, and the one-card-per-decision test refuses a card for it.

**R-048 A power is laid down once the setting has a value, and a pre-start release lands at the start** (Ed, 2026-08-25, backlog 34: *at the moment we prevent relinquishing of pen and shield on settings before Begin; it should be possible as soon as the Pen and Shield are accepted, after each setting is set for the first time* — and *I think they should take effect at begin*). Replaces R-007's timing, which put the two powers on two calendars: 🛡️ free from creation, ✒️ waiting for the text to confirm. Both were wrong in the same way. **The text-confirmed gate is one setting's value standing in for the whole document's clock** — 📄 is one setting among nineteen, and a founder who has decided the threshold and wants the room to hold the pen on it should not be waiting on a paragraph they have not written yet. And **assent from creation was free on a setting nobody had set**, which is a founder handing over a rule that does not exist: nothing has been decided, so there is nothing to promise not to overturn. One gate replaces both, and it is a gate on the setting rather than on the calendar: set it once, then hand over either power or both.

What made the old ✒️ rule defensible was that the shield-only state is inert before the start — no motions to assent to, and §9.6a lets the founder set freely regardless — so a pen given up pre-start would have been a power that stopped working before anything could use it. **Deferring the effect answers that instead of the timing**: the act is recorded when it is made, the clause reads *From the start, the Founder may not amend this at will*, and the hand stays on the setting until 🍾, so nothing pre-start has to reason about a half-held power. `reclaim` still works pre-start, so a release is revisable exactly as long as the value it is about is. Modelled as `SettingState.pendingRelease` and spent in the `constituted` fold beside the Text's own lay-down (rule 8) — **derived, not emitted**, so no event shape changed and the frozen golden log replays byte for byte.

One consequence had to be made symmetric. *Delegation is the state of holding neither power* (R-007/Q403) is a **pre-start** state that must take effect at once, because a founding question that waited for the start would never be collected — so the second release on a delegable setting is still a delegation and still immediate. It used to be tested only on ✒️, the pen being the only one that could go second; with either power now able to go first, the order the founder happens to press them in must not decide whether they end up delegating. Not taken: making delegation pending too (the question would never open), and letting an unset setting be relinquished with the value gate falling only on the pen (the asymmetry is what this ruling is removing).

**R-049 A founding question's electorate drops abstainers** *(superseded by R-088, v0.99)* (Ed, 2026-08-25, Q647; v0.72). Two counts of the same electorate disagreed: `view.ts` showed *n of E have answered* against `motionElectorate()`, E minus abstainers, while `maybeResolve` and `readiness()` resolved against `eOf`, the whole of E. The failure was not cosmetic and not symmetric — a member who signs out **abstaining** left the line every reader watches and stayed in the set the resolver waits on, so the room saw a question that read complete and could never settle, with no surface anywhere naming the person holding it up. Ed took the resolver's side rather than the view's: **the resolver drops abstainers, matching what is on screen**. It is a change to the consent rule and not a bug fix, which is why it wanted him. It is also the reading the rest of §9.5 already takes — abstaining is *leaving the quorum base*, an explicit act with the words *I trust you to finish up* in it, where holding is the deliberate opposite; a rule that let an abstainer block a question they had declined to answer would be the one place on the surface where signing out costs the room more than staying. Holders are unaffected: a member who signs out **holding** stays in the base and stays in the electorate, and silence is never sign-out (§9.5). Superseded: R-015's *the electorate is E*, narrowed rather than replaced — the never-on-one-voice rule and the outstanding-invitation rule are unchanged, and both are now read against the smaller set, so a room of two where one abstains no longer resolves on the survivor's single answer. Not taken: (a), the view counting against `eOf` — spec-faithful and the recommendation, but it would have made the honest reading of the line *this question is waiting for somebody who has told you they are done*, which is a true sentence about a rule nobody wants. **Built 2026-08-27** (backlog 64): `maybeResolve`'s electorate and `waitingWith`'s `soleVoice` read `motionElectorateOf` rather than `eOf`, so the resolver's own gate and the 🍾 readout's `one-voice` reason are one test on one set; `readiness()` counts a question's `answered` over that set too and lists an abstainer owing nothing (Q648, `owed: 0, answered: 0` — the row stays, since they are arrived and not removed). And **a sign-out completes a founding question**: `signOut` now calls `maybeResolveAll(t)` beside its existing `maybeSettleMotions(t)`, so an abstention that shrinks the electorate to exactly the people who have answered settles the question at that act rather than at the next unrelated answer. No `ceremony-ground-shifted` rides it — the roster has not moved and the abstainer is still a member, so nobody's answer has had its ground changed — and the golden log re-emits byte for byte. An abstainer may still answer; it is recorded, not counted, and `view()` still serves them their own `myAnswer`. `view.ts`, `populations.ts` and the page were already right and are untouched. **The timing was put to Ed and confirmed** (2026-08-27, Q945; v0.79 states it in §9.0a): *yes — the sign-out resolves it immediately*, so a room whose last outstanding answer is withdrawn by an abstention does not sit on a settled question until somebody else happens to act. The three readings the build took beside it were offered for refusal and none was refused, so all three stand as the rule: the abstainer **stays listed** in the 🍾 readout owing nothing rather than dropping out of it, since they are arrived and not removed; the members who have already answered are **not told** of the abstention, because a ground shift is a roster move and this is not one; and an abstainer's own answer is still **accepted and served back**, recorded and counted towards nothing, which is what `view.ts` already did.

**R-050 A proposal keeps the privacy it was made under, and the sign control is real** (Ed, 2026-08-25 and 2026-08-26, backlog 30 and 31; Q770, closing Q634 (i); v0.78). Two rulings on one record. **The control** (30): the two elective rungs were offered on the card with the words *unless they sign a proposal themselves* and nothing implemented the signing, so the sentence was a claim the document did not honour. Ed's spec is a control in the rationale composer area switching a draft between anonymous and signed, shown only when anonymity is allowed — which is to say only under an elective rung, since a fixed rung offers no choice. The default is the rung's base and signing is the opt-in, per proposal, fixed at Propose, because once submitted the choice is part of the record. The server is the narrow gate (the Q812 rule): a stale page cannot sign under a fixed rung. **Honour** (31): 👤 is constitutional and can move by motion after the start; moving towards privacy reveals nothing already hidden, and moving towards openness was the question. Ed ruled **honour** — a proposal keeps the disclosure it was made under; the record shows the rung then and the rung now; the 👤 card says so while a motion on it runs, so a voter reads it before answering. Rejected: **follow** — *the constitution governs the record*, so a loosened rung names every proposal ever made — which hands a member an exposure they never accepted, exactly what the most-private-wins consent rule exists to refuse; and **ask** — offering each earlier author the choice again at the change — which is elective signing by another route, a second control for a decision the author already made. The mechanism is one stamp: the engine records on each candidate the base standing at submission, and one exported reveal rule (`authorVisible`) reads *that*, never the constitution's current value. **One reveal rule, written once**: there were three readers of the rung — the live option view, the closed record, the 🥂 card's *names* sentence — and Q767 already had to teach all three the elective mapping; every reader now calls the one function, and the 🥂 card reads the record rather than deriving a sentence from the rung, which would be wrong the moment one person signed. `authorshipBase` stays the door's mapper: it answers *what does this rung do by default*, still the right question at the door. Not taken: a migration of older logs — a candidate with no stamp reads the constitution as it stands, and Ed's alpha ruling (*wipe all existing documents*) means none survives the deploy.

**Symmetric, confirmed** (Ed, 2026-08-27, Q948; v0.80 states it in §3.5a). The **honour** ruling above was argued only for a rung that loosens, which is the direction that hands somebody an exposure they never accepted; the tightening direction — a proposal made under *names at the close*, in a room that afterwards moves to *nobody's name, ever* — was left unnamed, and the plan that built the stamp assumed the symmetric answer. Ed: *Symmetric — the rung at submission governs, always.* So the earlier proposal is still named at the close: its author accepted that rung when they proposed, and the room's later privacy is a promise made to the proposals that come after it, not a retraction of one already made. The alternative — hiding it, so that a tightening reaches backwards while a loosening does not — buys a little more privacy at the price of **two rules where there is one**, and of a record whose disclosure depends on the order two settings moved in. Nothing in the code moves: the stamp is on the candidate and `authorVisible` reads *that*, which was already direction-agnostic, so the confirmation is a sentence in the spec rather than a change to the mechanism. That is the whole point of having built it as one stamp and one reveal rule.

**R-051 A non-converged fit adopts and says so** (Ed, 2026-08-27, closing Q945; v0.81). The alpha-readiness pass (plan-queue 77) taught `fitDavidson` to report how it stopped — `stop`, `iterations`, `gradMax`, `converged` — and found that nothing read the report: a fit that ran out of iterations with its gradient still moving produced a `probBeats` like any other and `sweepAdoptions` adopted off it without knowing. Three answers were put. **(a) Adopt and record** — the receipt carries *fitted to the iteration cap*; nothing in the room changes; an auditor can see it. **(b) Refuse the batch** — the metronome skips and the next batch refits with one more judgment; safest numerically, but it turns a numerical event into a governance one: a race stays open for a reason no member can be shown, which is exactly the kind of invisible state §4.2 refuses (the same argument that keeps *cleared and cooling* off the surface). **(c) Raise the cap and assert** — a test rather than a rule, silent if it ever does fire. Ed took (a) with (c) beside it. The deciding fact is how rare the case is: perfect separation exits at `no-ascent` and counts as converged, so a false flag is a numerically awkward judgment pattern, not a disagreement — worth *seeing* on the record, not worth acting on in the room. The auditor (§10) is the thing that should act on it if it ever recurs. Build: backlog entry 172.

**R-053 A shape is folded as the convenor's own sets, never as defaults** (Ed, 2026-08-27, plan-queue entry 166; v0.82). User testing found the founding hard: twenty-odd cards, some baffling to somebody who knows docs.vote only as a member of one document whose rules never moved. Ed's answer is one quill act before 📧 — 🧭 *What Type of Document Is This?*, four rungs: *a meeting* (hours), *a conference* (days), *ongoing* (perpetual), *custom* (today's founding, untouched) — with the shapes as one table in one file (`packages/constitution/src/shapes.ts`). The ruling that matters for §9.0a is **how** a shape lands. Three readings were possible. **(a) Defaults**: the shape pre-fills the radios and the founder commits each card — which is the anchoring the no-defaults ruling (Q511) exists to refuse, and it shortens nothing. **(b) A delegated bundle**: the shape hands the shaped settings to the room — which is the exact damage of 2026-08-21, ten questions collecting from the instant of creation. **(c) The convenor's own pre-start sets**, taken: the save emits an ordinary `setting-set` per shaped value at the birth's own `t`, so every setting stays convenor-held with both powers and its question shut, nothing is delegated, and — a set before the start being owed nothing (§9.6a) — nobody is owed an OK for any of it. Provenance keeps (c) honest: `SettingView.shaped` says a clause is *given by the shape and untouched, before the start*, the band prints *As for a meeting.* while it is, and 🍾 states the diff (*except 👥 Quorum, which the Founder changed*) rather than the list — so the founder sees the choices once as choices, and which of their touches mattered. The shape is not a setting and leaves no object behind: the card is never re-offered, no motion about meeting-ness exists, and the marks vanish as clauses are touched and at 🍾. Ed's fixed points, not reopened: 🪪 🤝 🎩 ✋ 🖼️ 📄 🍾 are unavoidable and never shaped; 💤 is hidden outright for a meeting and a conference (a decision nobody has — the 🎲 rule) and about 30 days for ongoing; 🤖 off; 🌡️/🪜 a ramp 60→80, fixed 80 for ongoing; 👥 as a share; ⏱️'s drip in minutes / hours / days; the shape is ⏰'s *unit* and never its answer — *never* being what *ongoing* already said. The numbers in the table are placeholders until `alpha-preset` sweeps each shape (Q960); the meeting's ⏱️ is the sweep's own *ALPHA PRESET*, the one cell with evidence behind it. Deliberately not built: wiring ⏩ or the phase ladder to a shape (both exist to reach states the shapes avoid), a per-shape ⏰ default, a schema bump (an optional field on `created` replays byte for byte), and the stranger's door printing provenance (a stranger is owed the rule, not its history).

**R-052 The cooldown belongs to the operator** (Ed, 2026-08-27, closing Q946; v0.81). Its value was already settled — one minute, Ed 2026-08-21, inside §4.2's five-minute ceiling — and built as `DRAFT_COOLDOWN_MS`, read at boot. The question was its home. **(a) An operator env knob**, as built: engine tuning that re-rates no past decision, so correctly outside the catalogue and the record; the cost is one value for every document on the host and a restart to change it. **(b) A birth-time engine parameter** beside the grant and the drip — per document, chosen once, never afterwards — which is what lets a fifteen-minute room and a six-month charter differ, as they plainly should. **(c) A real setting on ⏱️** — rejected on §4.2's own ground (pacing is a legibility device, not a decision) and listed only so the rejection is on the record. Ed took **(a) now, (b) when a second kind of room exists** — and the shape table of entry 166 is the likely moment, since a shape already carries everything downstream of the window. Nothing about (a) is undone by (b): the env value becomes the default the shape overrides.

**R-054 A later stricter rule wins for reveals** (Ed, 2026-08-28, closing Q993; v0.83, reversing R-050's symmetric half of the day before). R-050 settled that a proposal keeps the disclosure rung it was made under, and argued it only for a rung that **loosens** — a proposal made in anonymity is never named later. The other direction was put to Ed twice. On 2026-08-27 he answered *symmetric — the rung at submission governs, always* (Q948, v0.80); on 2026-08-28, reading the edge stated plainly — *a proposal made under names at the close in a room that later tightens to nobody's name, ever is still named at the close; honour reveals something the later, stricter rule would have hidden* — he answered **asymmetric**. The later answer stands. The rule is now the protective conjunction: a name appears only where the rung at submission **and** the rung standing at the reveal both allow it. The argument for it is the consent rule's own, and the one R-050 already used against *follow*: a room's protection of its members is not something an earlier rung can spend, and *most protective wins* is how every other blind answer on this surface aggregates. What it costs is the honour reading — an author who accepted being named does not get to be named once the room has decided otherwise — which is the smaller loss, since being named is the exposure and not the entitlement. **The code does not do this yet, and the disagreement is deliberate**: `authorVisible` reads the stamp on the candidate and never the constitution's current value, which was exactly right under the symmetric rule and is now half a rule. The build — `authorVisible` taking the standing rung as its second input, the §3.5a server tests, and the 🥂 card's *names* line — is a separate decision, filed as a backlog entry. Recorded per the spec-pass rule that where spec and code disagree the disagreement is written down.

**R-055 *At the end* is the word for the close, on both ladders** (Ed, 2026-08-28, closing Q994 and Q995; v0.83). §3.5a's judgments ladder read *revealed after the decision they contributed to*, which describes a per-decision reveal the mechanism has never done — the rung means the end of the document, and the surface label was changed to say so in batch O. The spec sentence that produced the ambiguity is now the end of the document too. Ed also settled the word: **at the end** everywhere, so the 👤 authorship ladder's *Names at the close* becomes *Names at the end* and a member reading both cards meets one word for one moment. The surface half is copy across two ladders and every site that quotes a rung label (T5, one label per rung everywhere), and is filed as a backlog entry rather than built here. The **genuinely** per-decision rung — names shown as each decision closes, decision by decision — was asked at the same time and is a change to what the sealed record shows and when; it is filed for a plan of its own rather than folded, because §3.5a and the record's own semantics both move with it.

**R-056 A shielded Text parks an adoption, it does not revert one** (Ed, 2026-08-27, answering the feasibility pass on Q440; v0.85). Q440 built the road to a shielded Text and the refusal at the end of it was hollow: `reportAdoptions` opened the 👑 question only *after* engine-core had emitted `adopted` — the version bumped, the document rewritten, every rival rebased onto the new text — so answering *reject* wrote `rejected` onto a record beside a clause that stood anyway. The largest single power in the design was a note in a log. Ed's ruling is that **it is never adopted**: a race that clears bar and floor while the convenor holds 🛡️ on the Text **parks awaiting assent** — races in flight are not rebased, the ramp keeps running, and the candidate leaves every feed. **Accept** adopts it then, through the ordinary `adopted` path, rebasing as usual and on the confidence and the bar it cleared at the moment the room decided rather than at the convenor's convenience. **Refuse** retires it as a failed proposal — refund 0, carrying *Proposal refused by ‹name› 🛡️*, which is what its author reads on their sealed record; a refund would price a refusal as a withdrawal. The rejected alternative is **adopt-then-unwind**, which is what the code did: in an event-sourced engine there is no rollback, only a compensating event, and *un-adopt* cannot be written as one — the versions are a stack, the rivals have been rebased onto a text that would have to be un-published, and every judgment cast since was cast against it. Parking is the same decision taken one beat earlier, where it costs nothing. Its **consequence, which is this build's own reading and not Ed's ruling, is one at a time**: while a candidate is parked the batch performs no text adoption and makes no second park, and a batch parks at most one text leader. That is what lets accept be the ordinary adoption with no new machinery — nothing has changed the text since the park, so the parked patch is still against the current version — and the alternative, letting the text move under a parked candidate, needs a rebase path for parked candidates and a story for one whose rebase fails, buying nothing a convenor's answer does not already unblock. **The invariant is stated in full at `sweepAdoptions`, because it binds every door and not only that one**: no text adoption of any kind while a candidate is parked. `adopt`'s rebase loop takes only `live` candidates, so a parked one is never rebased and cannot be made safe after the fact — a second adoption door added later (a convenor's ✒️ on the Text, say) that does not honour the rule applies hunks against a version they were never written for. The mechanism costs the engine one switch and no knowledge of powers: the host declares `textAssent` through `ConstitutionAmendment`, and it declares *whether assent is owed* rather than the raw power, so a lapsed crown grants by itself and lapse auto-passing falls out of the mechanism instead of needing a second rule. A question pending at the close fails closed (§4.6) and its candidate is recorded *undecided* at refund 0 — a text leader clearing the bar in the final batch included, which is neither adopted nor left waiting for an answer nobody has time to give. What the room is told while an adoption waits is filed open (Q1015): the surface says nothing today, and the build deliberately adds nothing rather than inventing copy. **The one-at-a-time consequence was narrowed to *per footprint* by R-100** (Ed, 2026-09-09, Q1179): any number of parks stand so long as none overlaps another, and *never rebased* became *never rebased across its own span*.

**R-057 What the convenor carries past the start is 🍾’s own question** (Ed, 2026-08-27, backlog entry 158; Q1018; v0.86, overturning R-043’s first clause). Rule 8 read *the start lays down both powers on the Text*, and Q387 had put a reserved text out of scope from creation: every change needing the convenor’s assent to merge is how suggested amendments work in many editing products and against the spirit of this one. What changed is not the objection but where the question is asked. **🍾 is the one moment at which *what do you carry across the line* is actually put to the convenor** — eighteen power-holders and two powers each, and until now nothing surveyed the whole set: powers came off one tab at a time, and the card stated a fixed batch of which the first line asserted that the hand came off the Text whatever the convenor wanted. Rule 8 now reads **the start lays down whatever 🍾 was not told to keep**, and the Text is a switch on that card like every other zone. The objection survives as the **default**: not told is laid down, so the 📄 switch starts at *lay down* and a convenor who never opens the table gets exactly the document they got before — a reserved text is a thing they must ask for, in the one place the asking makes sense, and never a thing they drift into. **Rule 1 is untouched, and is why every other zone starts at *keep***: a power is born the convenor’s, so a switch sitting at *keep* asserts nothing new, where 📄’s position is rule 8 as amended read literally.

Three consequences worth stating. **This is the post-start hand-over, not a pre-start release** (rule 2): the batch rides the `constituted` event and is folded *after* the constituted-at time is stamped, because before the start a release that would leave neither power on a delegable setting **is delegation** and opens a blind founding question — up to ten of them, every one of which then refuses the start (§9.0b, R-045). A card that laid a zone down one key at a time would seal the document it was starting. **The act is the batch** (R-044): one command carrying one list at one time-stamp, so what a press lays down is one news entry and one acknowledgement however many powers it moved; N acts at N stamps would be N cards. And **the answer only ever lowers** — a switch returned to *keep* means *lay nothing further down here*, never a reclaim, because a power promised away on a setting’s own tab comes back only where it went from. The same rule is what makes a zone read **mixed** rather than *kept* once part of it has gone: two controls over one power must not disagree about it. Rejected alongside: per-setting switches on the card, which is the tabs again with a worse view of them; and zones editable from the browser, the zones being Ed’s and a source literal one table wide. **R-043 stands for Q388** — delegating powers to individual members, transferring or inheriting the foundership — and loses only its first clause.

**R-058 The Founder's pen on the Text is a route, and its amendment passes the instant it is submitted** (Ed, 2026-08-27, backlog entry 160; Q1020, Q1021; v0.87). What it replaces: the Text was **the one setting with no route at all**. §9.7.1's row read *none — changed by drafting*, X1 said so in the exceptions, and the code agreed all the way down — `setSetting` refuses `startingText` outright, `confirmStartingText` refuses after the start, and engine-core changed the document in exactly one way, a candidate winning a race at the adoption threshold. So a convenor who kept ✒️ on the Text under R-057's new switches could do nothing whatever with it: a power that named an act the system had no verb for. Rule 5 already said *where the convenor holds ✒️ the route is the pen*, on a setting of either kind; this is that sentence reaching the last setting it had not reached, and X1 survives, narrowed to what it was always really about — the Text has no **motion** route, a pen amendment never being raised, and a motion button on 📄 still being a second door to the same room.

The mechanism is a **direct adoption**: submitted and adopted in one act, no stake, no race, no judgment. It could not honestly reuse `adopted`, which requires a posterior and a threshold and has neither here — there was no room to have been confident. What it must not lose is the **ground shift**: a race in flight on the same footprint is rebased, not killed, exactly as under an ordinary adoption, which is why both doors run one shared loop rather than two that agree today.

Three things follow rather than being decided. **A pen adoption opens no 👑 question**, even where the convenor also holds 🛡️ on the Text: R-056's question exists so a shielded convenor can refuse *the room's* adoption, and asking them to assent to their own act is asking them twice. **A lapsed crown does not act**: lapse grants assent (R-056's road) and performs nothing, which is `doorPen`'s rule and not a new one. And **nothing is decreed while a candidate sits parked awaiting assent** — R-056 rests on the invariant that nothing has changed the text since the park, and a decree is exactly what would change it; the parked candidate is not live, so the shared rebase loop skips it, and teaching the loop to rebase a parked patch would need a story for a rebase that fails and re-opens the question R-056 declined. The refusal is R-056's own one-at-a-time rule reaching the second door rather than a new rule, and it is honest: a convenor holding both powers already owes the room an answer.

**Where the acknowledgement is served — amended by Ed, 2026-08-29** (decision D47, answering Q1021; Q1034; v0.89). The rule above says the amendment is owed an acknowledgement and does not say where, and the question is not incidental: the module's acknowledgement channel is `ok-owed`, whose payload is a list of **setting ids**, so the cheap answer was 📄's own key `startingText` — one owed OK however many amendments it stood for, reading *the Founder changed the text*. **That is what the R-058 build shipped**, and the record is worth keeping plainly: the build put the question to Ed, did not wait on it, and took its own recommendation, naming the richer answer and leaving it unbuilt behind a number. Ed has ruled the richer answer: **a news card beside the amended clause**, in the charter rail against the paragraph that moved, **one card and one OK per amendment**.

What the ruling buys is *which paragraph*, which 📄's key cannot say — and the cost of not saying it is not small on a document of any length: a member told *the text was amended* has the whole charter to search. It costs an owed kind of its own (`amendment-owed` / `amendment-ok`, `oweAmendment` / `ackAmendment`), modelled on entry 162's release batch — this project's one worked example of an owed kind that is not a setting — but **deliberately without its batching**: 162 groups because one press of 🍾 lays down thirty-four powers that belong to no clause, where one ✒️ commit is one amendment at one place and the card's whole job is to name that place. And 📄's owed OK is **removed** with it rather than kept as a fallback, because the moment the card exists the OK is a second home for one fact (STYLE **T36**).

One thing follows rather than being decided, and it is a persistence rule the reversal happened to enforce: the owing moves from the `text-amended` **fold** to `recordTextAmendment`, on the **command path**. `replay` calls `apply` directly while `emit` pushes to the log, so an owing performed in a fold appends events to every session that replays that log — the log growing every time it is read. Entry 162 stated the rule; the first reading of this one broke it invisibly, since `rollingHash()` returns the last *pushed original* entry's own hash and cannot see an appended event. The replay test now asserts the log's **length** as well as its hash.

Where a member **acknowledges** it (Q1021): 📄's own key, through `oweOks`, which collapses a run of amendments into one owed OK reading *the Founder changed the text*. The richer answer — a news card on the amended clause itself, in the charter rail beside the paragraph that moved — is a new owed-kind and a surface of its own, and is filed rather than built. The record names the **office** and never the wording: a document's text is pages long, and the document is its own record of what it says.

**R-059 Every hold on the surface is one second, and gravity is said by what flies** (Ed, 2026-08-29, backlog entry 206, QA on the commit-gesture switch of backlog 184: *the click gesture is good but all "hold" times should now be only 1 second long*). What it replaces is a **graded ladder**: 🪶 and ✒️ one second, the pencil's ✏️ three, the 🏛️ `assembly-press` ten, on the principle SURFACE §7.2 stated outright — *an act that spends something out of a wallet is held, and its gravity is the length of the hold*. The idea was sound and the numbers were not. A hold is a gesture somebody performs with a finger down, and three seconds of it is long enough to feel like a fault in the page rather than a ceremony; ten is long enough that a member who means it waits out a progress meter for an act they had already decided on. The click gesture, adopted a day earlier, made it worse rather than better: under `click` the length is no longer time the member is *spending*, it is time they are *waiting*, with the control inert and nothing to let go of. **The ruling is one number, 1000 ms, for every hold on the surface**, and Ed asked for it to be one constant rather than five agreeing literals — `HOLD_MS` in `design/session.js`, exported as `SESSION.holdMs` beside `SESSION.gesture` and read by `design/session-view.html` for its wallet commits and its assembly, so the five that disagreed by a factor of ten cannot drift again. `spec-check`'s `checkWallets` asserts every numeric *hold ms* cell in both of §7's tables against it, which is stronger than the three per-literal checks it replaces.

**What it costs, stated plainly: the ladder stops saying gravity by length.** That was a real signal and nothing replaces it exactly. Two things carry it instead, and both were already there. **What flies** — a feather, a pen, the cork out of the button into the document title, the pencil the width of the screen, and for 🏛️ the whole room convening into a ring around the control — which is a difference in kind rather than in degree, and reads at a glance where four durations only read in comparison. And **the words on the control**: 🏛️ says *Ask all members* where ✏️ says *Propose*. Two consequences follow rather than being decided. The pencil's **quarter-way floor comes down with the hold**, 864 → 288, because the floor is a quarter of the *distance* and `cubic-bezier(.45, .05, .3, 1)` reaches a quarter of the way at 0.288 of its duration whatever that duration is — 864 of 3000 and 288 of 1000 are the same point on the same arc; the throw's **minimum rate of 1.8× stands**, Ed naming it, and at the new floor a standing-start push runs at exactly that minimum where it used to run at 5.4×. And the assembly's **stagger compresses to fit rather than being dropped** — *the ring is the meter*, so the seats now spread over `HOLD_MS − 80 − 320` (the lead-in, and the seat's own 320 ms transition in `design/setup.css`) so the last one lands as the hold ends; a ring that completed early would stop being the meter, which is the one thing the assembly is for. Untouched deliberately: the commit gesture itself, the flights that are not holds (`REFUND_MS` 640, each grant's inbound 640, `STORM_MS` 900), and `nudgeHome`'s `rewind` 4, `push` 160 and `hang` 90.

**R-060 A park with no convenor auto-passes, and a vacant seat holds no shield** (Ed, 2026-08-29, answering the batch review's deferred finding F7 on the R-056 build; v0.88). R-056 made a shielded Text **park** an adoption rather than revert one, and rested two things on that park being answerable: `sweepAdoptions` performs no text adoption of any kind while a candidate is parked, and the 👑 question is served only to the convenor — `crownTasks` and the server's `awaitingAssent` are both gated on the reader being them. The one thing that answers a park nobody is left to answer was the **crown lapse auto-pass**, and that fires once, on the transition *into* lapse, inside `runClocks`. So a seat vacated **after** the park — the convenor's own member row removed by a carried removal motion, which was then the one door that did it — served the question to somebody who had gone, left no clock that would ever auto-pass it, and blocked every text adoption in that document for the rest of its life. The room's drafting stopped permanently and nothing on any surface said why. Ed's words: *a park with no convenor auto-passes, the way a lapse does.* The rejected alternative was **leave it standing and let the close resolve it** — §4.6 already fails a pending question closed and records its candidate *undecided* — which is technically a resolution and is the wrong one: it prices a convenor's departure as a permanent freeze on the document's text, and the document may have no close at all, a perpetual one having only the freeze line. Auto-passing is the same judgement lapse already makes, that a seat with nobody in it abstains and abstention on an assent grants.

**One half of this is the build's reading and not Ed's ruling, and is marked as such: the shield reads *down* while the seat is vacant.** `textAdoptionNeedsAssent()` already carried `!crownLapsedFlag`, and R-056 chose to declare *whether assent is owed* rather than the raw power to the engine precisely so a sleeping crown falls out of the mechanism instead of needing a second rule; a vacated seat is that fact in a stronger form — nobody asleep, nobody at all — so the same clause covers it. Without it the auto-pass clears the park that is standing and the very next race parks against an empty seat, which is the finding again one adoption later. The predicate is `convenorSeatVacant()`, a claim about **state** — the convenor's own member record present and `removed` — never about which event produced it, so it reads the same after a replay as after the act — and §9.6a's free resignation duly arrived with no second rule and no second predicate. `resign` refused the convenor outright when this was written, pointing at a pre-start 🎩 untick that is no route at all once 🎩 is locked; entry 248 put that spec/code disagreement to Ed, and under the reading this build took the spec was right and the code moved: after the start a member convenor resigns like anybody, `resign` calls `crownSeatVacated` as the carried-removal arm does, and the seat it leaves is this vacancy. Before the start the refusal stands, since a pre-start resignation would mark the record `removed` and manufacture a vacant seat in a document that has not begun. **A convenor who is not a member is not a vacant seat**: unticking 🎩 deletes the record from the membership altogether, and exception X15 says a convenor with no powers and no membership is still a person the room may restore powers to — the clerk convenor is the ordinary founding and their shield stands.

The mechanism reuses what a lapse already emits: `crown-question-auto-passed`, whose fold sets `auto-passed` and treats it as accepted and whose bridge arm already becomes `engine.assent(t, parked, 'accept')`, so the constitution log gains no event kind and the replay case stays bit-identical. It does **not** emit `crown-lapsed` — a vacancy is not a lapse, `crownLapsedFlag` being about a crown that may wake up again where a removed member does not return to the seat. And it takes **text** questions only, which is a deliberate narrowing of the lapse loop rather than a copy of it: applying a carried invitation or removal without assent because the convenor left is a governance consequence nobody has ruled on, and such a question blocks nothing while it stands, where a parked text adoption blocks everything. That asymmetry is filed open as **Q1033**, with the wider version of it beside — `crowned()`, `holdsAnythingReserved()`, `reservedTarget()` and the doors' pairs all still read a vacated seat as held, and this build changes exactly one of them, because exactly one of them blocks the engine.

**R-061 Proposing no longer forfeits the pair, because judgment is blind anyway** (Ed, 2026-08-16; SPEC v0.16, §3.3 and §3.5). The forfeit priced a peek at mid-flight state: drafting against a live race once showed you something the card did not, so the pair you had been asked about was surrendered as the price of looking. §3.5's blind-composition rule removed the thing being paid for — drafting against a race still in the judgment stream now reveals the text and nothing else — so the charge had nothing left to charge for. What the drop buys is the most informative judgment the system can collect: an author who drafts still judges the pair they were asked about, and may well end up preferring somebody else's draft to their own, which is a judgment made against interest. The dependency runs one way and is worth stating: §3.5's constraint is what allows the forfeit to be dropped rather than merely relocated. Weaken the blindness and the forfeit's argument returns.

**R-062 The author's preference is derived, not recorded** (Ed, Q245; SPEC v0.18, §3.3). *While a candidate is live, its author prefers it to the current text* is what a live candidate means — an author who stopped preferring it would withdraw it (§3.3a) — so the preference is computed against the incumbent as it now stands, every time, and never goes stale. Assuming it saves a pointless question; assuming any other would be fabrication. It was built the **recorded** way first, and that was the fault worth remembering: a recorded comparison is stamped with the ground it was cast on, and a race's ground widens whenever a new candidate joins it, locking everything cast on the old one (§4.4). A human recovers, because the pair is re-served and they answer again — nobody re-asks an automatic vote. So the first author into a contested area lost their voice the moment anyone joined them while later authors kept theirs, and the adoption floor became a function of submission order. The fault was modelling a standing fact as a dated one. It counts toward the floor because an author is a voice on the roster, and excluding them would systematically under-count the preferences of the people who cared enough to write.

**And therefore the pair is never served** (Ed, 2026-08-29, backlog 253, SPEC v0.94). *I seem to be able to vote on my own text amendment proposals. I think that I should automatically be counted as preferring proposals that I have made vs the status quo.* The counting half was already this ruling; what was missing was the asking half, and the two are the same sentence read forwards and backwards. If the preference is derived rather than recorded, the engine holds the answer before the card is drawn — so serving the author their own candidate against the incumbent asks a question the engine wrote itself, and whatever they press either agrees with it or is overridden by nothing (an explicit judgment *does* still win, which is the point: it is a second answer to a settled question, offered for no reason). The limit is exactly where the derivation stops. *By proposing you only know that it's preferred to the status quo* — nothing is implied about which of two challengers is better, so a **rival** pair of the author's is a real question and is still asked. The exclusion is the router's (`bestPairFor`'s shared scan and `explorationCard`, the two doors that serve against an incumbent) and never `judge`'s: the ruling is about what is put in front of somebody, not about what the engine will accept from a host that asks directly.

**R-063 Preference is not measurement** (SPEC v0.18, §3.3, beside R-062). A derived preference is a real preference — it feeds the ranking and counts as a voice — but no sampling effort was spent on it and its answer was known in advance. So the line runs between two questions the mechanism asks separately: everything asking *what does the room prefer, and how many voices are in* counts it, and everything asking *have we measured this enough* does not — the deadlock test (§8.3), the rival-pair gate, the per-race comparison counts in the record, and the performance a refund pays on (§7). The refund is the case that forces it: one favourable comparison already reaches the refund cap, so if an author's own preference were performance, submitting and retiring would pay back more than the stake with nobody else involved.

**E = 1 is the one case measurement cannot be asked of anybody else** (Ed, 2026-08-29, backlog 253, SPEC v0.94). *Have we measured this enough* is a question about a room, and in a document of one there is no room to ask it of: the only person who could answer is the author, and since the pair is no longer served (R-062) their answer can never arrive. The gate would then mean *never*, which is not a stricter reading of the rule but the abolition of the document. So at E = 1 the derived preference is both the floor and the room, and the proposal adopts on submission — under the bar and the cooldown like any batch, and still under the ceiling a room of one can reach (Q840: 0.798, so a bar of 80 refuses it). **Q835's exception is overturned by this.** That ruling met the same problem — a sole member with no card — by serving them the self-judgment so their explicit answer could clear the gate; `design/DECISIONS.md` records *excluding the author from `feed` and letting the floor carry the adoption* as the reading it rejected, for fear of a document that adopts on nobody's judgment. Ed's ruling is that at E = 1 nobody's judgment is the only judgment there is, and that asking somebody a question whose answer you already hold is the worse of the two. Nothing above E = 1 moves: two rival authors still meet a floor of 2 on derived preferences alone and still wait for the room.

**R-064 Withdrawal is retirement, and a ground shift** (Ed, 2026-08-16; SPEC v0.16, §3.3a). Judgments already cast stay in the log and in the ranking because other candidates' estimates rest partly on comparisons against the withdrawn one, and removing a node degrades everything it touched. The stake refunds in full because charging for withdrawal would price exactly the behaviour worth encouraging: an author who must pay to tidy up will instead leave a candidate they no longer believe in standing in the race, consuming judgments that could have gone elsewhere. The ground shift is what makes withdrawal **safe to offer**. A candidate splitting a camp could otherwise be pulled at a chosen moment to change which of the survivors wins; re-serving on the new ground means the room decides that question again rather than having it decided by the timing of a withdrawal.

**R-065 Why disclosure is constitutional, and what anonymity rests on** (Ed, 2026-08-17; SPEC v0.19, §3.5a). The section was revised the day it was written: it had said the disclosure family was never the convenor's and the founding was therefore compulsory. Ed's ruling is that the convenor chooses which, and may delegate the numbers and the disclosure independently — a convenor may fix the quorum and still hand the room its own privacy, or the reverse. It is constitutional rather than cosmetic because **disclosure changes what the mechanism measures**: a room that can see whose text it is can prefer the person, which is §3.3's *preference is not measurement* problem again in the form of deference. And in some rooms that is fine or even wanted — a standing committee that knows each other, a body whose members are accountable for their positions — which is exactly why the answer belongs to the roster rather than to this spec. The price of making the founding optional is stated where it falls: where the **roster** decides, anonymity is structural, sitting at the top of the privacy lattice so that it holds unless every member is content with more and a single person keeps the whole document unnamed; where the **convenor** decides, that guarantee is not available, anonymity is a default like any other, and a convenor may open a document in which members are named without their having agreed to it. Worth a convenor knowing they are paying it.

**R-066 Elective signing is two rungs, and it is a costly option** (v0.70, beside R-047; SPEC §3.5a). Signing was its own setting until v0.70, with rungs *nobody · each · everybody*. *everybody* was the same fact as authorship **public**, said a second time on a second card, which made *nobody named* and *everybody signs* both settable at once; *nobody* and *each* are what the two pairs of authorship rungs now say. The ordering is the argument: a document that names nobody unless they volunteer is more private than one that names everybody at the close, and one that names everybody at the close but lets you volunteer sooner is more private than one that names you as you propose. The cost is real and is stated rather than designed away: in a small roster an unsigned candidate among signed ones says something about its author, so a document that allows the choice is not neutral between the two. Ed's ruling (2026-08-17) is that this is a cost for the roster to weigh, not a reason to withhold the option.

**R-067 *End* means the end of everything, not the close of each decision** (Ed, 2026-08-28; v0.83, SPEC §3.5a). The judgments ladder's second rung read *revealed after the decision they contributed to*, which read as a per-decision reveal and was never that: the rung means the end of the document, and the surface label says so. A genuinely per-decision rung — names shown as each decision closes — is a change to what the sealed record shows and when, and is filed rather than built. Live disclosure is not on the ladder at all: §8.3's no-standings rule is not a privacy preference but the thing that keeps judgment blind while it is still being collected.

**R-068 The hot set is three, and v / c_p does the division of labour** (calibration sweep 2026-08-13; SPEC v0.7 and v0.16, §8.1 and §8.3). The hot set was ~6 and is ~3: depth of evidence per race beats breadth at small-roster scale, the best welfare and the lowest variance of any setting tested. The routing formula needs no further rules to allocate work, and the consequence is the reason it was chosen: abundant cheap judgment sees everything, including the exploratory tail; scarce expensive judgment only ever reaches the top of its list, so its whole budget lands at decision margins. Frequent participants discover the edge cases; occasional participants cast them. At population scale the same formula prices the participation long tail instead of fighting it. Also recorded here rather than in the spec: at small rosters an author's own derived preference (§3.3) is a large fraction of a floor that may be three or four — worth watching.

**R-069 A salience diagonal is served, not offered, and only when prioritising is the work** (Ed, 2026-08-17; SPEC v0.20 and v0.21, §8.3a, replacing the flat ~1-in-10 rate). Prioritisation is only worth anybody's attention when there is genuinely too much to do; below that, asking which of two questions matters more is asking which one is the hot one, and the answer is already on the surface. Hence the **volume** gate: below E live questions the salience model simply goes uninformed, which costs nothing it was going to be used for — salience sets routing weight, bounty order and backlog order, and none of those has work to do in a document with fewer open questions than members. Hence the **audience** gate: everyone else's attention is better spent judging, which is the act that resolves questions rather than ordering them, and this is not a heuristic about who is keen — a diagonal costs a member with an empty queue nothing and costs a member with a full one a judgment. **Served, not offered**, because asking first (*would you like to help prioritise?*) needs somewhere to put the question and the answer is not in doubt. **And it terminates**, because an empty queue must not become an endless stream of prioritisations. **No completion state**, because salience is a continuous ranking with no threshold to clear and no quorum to ratify — which is precisely what allows it to be advisory — so a progress bar on a diagonal claims a finish line that does not exist.

**R-070 A second gate reopens the diagonal stream, and its bias is known** (Ed, 2026-08-17, answering the timing objection to R-069; SPEC v0.21, §8.3a). The audience gate starves salience of exactly the data it needs *while a session is busy*, because that is when nobody has an empty queue. So above 2E live questions the room is saturated enough that ordering the work is itself the valuable act, and diagonals return to the judgment stream at a low rate for everybody — the old ~1 in 10. The known limitation, recorded rather than corrected: below 2E salience is measured on a biased sample, the members who reach the end of their queue, who are systematically the most active. The ranking this feeds never touches adoption (§4.2), which is what makes the bias tolerable, and the alternative — spending scarce judgment on ordering instead of deciding — is worse.

**R-071 Rival pairs ask a conditional question, and it cannot be recovered elsewhere** (Ed, 2026-08-14, resolving Q48; SPEC v0.9, §8.3). *If this text changes, which change is better?* Its signal is precious precisely because it cannot be recovered from incumbent comparisons. Two serving rules follow. Cards never offer "keep the current text" on a rival pair, because a pro-incumbent judge expresses that on incumbent-involving pairs, which the router owes them. And rival pairs are served sparingly until the race shows evidence that at least one challenger plausibly displaces the incumbent: there is little decision value in finely ranking challengers that are all losing to the status quo, their order mattering only to the backlog.

**R-072 Deadlock disclosure is a rule about evidence before it is a rule about clarity** (Ed, 2026-08-17; SPEC v0.23, §8.3b). Deadlock detection is a property of the race; disclosure of it is per-participant, and withheld until the race has nothing left to ask that participant. Judgments from members who have not yet judged the race are exactly the ones most likely to separate its candidates, so **a deadlock disclosed before the room has finished judging is disclosed early** — the old behaviour told a member the race was stuck before they had contributed the one thing they could still contribute. It also keeps the disclosure ladder consistent with §3.5: what a participant is shown is a function of what has been asked of them and what they have given, never of what the machine knows. There is no public board that could disagree with the rule, the bounty board having been retired the same day (Ed, 2026-08-17: *I don't think we need the bounty board anymore; the sidebar-queue does its job*); its score survives and does the ranking it always did, in each participant's own queue.

**R-073 The quorum and the formula floor are one term, and the room's number can only raise it** (Ed, 2026-08-18, closing Q338; SPEC v0.48, §4.2 and §8.2). Before the review of 2026-08-18 the spec carried the room's quorum and the statistical floor under different names in two places, and the engine implemented only the formula — so a room that had chosen its own number was adopting at a floor it had never agreed to. F = max(Q, min(⌈E/3⌉, F_max)) is the two folded into one term: the formula half is a statistical minimum the room's number can raise but never lower.

**R-074 The cooldown paces the room; the ramp stabilises the document** (Ed, 2026-08-19, closing Q399, and 2026-08-27, closing Q946; SPEC v0.8 and v0.81, §4.2, beside R-052). Ed's words on the kind of thing it is: *effectively a convenience rather than a constitutional decision, part of the UI*. Batching is what keeps it cheap — since everything cleared lands at each tick, no decision waits longer than one cooldown however busy the document is, where the serialized form (one adoption per cooldown, globally) queued a busy document's sixth decision for half an hour, which is why it went. It must stay short (≤5 min): the rising threshold is what makes the session stabilise, the cooldown only paces how changes land for the people in the room, and rationing adoptions by time starves the chains of sequenced adoptions an interconnected document needs (calibration sweep 2026-08-13 — 15–30 min cooldowns cut welfare and halved resolution; re-verify under batching when the sweep is next run). The two back each other up against hasty adoption, each looking redundant while the other is intact, which is why neither may be weakened without the other being checked (sweep: no cooldown plus a softened ramp over-adopts and churns). A cleared race is not surfaced as waiting because there is no "cleared and cooling" state to show — the batch evaluates fresh at its own moment, a leader that slips back before the tick simply not adopting — and deriving one would be a standings reveal (§3.5), telling the room what has won before it lands and exactly how long remains to counter-judge it. On the operator home, R-052 carries the alternatives; when a second kind of room exists (a six-month charter beside a twenty-minute meeting) it becomes a birth-time engine parameter beside the grant and the drip, and nothing about the operator value has to be undone to get there.

**R-075 The ramp tracks irreversibility, which is why it starts at the start** *(superseded by R-117, v0.128 — the ramp is retired)* (Ed, 2026-08-18, Q342; SPEC v0.7 and v0.49, §4.3). Nothing is irreversible before a judgment can exist, so a founding that takes two days must not eat two days of ramp: the ramp runs from the moment judging opens (§9.0b), not from creation. The same argument settles both directions of a moved close. Re-stretching the original ramp would drop the current bar, and a bar that fell because time was added would let a change through at a confidence the room had already moved past — hence *a bar never jumps because timings changed*. And the ramp is why no proposal deadline is needed: an early adoption can still be challenged within the session, a late one is permanent, so the outcome is never a surprise; early low-threshold adoptions give a distributed window visible motion from its first hours, and late activity self-limits, a near-unanimous fix clearing T_end where a 60/40 preference cannot. Unscrutinised text is protected not by the clock but by the adoption floor and the posterior itself. Rejected and deferred: an **evidence-clock** variant — the threshold as a function of total comparisons made, so the document stabilises in proportion to the judgment it has absorbed — left to the sim to explore.

**R-076 Revision is an open question's privilege** (Q50, Ed 2026-08-14, revised the same day; SPEC v0.10 and v0.11, §4.4). Judgments are living while their question is: a participant may freely revise any judgment on an open race whose ground has not moved, the new judgment superseding the old. A judgment locks when its context ends, and only then — the race seals, or its ground materially shifts, the old judgment standing as a locked fact about text that no longer exists. The same-day revision was from a flat *judgments are final* to this: finality belongs to the question, not to the act.

**R-077 Quorum-busting must be an act, never attrition** *(superseded by R-088, v0.99)* (SPEC §9.5, beside R-038). The quorum base is E minus abstainers and never the still-active remainder, because otherwise every departure would lower the bar and the last two members could adopt anything. Quorum-busting remains possible, as it does in every deliberative body, but it requires the explicit, logged act of signing out *holding* and cannot be disguised as attrition. Where abstentions leave a handful carrying the document, that is the outcome the abstainers chose over freezing it. And plain silence is not sign-out because §8.2's refusal to impute silence is the whole reason "didn't log in" cannot be read as "consented".

**R-078 🤖 leaves the surface; the `machines` setting stays in the catalogue** (Ed, 2026-08-29, plan-queue backlog 251: *remove 🤖 — it doesn't do anything yet and there are more important priorities. We can bring it back in the future.*; SPEC v0.91, §9.7.1). The `coherence-auditor` (§10) is a design with no drafter behind it: the founder was asked a question, and every answer to it — the founder's own, the room's blind collection, a later motion — changed nothing in any document, every shape in `shapes.ts` folding it to `{ enabled: false, budget: 0 }` anyway. A question whose *yes* changes nothing should not be asked, so the card, its `ORDER` slot, its clause, its composer lane and the member's ladder all leave. **The setting itself stays**: `packages/constitution/test/golden/founding.jsonl` and the live docs.vote logs carry `machines` `setting-set` events, `golden-log.test.ts` asserts bit-identical replay, and a catalogue deletion would be a log-format break for no gain. The catalogue count stays **18**, §9.7.1 keeps its row, and SURFACE §4's page-key map keeps `machines → machines`, which is what a replayed event maps through. Bringing it back is one `ORDER` row, the card literal and the composer lane: the setting, its value type (`{ enabled, budget }`) and its ordinary motion route all stand untouched. Deliberately not done: any change to `values.ts`, `shapes.ts`, the engine-core auditor hook, §10, or `dev-ladder.ts`, whose shuffler draws a value for every catalogue setting and whose seeded history is history.

**R-079 A name and a picture are offered at the founding, and never required** (Q981, Ed 2026-08-29: *amend the sentence to say the pair is offered at the founding and never required*; SPEC v0.92, §9.0c). §9.0c's *never asked at the founding* predates ✋ and 🖼️ being founding tasks at all: Q645 made them steps of `ORDER` — the fix for their having been dead on every live document, `ready()` being permanently true for both — and Q980 moved them to the save, where they are the first thing a founder meets after the birth. The sentence was therefore contradicted by the surface at its most visible point, and a reader of §9.0c would have concluded the pair could not appear where they now appear first. What the sentence was reaching for survives and is what the amendment states: the pair binds nobody, so neither card is a **condition** of anything — neither blocks the start, a blank name is Anonymous and a blank picture is initials, and both stay settable by their owner alone for the life of the document. Rejected: deleting the sentence outright (§9.0c would then say nothing about when the pair is put, and the *offered but never required* rule is exactly the thing a reader needs); and treating the surface as the error and taking the pair back out of the founding, which would reopen Q645 and Q980 together and re-create the bug Q645 fixed.

**R-080 A question nobody can be asked reads as answered at its default, once, with a line in the log** (Ed, 2026-08-29, plan-queue backlog 259; SPEC v0.93, §9.0b and §9.7.1, beside R-078). R-078 took 🤖's card off the surface and kept the `machines` setting in the catalogue so that every existing log replays; it left one case open. A document whose founder had **delegated** 🤖 before the card went holds a question that collects for ever: no member has a body to answer it with, the founder has no card to reclaim it on, `waitingWith` still returns `{ setting: 'machines', why: 'collecting' }`, and `begin` refuses — a permanent pre-start wedge, with the 🍾 readout naming the bare id `machines` because there is no card to take a title from. Ed's reading is that such a question reads as **answered at its default**, so the document can begin. The shape follows from where the facts live. **The catalogue carries the fact**, as `retiredAnswer`: "no card" is a surface fact and the constitution cannot read the page, but §9.7.1's row already records *surface: none* and `scripts/spec-check.mjs` already keeps its own retired list, so the module holding the value it resolves at is a third record of a fact the project has decided twice. The value is `{ enabled: false, budget: 0 }` — what every shape in `shapes.ts` folds 🤖 to anyway, so nothing about any document changes. **The existing `question-resolved` event carries the line**: no new event type, because a new type is a log-format break for a fact the old one states exactly, and the fold at that case already sets `collecting = false`, the value, and `settledBy = 'ceremony'`. `distribution` and `electorate` are **empty**, which is the record that nobody answered — the two arrays are what a reader of the log will ask about, and empty is the truthful answer rather than an absent field. `settledBy: 'ceremony'` is kept rather than a new word: it is a small stretch — the ceremony resolved it without asking anybody — and the alternative changes the log's vocabulary for every reader of every document to distinguish a case that the empty electorate already distinguishes. **The moment is 🍾** because a fold cannot emit (the resolution has to be an act, and the only act available is the start itself), and a sweep at load or at first sight would be a second mechanism doing what one already does. It sits past both of `begin`'s refusals and past `laidDown`'s validation, so **a refused Begin writes nothing** — a founder pressing 🍾 while a question that *does* have a card is still collecting must not spend the one resolution the retired question ever gets — and after it the question is no longer collecting, so nothing writes the line twice. **Existing logs replay byte for byte**: they carry no such event, the fold is untouched, and `golden-log.test.ts` passing unedited is the assertion of it. The readiness readout leaves the setting out either side of the start, collecting or settled, since a *machines · settled* row would be the same bare id the founder could still do nothing with. Rejected: a page-side auto-reclaim (`reclaim` then `setSetting` — two events, written by a surface that has no card for the setting, acting on the founder's behalf on load); treating the retired question as answered in the fold with **no** event, which changes what an existing log means with no line saying so; a new event type; and simply accepting that no live document has delegated it, which was the other reading and not the one Ed took.

**R-081 The start confirms whatever text stands where the convenor never did** (Ed, 2026-08-29 21:20, plan-queue backlog 204 / Q1080; SPEC v0.95, §9.0b's last sentence). Since 2026-08-22 §9.0b made 🍾 wait on a *confirmed decision* about the text, and the press that confirmed it was 📄's OK on the founding surface — an acknowledgement card standing in the constitution band *about* the text, apart from the text it governed. Backlog 204 retires that card: the text is the open card itself, with a read mode and an edit mode, and the founder's ✒️ on its row is the confirm, pressed as often as they like (entry 56, Q821's road). That leaves one case the OK had covered — a founder who never pressed ✒️ and presses 🍾 — and two readings of it. **🍾 stays held until one ✒️** keeps the module's readiness hold and makes the row's ✒️ a task after all, which is the card the ruling had just removed under another name; and a hold on the text is exactly the wedge the 2026-08-22 comment in `waitingWith` warned of in the other direction. **🍾 confirms whatever stands** is what Ed chose: the start is the decision, so the confirmation moves from a press to the start and §9.0b's *a confirmed decision, not content* is unchanged in substance. **Where it lives is `begin()` in the module**, not the page's flush (Q1081): `begin` is reached from the server's command whitelist and from the ladder as well as from the founder's press, and a start that lands without a confirm leaves `canPropose` false for ever — so the confirm is written by every caller's road, past both of `begin`'s refusals and `laidDown`'s validation, with the same `starting-text-confirmed` event the OK always wrote, carrying `''` because the module holds no column; the page flushes its column into `confirmStartingText` before it sends `begin`, so a founder's words are never confirmed empty. Existing logs replay byte for byte — a log with a confirm in it takes the `begin` branch that writes nothing. `text-unconfirmed` leaves `WaitingWhy`, since nothing produces it. Rejected: keeping the readiness hold and having the row's ✒️ mark the text seen (a task by another name); confirming in the server's `begin` handler from the provisional stash (a second road, and the ladder and the tests reach `begin` without a server).

**R-082 The quorum question collects the form too, and mixed answers resolve strictest at the settle** (Ed, 2026-09-02, Q1162 and Q1172, from the card review; SPEC v0.96, §9.0a — Q341 reversed for 👥 alone). Q341's rule — *delegate the decision, not the field* — kept the quorum's form (share or fixed count) with the convenor as pacing and collected only the number, which is why the 👥 card had two eyebrows. Ed's review folded the form into the option blocks: the member's answer card shows both forms, each with its own picker, so a member states a form as well as a number. That makes the two one question again, and mixed answers need a resolution rule where a scalar comparison had sufficed: every answer is evaluated against E as it stands at the moment the question settles, and the answer demanding the most voters wins — its form and its number both stand. A member who asked for a share can end up bound by a fixed count; the promise kept is *no looser than what I accepted, judged when it settles*. Q341 stands untouched for the threshold and the ramp. Rejected: reading the review's two blocks as layout only, with the form still the founder's (the other half of Q1162's fork — Ed chose the reversal); resolving each answer against E at answer time, which would let a roster change reorder already-given answers without their authors being told anything.

**R-083 The proposal wallet's grant and maximum are fixed at 3; the interval is the setting** (Ed, 2026-09-02, Q1160 and Q1161, from the card review; SPEC v0.96, §7). The ⏱️ card asked three numbers — the grant, the cap and the drip — and Ed's review reduced the sentence to one: *Members may make a new proposal ✏️ every [n] [unit]*. The mechanism does not move: `RateValue` keeps `{ grant, cap, dripMinutes }` so every existing log replays at its own values, the stake stays a flat 1 with refunds unchanged, and the accrual-cap clamp (Q949) is untouched. Every new document is created with grant = cap = 3 — the burst of three still works — and the consent order on a delegated ⏱️ reduces to the drip alone, the grant and cap comparisons being dead between equal values. The unit selector is display only: minutes are stored everywhere (the drip runs on real minutes — Q353), and the clause prints the largest unit that divides the stored minutes exactly (1440 → *every day*, 120 → *every 2 hours*, 45 → *every 45 minutes*). Rejected: removing the fields from `RateValue` (a log-format break for no gain); a stored unit (two representations of one number, and a conversion owed at every reader). **The clamp is moot under this ruling** (Ed, 2026-09-07, Q1276; SPEC v0.104): with the maximum fixed at 3 and no control to cut it, v0.80's clamp on a cut could never fire, so §7 stopped promising it and the ledger row went; it returns with the cap if the cap is ever a setting again.

**R-084 Anonymous is chosen, never inferred** (Ed, 2026-09-02, Q1164, from the card review; SPEC v0.96, §9.0c). §9.0c had read a blank name as Anonymous — *a name, not a gap* — which was the right rule for a card whose only control was a text field: the member had been asked, and silence in the field was the answer. The review rebuilt ✋ as an option-block card with *Anonymous* as its own block, and with an explicit route to anonymity a blank first block and the second block would have meant the same thing by two doors. The gap therefore stops meaning anything: the name block stays inert until something is typed, anonymity is reached by choosing the Anonymous block, and a member who has answered nothing keeps the default appearance (initials where the room knows a name, the anonymous mark where it does not) until they answer. ✋ still never blocks the founding queue (Q980) and still binds nobody (R-079). 🖼️ takes the matching shape with the anonymous picture as its first block (Q1165) — an anonymous picture still draws initials where the member has a name, `avHtml`'s own fallback. Rejected: keeping blank-is-Anonymous beside the block (two routes to one state, and a card that answers itself).

**R-085 🌡️ speaks the founder's language; the explainer carries the precision** *(superseded by R-117, v0.128 — 🌡️ and the explainer left the surface)* (Ed, 2026-09-02, Q1156–Q1159, from the card review: *use my language — if someone wants to understand they can read the explainer*; SPEC v0.96 recording only — the rules land in STYLE.md T15 and SURFACE §9). Four reversals in one card, all deliberate. The card now names the method (Bradley–Terry–Davidson, which is what the engine fits) and describes the threshold as a share of voters, where the number is a win-probability — the deliberate simplification, with `pairwise.html` at `/pairwise` carrying the exact account and `methodNote` keeping its link to it. STYLE T15 (*never the maths*) is amended for 🌡️'s method note alone, not repealed. *A number of my own* leaves 🌡️ (Q1104 (b) reversed for this card only — the pattern survives on 🪜, 👥 and ⏱️): three rungs, 90 / 80 / 60. And the ceiling notes leave (Q840's note retired): a founder of a one-member room can set a bar nothing can clear and the card says nothing — `/pairwise` covers it for whoever goes looking, and Q840's mechanism finding stands. 🥾's *a majority of members must agree ✏️* lands on the same ruling (Q1163). Rejected: precision on the card (the audience is a founder mid-founding, not a reader of the spec); keeping the ceiling notes as the one exception (the review cut them by name).

**R-086 The host paces nothing unless told to** (Ed, 2026-09-05: *the "update frequency limit" which stops the document changing too quickly is really confusing for testing, can we change it to instant*; SPEC v0.97, §4.2, beside R-052 and R-074). What prompted it was a finding as much as a wish: R-052's one minute had been built as `DRAFT_COOLDOWN_MS` and **never set on any host** — the blueprint does not declare the variable, the dashboard never carried it — so docs.vote paced at the engine's shipped five minutes from the day it went live, and `/healthz` said so to anybody who asked. To a founder testing the product a five-minute silence after a decision reads as the document ignoring them, which is the opposite of what a legibility device is for. So the unset variable now means **no cooldown** (`HOST_COOLDOWN_MS`, 0): adoptions land the moment they clear bar and floor, and setting the variable is how an operator switches pacing on, still refused above §4.2's five-minute ceiling. The rule itself is untouched — *must stay short* is satisfied by nothing — and R-074's warning is honoured rather than broken: only one of the two brakes is weakened, and the ramp (§4.3) stays intact on every document. Two consequences stated plainly. **The host re-paces every document it serves** (Ed's pick of three, the same day, over leaving the twenty-three live documents at their five minutes or re-pacing named ones by hand): the value rides the engine's constitute event, so a document begun under one cooldown would carry it for ever — but the cooldown is the host's, one value for every document, so the bridge treats the host's pacing as ground exactly as it treats a standing value, and at the first sweep after a resume (the minute tick, or the first command) a document born under another value takes an amendment in its own log stating the host's. In the log, so replay is bit-identical; once, since the next sweep finds nothing to say; and only where the host stated a pacing — a bridge given no tuning, a sim's or a test's, re-paces nothing. Guard: `packages/constitution/test/bridge.test.ts`. And the home is unchanged: R-052's (b) — a birth-time parameter beside the grant and the drip, where a six-month charter and a twenty-minute meeting differ — is still where the one-minute value returns when a second kind of room exists; the env value is the default that shape will override. Rejected: setting the variable on the host instead (a blueprint edit does not re-sync an existing service, and a dashboard value is invisible to the repo — the default had already gone wrong exactly that way once); making 0 the engine's own `DEFAULT_TUNING` (that is the simulator's operating point too, and the calibration sweeps were run at five).

**R-087 The start owes no acknowledgement for what it lays down** (Ed, 2026-09-05, Q1184, from the card review's A13 — *when is someone shown this card?*; SPEC v0.98, §9.7 rule 3, beside R-048). Q1013 and SURFACE C8a had held *one act, one card* without exception, and 🍾 is one act: pressing it laid down both powers on the Text and every member was served a release card saying so. What the review found is what that costs a member at the one moment the document is loudest — **three news cards for one press**, 💡 and ⚖️ arriving with the start and *What the Founder Has Laid Down* beside them, all three about the same event and two of them already saying it. The start's own news is the whole of what beginning means: the gates state that proposing and voting are open, which is only true because the Text stands by itself, and the **settled 🍾 card lists the batch** for anybody who wants the powers named. A member who arrives after the start meets 💡 and ⚖️ as their first tasks and the constitution's own clauses for everything else, so nothing is withheld from them either. The exception is therefore the start alone: **a release after the start is still one news entry and one OK per act**, which is the case C8a was written for — a founder giving something up in the middle of a running document is news precisely because nothing else announces it. Rejected: **dropping the release card entirely**, which would leave a member never told when the founder lays a power down — the whole point of E9, and a rule change by omission; and **keeping it everywhere with its copy stripped**, which fixes the words and not the count, three cards about one press still being three presses at the noisiest moment of the document's life.

**R-088 Quorum is the adoption floor, and only that** (Ed, 2026-09-06, Q1196; SPEC v0.99, §9.5, superseding R-032, R-038, R-049 and R-077). Ed opened the session with: *I have realised that I misunderstood what Quorum does. I don't think we need it; the functionality I want is covered by Lapse and Leave, and we won't have "Sign Out". I think we should set it to 0 and then remove it from the surface and path. Fewer options is good!* Told that 👥 did two jobs — the **freeze** (§9.5 as it stood: the document parks when the counted base falls below quorum), which is the job he meant, and the **adoption floor** (§4.2: `F = max(Q, min(⌈E/3⌉, F_max))`, the distinct members who must have judged a race before it may adopt), which Lapse and Leave do nothing about — he chose, from four options: **keep 👥 as the floor, remove the freeze.** *The card stays and its copy says plainly what it does: how many must have judged a clause before it changes. Sign-out and freeze go; /pairwise is corrected. The form-and-number exception (Q1162) stays with the card.* Three reasons, in the order they weighed. **The freeze and lapse pulled opposite ways**: a freeze says *stop until they return*, a lapse says *carry on without them*, and a document that had consented to 💤 was being asked to do both about the same quiet member. **Sign-out never had a surface** — no card, no control, no copy ever offered *holding* or *abstaining* to a member — so the only way the freeze could fire in a real document was a fixed count larger than the roster, which the meaning line already described as *nothing can pass until more members arrive* and which needs no second mechanism to say twice. And **the floor is the job nothing else does**: the lapse removes the absent from E, resignation and removal remove the departed, but only Q says how many of the present must have been heard before the text changes, so that is the whole of what the card is for now. What follows in the rules: §9.5 is one paragraph, *Presence*; E is the only base, and the electorate of a founding question and of a 🏛️ motion is E as it stands (R-049's *E minus abstainers* had no abstainers left to subtract); the ways out of E are resignation, removal and lapse; judgments already cast keep counting; and a count-form quorum above E holds every race at the floor and holds nothing else — judging stays open. Legacy logs still load: `signed-out`, `frozen` and `thawed` stay in the event union and replay as no-ops, since the record no longer has a field for them to set. The source of the misreading was the product's own explainer (Q1198): `/pairwise` said 👥 *is about the membership being present, never about how many votes one proposal has* — flatly false against §4.2, and the sentence that had taught Ed the wrong quorum. Rejected: **removing 👥 outright** (Ed's opening proposal — it would have left the floor at ⌈E/3⌉ alone, a number nobody consented to); **setting it to 0 and hiding the card** (the same thing with a stored value); and **keeping the freeze without sign-out** (the fixed-count case only, which the meaning line already covers in words and which contradicts 💤 whenever both are set).

**R-089 Silence is never imputed** (Ed, 2026-08-14, the ruling §8.2 has cited as Q43 since v0.9; recorded here 2026-09-07, Q1278, **reconstructed from §8.2's sentence and its date alone** — the question itself survives in no file). A member who has not judged is not read as preferring anything: no comparison exists until one is cast, so an absence of judgments is an absence of evidence and never a vote for the incumbent, for the challenger, or for indifference. What rests on it: E is the arrived, non-removed, non-lapsed membership and nothing is inferred about anybody in it who has been silent (§8.2); the adoption floor counts distinct movers who actually judged; and R-088 — quorum is the floor and only that — could retire the freeze and sign-out because a member who has gone quiet had never been counted as anything but quiet.

**R-090 The membership system, settled** (Ed, 2026-08-26; SPEC v0.74, §9.7 rule 9 and §9.7½; lifted from DECISIONS on 2026-09-07, Q1279). The membership is a fact — who is a member — and three settings and two doors govern how it changes. **One price scale** for every act on it, in the document’s own verbs: 🏛️ *everyone must agree* · ✏️ *the membership decides* · ✒️ *the act is its own consent*. 🪪 **Admissions** is the price of admission, paid by every route in; 🤝 **Applications** is only whether strangers may apply, an application being *a stranger proposing their own invitation* — so *open*, in the old sense, is 🤝 yes with 🪪 at ✒️; 🥾 **Removal** prices the way out on the same scale, keeping *consent* (the subject included, so nobody leaves but by their own word). **The doors** ✉️ and ❌ are not settings but each holds the Founder’s ✒️/🛡️ pair **over the act**: ✒️ invites, or exiles, at will — exile immediate, every answer the member stood on leaving with them (Ed: *arguably a 👑’s biggest power*); 🛡️ refuses any one invitation or removal as a 👑 question. Born held like every setting, laid down one-way, worn as power tabs in the door’s own stack. **✒️ means any unilateral act; the Founder only starts with it** — 🪪 at ✒️ is every member’s word. **Resignation is free**, immediate and nobody’s to refuse. **A lapsed member counts as abstaining** in every decision and is active again the moment they log in, no price paid twice. The six rulings that closed the consistency review of that day: one price; one scale; applications pass through ✉️’s powers; exile is immediate; the lapsed abstain; 🪪 re-typed as the price of admission. They were recorded in plan-queue’s BACKLOG entry 94, which is a stub today; this paragraph and DECISIONS’ *The membership system, settled* are the record. Superseded in part since: the lapsed no longer abstain in a quorum sense, quorum being the floor and only that (R-088).

**R-091 The care map is incumbent-relative** (Q13, the sim evidence pass of 2026-08-14; SPEC §3.2, lifted from the section on 2026-09-07 with the v0.109 tidy). SPEC's own argument, verbatim: *"cold" is incumbent-relative, and rival-pair ties can come from people who defend the status quo.* So the map counts indifference on **incumbent-involving pairs only**: a tie between two rivals is no evidence that the room does not mind about the span, since it comes as readily from somebody who minds a great deal and prefers the text already there — the two readings are indistinguishable in the tally, and only the incumbent-involving pair asks the question the map claims to answer. All-pairs indifference is still logged, and where it runs hot above the map's reading that excess is a camp-split hint for the composer's briefing (§6.1) rather than a colder span. The evidence is the deferred-question study of 2026-08-14, `packages/sim-harness/REPORT-deferred-evidence.md` (Q13), reproducible with `npm run evidence -w @draft/sim-harness`.

**R-092 The clock closes the document; nobody presses anything** (Ed, 2026-08-20, closing Q467; SPEC §4.6, lifted from the section on 2026-09-07 with the v0.109 tidy). Ed's sentence is the rule, and SPEC's own argument for it is this, verbatim: *The closing act happened when the close was set: the ⏰ question was consented at the founding or moved by motion since, so T=0 is the room's own past decision executing — a founder press would be a power nobody granted, and "close it now" already exists as the motion moving the close earlier.* Three rulings of the same day belong with it, recorded here so §4.6 can stay a table. **Every member and invitee is mailed that the document has closed, with a link to the record** (Ed, 2026-08-20) — the invitee included, because an invitation outstanding at T=0 expires and the mail is the only thing that will say so (X14). **The record collects each member's closing comment** (Ed, 2026-08-20): the close is acknowledged per member on their own clock, acknowledging it signs the final document, and the comment — freely blank, dissent as welcome as praise — is the signing rationale, which is what makes the signature an act rather than a receipt. And the **third outcome**: a race unresolved at the close leaves the incumbent standing but records *undecided* as distinct from *kept*, because the minority map and the backlog's stake-waived re-entry both live on the difference between a text the room settled and one it never reached.

**R-093 The drip is slack by design, and 240 is the order of the old default** (Q8, the sim evidence pass of 2026-08-14; SPEC §7, lifted from the section on 2026-09-07 with the v0.109 tidy). Two calibration notes §7 carried inline, both kept verbatim. On the number: *240 minutes is the order of the old default — 10% of a two-day window; calibration owns the number, Appendix A.* On what the sim found: *at these defaults the economy is deliberately slack — no starvation observed at rosters 5–14, participants sit near the cap — so the drip is close to inert; it stays for population-scale headroom, and the wall-clock drip/threshold pairing is confirmed to feel soft-early/hard-late as intended.* Which is to say the drip earns its keep at populations this project does not yet serve: at a Newspeak House cohort it is very nearly a formality, and that is the intended reading rather than a defect to tune away — anti-flooding is the whole of what tokens are for (§7), and a room of nine floods nobody. The evidence is `packages/sim-harness/REPORT-deferred-evidence.md` (Q8), reproducible with `npm run evidence -w @draft/sim-harness`.

**R-094 The refund curve is continuous, and w is the peak** — **superseded by R-133** (Ed, 2026-09-18, Q1454: only a proposal that passes is refunded, and the curve is gone). (SPEC §7, the pre-§9 era; no separate ruling date — the argument is the spec's own, lifted from the section on 2026-09-07 with the v0.109 tidy). Verbatim: *The curve is continuous because cliffs concentrate gaming at the boundary; junk self-punishes in proportion, near-misses cost little. w is the peak rather than exit-time probability by design: a good early candidate displaced by a later, better draft is not punished for the improvement it provoked, and junk never peaks high.* Both halves price a withdrawal without pricing the decision to withdraw. A step function would tell an author exactly where to stop and turn the refund into a game played at one number; the continuous curve leaves nothing to stand on. And an exit-time w would make provoking a better draft the most expensive thing a good author can do, which is precisely the behaviour the mechanism exists to buy.

**R-095 A change of rule never moves a person; lapse is the exception, being a reading** (SPEC v0.75, plan-queue entry 97; §9.5a, lifted from the section on 2026-09-07 with the v0.109 tidy). Verbatim: *a lapsed member is in that status by no act of their own, so when 💤 is turned off, or lengthened past their quiet, they — and a lapsed crown — are returned at once, the room having chosen to count them again; a shorter spell moves nobody until the clock does.* The general rule this excepts is §9.7½'s: people stand in a status because of an act — an invitation sent, an application lodged, a motion opened — and the act stands under the rule it was done under, so a 🪪 re-pricing moves no invitation and a 🥾 re-pricing fixes no motion's route. Lapse is the one status nobody enters by an act: it is a reading of 💤 against a clock, so it is re-read whenever 💤 changes. The asymmetry is deliberate — lengthening returns people at once, shortening waits for the clock — because the direction that returns a member to the room is the one that may act immediately.

**R-096 Seeing is presence: a read returns a lapsed member** (SPEC v0.110, Ed 2026-09-08, the follow-up to Q1284). Ed's words, ruling the admit card's audience: *Lapsed members never see it as a settled grey tab, because if they were seeing things they wouldn't be lapsed. If they log in while the question is still open, then they stop being lapsed and they are served it.* Until this date §9.5a read *returning is logging in again, and nothing else*, and the module held it narrowly: the magic link, the invitation link and any command revived a lapsed member, and a read of the document deliberately did not (`seen()`: *an act, not a read*, from the 🍾 build of 2026-08-21, with no reason recorded). Cookies last ninety days and 💤 can be days, so a member could open the page with a live cookie, lapsed, and read every open question as a settled grey tab — a room showing itself to somebody it was not counting. What replaced it: presence is the thing, and a read is presence (it already stamped an active member's clock hourly), so a lapsed member's first read returns them, E grows, and the questions still open are served to them from that view. Rejected: keeping act-only revival with a sentence on the page saying so (the sentence would be explaining a state the ruling says should not exist); ending the cookie at the lapse so revival is always a real login (cleanest with the old wording, but a lapsed member could not even read the document until the mail arrived). The ladder documents' presence exemption (Q681) stands unchanged.

**R-097 Three warnings before a lapse: a week, a day and an hour** (SPEC v0.111, Ed 2026-09-08, Q1285). Ed raised it for the backlog that morning — *we should notify a member by email when their membership is about to lapse (proportionate to lapse rate, e.g. one day / one hour before lapsing depending how fast it is)* — and ruled it that evening: *only three — a week, a day, and an hour. And you get all three.* Until this date one warning went at three quarters of the consented spell (`WARN_FRACTION`, an author call from the 💤 build with no reason recorded), so the lead was a quarter of the spell: a week on a month, forty hours on a week, fifteen minutes on an hour. A fraction scales the lead with the spell, which is the wrong shape at both ends — a week's notice on a month is more than anybody needs to act on, and fifteen minutes on an hour is less than a mail takes to be read — and it gives one warning where a person who missed it has nothing. What replaced it: three fixed leads (`WARN_LEADS`), each firing where it fits inside the spell (a lead equal to the spell would go at the moment of the member's last act, which is no warning; so the card's shortest spell, seven days, warns twice), each event carrying its lead and each mail naming it (*will lapse in about a day*). A host that was down across two points sends the shortest lead that has passed and never the stale one after it (`warningDue`; the fold keeps the shortest lead sent, `lapseWarnedLead`, and a return clears it). A spell under an hour — the sim rooms and most tests, since the card collects whole days — warns nobody, which is the ruling's *only three* taken at its word. Offered and not chosen: one lead chosen from the spell by a ladder (a week at ninety days, a day at four, an hour at four hours, else a quarter) — built first on a misreading of *a week, a day, an hour* as rungs rather than as the three warnings, and replaced the same evening on Ed's clarification; a day capped at a quarter; the quarter kept with an hour's floor. Since R-096 a read revives, so the warning is the whole of what brings a member back before the clock does.

**R-098 🍾's table is per setting, in document order** (SPEC v0.112, Ed 2026-09-09, Q1195 (c)). Asked what 🍾's *mixed* zone should look like — a zone one of whose settings had already promised a power away on its own tab — Ed answered the question underneath it: *I think we should instead give the user a table of settings with three columns; setting, ✒️, 🛡️; with the second two as toggles (as we have now), i.e. 🪶 Title, ✒️, 🛡️; 📍 Link, ✒️, 🛡️; 🌍 Visibility, ✒️, 🛡️; etc — in the order that they appear in the document.* This reverses one clause of R-057 and only that: R-057 had rejected *per-setting switches on the card, which is the tabs again with a worse view of them*. What the zones cost, found the same morning: a zone's mixed state was drawn struck like laid down, and pressing it moved the zone to *down* — laying down everything the zone still held — which was also drawn struck, so the press changed the act and the glyph did not move, and the tooltip said the opposite of what the press did. With one row per setting there is no mixed state to draw: a cell is one power on one setting, kept or laid down or — already given on its own tab — struck and disabled, with the tooltip saying where it comes back. The table is also the survey R-057 wanted, now legible: eighteen settings and two powers each, read down the page in the order the settings stand in the document, so what the convenor carries across the line is seen per setting rather than per bundle. What R-057 keeps: rule 8's reading (*the start lays down whatever 🍾 was not told to keep*), the Text alone starting at *lay down*, the batch as one act at one stamp, and *the answer only ever lowers* — a cell returned to *keep* lays nothing further down here and never reclaims, a power promised away on a setting's own tab coming back only there; the zones' source literal survives as `BEGIN_ROWS`, one array of page keys whose order `spec-check` holds against the band's `ORDER`, the doors after the four membership rules, 🪜 beside 🌡️, the Text after the constitution; `machines` (no card since R-078, in the catalogue for replay alone) has no row — Ed, the same hour: *we are not having machines* — and `spec-check` exempts it by name, the start keeping both powers on a setting nothing can reach.

**R-099 Every live race can ask a member from the document; the hot set concentrates the feed and gates nothing** (SPEC v0.113, Ed 2026-09-09, Q1178 and Q1180). What §8.3 said: a hot set of ~3 receives concentrated sampling, and *feeds are suggestions — participants can browse, search, and judge anything live*. What the engine did: the feed was a hand of `n` from the hot set, the only thing `judge` accepted was a dealt pair, and the rail drew an entry for every contested clause — so a press on a race the hand had not reached was dropped in silence (Q1180), and a fresh proposal reached nobody until members had cleared 3–11 older cards (Q1178, the room-walk of 2026-09-05). Two things had already moved by the morning this was ruled: Q1202 (2026-09-08) gave the view a per-race `askOn` pair and the rail a working card on any race that can still ask you, which is the *judge anything live* sentence made mechanism; and the 2026-09-05 stopgap had reserved the hand's leading slots for the least-measured unjudged races, against Ed's own ordering (*serving priority should never consider how recently a card was made, only how close it is to being resolved; we should not prioritise least-measured races*). Ed's rulings, one at a time: **serve all, ordered by value** — every live race a member has not judged is in their hand, ordered by v / c_p, the hot set an emphasis and not a gate — **v / c_p alone**, the least-measured slot removed, the exploration roll the only push toward a new race; and the sentence written only after a sim A/B against the hot-3 hand. The A/B (`packages/sim-harness/REPORT-serve-all-ab.md`: 30 seeds per cell, 8-hour windows, rosters 5 · 10 · 14 · 20, both scenarios) found serve-all **matches** hot-3 on welfare within seed noise at every roster — identical runs at five, where the hand is never exhausted; a two-standard-error hint against at twenty, on a cast padded with six cloned personas; two to six points fewer idle turns; 3–25% more judgments per adoption spent on low-value races, which is R-068's *depth beats breadth* seen from the other side. Ed chose to state it as built. R-068's calibration of the hot set is untouched: it still says what the feed concentrates on; what it no longer says is what a member may not judge. Rejected: keeping the hot set as the stated design with the rail as a footnote (the −0.005 at twenty is a hint, not a finding, and the product already serves everything); more evidence first (a real twenty-persona cast — a day's work for a difference the sim could not find at four rosters). Recorded beside it, from the build: the kept ×1.25 unheard boost already lifts a fresh race above a unanimous one, so a zero-evidence race does not sit at the bottom of the order, and the engine test asserts that real order.

**R-100 Parks are per footprint** (SPEC v0.114, Ed 2026-09-09, Q1179). R-056 parked one text adoption at a time: while a candidate stood `awaiting-assent` the batch performed no text adoption and made no second park, and a batch parked at most one leader — the build's own reading, marked as such, chosen because it let accept be the ordinary adoption with nothing rebased and no story for a rebase that fails. What that cost was found by the room-walk of 2026-09-05 on the seeded ladder: a second race, unanimous and floor-clearing, meter full, adopted nothing, parked nothing and told nobody — its author read *live* with closeness 1 for ever, the founder got no new 👑 task because one already stood, and rejecting each standing park immediately parked the next, a queue three deep and invisible from every seat (Q1179). Ed's words that night: *there is no limit to how many 🛡️/👑 cards the founder can see* — every passed change under the veto reaches the founder as its own question. The mechanical tension put to him on 2026-09-09: a park is frozen against the text as it stood, so two parks on one span cannot both be accepted as written, and accepting any park shifts the line offsets under every other. Three readings were offered. **Park per footprint** — any number of parks, each its own 👑 question, so long as none overlaps a standing one; non-overlapping parks accept in any order; a leader whose span overlaps a standing park *waits like anybody* (§4.2's own phrase for a failed mid-batch rebase), stays live and judgeable, and Q1015's ⏳ says what it waits on — was recommended and **chosen**. Rejected: **park everything** and rebase the rest on each accept, a failed rebase returning its patch to its author as rebase-pending, which lets a park stop being the change the room passed; and **R-056 untouched** with the queue merely said aloud, which keeps the freeze the walk found. Why non-overlap is the condition and not a convenience: a three-way rebase changes a patch's lines only where the adopted hunks overlap its own, so a park that overlaps nothing standing and is overlapped by nothing adopting meets only textual composition (gate 1) — its offsets move, its words cannot, and the numbers recorded at the park (`awaiting`) never move at all. That is the exact sense in which a park is still *the change the room passed* after its neighbours have gone in, and it is what makes accept-in-any-order safe with no survivor rule. So R-056's *never rebased* narrows to *never rebased across its own span*: the sweep parks nothing over a standing park and adopts nothing over one, and the shared rebase loop, which now reaches parked candidates, treats a failed or word-changing rebase of a park as a bug in that guarantee and throws naming both candidates rather than quietly returning a passed change to its author. The decree door is the one place the old breadth survives: R-058's refusal while any park stands is kept, because a decree is bound by no footprint test and could break the guarantee from the other side; whether a decree touching no parked line should pass instead is left to Ed (the build's report, 2026-09-09). The view carries the wait — `blockedByPark` on a race whose leader clears bar and floor and overlaps a park, computed by the sweep's own readiness test so the flag and the batch cannot disagree — and the room's side of it is SURFACE E36 and E37 (Q1015).



**R-101 Closeness is the lesser of two distances** *(superseded by R-118, v0.128)* (SPEC v0.115, Ed 2026-09-10, Q1305). Ed's first bot room: *the wash's saturation rises but there is no left-to-right fill*. Measured from the engine: the meter was full at birth. Closeness was |2p − 1| over the bar's span (2θ − 1), and a newborn race already carries its author's derived preference (§3.3), which alone fits p ≈ 0.80 — past the span of a bar of 60 (100% before anybody judged), 99% of a bar of 80, 74% of a bar of 90 — so the width had nowhere to go and only ever dipped when the room split, while the colour (urgency) moved. Three readings were put to Ed: **the lesser of the two distances a race must cover** — the bar's as before, and the floor's, distinct movers over F (§4.2), since adoption needs both — recommended and **chosen**, with his amendment that *nothing need say which of the two a race is waiting on*: it stays one number, a magnitude, never a direction (R-072's rule and Q501's). Rejected: dropping the derived preference from the closeness fit alone (0 until a second voice, then a jump to wherever one judgment lands, usually past 70%); leaving the engine and drawing measured comparisons over the floor instead (a count everybody can read, but the bar's distance vanishes from the surface and the record's backlog ranking would say something else). Consequences: a newborn race reads 1/F, its author being one mover; a judge who moves again is not a second mover; the floor's distance is 1 at E = 1 as the bar's already was (R-063); the record's backlog ranking (§1) and the router's closeness × salience score take the new number. Q836's floor on the span is untouched.

**R-102 The floor counts judges of the winner** (SPEC v0.116, Ed 2026-09-11, Q1337). The moon room, two hundred bots under a quorum of 60%: *the setting is currently that 50% of the membership must vote before something can pass, but I see accepted proposals that have 16/168 votes, is this really correct.* It was the mechanism as written. §4.2 carried X once *≥ F distinct participants had moved on the race*, and a race is every rival on one clause: a room of fifteen (F = 9) reproduced it the same evening with forty-four adoptions in two minutes, one of them at p 0.88 with a single comparison touching the winner — the other movers had judged its rivals against each other, and the winner's confidence came from its author's derived preference and one judgment under the fit's prior (Q840). A crowded race met a quorum nobody had given the winner. Three readings were put to Ed: keep the race-wide floor; **count the floor on the winner** — F distinct members must have judged the winning candidate, a judgment of it against the incumbent or against a rival alike; or both, a race floor beside a smaller floor on the winner. **Ed chose the winner's** (20:22): it is what *must vote before something can pass* says, and the record's count becomes the same number, so a reader's *n of E* is the number the floor tested. Rejected: race-wide, which is the defect; and the pair of floors, which keeps a second number nobody asked for and lets the winner's be the smaller. What counts as a judgment of the winner, decided at the build and stated here so it can be reversed by one line: the author's derived preference (§3.3) is one judge of its own candidate, as §8.2 already had it a mover like any other and R-063 a voice — so a newborn race reads one judge, and at E = 1 R-063's exception stands untouched; a rival's author is no judge of the leader, their preference touching their own draft alone, which is exactly the difference between the two counts. The alternative — measured judgments only — would have made a quorum of the whole room unreachable for a single proposal, since an author is never served their own text against the incumbent (R-062) and cannot be one of E judges of it; it survives as the second clause: the room must have judged the winner at least once, measured, R-063's line drawn at the winner rather than anywhere on the race. Consequences: the floor half of closeness (R-101) is the leader's judges over F, so the meter cannot read full while the winner has one judge; the router's unheard boost keys on the same count; the close renders through the batch's own test; and `distinctMovers` stays on the race view for the sim's evidence, gating nothing.
**R-103 An identical motion is refused on either route** (SPEC v0.117, Ed 2026-09-12, Q1348). The Q1323 re-try on moon2 found twenty-six 🏛️ motions on 💤 running at once, each proposing the same fortnight, because the only refusal at the open was *the motion proposes what already stands* (R-018) and the ordinary route's *identical values are one proposal* (R-019) was the engine's rule, never the module's. A member who had answered the first stood at ⏳ with twenty-five they could never reach. Ed: *there should be dedup.* So the module refuses a payload already running — the same value on the same setting, the same person, member or applicant, the same power back — and names the twin, so the mover answers it instead. Rejected: refusing any second motion on a setting while one runs, which would let the first mover block every rival wording for the life of their motion.

**R-104 A rival motion outlives a carry on its setting** (SPEC v0.117, Ed 2026-09-12, Q1348). Two different values moved on one constitutional setting are two unanimity votes, and the question was what happens when the first carries while the second still collects. Ed first ruled against order deciding it — *I don't like that the order in which people answer constitutional questions affects how they get adopted; if there are two that are both live and pass, the engine should find out which one is preferred* — and a preference race between the consented values was drafted here; then, choosing *adopt the first, and if the second carries too, race it* over holding the first until every rival settles, he saw the race was unnecessary: *the second is indeed against the status quo, which is the one that just passed, so in fact it's moot.* A motion is a proposal against what stands; once the first value stands, the second is a proposal to move from it, and its carrying is the room's consent to prefer it. So the rule is what the module already did — a carry sets the value at once and every rival on the setting runs on — stated so that nobody reads the survivor as a stale vote against the old value. Rejected: holding a carried value until its rivals settle (one stalled rival stalls the setting for its life); a preference race (redundant with the second vote); re-running the founding question with the strictest answer taking (a change to the consent rule itself, filed as its own question if Ed wants it). Residual: a member's answer on the survivor was given against the old standing and is not re-asked — Q1348 carries it.

**R-105 A carry is a ground shift on its rival** (SPEC v0.118, Ed 2026-09-12, Q1348). R-104's residual: the survivor's answers were given against the value the carry just replaced, so a member who accepted 90 over 66 had never been asked whether they prefer 90 to 80, and a keep on the rival was a keep of 66, which nobody can keep any more. Ed ruled, at 15:02, *clear every answer but the mover's, and ask again*: the carry is a ground shift on the rival — the module wipes its answers except the mover's standing accept, the count restarts, and the motion is served to everyone as a fresh ask. *Truest to the meaning of consent, since every answer is then against the value that actually stands.* The mechanism is one event, `motion-ground-shifted`, naming the motion and its cause, folded as the wipe; the motion keeps its id, its stake, its rationale and its running status, and the surface needs nothing new, because a wiped answer reads as no answer and the card asks by itself. Rejected: leaving the answers standing with the card restating the baseline (a vote against one value counted as a vote against another, dressed as a caption); clearing accepts only, keeps and abstains standing (a keep is a keep of the old value, and is exactly as stale as an accept); withdrawing the rival when the first carries (the second mover's proposal stays a proposal against what now stands, and R-104 keeps it alive for that reason). Two decisions the ruling left open, made here. **The mover's answer is restored to accept**, not kept as it was: the motion is theirs and it is being re-put, so it stands as it stood at the open (§9.6), and a mover who had stood down to abstain may stand down again. **The same shift applies to the Founder's ✒️ and to an ordinary carry** on the setting — a date move on ⏰ carrying at the bar beside a constitutional *never* — because *the ground moved* is the same fact whoever moved it, and the rival's answers are exactly as stale; `cause` records which, the carried motion's id or `'pen'`. A pen that restates the standing value moves no ground and shifts nothing. **A rival already at the crown's door (`awaiting-crown`) is shifted too** (SPEC v0.119, Ed 2026-09-14, Q1348 (a), reversing this rule's own last sentence, which had left it parked on the ground that it had carried by unanimity and waited on the crown alone). The reversal's ground is the rule itself: *a value must never stand on consent given against a different baseline*, and a park is exactly that consent, one assent short of landing — so it goes back to `running`, its answers wiped but the mover's, and its pending 👑 question is withdrawn (a new `'withdrawn'` status on the record, which stays in the map for the replay and the record, and is neither pending nor answered, so nothing is served on it and nothing is owed). If the membership consents afresh and it carries again, a fresh 👑 question opens as usual. Rejected: leaving the park standing because the crown's assent is all that is left (it makes the crown's hand, not the membership's, the thing the value stands on); withdrawing the parked motion outright (the mover would have to put it again, and R-104's whole point is that a proposal against what now stands is still a live proposal).

**R-106 A rival proposing what now stands carries, moot** (SPEC v0.119, Ed 2026-09-14, Q1348 (b)). R-105's other residual: the ground can move to exactly the value a rival puts — the Founder's ✒️ setting what somebody had already moved — and R-105 would then wipe the rival's answers and ask the membership again whether they consent to the rule they are already living under. Ed ruled that it settles instead: **the value it wanted is what stands**, so it passes, moot. The mechanism is one event, `motion-carried-moot`, naming the motion and the act that moved the ground, folded as carried with a `moot` field on the record and **no payload applied** — the value is already there, and a second apply would write a second change record over the provenance of the act that actually made the change. Its 👑 question, if it had got that far, is withdrawn with it (R-105). The check runs before R-105's wipe, so a moot rival is never asked again, and the mover's 🏛️ comes back with the settlement like any other. **What it owes is the mover, and only the mover**: a carry owes an acknowledgement to everyone who had no say because the rule they live under changed, and here it did not — the act that moved the ground is telling the membership in its own right, so a second card about a change that did not happen is the flood entry 162 exists to prevent; the mover is the one person with something to be told, which is that what they proposed is now the rule. Rejected: withdrawing it with a line to its mover — honest about whose motion it was, but it reads as dropped when in fact it got exactly what it asked for; leaving it running to be re-asked (asking a membership to consent to nothing). **Reachable through the ✒️ alone today**, and deliberately written for every cause anyway: a motion cause would have to carry the very payload a live rival puts, which R-103 refuses at the open, and ⏰'s route split — the one place an ordinary carry meets a constitutional rival — puts *never* on one side and dates on the other, so the two can never be equal. **Constitutional rivals only**, as R-105's shift has always been: an ordinary motion is a race the engine runs, and settling one from the module would leave its candidate racing with nothing to report back to.

**R-107 There is no skip; ❄️ is the whole of it** (SPEC v0.120, Ed 2026-09-14, Q208, asked on the open-questions walk). §3.1 had promised *skipping is not a move; the card returns later* and §8.3 that *skipped cards recirculate personally with decay*, and nothing held either: no skip command or state existed in engine-core, the server or the page, and the promise was not on §13's ledger. What Ed had chosen instead, on 2026-08-17, is ❄️ — a toggle on the flame, on the commit row of the one card the surface insists on: the card stops being the most urgent, the next most urgent takes the flame, the card stays open, and pressing again puts it back; nothing is skipped, recirculated or decayed and no evidence changes (SURFACE M8, §9.1). Ed struck the promise rather than ledger it: *skip belongs where the surface insists and nowhere it merely offers* (Q202), so a cooling toggle on the insistent card is the whole of what skip was for. Rejected: keeping the promise as two §13 rows (honest, but it keeps alive a calibration question — Q175's decay rate — for a mechanism nobody intends to build); building skip beside ❄️ (a second act on every card whose decay rate would have to be chosen before the simulator could test it). Q89 and Q175 closed with it.

**R-108 Urgency is kind-blind by design** (SPEC v0.121, Ed 2026-09-14, Q49 and Q102, asked together on the open-questions walk). Since 2026-08-14 the `race-labeler` has produced an advisory type per race — copy-edit, substantive, structural — stored and read by nothing: the live server and the page never call the labeller, and §8.1 prices every pair by pivotality alone, so a comma fix near its floor can sit brighter in the margin than a wide-open structural fight. Ed ruled that this is the design, not a gap: v prices *movement*, never importance, and a member's attention goes where the next judgment moves the most, which is what makes most of a session feel like waving through small corrections. Rejected: letting the type act only on the salience diagonal (copy-edits never offered as *which matters more* — the original worry, but it needs the labeller wired into the live server for one exclusion); the type as a multiplier on v (a mislabel quietly demotes a real question, and nothing on a blind surface would let a member see that it had); wiring the model's *name* first and leaving the type (a build, with the type still undecided). The labeller stays what it was made: advisory, never logged, gating nothing (Q49). Q49 and Q102 closed with it.

**R-109 The judgment cost stays in the engine** (SPEC v0.122, Ed 2026-09-14, Q100, on the open-questions walk). c_p is one scalar per member dividing every v, so it can never reorder that member's own list, only how much they are handed; today it gates the exploration slot (a member cheaper than the room's median gets the occasional under-measured card) and nothing else. The August proposal — a slow judge shown fewer questions in the margin, the remainder reported in a count at the foot — lost its reporter when Ed removed the foot count on 2026-08-17 (*a permanent apology for a limit nobody experiences as one*), so thinning would now be silent. Ed ruled: the cost stays in the engine, the margin fits what fits (SURFACE M2). Rejected: thinning the margin by pace silently (honest about their pace, invisible to them); leaving the row open for a big room (a rule nobody has to remember beats a question nobody has to answer). Q100 closed.

**R-110 Standings are public at the seal** (SPEC v0.123, Ed 2026-09-14, Q122, on the open-questions walk). The sealed record has shown the whole field ranked with percentages, the judge count and the bar since 2026-08-22 (Q611), the surface rule stating it (§9's record row) but no spec sentence making it deliberate; §8.3 governed live feeds only. Ed ruled it at the seal, as built: once a race has sealed there is no judgment left for a standing to contaminate, and an author learns at once how their proposal did. Rejected: holding the numbers to the close (the winner and the field named, the percentages arriving with the final record — a page change that denies authors feedback for the whole window in exchange for nothing the blindness rule needs). Q122 closed.

**R-111 An outside pair is priced against the hand's own top** (SPEC v0.124, Ed 2026-09-14, Q98, on the open-questions walk). Since Q1202 a race outside a member's hand that still holds a pair for them is served through `askOn`, and the pair came back with `urgency` 0 — *a pair outside the hand is by construction below everything in it and has no feed to be relative to*, the build's own note. The first half of that is not quite true (the hot set orders *races* by closeness × salience, and a race it leaves out can hold a pair worth more than a dealt one), and the second half is a choice rather than a fact: the top the hand was priced against is a denominator an outside pair can be measured by as easily as a dealt one. What the zero cost is on the surface, where urgency does two jobs — the 🔥 flame, and what the margin drops when it is full (SURFACE M2): every outside race scored the same nothing, so they sat last, which is right, and unordered among themselves, which is a tie the router has no reason to declare; and a member reading the wash saw *no value* on a question the engine would hand them next. Ed ruled the real value: an outside race scores its own pivotality over the same top, so the reading of urgency is untouched — it is still the value of the next comparison you would actually be handed. Rejected: the zero as it stood (consistent as an ordering and nothing to build, but it ties every outside race and reads as worthless); a flat floor of 0.3, the page's own fallback constant (it lifts an outside race above a low-valued dealt one, contradicting the router's ordering to cure its silence); pricing a race by the best comparison it could offer anybody rather than the one it would offer you (an engine change, and a different reading of urgency — how much the race matters, not how much this judgment moves it). Three things settled at the build. **One top, or the two reads are on different scales**: `askOn` takes the caller's own `n` and `now` and reads the memoised hand (Q1324), so pricing every race in a view costs one deal and the 4s poll is where it was; the server view holds the hand size as a single const for the same reason. **The ratio is clamped to [0, 1]** as the hand's always was, an outside pair being able in principle to out-value everything dealt. And **where the hand prices nothing** — empty, or holding only the zero-valued exploration card — `askOn` reports 1, as `nextCards` has reported 1 for a whole hand since stage 8: with no magnitude to be relative to there is no order to report, and putting a real question below cards worth nothing would invert the very thing this ruling fixes.

**R-112 The subject judges their own removal at the bar** (SPEC v0.125, Ed 2026-09-14, Q401 (b), on the open-questions walk). X5 excludes the subject from a 🥾 *assembly* — unanimity of the others — and said nothing about the *proposal* route, where `enterRemovalRace` enters the race with no judge restriction and the subject is served pairs like anybody; the two routes treated one person differently and nobody had chosen it. Ed ruled: let them vote, and say so — the project's principle is that everyone votes their own preference and nothing is imputed (an author's preference for their own proposal already counts), and at the bar one voice is one voice, where under unanimity one voice is a veto, which is why the assembly route shapes its electorate and the proposal route need not. Rejected: excluding the subject on the threshold route too (one rule across both routes, at the cost of a fifth of a five-member room and a bridge change); a rung on 🥾 letting the room decide (a founding question most founders could not answer). Nothing to build; the code already does this. Q401 closed.

**R-113 The close files a stranded proposal as undecided** (SPEC v0.126, Ed 2026-09-14, Q1353, found by the Q170 build). A patch whose rebase onto a newly adopted text fails sits at `rebase-pending` (§2.4, §2.6) — out of every race, held for its author to re-make or withdraw, its stake still with the candidate. The close sweeps two sets into *undecided*: the live members of every race, and the candidates parked awaiting the convenor's assent. A stranded proposal is in neither, so the clock left it exactly where it stood: out of the record, out of the backlog, outside the stake waiver — a proposal that appears nowhere and can never again be acted on, its author's edit spent on nothing. Ed ruled that it files like a race caught running — the same `candidate-undecided`, the same waived stake — so it enters the record's backlog with its last wording and its reason, beside the clause its patch now descends to: the view already carries a stranded candidate's spans forward (Q170's ↻ entry, on Q1333's machinery), and the record places it by the same rule every sealed record uses, off its own `baseVersion`. Rejected: **withdrawing it at the close with the stake refunded**, the kindest thing to do to its author and the worst thing to do to the record — a withdrawal is a decision somebody made and this is one nobody made, and the wording would leave the backlog, which is precisely the text the next session wants; **leaving it stranded**, which is the defect. Two things settled at the build. **It files under its own name** — `r:<candidate id>`, which is what `raceIdOf` returns for a candidate in no race — rather than under the race it was in when the ground moved: that race's record is in the coordinates of the version it resolved against and a stranded patch's hunks are in an older version's, so folding the two into one record would take the span of one and the coordinates of the other. **And the sweep runs after the final batch**, so it catches a proposal stranded long ago and one the final batch has just stranded alike. Nothing in the fold changed, so every log written before today replays bit for bit: a close recorded yesterday holds exactly the `candidate-undecided` events it always held.

**R-114 The status quo is a peer** (SPEC v0.128, Ed 2026-09-15, Q1362 (a)). The adoption test was *P(X beats incumbent) > T(now)* — a confidence, held or ramping — and Ed's members did not understand confidence at all (*does 10 people agreeing out of a population of 200 mean anything useful?*). The road out ran through three questions in one morning: what a bar means in people (Q1361's votes-needed ladder, which could not offer 14 or 15 of 15 because the fit compresses at the top and dips at unanimity — 15 of 15 scores 99.664%, 14 of 15 99.708%); a gate-then-rank rule (a strict majority against the current text, then the ranking among the survivors), which took the confidence out of the decision; and then, at a strict majority, the observation that the current text is *just another proposal, not different from any other pair — it just happens to be the adopted one*. A peer status quo makes the gate redundant: the ranking's top is the text. Ruled as **pure ranking** — the leader is the argmax of fitted strength over the field including the current text, adopts when it is not the current text and the floor is met, a tie (equal within the fit's noise, the stage-1 build's finding) leaving the current text standing. The cycle case — rivals in a cycle where the model puts on top a wording a direct majority preferred the current text to — is accepted and recorded, not fixed. The floor (R-102) is untouched and becomes the whole legitimacy control; confidence stays in the engine for routing, deadlock and the record, where nobody reads it. Rejected: holding the *share* of voters fixed instead (it abandons the posterior and has no meaning with rivals); keeping the bar as a room setting presented as counts (Q1361 — overtaken).

**R-115 Why pure ranking and not gate-then-rank** (SPEC v0.128, Ed 2026-09-15, Q1362 (a)). Gate-then-rank keeps the current text half a peer: a candidate must first win a strict majority of its direct pairs against it, and the ranking chooses among those that do. It is more legible in the one case the rules differ — the cycle — and it costs one more condition in the spec and the engine. At fifteen members with mostly single-proposal races the two coincide almost always, so the choice was one of principle: Ed took the simpler rule and the honest reading of his own framing, with the cycle property stated in §4.2 so that the first time it arises nobody calls it a bug.

**R-116 The floor is the brake; the cooldown batches** (SPEC v0.128, Ed 2026-09-15, Q1362). §4.2 said the cooldown and the ramp backed each other up against hasty adoption. With the ramp retired the plan first wrote *the cooldown is the one brake*; pass 6's churn study (`packages/sim-harness/REPORT-churn.md`, `npm run churn -w @draft/sim-harness`) measured it and found otherwise: at fifteen members over a three-day window, cooldowns of 1, 5 and 15 minutes give 14.1 / 14.4 / 13.0 flips and 11.6 / 11.8 / 10.6 reversions — indistinguishable — while floors of 5 / 8 / 12 give reversions 11.6 / 7.4 / 2.3. So the floor brakes and the cooldown only batches. Churn itself — a peer status quo loses on 8–7 and returns on 7–8 — is the recorded cost of symmetry: the worst seed alternated one clause between two wordings thirty times in sixteen hours and welfare still read 0.99, so no outcome measure will flag it; whether a room may feel that way is Ed's to rule, and the shapes' quorum (only *meeting*'s 50% raises the floor above ⌈15/3⌉ = 5 at fifteen) is the lever the rule already has. A supermajority, if a room ever wants one, lives at ratification (the close, where the closing card already signs the whole), never per clause.

**R-117 The threshold is pinned, not deleted** (SPEC v0.128, Ed 2026-09-15, Q1362 (b)). The ramp (§4.3), its re-anchoring on a moved close, the certification gap (§4.5), the votes-needed table, the ceiling and their tests are real code every live log's `adopted` events name; and `bar` and `pace` are settings every document's log has set. So the engine's threshold is pinned at 0.5 — the adoption test reads the ranking and the floor alone — and the two settings leave the surface the way `machines` did (R-080: `surface: none`, a `retiredAnswer` the start resolves a delegated question at, every shape pinning them), with the deletion one release later. The door back is one line. Rejected: deleting now (a much larger diff across three packages and no cheap way back).

**R-118 Closeness is progress toward the quorum** (SPEC v0.128, Ed 2026-09-15, Q1362 (c)). R-101 made closeness the lesser of the distance to the bar and to the floor. With no bar there is one distance: the leader's judges over F, clamped — voters so far over voters required, the one number the room controls and can read. Still a magnitude and never a direction (§8.3). The rail's meter fills to it; the hot set keeps its own |2p − 1| for routing, and the backlog ranks by the peak probability a candidate reached rather than by its distance to a bar it no longer has. Rejected: retiring the meter (the room would lose its only live signal of a race nearing adoption); an Indifferent answer not counting (Q1362 (d): the member was asked and answered, and the ranking already treats it as a tie). **Its measure is superseded by R-142** (v0.142): the meter counts every vote still required on the pairs the leader waits on, and is never full on a live race.

**R-119 ⏰ is constitutional because the document seals** (SPEC v0.128, Ed 2026-09-15, pass 6 D1). R-033 derived ⏰'s kind from the ramp: windowed-to-perpetual abolished the ramp, and the ramp was the bar. The ramp is gone. Ed kept ⏰ constitutional on the reason that survives it: every judgment was cast under a promise that the document seals on a date and is signed, so removing or adding an ending changes what every past adoption was for. Moving the date stays ordinary (X2). Rejected: demoting ⏰ to ordinary — a majority could remove the close a minority was counting on.

**R-120 Nothing offered at the birth pre-sets a setting** (SPEC v0.129, Ed 2026-09-15, Q1363: *please remove the presets 🧭 from the birth*; replaces R-053). R-053's shape was the one exception to R-001's *nothing arrives answered*: one quill act before 📧 that folded a row's values as the convenor's own pre-start sets, given rather than defaulted. It leaves the birth: the founding is 🪶 → 📍 → 📧 again and every setting is the convenor's own hand or a delegation. What R-053 argued for the fold — the convenor's sets, never defaults, never a delegated bundle — stands as the reading of any shaped log that already exists; the surface just no longer makes one. The provenance (*As for a meeting.*), 🍾's diff line, 💤's hide under a meeting or a conference and ⏰'s chips in the shape's unit all existed only to keep the fold honest, and go with it. The table (`shapes.ts`) and `created.shape` stay in the engine: an older log replays bit for bit, and the sim sweeps (`alpha-preset`, `churn`) take the rows as candidate constitutions. Supersedes the *the shapes' presets stay* clause of pass 6's D3, ruled the same afternoon. Ed gave no reason in the instruction; the one the tree records is pass 6's own — 🌡️ and 🪜 having left the founding, the shortcut the shapes were built to be (twenty-odd cards, entry 166) is shorter to walk without them, and each preset carried numbers that were placeholders until a sweep read them (Q960), which none had.

**R-121 The road back is closed** (SPEC v0.130, Ed 2026-09-16, Q1404: *when a founder relinquishes a power ✒️🛡️, that power tab should disappear from the tab stack (so there's no longer a route to regaining it)*; reverses R-008's surface and Q386's build). R-008 (Ed, 2026-08-19, Q394) made the road back a constitutional `reserve` motion landing without the convenor's assent, and drew two consequences from it: the zero-reservation founder kept one standing property, that the room could restore powers to them; and a reservation returned to a lapsed founder was R-011's constitutional monarchy. For four weeks nothing on the surface put one — the road existed in the module alone — until Q386 (Ed, 2026-09-14) offered it to a member on the laid-down power's own tab, and Q1378 cut that card to one block. Ed's ruling two days later reverses the direction, not the mechanism's shape: laying a power down is one-way in fact as well as in name, and the tab is the route, so the tab goes — for every reader, the founder included, since a tab that opens a sentence saying the power is not held is still a place to stand and ask. What the release from an unwanted crown rested on is unchanged: delegating was always the convenor's own free act, and it still is. What follows for rule 7: a convenor who holds nothing and is not a member is a name in the record and nothing more — the *address the room may restore powers to* went with the road; and R-011's monarchy is now reachable only by 🍾 keeping 🛡️ and the crown lapsing later, never by a return. What survives in the engine: the `reserve` route, its fold, `motions.ts`'s carry and the surface's readers of a settled or running one (`pwSaid`, `motionDisplay`, `recordBody`, the consent card) — a log that carries one replays bit for bit, and the dev ladder's stagehand still puts one to stage the crown on the Text; nothing on the surface composes one. The 👑 release news (rule 3, R-087) and the record filed behind the rule stand as they were: laying down is still news, it just leads nowhere back. Rejected: keeping the tab read-only (the tab is the route in the reader's mind, which is the thing the ruling names); hiding the release record with it (the laying-down is a change of rule and is owed its OK); deleting the `reserve` command (a larger diff across two packages for a route that costs nothing to keep and makes old logs unreadable to lose).

**R-122 Withdrawal retires; the standings stand** (SPEC v0.131, Ed 2026-09-17, issue #14 (B), after a reviewer's reproduction: three proposals and six judgments, the leader withdrawn, the race dropping to three comparisons and a new leader with nothing locked and no fresh pairs served). The sentence as written since v0.12 said a withdrawn candidate *stays in the ranking* and that withdrawal is a ground shift — comparisons frozen as a record, fresh pairs on the smaller field — and the engine has never done either: `withdraw` retires the candidate and the ranking is re-fit over what remains, so the leader can change without anybody being asked again before adoption. Ed's ruling makes the rule say what the engine does and moves the reopening to §13's ledger as a promise, on the recommendation that building it properly is a plan of several days and not a change for the last deploy before the first live test. Rejected: building the reopening now (several days, against a Sunday test); leaving spec and engine disagreeing until a real withdrawal had been seen (CLAUDE.md's rule is that the spec wins until Ed amends it, so the disagreement was a standing instruction to change the engine). What the ledger row keeps: the ground shift as designed — everyone's earlier comparisons frozen, fresh comparisons asked on the smaller field — for when a room has shown it matters.

**R-123 Three proposals, fixed on the surface** (SPEC v0.131, Ed 2026-09-17, issue #27). §7's *both fixed* was true of the page — the founder's ⏱️ card hard-codes grant 3 and cap 3 and offers neither — and false of the service: `set-setting` passes any budget through, and a blind answer carries its grant and cap with it, so a direct API call can set any number. Ed's own instruments depend on exactly that: the constitution test helper builds at 4 · 8, the journey walk, room-bots and several walks use other budgets. Enforcing 3 · 3 on the service is a day's sweep of about eighteen test sites and five walks and would change how the bot rooms behave; amending the rule to say what is built costs one line. Ed chose the amendment, with the enforcement recorded in §13 as a promise rather than dropped. Rejected: enforcing on the service now (the sweep, and a behaviour change to every instrument on the last deploy before a live test); leaving the rule false (a rule the service does not hold is by §13's own discipline a ledger row, never a sentence left standing as if held).

**R-124 The drip is a whole minute or more** (SPEC v0.131, Ed 2026-09-17, issue #4, reproduced: a drip of 1e-9 minutes validated, and the engine's drip loop, adding an interval below the clock's float precision, never terminated — the host hung, and since the fold re-validates every value on replay, it would have hung again at boot with every document on the host behind it). §7 always said *real minutes*, and nothing on the surface can produce a fraction — the ⏱️ box is a whole number of minutes, hours or days — so the floor is what the design contemplated, now stated: `dripMinutes` is an integer ≥ 1, refused alike on the founder's card, a member's answer, a motion and at boot, where a log carrying a lesser value is quarantined and named. Ed took the stronger of the two options offered: the floor, and the drip rewritten as arithmetic so the engine library cannot spin on its own however small the interval, the two proved equivalent over every valid interval (320,000 whole-minute cases, no mismatch) so the engine log replays unchanged. Rejected: the floor alone (closes every reachable path but leaves the library a hang waiting for the next caller); nothing before Sunday (the page cannot reach it, but the API can, and the consequence is the whole host).

**R-125 The floor counts approvals** (SPEC v0.132, Ed 2026-09-17, Q1439; amends R-102, which chose *where* the floor is read — on the winner rather than on the race at large — and stands). The floor counted distinct **judges** of the leading candidate whichever way they judged, and silence was never imputed (R-089). So a judgment *against* a proposal helped it reach its floor and an absence did not, and wherever a proposal's supporters outnumbered its opponents but were fewer than F, its opponents defeated it by staying away and lost by voting: at a quorum of 100% one member did it alone, for ever, a race short of the floor simply waiting (§8.2). **Not voting beat voting no.** The parallels are the ones referendum law knows: a participation quorum turns a boycott into the winning strategy, and a *disappearing* quorum — counted against the people actually deciding — is what removes the incentive. So the floor counts approvals, and F is read against the group the candidate is waiting on. Honest voting is dominant again: an opponent's best move is to say so, an absence delays by one period and no more, and the number the room agreed to is measured on the people who are actually deciding the question. **Strict over loose approval** (Ed's ruling k, chosen from two): only *this over the current text* counts, never a win over a rival — a rival-pair win counted as backing would count a member for a change they may well oppose, since preferring A to B says nothing about either beating what stands, and it would reward dodging the pair that decides. Rejected: counting judgments and imputing nothing (the defect); reading the quorum against the whole of E with approvals (a silent member then blocks for ever, which is the same defect wearing the new arithmetic).

**R-126 No quorum above half** *(reversed by R-139, v0.139)* (SPEC v0.132, Ed 2026-09-17, Q1439 ruling a: *an approval quorum (perhaps even capped at 50%?)*). ✏️ is *enough of the room* and 🏛️ is *everybody* (§9.6): an approval quorum of 100% makes them one rung, and the ordinary route stops being a different kind of decision. And the consent rule takes the strictest answer (§9.0a), so uncapped, **one founding answer hands every member a standing veto** — over the text, and over the motion that would lower the quorum again. So Q′ is never more than half of the group, rounded up, in either form. The **share** is refused above 50 at the value (`values.ts`), where a member can be told why; the **count** form cannot be, because E moves under it, so the engine caps it against the group at every read. Two consequences recorded rather than fixed: R-088's *a count-form quorum larger than E holds every race at the floor until members arrive* is reversed — a shrinking room takes its quorum down with it, which is the point; and at small E the cap and the ⌈E/3⌉ minimum can meet at 1, where the author's own derived preference is the only approval a race needs once anybody has measured the leader at all (§4.2's measured clause is unchanged). Rejected: leaving supermajorities available (the two rungs collapse); capping at E rather than at the group (the veto survives).

**R-127 Silence on one candidate is an abstention after 💤's period** (SPEC v0.132, Ed 2026-09-17, Q1439 ruling c; amends R-089 and reverses §8.2's *never adopting around a legitimate but silent member*, knowingly — Ed's ruling g: *that's ok*). R-089's letter survives: silence is read as preferring nothing, and a member who has not answered approves nothing and opposes nothing. What changes is that a candidate does not wait on it for ever — after 💤's period the silent member leaves the group the quorum is a share of, an abstention **on that candidate alone**, undone by answering. **One control** (Ed: *we can still have one control; if they're silent on everything they lapse, if they're silent on individual motions they abstain*), so 💤's period does both jobs and there is no second setting to answer. **The period runs from the later of** the moment the pair as it stands became answerable — the candidate's submission, or whatever last voided earlier answers to it (§2.4's evidence reset, §4.4's text change) — **and the member's own arrival or return**, so nobody's silence is counted before they were there to be asked. **The statistical minimum stays on the whole of E** (ruling d): it is a sufficiency floor and not a consent rule, and reading it against a shrinking group would let two people carry a room of a hundred — the moon room's defect (R-102) returning by the back door. And the router asks the unheard the leader against the current text first, so they are asked at the moment their silence would be foreclosed. Rejected: a per-proposal period as its own setting (one control); imputing only for members who were served the pair and have been present since (it needs records the engine does not keep, and an open tab is not presence).

**R-128 💤 in minutes, hours or days** (SPEC v0.132, Ed 2026-09-17, Q1439 ruling j, chosen from three). A card offering 7 to 365 days could never turn silence into an abstention inside a room that lasts an afternoon, which is the room this mechanism is for; the unit picker is ⏱️'s, with a minimum of five minutes. The warnings are untouched: a warning whose lead does not fit inside the spell has never fired (`clocks.ts`'s `lapseDue`, R-097), so a spell of twenty minutes simply has no week's notice to give.

**R-129 A judgment's ground is its own pair's** (SPEC v0.132, Ed 2026-09-17, Q1441: *a new rival joining a clause shouldn't change a preference between two other rivals*; amends R-076, whose reason stands and whose implementation was wider than it). A race's ground was the hash of the current text under the **union** of every live member's footprint, and a judgment was stamped with that when it was cast; every comparison whose stamp was not the race's ground *now* was dropped. So a newcomer whose patch touched other lines as well — or that newcomer leaving — widened or narrowed the union, changed the hash, and voided **every** judgment in the race, rival-vs-rival pairs included. The race-wide fingerprint conflated *the text changed* with *the contested area changed*. R-076's reason is only the first of those: a judgment about text that no longer exists must not count. So a comparison is usable while the current text under **its own pair's** lines is what it was when it was cast — X's footprint where X is judged against the current text, footprint(A) ∪ footprint(B) where two challengers are compared, the standing value on a setting race — and a rival joining or leaving voids nothing. The field case is the tims-birthday room (2026-09-16), where one whole-document rewrite joined every one-line proposal's race and wiped the room's votes; and under Q1439's approval floor a wiped vote is a lost **approval**, so the race-wide ground was a second road to the very deadlock Q1439 exists to end. Three seams the narrowing opened, each of which would have been silent: the incumbent endpoint leaves a pair's identity (`contextKey`), since a race can widen — changing the incumbent id every new judgment is cast against — while a pair's ground stands, and keyed by the raw ids one member's judgment and their own revision of it sat under two spellings of *the current text*, neither superseding the other nor counting as served; an old comparison's incumbent id is read as the incumbent of the race the candidate is now in, or the room's votes land on a node of their own in the fit; and the ground-keyed judgment index goes, a judgment sharing no key with its race any more, leaving the per-candidate index the Q1324 fix already maintained. Rejected: keeping the race-wide ground and merely restarting the waiting period on it (the votes are still gone, which is the loss that matters); narrowing to the candidate alone on both sides of a rival pair (a preference between two wordings is about both of them, so both footprints are its ground).

**R-130 A failed motion tells its mover** (SPEC v0.132, Ed 2026-09-17 23:45, Q1447: *someone that proposes a motion should get an acknowledgement task if it fails*). A motion that carried was news to everybody who had no say (§9.6a, R-016) and a motion that failed was news to nobody. Its rail entry and its live tab went for every reader, a grey ✖ record filed behind the rule (SURFACE §8, Q942) and the mover’s 🏛️ or ✏️ came back to the wallet with no word said — so the only signal that a proposal had been decided against was the absence of the thing that had been there, which is exactly the signal a member who was not looking that hour never receives. The defect is a wallet that refills in silence. **The audience is one person**, which is what makes this the sixth kind of owing rather than a use of the fifth: nothing about the document changed, so there is nothing the room is owed; the person who asked for the change is the only one with something to be told. What counts as failing is every road to a settled refusal and no road to a choice of the mover’s own: held by the membership, refused at the crown by the Founder’s 🛡️, and withdrawn by the host where the race would not take the candidate (issue #26) — the last being the case where the mover pressed and nothing at all happened, which is the silence at its worst. Rejected: telling everybody (E5’s job for a change that happened, and noise for one that did not); a card for the mover’s own withdrawal (they know); a card at the close (the 🥂 card already states what was left unresolved, and an OK is refused on a shut document, so the card would sit unanswerable for ever); a card for a refused application (the applicant is not a member and their own 🪪 card already reads *refused*, E33).

**R-131 The room’s own number, with a seconder under it** (SPEC v0.133, Ed 2026-09-18, Q1439 rulings s and u; **reverses R-073**, and voids ruling (d) of the same question with it). Ed: *I think we should lose the built-in minimum of a third of the membership. If the membership want a smaller quorum they should be able to choose it.* F was `max(Q′, min(⌈E/3⌉, F_max))` — the room’s quorum riding on a statistical-sufficiency term the room could raise but never lower (R-073) — and is `max(Q′, min(2, E))`. The consequence was put to him before he confirmed: **in a room of ten at 30% with seven silent, a proposal adopts 2 to 1 once 💤’s period has run**, because the quorum governs the waiting period and a majority of those who answered decides after it. He took it. Three reasons stand behind the ruling. **A number nobody consented to is not a floor**: ⌈E/3⌉ was never on any card, so a room reading *At least 30% (3 of 10) of the membership must prefer a proposal* was in fact held to 4 — and under Q1439’s group-based quorum the two diverged further, the term sitting on the whole of E while the quorum shrank with the group. The card said one number and the engine applied another, which is the defect Q1449 was raised to name and this ruling closes. **The term was the last thing that could deadlock a room**: under the approval floor a candidate short of ⌈E/3⌉ waits on silence for ever even after every period has run — exactly the shape R-125 and R-127 exist to close, arriving from underneath. And **the cap already answers the fear the term was for**: R-073’s worry was a change carried by too few, and no quorum may ask for more than half of the group (R-126), while §4.2’s measured clause still requires somebody who is not the author to have judged the leader and the ranking still requires the leader to sit above the current text — so nothing carries on the author alone, whatever number the room chose. **And under it, a seconder** (ruling u, the same day, 01:48, out of the churn re-run): **F is never fewer than two approvals**, which is the author and one other member preferring the candidate to the current text. One is no floor at all — the author’s own derived preference (§3.3) is the one — and the sims priced it: at `max(Q′, 1)` a room of fifteen made 904 adoptions in a month, 888 of them reversions, the text never settling, against 33 reversions at a floor of two and 13.5 at three (`packages/sim-harness/REPORT-churn.md`, the 2026-09-18 addendum). It is `min(2, E)` rather than a flat 2 because at E = 1 the sole member is the room (R-063 stands, `soleMemberIsLeadersAuthor` untouched) and at E = 2 the floor is unanimity; and it is counted on **E**, not on the group, because a seconder is a sufficiency rule about the room rather than a consent rule about the people still deciding. `adoptionFloorMax` stays a field on the engine `Constitution` and in `DEFAULT_CONSTITUTION`, read by nothing, because every live log carries it in `session-created` and the golden logs must fold unedited — the deletion pass takes it, exactly as it takes the pinned threshold’s pair; `adoptionFloorTerm` survives in `populations.ts` for `floor-recomputed`’s `floorTerm` field alone. Rejected: **keeping the third and printing it on the card** (Q1449 (1)), which makes the surface honest about a number the room never chose rather than removing it, and Ed’s answer was to remove it; and **a middle in which silence may shrink the group by at most half**, which keeps a hidden second number and is harder to state than either rule.
**R-132 A proposal that can never win is closed** (SPEC v0.134, Ed 2026-09-18 ~02:08, Q1440: *as soon as it's dominated by another option (e.g. it can never win unless people change votes they already cast) then it should be counted as closed* — and *we should build this now*, before the live room of the 20th). §4.4 had opened with *Nothing closes* since v0.12: a race was never shut while the document ran, and a wording the room had plainly refused stayed in the field as a live alternative until T=0, where `finishClose` adjudicated it. Four costs, all of them things Ed had met in a real room. **The rail carried every loser and its pair tabs to the close** (Q1431's crowded rail). **An ordinary motion never failed during the document's life**, so Q1447's card for its mover could only be raised on a document that had just shut, where an OK is refused and the card sits unanswerable for ever — R-130 had to name that as a road it does not take. **A losing invitation held its address under the twin rule** (R-103) until the close, so the room could not put the same person again. And **a wording beaten 8–7 came back on one changed vote**, which is the reversion the churn study counts.

The rule is three counts on the numbers a race already holds — a preferring X to the current text, o preferring the current text, w having not answered that pair at all — and a candidate is closed when its best possible future still fails §4.2. *Indifferent* is in none of the three, because answering again is revising a cast vote and Ed's sentence declines to wait for that; **w counts the abstained**, because 💤's period takes a silent member out of the group a quorum is read against (R-127) and takes nothing from their right to answer. That second choice is what makes the whole test time-free: a domination arrives only when a judgment does, never when a clock passes, so it lives in the state's own memo beside the counts and needs no key of its own (contrast the floor, which R-127 made a function of `t`). **The counts stay time-free; the ranking the guards read does not, since R-143**: smith-rank moves when a pair becomes measured, which a silence running its period can do, so a domination can now arrive with no judgment, and it is read in `viewAt` beside the floor.

**The floor clause is exact; the other two are approximations, and the spec says so.** `a + w < F(a + o + w)` is arithmetic about a number §4.2 tests directly, and that the best future is *every one of the w approves* is R-125's properties 2 and 3 read together — an approval raises approvals by one and cannot raise the floor, the approver having already been inside the group, while an abstention lowers the floor by at most one and raises approvals by nothing. The majority clause and the rival clause are counts standing in for *being the top of the fitted field*, and R-114 knowingly accepts that a cyclic field can put on top a wording a direct majority preferred the current text to. So each carries a guard against the thing its own clause measures against: the majority clause does not close a candidate the ranking puts above the current text, and the rival clause does not close one the ranking puts above that rival. Between them a race can never close its own leader. What survives the guards, stated so it can be reversed by one line: a judgment still to come on a rival pair can move a candidate's fitted strength without moving a or o, so a candidate closed by the majority clause in a crowded race is closed on the room's direct preference and not on a proof about the fit.

**The moment is the adoption batch, and the reading is taken after it.** One movement of the document rather than two, which is what a reader can follow. It is read on the state the adoptions leave rather than on the snapshot the batch was decided on, because an adoption locks every judgment cast against the wording it replaces (§4.4): a rival on those lines has a = o = 0 and the whole of E still to answer the instant the batch lands, and is by construction not dominated, where a pre-batch reading would have closed it on counts the adoption had just voided. That is the one way this rule could take away a proposal nobody had refused.

Rejected: **leaving losers live until the close**, the rule as it stood — §4.4's *stability is an equilibrium* is still true of what stands and is no longer true of what has lost, and the four costs above are what the equilibrium was buying. **A rival clause read off the fitted ranking rather than off the pairwise count** — it would be the exact question (*can this ever be the top of the field*) and it is not a question a count can answer, because the fit moves with every judgment anywhere in the race; the pairwise count is conservative, legible, and states its own approximation. **Refunding nothing**, as R-056 does for the Founder's refusal — the argument for it is real (a stake that comes back prices the room's refusal as a withdrawal, and the sims show re-proposal becoming free) but §7 says a retirement refunds on performance and Ed's ruling said nothing about the economy; it is raised as a finding rather than taken.

*(The refund finding was put to Ed the following morning and ruled: R-133.)*

Two consequences measured at the build and recorded here because they are larger than the rule reads. **A retirement deletes evidence.** A comparison naming a candidate that is no longer live drops out of its race's fit, so closing a rival raises the fitted strength of everything that lost to it — which is exactly the defect R-122 named in withdrawal (*the leader withdrawn, the race dropping to three comparisons and a new leader with nothing locked and no fresh pairs served*) and deferred to §13's ledger as several days' work. Before this ruling a retirement was rare; now it is the ordinary end of a losing proposal, so R-122's deferred work is on the critical path of this one. **And re-proposal is cheap.** §7's performance refund pays a stake back whole or better wherever the room ever rated the wording above the current text, and the dedup gate (§5) compares against **live** candidates only, so a wording closed by domination may be proposed again immediately by anybody. Ed's *the author re-proposes* is that, taken literally; a scripted room does it until the window ends.

**R-133 Only a proposal that passes is refunded** (SPEC v0.135, Ed 2026-09-18 ~10:40, Q1454; **supersedes R-094**, and takes the finding R-132 raised the night before). Ed: *I thought that ✏️s were refunded only on successful proposals, not on failed ones — the idea is to stop people from spamming bad proposals — please check*. He was wrong about the code and right about the mechanism. §7 refunded **every** exit on `stake × min(w / 0.5, 1.5)`, w the candidate's **peak** modelled probability of beating the current text — so a stake was never really spent, only lent against how well the wording had once been doing. Two cases were measured on main before the rule changed, with a throwaway probe at a 50% quorum: a proposal **everybody** voted against closed with **0.40 ✏️** back, because the first vote against still left the model at 20%; and a proposal with one early vote for and five against, in a room of ten, closed as rejected with **1.50 ✏️** — the cap, a **profit on a rejected proposal**, because the peak after the one friendly vote was 80% and a peak never comes down. Q1440 had just made both cases ordinary rather than rare: a losing proposal used to sit in the field until T=0, and is now closed at the sweep that discovers it, so its stake came back mid-document and its author proposed again. That is the finding R-132 filed and declined to take on its own authority (*§7 says a retirement refunds on performance and Ed's ruling said nothing about the economy*); this is the ruling it was waiting for.

**Three answers, and the third is the one to notice.** (1) **Only a pass is refunded** — rejected, closed early as dominated, refused by the Founder's 🛡️ (already 0 since R-056) and undecided at the close (already 0, tokens being worthless there) return nothing. (2) **Withdrawal keeps its full refund, at any time**: Ed was offered *refunded only before anybody has voted* and declined it with the cost stated — the rule as it stands stops careless spam and not deliberate spam, since an author who reads the room right can withdraw ahead of the verdict and pay nothing. He took that trade rather than price the decision to withdraw, which is the half of R-094 that survives it (*a withdrawal is priced without pricing the decision to withdraw*), and §2.4's failed-rebase hand-back and §5.1's co-sign keep their full refunds on the same reasoning. (3) **A pass returns exactly the stake — 1 ✏️, no 1.5**: proposing well is free and never profitable, and the surface has no fractions in it anywhere. The 1.5 cap was the last thing in the mechanism that could mint a token for being liked.

What R-094 argued for is not wrong and does not apply. Its two halves were about **where on a continuum** to pay and **which probability** to read: a cliff concentrates gaming at the boundary, and an exit-time w punishes an author whose good early draft provoked a better one. Both are arguments about a refund that measures performance at all. This rule does not measure performance — it reads the one fact the room actually decided, whether the wording carried — so there is no boundary to game and no probability to pick a moment for: the cliff is at the outcome, which is the one place a cliff is not a number to stand on. `peakW` stays, moving in the fold exactly as it did, because it ranks the graveyard and the backlog (§8) and its replay determinism is what the ledgers used to rest on; only the refund stopped reading it.

The cost, stated. **A room's drafting capacity drops to grant + drip + whatever it passes**, which is the point and is also a real tightening: the alpha preset's rooms propose fewer times, and a room that rejects most of what it hears will run its wallets down. The sims say it is not a starvation: the charter run still makes 342 candidates where it made 413, because a room that adopts most of what it proposes is refunded most of what it stakes. **And a member who proposes badly twice is out of ✏️ until the drip**, with no way to earn any back — deliberate, and the whole of *the idea is to stop people from spamming bad proposals*. Rejected: **refunding a proposal the room never got to judge** (it is the close's case, and §7 has always said tokens are worthless there); **a half refund on a rejection**, which reintroduces a number to tune and says the stake was half a cost; and **withdrawal only before a vote**, offered and declined above.

**R-134 The record says how many did not answer in time** (SPEC v0.135, Ed 2026-09-18, Q1452). Since R-125 a member silent on one candidate for 💤's period has abstained on it, and the quorum is read against the members still deciding — so in a room of ten a proposal can pass on its author and a seconder while 👥's clause goes on reading *(5 of 10)*. Put to Ed as a question about that sentence, he left the sentence alone and added his own: *we could communicate how many people didn't answer in time on the outcome card* — then, *build it now*. So the adoption event carries a third number beside R-125's approvals and floor: how many members of E were awaited on the winner against the current text and had run out their period when the batch decided, read on the batch's own clock. It is written at every adoption, zero included, so that an absent key goes on meaning a log from before the field existed and nothing else. §3.5 is untouched — the record is drawn only once the race has sealed. What the count is, exactly: silence on **that pair**, so a member who judged only a rival pair in the same race is counted, which is how the group and the approvals beside it are defined. Rejected: a second sentence on 👥's clause (*members who stay silent … are not counted*), and dropping the *(x of y)* — both offered, the clause staying as Ed wrote it.

**R-135 The clock is not a verdict: a motion the close cuts off owes nothing** (SPEC v0.135, Ed 2026-09-18, Q1450). R-130 gave a failed motion's mover an acknowledgement, and the close was one of its roads — on a document that refuses every OK but 🥂's signature, so the card could never be cleared; and SURFACE said the closing card spoke for a constitutional motion the close kept, which it did not. Ed's ruling: the 🥂 card gains one line for motions still open at the close, both routes, and the close raises no card at all. So the ordinary route's close is its own word in the log, *held-at-close*, landing at the status *held* every reader already knows, and owing its mover nothing; R-132 had already moved the common case earlier, a motion the membership can no longer carry being held while the document is open and its card pressable. An application the close never decided is still told it was refused, that being a fact about the applicant and not a task. Rejected: drawing the card on a closed document as news without an OK, and leaving both.

**R-136 A proposal says what it is replacing, or it is refused** (SPEC v0.136, Ed, 2026-09-19, Q1463 (1), his pick of three: *every proposal must carry the old wording or it is refused*). A patch is line numbers against a version, and the engine has always refused one whose version is no longer current (*targets version N*). That guard is blind to the case a live room actually meets: a member drafts against line 8, a line is adopted **above** them while they type, and the version they quote at the press is the new one — current, because they have been typing rather than sleeping — so nothing fires and the words go out against whatever clause line 8 now names. Reproduced in the residency room (Q1461 (iv), Q1463): a draft on the accounts clause proposed, and accepted, as a rewrite of the treasurer clause. Nothing but the wording can tell the two apart, because every *number* in the request is honest. So a hunk states the exact lines it means to replace, a pure insertion the exact line it means to follow, and a boundary given a statement its base version does not hold refuses the act. **Required rather than optional**, which is the strongest of the three Ed was offered: an optional attestation protects only the clients that already behave, and the clients this exists for are the ones nobody in the room wrote — a bot, a personal AI, a script against the API. **Page-only was rejected for the same reason.** The page's own following of the paragraph (Q1463's first ruling, *follow the paragraph, and refuse if lost*) is built and is the better experience, but it is one client out of an open set, and §1's promise that humans, sim personas and personal AIs are interchangeable behind the participant API puts the check where they meet. **The comparison is exact** — no trimming, no marker normalising, blank lines counted: the page compares loosely to decide whether it can still *carry* a draft, which is a question about the member's intent, where this is a question about the document's bytes. **And none of it is recorded.** The attestation is validation at the door and is stripped before any event is emitted, so an event's shape does not move, every log on disk replays byte for byte and the golden logs fold unedited; writing it into `candidate-submitted` instead would have changed the shape of every past log for a field that says nothing about what was decided. The engine's own `submitCandidate` and `decreeText` take it as optional and check it wherever it is present, which is what leaves several hundred direct-call tests and the whole replay path unmoved — whether the engine should demand it of its own callers too is left open. Rejected: the version guard alone (the defect itself); optional (protects the well-behaved only); page-only (one client of an open set).

**R-137 The membership's numbers carry through the 🛡️ park** (SPEC v0.137, Ed, 2026-09-18, Q1458: *carry the numbers through the park*). R-134 gave every adoption's record the winner's approvals, its floor and how many of E had run out of time, and §8.2 states it without qualification. One road did not honour it. Under the Founder's veto on the Text a carried change does not adopt: it parks (R-056), and comes back through the convenor's accept, which adopted on the `p`, the threshold and the cap mark recorded at the park and nothing else — the three numbers were computed by the batch, used to decide, and dropped on the floor. So the one adoption a human being deliberated over, and therefore the one likeliest to be read afterwards, was the only one whose passed card said nothing about how many of the room had weighed in: the same asymmetry R-051 closed for the cap mark, found in the same place two rulings later. **The park is where they are read, not the accept**, and that is the whole substance of the ruling: all three move with the clock (§8.2 — an approval arrives, a silence abstains, the group the floor is read against shrinks), an accept can come days later, and the quorum itself may have been moved in between, so a number re-derived when the convenor presses would be a fact about the convenor's Tuesday and not about the decision. The park records what stood when the vote carried and the accept copies it. **The shape is R-051's exactly**: one optional field on `candidate-awaiting-assent`, absent rather than `undefined`, held on `awaiting` by the fold and handed to `adopt` by the accept — so every log written before it folds and replays byte for byte, no golden log is re-frozen, and nothing downstream changes at all, the server's record row and the page's sealed record having always read the three off the `adopted` event. One object rather than three optional fields, because the three are one snapshot of one moment and are never separately known. Rejected: **re-deriving at the accept**, which is the defect stated as a design; and **leaving the record silent**, which is what stood — a receipt that omits the numbers on the road where a member is most likely to ask for them.

**R-138 A vote against ends a constitutional proposal** (SPEC v0.138, Ed, 2026-09-19, Q1473; **supersedes the last half of R-021**). Ed, testing a live room with real people: a stranger applied, admission stood at 🏛️, one tester voted against, *but their application remains in Applicants and is ⏳ in the rail*. As specified: a standing keep blocked and did not kill, because the keeper might change their mind, and R-021 gave a blocked motion one way out — the mover's withdrawal. **An application has no mover.** §9.7½ opens it with a null mover, the application being its own proposal, so nothing in the mechanism could end it: the person waited under *Applicants*, told nothing, until the clock closed the document — and the same wedge, without the applicant's helplessness, stands over any invitation whose mover will not withdraw. Ed's ruling, the same hour: *it should fail as soon as someone votes against on a 🏛️* — and, asked whether that meant admissions alone or every constitutional vote, his pick of three: **every 🏛️ vote fails on the first vote against.** So the ask-everyone route has both halves of its own verdict, and one rule holds for one glyph whether the proposal is about a rule, a power, an invitation, a removal or a person at the door. **What is unchanged is everything that made the old rule reasonable**: abstention is still an answer and still never blocks; an accept or an abstain is still revisable while the motion runs; the vote is still blind while it runs; the mover still stands at accept from the open. What a keep does now is settle it, so there is nothing left to revise — which is not a new rule about revising. **It is R-132's principle on the other route**: *a proposal that can never win is closed*. Q1473 noted that R-132 could not reach a 🏛️ motion, since one under a keep could still win **if the keeper changed their mind**; the ruling's substance is that waiting on that change of mind is not a thing a document should do to a person. Two costs, both stated and both taken. **A proposal can be ended by one member before anybody else has looked at it**, which on a rule is a blunter veto than the old blocking one felt like — the remedy being to put it again. And **the count names the keeper by arithmetic**: the motion fails on the answer that ends it, so a member watching the count across a poll can tell who cast it — the same inference §3.5 could already not prevent in a room of two (R-018), now available in any room. Nothing is added to any projection: a failed motion reads *the membership* and names nobody. **Mechanism**: one new event, `motion-held`, landing the motion at the status `held` every reader already knows, with `heldAtClose` false, and the applicant's own refusal and the mover's card raised by the roads §9.7½ and R-130 already gave them. A log written before v0.138 carries none of these events and re-folds exactly as it did — replay folds events and never calls the settle check — so a document holding a standing keep at the moment it is loaded is not retroactively failed, and the next answer on it settles it under the new rule. Rejected, from Q1473's own list: **a period before the refusal is final** (💤's, so a first *no* could be talked round), which keeps the person waiting and puts a clock on a rule that has none; **leaving it as specified and saying so on the applicant's card**, which explains the wedge rather than closing it; and **a withdrawal of the applicant's own**, which answers the applicant's case alone and leaves the invitation's. **And one thing R-130 believed was not so**: it declined a card for a refused application on the ground that *their own 🪪 card already reads refused, E33* — E33 is the door shutting before a submission, and a refused application in fact went on saying *the members are deciding* for ever, with no mail either. SURFACE E42 is that card, written here because this ruling is what makes the refusal ordinary; whether it should also be mailed is open, the pending card still promising *you will get an email either way*, which only the admission keeps.

**R-139 A quorum may ask for everybody** (SPEC v0.139, Ed 2026-09-21, Q1490; **reverses R-126**). Ed, after watching a 50% quorum met at exactly half in a live room: *I think we should allow for quorums up to 100%, and let the lapse mechanic compensate.* Q′ was capped at half the group a leader waits on, in either form (R-126); it is capped at **the whole of that group** now, so a share runs to 100% and a count to however many are still deciding. `validateValue` opens the share from 0–50 to 0–100; the count form keeps a cap at the group, which is the one thing the old cap did that this ruling keeps — R-088's property, that however few are left the quorum never outgrows them, is untouched.

**A share always means *at least n%*, rounded up** — the second of two picks put to him the same minute. ⌈n·G/100⌉ is unchanged arithmetic: 50% of twelve is 6, nothing is special-cased, and a member who wants a majority types 51. This **withdraws the ruling of the day before** (Q1490 (b), *the top rung means a majority*, ⌊G/2⌋ + 1), which was built on a scale that ended at 50 and has nothing left to stand on once it does not: a special case at one rung out of a hundred is a rule no member could read off the card. Rejected with it: **every share meaning *more than n%***, which moves every existing room's number — 25% of twelve from 3 to 4 — for a reading nobody had asked for.

**What was put to him before he confirmed, and taken.** 💤 covers silence: a member silent for the period leaves the group the quorum is read against (§8.2), so 100% of twelve is four of four where four answer, and a room locks only while people are still inside their own period. What 💤 does **not** cover is an objection — at 100% one member preferring the current text ends a proposal, so ✏️ becomes 🏛️'s rule on a shorter clock: 🏛️ fails on the first vote against (R-138), ✏️ waits the period out and fails when the arithmetic can no longer be met (R-132). And **a delegated founding lets one answer put the document under unanimity**, the consent rule taking the strictest answer (§9.0a), which is exactly the cost R-126 was written to avoid.

**R-126's second reason was weaker than it was written.** It feared one founding answer handing every member a standing veto *over the motion that would lower the quorum again* — but 👥 is constitutional (§9.6), so lowering it goes by the ask-everyone route whatever the quorum is: that veto exists at 5% and at 100% alike, and the quorum has never been what guards that road. What survives of R-126 is its first reason alone, that at 100% ✏️ and 🏛️ are one rung in effect; Ed's answer is that a membership wanting that rung should be able to choose it, which is R-131's argument one setting further on.

**And the same afternoon, the other end.** Ed: *the lower bound should go down to one member (which I think would mean that every proposal passes as soon as it is seconded - I think that should be described on the card that the minimum quorum is actually 2 (author plus one))*. The share box began at 5, a number chosen as *the lowest quorum worth stating* and never ruled on; it begins at 1. **No mechanism moves**: R-131's seconder already put `min(2, E)` under every quorum, so a membership asking for one member is held to two — the author and one other preferring the candidate to the current text — and it passes when they also outnumber those preferring the current text, a tie leaving the current text standing (§4.2). What is new is that the card says so. That is R-131's own principle applied at the bottom of the scale, which is where it was found: a room reading *at least 30% (3 of 10)* was in fact held to 4, and the ruling then was that **the card's number is the number the membership is held to**. A card offering 1 and meaning 2 is the same defect the other way up, so the sentence is not an ornament on the choice — it is the condition of offering it.

**And one sentence goes back on the card** — the first of the two picks, and a deliberate exception to Q1439 ruling u, which took every meaning line off the surface. Above half, and only above half, and only once a number has been typed, 👥 says what the number comes to for the people under it: with a floor of F in a membership of E, *k members preferring the current text can stop a proposal*, k = E − F + 1. It is the honest reading of a high quorum and the thing a founder typing 90 is least likely to have worked out; between the two ends there is nothing to warn about and the card says nothing, which is where ruling u still governs. **Below two it says the other sentence instead** — *The minimum quorum is 2: the author and one other member* — and that one wins where both would apply, which is only at E = 2 and E = 3, because a floor the membership did not choose is the more surprising of the two facts. Neither is printed in a membership of one, where every quorum is the whole of it and there is no choice to describe. Rejected: **no guard at all** (the consequence is not obvious and nothing else on the surface says it); **stopping the scale at 99**, so that unanimity can never be chosen (it is the rung a membership most plausibly wants, and 99% of twelve is twelve regardless); and **leaving the cap**, which is the ruling itself. What did **not** widen is any generator: `room-bots`, `dev-ladder` and the rehearsal go on drawing the shares they drew, because a bot room at 100% would never adopt — only the validator and the card moved.

**R-140 Nothing is dominated while E is empty** (SPEC v0.140, Ed 2026-09-22 evening, ruling *build* on issue #65, all three findings — its *Decision for Ed* recommending exactly this clause). §4.4's arithmetic is stated over the members of E, and with E momentarily empty it literally produces a retirement: every one of a, o and w is nought, `a + w ≤ o` holds at `0 ≤ 0`, and a pool with no comparisons fits flat, so no candidate sits above the current text. A room whose whole membership lapsed at one tick therefore lost every live proposal at the next batch — for good, since a dominated candidate does not come back — with no rail entry, no card and no mail, found live on 2026-09-19 with two controls pinning the trigger at E = 0 rather than at a shrunken room. **The premise of domination is that no answer still to come could carry the proposal**, and §9.5a returns every lapsed member on their next read: an empty E is a room not yet back, which is the one state in which answers to come are certain rather than impossible. So the engine closes nothing until somebody is in E. The same issue's first finding needed no clause: §8.2 already says an author's preference counts *unless its author is out of E*, and §9.5 names removal and resignation among the roads out, where the engine had honoured lapse alone. **A resignation can now retire the author's own proposal**, since `a` falls by one and `a + w ≤ o` can flip — which is §4.4 working, the author having left the room that would have passed it. Rejected: **a floor on E larger than nought** (a shrunken room is a real room, and its answers are the ones that stand); **reviving a retired proposal on a return** (§4.4 says it does not come back, and the fix is not to close it in the first place).

**R-141 A rival that covers the winner stays in the race** (SPEC v0.141, Ed 2026-09-24, Q1534, option 9 of three: *why does a rebasing happen, instead of the proposal remaining as a rival?*; rulings 1–8 the same day). §2.4 treated any overlap with an adopted patch as a failed rebase: the rival went to *rebase-pending*, out of every race, its votes voided, its author asked to re-make or withdraw. On a contested clause that is every loser, every time. Since R-114 the current text is a peer in the race, so when a rival wins, the rest of the field has not lost to anything new: it now faces one of the wordings it was already being compared with. A judgment of A against B compared two documents, the text with A's words and the text with B's; once B is adopted, re-aiming A at B's words changes neither document where A covers every line B changed. So the judgment is still exactly what it was, and R-076's reason for locking (*text that no longer exists*) does not apply to it. R-129 had already narrowed a judgment's ground to its own pair; this ruling recognises that an adoption can leave a pair's two documents untouched while changing the text under it. **Covering is the line** (ruling 1), not "the same lines": it is the one test under which no carried vote is about a document nobody judged. A rival inside the winner's span, or one the winner's other hunks would now change, still strands; one spanning the winner's lines and more, or inserting at the gap the winner inserted at, is re-aimed, its votes just as true. **Carried votes feed R-132 straight away**: a rival the room had already preferred the winner to closes in the same batch that adopted the winner, which **supersedes R-132's sentence that a rival on the adopted lines has a = o = 0 when the batch lands** — that was true only while its votes were voided. **The stake is not refunded on the way through** (ruling 2): the rival is still live, so §7 treats it like any live proposal (R-133), and one closed in the adopting batch spends its ✏️ like any proposal that did not pass. Judgments against the displaced text lock, because that text has left the field and nobody proposed it — **and it is not revived** (ruling 4; Ed: *if the status quo was replaced, how is it possible for it to win?*): anybody may propose it again, but the engine manufactures no reversion. Evidence is not reset — only re-making a stranded patch resets it (§2.4) — and every member who has not answered the rival against the current text is awaited afresh from the re-aim, so no silence is counted against a question just put in this form. **One rule for every door** (ruling 3): the Founder's ✒️ decree carries rivals the same way, with nothing carried against the decree, which nobody judged; an accepted park is an adoption and carries the votes cast against the parked patch before it parked; a park itself is only ever rebased (R-100). **Settings too, in the same change** (ruling 7): an ordinary setting race's rivals always stayed live when one value carried (`adopt` rebases nothing) but lost their votes at the standing-set, the ground shift; a value means the same whatever stands, so every live rival is carried as a covering text rival is — its judgments against the value that carried read as judgments against what stands, the rival pairs stand, and those against the value that stood lock. A standing moved by some other road (the Founder's ✒️, a constitutional motion) carries the rival pairs alone. The constitutional route is untouched: R-105's wipe of a 🏛️ rival's answers is about consent given against a different baseline, and a unanimity vote has no pairs to carry. **Two records on one clause** (ruling 5): the winner's ✔ files at once, and the race that goes on files its own when it ends; a rival the batch closed straight after the adoption belongs to that second record, being part of the race that went on. **No extra card for the author** (ruling 6): their ✏️ entry stays blue and shows their wording against the new text, the ✔ beside it saying what happened. **Replay**: the command path decides and records its choice as a new event, `candidate-reaimed`, naming by log `seq` the judgments it carries; the fold restamps exactly those — the adopted wording renamed to the current text, the ground its pair now stands on — keeping the pair as cast for the record, and never re-decides. No earlier log holds the event, so every past log folds unedited, `rebase-failed` and all: a document live at the deploy keeps its stranded proposals, and the new road starts at its next change. The churn study was not run (ruling 8, Ed: *skip the study*). Rejected: **closing every rival as ✖ at the adoption** (option 10), which closes wordings the room preferred to the winner and ones it never compared with it, against R-132's premise that only a proposal no answer could carry is closed; **keeping §2.4 as it stood** (option 11), which spends the author's work and the room's votes on every contested clause and makes a beaten rival free to withdraw; **re-aiming without carrying votes**, which is what setting races did, and asks the room again what it has already answered; **"exactly the same lines" as the test**, which strands a rival covering the winner and more while its votes are just as valid; **re-deriving the carried set at read time** from a rule rather than writing it down, cheaper in log size and one more place replay could drift; **holding the winner's record back until the clause is quiet**, which would hide why the text changed; **a one-time line on the author's entry**, the ✔ beside it already saying so.

**R-142 The leader is measured against its rivals before it passes** (SPEC v0.142, Ed 2026-09-24, Q1538; the plan's twelve questions ruled 2026-09-25). The rival gate (R-071), active sampling's refusal to ask a pair the model already implies (§5.3) and Q1439's decisive pair (R-127) each made sense alone, and together they meant a leader was almost never put directly against its rivals: the fit filled those pairs in by transitivity, which is exactly the inference a clone or a cycle breaks. Ed, from the seat: *at the moment I feel like people are served surprisingly few non-status-quo pairs.* So before a race carries its leader, the leader's pair with each live rival must be **measured** — answered by the pair's own floor (§4.2's F, read on that pair's group, the seconder included: ruling 12), or settled, its lead beyond every member of E still to answer, which is R-132's rival count used the other way round. The floor rather than a smaller number because it is the one number the room chose, and *settled early* because a pair already beyond changing needs no more asking. A rival the batch is about to close is not waited on — it is leaving. **The router asks those pairs next**, after the decisive pair (ruling 5): once the rival gate is open, **or once the leader has met its floor** — the gate's minimum of three measured comparisons is not met in a small room whose leader carries on its author and one seconder, and there the wait would otherwise be served only as the fallback after every current-text pair. **The bar counts them** (ruling 1): Ed ruled that *the progress bar must not fill and then fail to adopt* and that it should count every action still needed, conservatively, with no explanatory line instead; so the meter is every vote the rules still require on those pairs over what they need, the pair against the current text always wanting one more once at its floor — direction-free, never full on a live race, its maximum growing when a rival arrives, which Ed accepted as honest. **At the close the wait is waived** and the record says how many rivals the winner was measured against (ruling 7): the clock is everybody's deadline. **The wait is all that is waived** (Ed, 2026-09-25, after the sim, overruling the plan's close rule): an unmeasured pair stays a gap in the Smith set at the close (R-143), so a leader passes there only where it reaches every rival through measured results — beating the current text that beat the rival, say — and one whose way to the top runs through an unasked pair is undecided, the current text standing. The deadline excuses asking a pair; it does not answer it. **Deadlock yields to it**: a race cannot stop asking while a pair it waits on is short. **Measured cost** (the Smith study, `npm run smith -w @draft/sim-harness`, 8 seeds, rooms 5–20, meeting and conference windows, no quorum and half): the rival-pair share of edge judgments rises in every cell (0.8% → 25.9% at five in a meeting, 8.2% → 15.8% at fifteen); judgments per adoption rise by up to about 30% (1.9 → 2.4 at five in a meeting); the median time from submission to adoption rises by up to 60% (room of seven, meeting, no quorum: 4.2 → 6.7 minutes), and by between −8% and +37% in the other cells at 5–10. **Synergy with R-141**: a rival carried past the winner arrives measured against the new current text, so R-132 closes the ones the room had refused in the very batch. Rejected: **a smaller fixed number of rival votes** (a second number beside the quorum, nobody's choice); **measuring every pair in the race** (quadratic in the rivals, and most of those pairs cannot change the outcome); **explaining a full-but-stuck bar in words** (Ed: *the purpose of the bar is to give a sense of progress and how close things are to resolving*); **re-seeding the demo's rival pairs** (ruling 9: a slower demo is accepted).

**R-143 The top of the ranking is found inside the Smith set** (SPEC v0.142, Ed 2026-09-24, Q1539; narrows R-114, keeps R-115's choice of a pure ranking). R-114 accepted that a cyclic field can put on top a wording a direct majority opposed. It did not foresee the commoner case: **no cycle at all**, and a clumsy near-copy of a losing wording lifting it over the current text by giving it something to beat. Fifteen members, 7 X>Y>cur, 7 cur>X>Y, 1 cur>Y>X: the current text beats both 8–7, and the fit with all three pairs asked puts X at 0.594 over the current text's 0.100 (P 0.863). So the ranking is read inside the **Smith set** of the head-to-head table: the fit still chooses — its tie rule, its record, its routing weight and certification untouched — but only among wordings not beaten, directly or through a chain, by everything outside. A result counts only once its pair is measured (R-142's line), so an unasked pair is never read as a win; and a chain may run only through measured results, so an unasked pair between two other wordings cannot smuggle a loser into the set (ruling 2: **a gap, never a draw**). With one challenger the Smith set and the fit always agree, so **a race of one candidate decides exactly as before** (a property test over random tallies at E 1–20, both quorum forms). **The domination guards read the same ranking** (ruling 3; R-132): a wording outside the Smith set is no longer protected by a fitted strength above the current text's, so the clone case seals at once; the cost is that a domination can now arrive when a silence runs its period. **The record explains the one case the percentages cannot** (ruling 6): a wording closed or left undecided while the fit rated it above the text that stood, a direct majority of those who answered it preferring the text, carries *n of m members preferred the current text to this*, m counting everyone who answered the pair. **What survives, stated so it can be reversed** (ruling 11): inside a genuine cycle the fit still resolves by weight of evidence, and a near-copy of one member of the cycle tips it toward that member (a cycle of 10–5 results, level in the fit, is won by whichever side files a copy — 0.534 against 0.175). **And a measured cycle is commoner than a true one**: each pair is measured by its own floor's worth of members, not the whole room, so sampling alone can turn a near-tie into a cycle inside which the fit decides. **Measured** (the Smith study, as R-142): majority-refused adoptions from a field of two or more fall 2,117 → 1,929 over every cell (those with a copy in the field 1,441 → 1,303); Condorcet efficiency over fields of two or more rises in most cells (61% → 80% at ten in a meeting with no quorum) and falls in some (100% → 93% at five), and over every decision it moves by at most ±1.6 points — within what eight seeds can resolve. **The named next step** (ruling 11): should the clone-filer show a cycle tipped more than rarely, **Schulze's weakest-defeat rule breaks a cycle inside the Smith set only, and the fit decides everywhere else**. Schulze and ranked pairs as the whole rule were rejected: each replaces the fit's decision outright, so the record's percentages, the routing weight and certification would describe a ranking that did not decide anything. Rejected also: **gate-then-rank** (R-115, again: it demands a direct majority against the current text and so refuses the genuine-cycle winner too); **Copeland** (counts wins, and is itself cloneable); **unmeasured pairs as level in the Smith set** (the textbook definition, and it lets an unasked current-text pair pull a Condorcet loser back in) — **at the close as well** (Ed, 2026-09-25, overruling the plan, which read the gaps level at the close so that a leader the room approved would not be undecided for want of a pair nobody reached): the clone field with only {cur, Y} unasked would pass X on the very day the document ends, and one rule for the whole life of a document is the one a member can hold in their head. At the close a gap still costs the leader its pass; the rival wait alone is waived (R-142).
