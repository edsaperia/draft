# Open and deferred items

Resolved items are folded into SPEC.md and removed — this file holds only what's still open. Numbers are from the continuous project-wide sequence; never renumber or reuse.

## Open

The index, in the section's own order (nothing is pinned — Ed, 2026-09-07, Q1228). *State* is what the tree said on 2026-09-07: **open** (nobody has ruled), **waiting on Ed** (readings offered, no ruling), **partly answered**, **answered, owed** (Ed ruled, the build has not landed), **parked** (Ed's word), **provisioned**. A state ending *(log Qn)* names a question the docs pass filed about the item itself.

| # | Title | Raised | State | Pointers |
|---|---|---|---|---|
| 1195 | Three residuals of the card review round 3 build | 2026-09-05 | waiting on Ed — (b) (c) (d) | SURFACE §7.2, F5; Q1181 |
| 1177 | `founding-walk --delegate=chamber` is red | 2026-09-02 | open — diagnose before trusting either reading | `scripts/founding-walk.mjs`; SURFACE K13 |
| 1178 | The unheard slots: a fresh race is guaranteed the hand's leading cards | 2026-09-05 | waiting on Ed — held for a design conversation | SPEC §8.2–§8.3; `scripts/room-walk.mjs` |
| 1180 | The dud rail entry: a vote on an unserved race is dropped in silence | 2026-09-05 | waiting on 1178 | `itemsFromView`, `LIVE_HOOKS.judge` |
| 1179 | A ready race queued behind a standing park is invisible to everyone | 2026-09-05 | waiting on 1178 | R-056; Q1015 |
| 925 | Three more things git knows about overlapping patches | 2026-08-27 | parked | SPEC §2.2, §2.4; Q924 |
| 26 | Evidence-clock adoption threshold | 2026-08-13 | open — a sim A/B once everything else works | SPEC §4; Q22 |
| 49 | Naming and typing disputes | 2026-08-14 | partly answered — interim `race-labeler` landed; the type is unwired from routing | SPEC §8; Q102 |
| 52 | Composer design decisions | 2026-08-14 | partly answered — narrowed to the briefing and the dedup-gate | SPEC §3.5 |
| 53 | Candidate states missing from the spec | 2026-08-14 | open — SPEC §2.6 still lacks `rebase-pending` (log Q20) | SPEC §2.6 |
| 70 | design/race-card.html is retired | 2026-08-15 | answered, owed — delete the file (Ed, 2026-08-28) | CLAUDE.md `race-card` |
| 72 | Queue-wire reach | 2026-08-15 | waiting on Ed | `queue-wire` |
| 77 | Where the scroll runway lives | 2026-08-15 | waiting on Ed | `design/system.css` (the 95vh) |
| 89 | Where Skip lives, and how its decay is shown | 2026-08-16 | partly answered — not on the card (Ed); (a) (b) open | SPEC §8.3; Q174 |
| 92 | Should a race lane be marked against the incumbent? | 2026-08-16 | waiting on Ed | `result-only` |
| 94 | A rationale is machine-drafted and human-edited | 2026-08-16 | partly answered — (b) built; (a) (c) (d) open | SPEC §2.6, §3.4 |
| 97 | SPEC §8.3 promises a prompt the card no longer has | 2026-08-16 | open — a one-clause spec amendment (log Q21) | SPEC §8.3 |
| 174 | Skip in a margin index: what does it actually do to the entry? | 2026-08-16 | waiting on Ed | SPEC §3.1, §8.3; Q89 |
| 175 | The skip decay function is unspecified | 2026-08-16 | open — wants sim evidence | SPEC §8.3 |
| 98 | What is a race's urgency? | 2026-08-16 | open | SPEC §8.1 |
| 100 | Where c_p goes now | 2026-08-16 | open | SPEC §8.1 |
| 102 | Urgency is kind-blind | 2026-08-16 | open | Q49 |
| 109 | Scale: the margin assumes a short rail | 2026-08-16 | open | `needs-you-queue` |
| 111 | An open patch cannot keep its sites level and stay on screen | 2026-08-16 | waiting on Ed — recommend (a) | SURFACE §6 |
| 113 | Where read state lives, and when a decision goes quiet | 2026-08-16 | partly answered — (c) by Q114; (a) is per-device today | `ACK_KEYS`; Q526 |
| 122 | When do rankings become visible — at seal, or at close? | 2026-08-16 | waiting on Ed — needs a SPEC §8.3 sentence either way | `sealed record` |
| 124 | Teasers made the rail say more about fewer things | 2026-08-16 | waiting on Ed | `teasersFor` (session.js) |
| 125 | The open card duplicates its own teaser | 2026-08-16 | waiting on Ed | Q124 |
| 159 | The re-served race after a ground shift isn't modelled | 2026-08-16 | open | SPEC §4.4; Q312 |
| 168 | Bridge wanted | 2026-08-16 | waiting on Ed — a mark | SPEC §6.3 |
| 169 | Your candidate was dominated | 2026-08-16 | waiting on Ed — a mark | SPEC §8.4; Q171 |
| 170 | Your candidate needs rebasing | 2026-08-16 | waiting on Ed — a mark | Q53 |
| 171 | Does the rail carry authorship at all? | 2026-08-16 | partly answered — the place is settled (`yours`); the marks are not | SURFACE M3 |
| 182 | A wide patch is now a walk, not a view | 2026-08-16 | open — wants a wider fixture first | `foldBetweenSites` |
| 185 | Does "2 of 3 places" go too? | 2026-08-16 | waiting on Ed | `placesOf` (copy.js) |
| 187 | Should a live card show the floor as a headcount too? | 2026-08-16 | waiting on Ed | SPEC §8.2 |
| 207 | Nothing now says which lane is the current text | 2026-08-16 | open — wants a real reader | SURFACE K19–K21 |
| 208 | Skip now exists only on the 🔥 card | 2026-08-16 | open — (a) a spec sentence, (b) a sim check | SPEC §3.1, §8.3 |
| 212 | Closeness is now only on the queue card | 2026-08-16 | waiting on Ed | `evidence-meter` |
| 264 | Green now means three things | 2026-08-16 | open — for the palette pass | `lifecycle palette` |
| 266 | One draft at a time | 2026-08-16 | waiting on Ed — should the surface say so? | `draft-site`; SURFACE K14–K16 |
| 282 | A rendered document and a source offset do not agree | 2026-08-17 | deferred past the alpha (Ed, 2026-08-27) | `richToSource` (cards.js) |
| 288 | A flatter glyph set for the lifecycle marks | 2026-08-17 | partly answered — the lifecycle family is drawn; the subject glyphs are emoji | `DRAWN`; SURFACE §6 |
| 293 | Revising your own proposal | 2026-08-17 | waiting on Ed — recommend (b) | SPEC §3.3a, §4.4 |
| 296 | The experience of writing a patch | 2026-08-17 | open — unstarted | `draft-site`; Q182 |
| 305 | Per-candidate advisory labels | 2026-08-17 | waiting on Ed | `race-labeler`; Q301 (DECISIONS) |
| 306 | The `camp-map`, finally built | 2026-08-17 | waiting on Ed — a spec amendment | SPEC §3.5, §8.3 |
| 308 | The one overhang a fitted pile cannot remove | 2026-08-17 | open — only when a case appears | `fitStacks` |
| 310 | The three per-type notes on a decision card | 2026-08-17 | waiting on Ed — recommend (c)/(a) | SURFACE §9 |
| 311 | Is a locked judgment card without a ground shift reachable? | 2026-08-17 | open | `reviseNote` |
| 312 | A ↻ never has its re-served question beside it | 2026-08-17 | open — a fixture first | SPEC §4.4, §6.2; Q159 |
| 313 | Nothing acknowledges the work | 2026-08-17 | waiting on Ed | `room-pulse` |
| 314 | Seeing which card other members have open | 2026-08-17 | parked — shelved by Ed until real humans | SPEC §3.5 |
| 325 | Should `founding-ceremony` keep its name? | 2026-08-18 | waiting on Ed — recommend (a) | CLAUDE.md glossary |
| 335 | Split `Constitution` into what the room agreed and what the engine is tuned to | 2026-08-18 | partly answered — SPEC's half done (Q562); engine-core's object still one | SPEC Appendix A, §9.7.1 |
| 346 | Sybil resistance at the front door | 2026-08-18 | parked | SPEC §9.6a, §9.7½ |
| 349 | Crown residuals | 2026-08-18 | partly answered — (a) whose clock is open | SPEC §9.7 |
| 350 | Setup-grammar residuals | 2026-08-18 | partly answered — (d) closed; (a) superseded by Q517 | SURFACE §6 |
| 384 | A creation-time holder choice for the title and link? | 2026-08-19 | waiting on Ed | SPEC §9.7 |
| 386 | No surface yet raises a reserve motion | 2026-08-19 | open — unbuilt; the page only displays one | `motionDisplay` |
| 387 | Reserving the text | 2026-08-19 | parked | SPEC §9.7 |
| 388 | Individual delegation, foundership transfer, inheritance | 2026-08-19 | parked — out of scope (Ed) | — |
| 401 | May the subject judge their own removal race? ((b) only) | 2026-08-19 | waiting on Ed | SPEC §9.7½; `remove:<id>` |
| 406 | Subsection headings don't fold | 2026-08-19 | partly built — they fold (entry 96); not in the TOC | `sectoggle` |
| 398 | The multi-candidate ordinary race card | 2026-08-19 | waiting on Ed's design pass | Q390 |
| 419 | Does the desktop focus order match the reading order? | 2026-08-20 | answered 2026-08-23, waiting on the mobile build | `design/MOBILE.md` §4 |
| 420 | The narrow form of the margin index | 2026-08-20 | answered, waiting on the mobile build | `design/MOBILE.md` §4; Q657 |
| 421 | The fold triangle is invisible until hover | 2026-08-20 | answered, waiting on the mobile build | `design/MOBILE.md` §4; Q658 |
| 422 | Judging offline | 2026-08-20 | answered, waiting on the mobile build | `design/MOBILE.md` §3.e |
| 423 | Does `room-pulse` need a third state on a phone? | 2026-08-20 | answered (no pulse), waiting on the mobile build | `design/MOBILE.md` §3.f |
| 424 | Multi-block drafting on touch | 2026-08-20 | deferred to MOBILE stage 6 | `design/MOBILE.md` §1.7 |
| 425 | Do lanes collapse or scroll on narrow | 2026-08-20 | answered, waiting on the mobile build | `design/MOBILE.md` §4 |
| 426 | One PWA icon or many | 2026-08-20 | answered, waiting on the mobile build | `design/MOBILE.md` §3.b; Q669 |
| 427 | `--border` contrast at phone brightness | 2026-08-20 | answered, waiting on the mobile build | `design/MOBILE.md` §4; Q659 |
| 428 | Where do push subscriptions live | 2026-08-20 | answered, waiting on the mobile build | `design/MOBILE.md` §3.d; Q673 |
| 429 | How big is the offline shell allowed to be | 2026-08-20 | answered (no budget), waiting on the mobile build | `design/MOBILE.md` §3.c |
| 463 | The close is undesigned, and it needs 🍾's treatment | 2026-08-20 | backlog (Ed, spec pass 1 item 567) | SURFACE E24 |
| 464 | The record is undesigned, and it is what the machine produces | 2026-08-20 | partly answered — (a) (b) ratified; (c) open, after Q996 | `closedBlocks`; SPEC §3.4 |
| 465 | Notifications are the surface nobody has designed | 2026-08-20 | answered as a commission — the design pass is owed | STYLE §6; `mail-fold` |
| 500 | The legal drafts' decisions | 2026-08-20 | parked — until go-live is scheduled (Ed, 2026-08-29) | `docs/legal/` |
| 517 | Does the same rule cover an *ordinary* delegated setting? | 2026-08-21 | answered (a), owed — two predicates | SURFACE §6 `wait` |
| 518 | Should the empty document be pre-filled with something to edit? | 2026-08-21 | answered (c), owed | `writeready` |
| 526 | A grant is acknowledged once, so a power restored later is never announced | 2026-08-21 | built (b); the drop is unverified end to end | `syncGrantAcks` |
| 631 | *Session* or *document*: Ed's rule-to-be is that it depends on whether the document has an ending | 2026-08-22 | deferred by Ed 2026-09-07 (Q1218): *return to this later* | `design/copy.js`; STYLE §1; ⏰ |
| 901 | Three acts have no communication designed | 2026-08-26 | built; two unnumbered residuals | SURFACE E31–E33 |
| 902 | A 💤 change names the members it returned | 2026-08-26 | answered, owed | SURFACE Y26 |
| 924 | Gate 1 merges at the paragraph | 2026-08-27 | answered, owed | SPEC §2.2; `text/compose.ts` |
| 1015 | What is a member told while a text adoption awaits assent? | 2026-08-29 | answered (b), owed — needs a SURFACE §2 row | R-056 |
| 1016 | The seat-matrix row for a shielded Text | 2026-08-29 | a rider on the next harness change | `scripts/seat-matrix.mjs` |
| 1033 | A motion-backed 👑 question at a vacated seat | 2026-08-29 | answered (a), owed | R-060; `crownSeatVacated` |
| 1205 | `seat-matrix` is red | 2026-09-06 | open | `scripts/seat-matrix.mjs` |
| 1229 | SPEC §2.6 (item 53) — `displaced` and `rebase-pending` | 2026-09-07 | waiting on Ed | QUESTIONS.md; Q53; SPEC §2.6; `engine-core/src/types.ts:135–144` |
| 1230 | SPEC §8.3 (item 97) — the prompt Ed removed | 2026-09-07 | waiting on Ed | QUESTIONS.md; Q97, Q95; SPEC §8.3 |
| 1231 | E20's quoted sentence has no source string | 2026-09-07 | waiting on Ed | SURFACE E20, E28–E30; Q567, Q572 |
| 1232 | Q1183's *Set to … / Set by the founder …* card "retires", but the string is live | 2026-09-07 | waiting on Ed | SURFACE §9; Q1183; `session-view.html:5941`, `:9192`; `setup.js:632` |
| 1233 | `text-unconfirmed` (Removed 4) | 2026-09-07 | waiting on Ed | SURFACE F19; `readiness()`; Q1080; R-081 |
| 1234 | §7.2's "two frozen instruments" (Removed 6) | 2026-09-07 | waiting on Ed | SURFACE §7.2; `spec-check.mjs:481–487`; `card-audit` |
| 1235 | `.switch` CSS is orphaned | 2026-09-07 | waiting on Ed | SURFACE CP1; Q1103, Q1181; `design/system.css:1288–1301` |
| 1236 | Y27's Ruling cell now carries a pointer | 2026-09-07 | waiting on Ed | SURFACE Y27; Q1005; DECISIONS.md |
| 1237 | F5, F19, F21 and K31 remain very long | 2026-09-07 | waiting on Ed | SURFACE F5, F19, F21, K31, §8, §9 |
| 1238 | §5's survival | 2026-09-07 | waiting on Ed | SURFACE §5, E12; Q1004, Q1015 |
| 1239 | The `L` lifecycle table and the setup-alphabet and socket-state tables have no marker | 2026-09-07 | waiting on Ed | SURFACE L1–L9; `seat-matrix.mjs` |
| 1240 | `design/spec-pass/` holds five files where its own rule says folded passes are deleted | 2026-09-07 | waiting on Ed | CLAUDE.md Documents; Q1096–Q1102, Q1122–1128, Q943, Q944 |
| 1241 | The double-fire child gotcha is unguarded on its own text | 2026-09-07 | waiting on Ed | CLAUDE.md Gotchas; `penHoldFired`; `npm run journey` |
| 1242 | The 🎩 gotcha names no guard | 2026-09-07 | waiting on Ed | CLAUDE.md Gotchas; `npm run journey`, `npm run ladder` |
| 1243 | `founding-ceremony` is one name for two things | 2026-09-07 | waiting on Ed | CLAUDE.md Names, Glossary; SPEC §9.0a |
| 1244 | The seat matrix is stated twice | 2026-09-07 | waiting on Ed | CLAUDE.md Glossary, Conventions; DECISIONS entry 201 |
| 1245 | The two tables grew the file by 310 bytes | 2026-09-07 | waiting on Ed | CLAUDE.md; `5e8a41a` |
| 1246 | The evidence figures have lost their only home | 2026-09-07 | waiting on Ed | README.md; `packages/sim-harness/README.md`, `runs/` |
| 1247 | Q1204 names the wrong package in the task brief, and did not reproduce | 2026-09-07 | waiting on Ed | README.md; Q1204; `constitution/test/ranking/ceiling.test.ts` |
| 1248 | The server's running test count fell from 99 to 86 | 2026-09-07 | waiting on Ed | README.md; `packages/server/test` |
| 1249 | `npx playwright install chromium` has no root script | 2026-09-07 | waiting on Ed | README.md; `ci.yml:164, 254`; `package.json` |
| 1250 | Should the README carry the CI/deploy paragraph at all? | 2026-09-07 | waiting on Ed | README.md; `docs/OPERATING.md` §3 |
| 1251 | Was stage 9's "one real end-to-end send, to a non-Resend address" ever done? | 2026-09-07 | waiting on Ed | PRODUCTION.md stage 9, go-live checklist |
| 1252 | Is stage 10's security review #2 satisfied? | 2026-09-07 | waiting on Ed | PRODUCTION.md stages 10, 19; `c8a732d`; `verify-deploy` |
| 1254 | Stage 15's gate is *somebody else can operate it*, and nobody else has tried | 2026-09-07 | waiting on Ed | PRODUCTION.md stage 15; `docs/OPERATING.md`, runbooks |
| 1255 | Stage 13 has no plan document | 2026-09-07 | waiting on Ed | PRODUCTION.md stage 13; `design/MOBILE.md` |
| 1256 | Three code comments point at content that moved | 2026-09-07 | waiting on Ed | PRODUCTION.md; `scripts/test-pg.mjs:15`; `soak.ts:215`; `render.yaml:48` |
| 1257 | Three code comments still send a reader to PRODUCTION.md for text that moved | 2026-09-07 | waiting on Ed | `docs/OPERATING.md` §7; `scripts/test-pg.mjs:15`; `render.yaml:48`; `soak.ts:215` |
| 1258 | OPERATING §1 states the managed Postgres is version 17 in frankfurt | 2026-09-07 | waiting on Ed | `docs/OPERATING.md` §1, §2; decision 493 |
| 1259 | `deploy-health-shutdown.md`'s *Configuration reference for this stage* table | 2026-09-07 | waiting on Ed | `docs/runbooks/deploy-health-shutdown.md`; OPERATING §2 |
| 1260 | `constitution/NOTES.md` is a chronological ledger of calls, and a third of it is now struck | 2026-09-07 | waiting on Ed | `packages/constitution/NOTES.md`; DECISIONS.md |
| 1261 | `server/NOTES.md`'s header still says *(Q368)* and its shape is the 2026-08-19 slice | 2026-09-07 | waiting on Ed | `packages/server/NOTES.md`; Q368 |
| 1262 | `engine-core/NOTES.md`'s *Dominated* and *Router v1* sections describe the first build's router | 2026-09-07 | waiting on Ed | `packages/engine-core/NOTES.md`; SPEC §6.2, §8, §8.3a; Q11, Q1178; `f345167` |
| 1263 | The sim-harness README's model ids | 2026-09-07 | waiting on Ed | `packages/sim-harness/README.md`, `src/` |
| 1264 | §3.a's stage pointer for an asset pipeline | 2026-09-07 | waiting on Ed | `design/MOBILE.md` §3.a; PRODUCTION stages 8, 14 |
| 1265 | `holdWallet` is a name the tree no longer has | 2026-09-07 | waiting on Ed | `design/MOBILE.md`; CLAUDE.md Gotchas; `penHold` |
| 1266 | The survey's counts | 2026-09-07 | waiting on Ed | `design/MOBILE.md` survey, stage 0 |
| 1267 | §7's table is the 2026-08-18 mapping and Q335 has moved under it | 2026-09-07 | waiting on Ed | REVIEW-creation-session.md §7; Q335 |
| 1268 | The footer names surfaces that are redirects | 2026-09-07 | waiting on Ed | REVIEW-creation-session.md footer |
| 1269 | The report's engine observations name a gap that may since have closed | 2026-09-07 | waiting on Ed | REPORT-deferred-evidence.md; `participant-api.ts`; SURFACE §9.2, K13–K26 |
| 1270 | The `runs/` CSVs are dated 2026-08-19, not 2026-08-14 | 2026-09-07 | waiting on Ed | REPORT-deferred-evidence.md; `packages/sim-harness/runs/` |
| 1273 | Should the pair be listed in CLAUDE.md's Documents table? | 2026-09-07 | waiting on Ed | `design/card-review-*.md`; CLAUDE.md Documents; SURFACE §9.3; Q1149–1195 |
| 1276 | Q949, the cap clamp (S7) | 2026-09-07 | waiting on Ed | SPEC §7; Q949; R-083; `tokens.ts:39` |
| 1277 | §12 Display (S9) | 2026-09-07 | waiting on Ed | SPEC §12, §3.5; SURFACE |
| 1278 | Q43 (H24) — the source of *silence is never imputed* | 2026-09-07 | waiting on Ed | SPEC §8.2; Q43; R-088 |
| 1279 | The membership R (H36) | 2026-09-07 | waiting on Ed | SPEC §9 `→ why:`; entry 94; DECISIONS.md:1664; R-024 |

1195. **Three residuals of the card review round 3 build (2026-09-05 evening), left for Ed** — a fourth, the consent card's withdraw, was Ed's own correction the same night (the word goes, the 🗑️ stays) and is built. **(b) An answer's 🏛️ has no hold-ladder row** (SURFACE §7.2): C4 now says 🏛️ commits every answer to a constitutional question, and the build commits it with a click that flies nothing — said in §7's wallet row, not in the ladder. Whether an answer is held for a second like the raise, or stays a click, is undecided. **(c) 🍾's mixed zone** — a power already promised away on one of the zone's own tabs — has no look of its own under Q1181's glyph toggle: the build draws it struck like *laid down*, with a tooltip saying *partly given up already* and the note under the table, and pressing it reads as *keep*. F5 keeps the mixed rule; whether the toggle needs a third look is Ed's. **(d) A reversal to say back:** Ed's note on 46 💡 cut *You hold 5 ✏️s to propose with*, the sentence he asked for at Q1129 and STYLE audit item 78 (`design/DECISIONS.md`) records as deliberately kept; built as written.
1177. **`founding-walk --delegate=chamber` (founder-answers) is red, and was red before the QA round of 2026-09-02 pm** — verified by running it at `7de54b2` in a worktree: *the line written after the first ✒️ is not in the charter after 🍾 — the column was live but its keystrokes went nowhere* and *🍾 did not spend the release — the wallet counted 17 settings before it and 17 after*. Not in CI (only `journey` covers the delegate mood there, and it is green), so nothing reddens on it. Two readings: the walk's typing path predates **📝 is the door** (K13 amended — it types into the column without pressing 📝, so the keystrokes are correctly refused, the same shape as Q1148's stash red), which would make both errors the walk's; or the release accounting really does not spend at 🍾 on this path, which would be a product bug the walk found. Diagnose before trusting either; the walk is the only instrument that drives this exact mood.
1178. **The unheard slots: a fresh race is guaranteed the hand's leading cards** (2026-09-05, from the room-walk brief; **discussed with Ed the same night — held for a design conversation**). Ed's three statements on the record, 2026-09-05: *serving priority should never consider how recently a card was made, only how close it is to being resolved*; *we should not prioritise least-measured races; we prioritise races that are closest to sealing* (so the built least-measured tie-break inside the reservation is provisional and against his stated ordering); and **"all races should be served"** — under which the reservation becomes unnecessary, inclusion is universal, ordering is the value sort, and Q1180 dissolves by construction. He chose **hold — talk first** on building serve-all (the tension to resolve: SPEC §8.3's hot-set concentration, his own 2026-08-13 calibration that depth beats breadth at small rosters, and what the rail looks like at 100+ members). Until that conversation: the fix stays live as pushed, SPEC stays unamended, and this entry is the record. Found by playing a whole room over HTTP: on a ladder document at `session`, a fresh proposal reached **nobody** — every seat's four-card hand held only older seeded races, and the new card arrived only after a member cleared 3–11 of them. The router's hot set is the top-3 *valued* races and a race with no evidence values below every race with some; its other doors in are a 1-in-7 exploration roll (cheap judges only) or §8.2's ×1.25 boost, neither a guarantee. SPEC §8.2 says *the unheard are asked at the moment their silence would be foreclosed* and §8.1 prices new-candidate measurement as exploration, but neither states a guarantee, so per the brief the fix landed as **at least one slot**: `feed()`'s first card goes to the least-measured race this participant hasn't judged while it stands short of the adoption floor (`session.ts`, the unheard slot; guard `scripts/room-walk.mjs`). Question for Ed: is this the rule — and should SPEC §8 state it as one (a sentence in §8.2 or §8.3), which this session may not add itself?

1180. **The dud rail entry: a vote on an unserved race is dropped in silence** (2026-09-05, found while answering Ed's questions on 1178). The rail draws an entry for **every** contested clause (`itemsFromView` iterates `v.clauses`), but a vote only lands if the engine served this member the race's pair (`raceCards`): `LIVE_HOOKS.judge` on an item with `card: null` discards the press with a `console.warn` and nothing the member sees — the pick appears to take, and nothing reached the server. Pre-fix this was the starvation's worst face (nineteen members, nineteen dud entries); post-fix it survives for a race whose floor is met but which the hot set crowds out. Ed's *all races should be served* (1178) would dissolve it by construction; failing that, the entry should refuse visibly or fetch the pair on demand. Waits on 1178's design conversation.

1179. **A ready race queued behind a standing park is invisible to everyone** (2026-09-05, from the room-walk brief; the sharper half of Q1015; **discussed with Ed the same night — held into 1178's design conversation**). Ed's ruling on the record: *"There is no limit to how many 🛡️/👑 cards the founder can see"* — the design intent is that every passed change under the veto reaches the founder as its own 👑 question, which means overturning R-056's one-park-at-a-time. The mechanical tension to resolve in the talk: a parked change is frozen as passed and never rebased (R-056, Ed 2026-08-27 — a park is not a revert), so accepting one of several simultaneous parks moves the text under the others; multiple parks need a survivor rule (re-rebase and re-ask · return to race · park only non-overlapping changes together). Sibling of 1178's serve-all: both are *the room sees everything, the mechanism rations*. R-056 is *no text adoption of any kind while a candidate is parked*, one park per batch and none beside a standing one — correct and deliberate. But the room-probe showed what it looks like from inside: a **unanimous, floor-clearing** race (20 of 20, meter full) adopts nothing, parks nothing, and *tells nobody* — the author reads `mine: live` with closeness 1 for ever, the founder gets no new crown task since one already stands, and nothing anywhere says "waiting on the Founder's outstanding answer". On the seeded ladder the queue was three deep (rejecting each standing park immediately parked the next). Q1015 asks what a member is told while an adoption awaits assent; this asks the queued case behind it — the author of the *blocked* race and the room deserve a line, and the founder's standing crown task arguably should say what is waiting behind it. Surface design, so a question rather than a fix.

925. **Three more things git knows about overlapping patches, parked** (raised 2026-08-27 with 924; **Ed: park for now** — each wants design work on how it is communicated before it is worth a mechanism). **(a) Dependent candidates.** `propose-edit` (K24) starts a draft from a candidate's wording, so C can be built on unadopted A; if A loses, C's rebase onto the incumbent fails over a decision made elsewhere. *Presupposes* is a different relation from *composes* (§2.2's lattice), and git's stacked patches say: declare it at submission, adopt in order, and let C fall with A or return to its author with A's fate stated. Verify what the engine does today first. **(b) A candidate's version chain.** §2.4's blanket *evidence resets* does not distinguish a clean rebase (no words changed) from an author's revision; Mercurial's obsolescence markers give a change an identity across rewrites, each recording what it superseded and why, so the rule can be *rebase keeps, revision resets*. With it, git's `rerere`: an author's confirmation against new text is remembered and reused when the same span moves again, instead of the leader failing identically every batch. **(c) Two cheap ones**, no spec change: a base-independent content hash of the normalised hunk (`patch-id`) before the dedup gate asks the LLM, and an interdiff — what moved under you, not the whole new paragraph — on the confirm-against-new-text card.

26. **Evidence-clock adoption threshold.** Wall clock chosen for v1 (Q22, options 23–25, 2026-08-13): simpler, knowable, one clock shared with the token drip. Revisit the evidence-clock (or a hybrid max-of-both) ramp as a sim A/B once everything else works.

49. **Naming and typing disputes** (Ed's question, 2026-08-14): the engine knows a dispute's *location* (span/footprint), not its *nature*. Labels in UI currently fall back to nearest markdown heading + excerpt, which fails on heading-less documents. P3 should generate both a name ("treasurer oversight") and a type (copy-edit vs substantive vs structural) per race — the type also informs routing (copy-edits shouldn't burn diagonal attention slots) and the record. *Interim landed 2026-08-14 (5e61969): advisory `describeRace` oracle capability + `race-labeler` with deterministic nearest-heading fallback; type stored but deliberately unwired from routing. Still open: wire type into routing (needs a SPEC §8 sentence), full P3 treatment.*

52. **Composer design decisions** (raised 2026-08-14, unanswered; **narrowed 2026-08-16**). The composer was rebuilt into session-view as in-place editing (224–241), and design/composer.html’s drafting desk and arrival bar went with it — so (a) and (c) below now describe a surface that no longer exists, and survive only as questions about the **briefing**, which is still to be rebuilt as an escalation state (SPEC §3.5). (b) is untouched, since the dedup-gate has not been built anywhere yet. Three calls made in design/composer.html that Ed has not ruled on. (a) The standings-panel draws the incumbent's bar as its *certification* — P(current text beats best challenger) — so the display shows why only a bridge has a path; the spec doesn't dictate how the composer renders standings. (b) The dedup-gate borders purple, extending the series' "changed since you looked" grammar to "the engine interrupting your submission" — defensible stretch or grammar dilution. (c) The desk deliberately never predicts how your own draft would poll (a self-poll would be a standings feed by the back door) — spec-faithful, but a participant might expect it.

53. **Candidate states missing from the spec**

    **Where it stands (docs pass, 2026-09-07):** still open — SPEC §2.6 lists neither state today (`rebase-pending` is declared in `packages/engine-core/src/types.ts:144` and appears nowhere in SPEC.md), so the fold is a spec amendment wanting Ed's sign-off.

    (found 2026-08-14): SPEC §2.6 lists state {live, adopted, retired, merged, carried, withdrawn}, but engine-core's types.ts carries `displaced` and `rebase-pending`, which the mechanism needs. Fold them into §2.6, or rule them implementation detail and note why.

70. **design/race-card.html is retired**

    **Where it stands (docs pass, 2026-09-07):** answered, owed — Ed ruled *delete* on 2026-08-28, and `design/race-card.html` is still in the tree; the deletion carries CLAUDE.md's `race-card` entry, `design/spec-pass/card-audit.html`'s citation and the race footer's stale promise with it.

    (opened 2026-08-15; settled by Ed 2026-08-16). The overlay model and the comparison modes (marked-up / fair copy / differences) are dropped rather than ported — session-view's lanes do the job, and a toggle between three renderings of the same pair is machinery a surface built for typo-fixes should not carry. The one thing race-card.html held that mattered, the **salience diagonal**, is now built here (Q221). What remains: **answered by Ed 2026-08-28 (from the CLAUDE.md audit's C4) — delete `design/race-card.html`.** Owed, not built, and left here until it is, because the deletion is not a one-liner: `CLAUDE.md`'s `race-card` glossary entry names the path with a `[file]` kind and `spec-check` resolves those, `design/spec-pass/card-audit.html` cites it, and the stale promise in session-view's race footer — *full race view has comparison modes and the whole queue* — goes with it. Nothing further should be built on that file meanwhile.

72. **Queue-wire reach** (found 2026-08-15, from Q71; trigger settled by Q78 — the wire belongs to the open card only). Two calls I made that Ed hasn't ruled on. (a) The wire lands on the anchored **paragraph(s)**; Ed's phrasing was "left into the decision cards", which could instead mean the open inline card. (b) There is no wire in the other direction — hovering a paragraph in the document doesn't light its queue entry — though the relationship is symmetrical and the return trip is arguably the more useful one when you are reading rather than working the queue.

77. **Where the scroll runway lives** (raised 2026-08-15, unanswered): removing the design notes (76) took most of the page's scroll with them, so the document now carries ~95vh of bottom padding — without it an anchor near the end of the charter can never be scrolled up to the queue rail and the wire-levelling (74) silently stops working down there. I put that padding *inside* `.doc`, so the middle column literally runs to the bottom as Ed asked; the cost is that scrolling to the end shows about a screenful of blank paper below the last line. The alternative is padding on `main` instead: the card stops just after "Adopted at the house, by the fourteen, over pasta" and the runway is page background. One line either way.

89. **Where Skip lives, and how its decay is shown** (raised 2026-08-16; Ed: skip should not be on the decision card at all — it will probably live in the `needs-you-queue` once that surface is worked on; left in place for now). Two parts remain. (a) The move itself, which waits on the queue work. (b) The mockup does not model SPEC §8.3's "skipped cards recirculate personally with decay" — skipping closes the card and leaves the queue entry exactly where it was (superseded in part: Q174 proposes it should drop the entry to the cool tier instead), so nothing conveys that it will come back less often. Whether that needs any acknowledgement at all, or whether silence is right, is undecided. Worth settling with (a), since a skip that visibly *moves* something in the queue would answer it for free.

92. **Should a race lane be marked against the incumbent?** (opened 2026-08-16 from Q91 as a question about the change card, which no longer exists — Q203 removed it, so only this half survives.) Quick and patch lanes light their new wording green; a race lane shows no highlighting at all, because both sides are whole alternatives with no baseline to mark against. Marking each candidate against the *current* text is possible and would make all three cards identical in treatment, but on a wholesale rewrite it lights up nearly every word, which is noise rather than information. Left unmarked on that reasoning, but it is the one place the three card kinds still differ.

94. **A rationale is machine-drafted and human-edited** (opened 2026-08-16; settled by Ed 2026-08-16). The composer generates a first draft describing the change in mechanical terms — what it drops, what it adds — and the author edits it into their own words before submitting. This is the option I had been most wary of, and Ed's addition of the edit step is what fixes it: the words are the author's by the time anyone reads them, so nothing machine-written ever appears on a blind card under a human's name. It also recovers what the removed ledger was for, because a mechanical draft names the losses by construction and an author has to actively delete them.

    Consequences to settle before the composer is built. (a) **SPEC §2.6** currently says a candidate carries "one pinned rationale (≤300 chars)" and §3.4 that "to argue is to draft" — neither is wrong, but neither anticipates a drafting aid, and the spec should say whether the draft is a *suggestion* the author may discard wholesale (it should be). Needs Ed's sign-off and a version bump. (b) **Conditional on the LLM API**, per Ed: with no oracle the field is simply empty and the author writes from scratch, so the composer must not depend on it. *Built that way 2026-08-16: the rationale is an empty editable with a placeholder, and nothing in the composer calls an oracle. A drafted first line would drop into it without any other change.* (c) The **event log** should probably record that a rationale started from a draft, since "authored by a human" is a claim the record makes; unresolved whether that is a per-candidate flag or nothing at all. (d) A drafted rationale must never be *submitted* unedited by default — an empty-handed author pressing through would put machine prose on a card, which is the failure mode the edit step exists to prevent.

97. **SPEC §8.3 promises a prompt the card no longer has**

    **Where it stands (docs pass, 2026-09-07):** open — SPEC §8.3 still carries *the rival card's prompt states the conditional framing plainly* (SPEC.md line 172), so the false half stands until Ed amends it.

    (found 2026-08-16, from Q96's resolution). §8.3 reads "cards never offer 'keep the current text' on a rival pair … and the rival card's prompt states the conditional framing plainly." Ed removed those prompts in 95 and ruled the absence fine — members needn't learn the rules on every card. The first half of the sentence is still binding mechanism; the second half is now a false description of the product. Needs a one-clause amendment and a version bump, which wants Ed's sign-off since it is the spec.

174. **Skip in a margin index: what does it actually do to the entry?** (raised 2026-08-16, Ed's question, from Q89.) Ed asked whether deselecting a card should mean skip; answered no — closing is browsing, and binding a signal to the navigation gesture makes the signal dishonest (SPEC §3.1: "Skipping is not a move; the card returns later", and §8.3's only effect is personal recirculation with decay). So skip stays an explicit control, and should move into the rail per Q89. But the rail is a **margin index**, not a conveyor, and that changes what skip can mean: the entry cannot vanish, because the clause still has an open question and the margin is supposed to tell the truth about the document. Proposal to settle: skipping drops the entry to the **cool** tier — one line, palest wash — at its own clause, so it stops asking without stopping being there, and the decay is legible as "this went quiet". A second skip on an already-cool entry could compress it further. Related: SPEC does not specify the decay function at all (Q175), so "comes back less often" has no numbers behind it yet.

175. **The skip decay function is unspecified.** SPEC §8.3 says skipped cards "recirculate personally with decay" and stops there — no half-life, no floor, no interaction with the re-opened-race boost or the unheard boost. Needs a number before the sim can test it, and the answer probably wants sim evidence rather than a guess.

### Queue ordering — what survives Ed's 104–108

Ed replaced ranked ordering with a **margin index** (2026-08-16): entries stand beside their own clauses in document order, sealed items are dots, a patch appears once per site. Urgency was carried by colour strength and card size; since 2026-08-16 the card sizes are equal and urgency instead decides **which** entries fit on the screen, so the number now gates visibility rather than compression — which makes 98 below considerably more load-bearing than it was. That closed 99 (document position doesn't move when others judge), 101 (sealed stay, as dots — the rail is a map, and a map shows settled ground) and 103 (there are no serving *slots* left to explain; everything is shown at once). Three remain, all about the urgency number itself rather than about order.
98. **What is a race's urgency?** SPEC §8.1 scores each *pair* by pivotality v. A race with six candidates has many pairs of differing v; the rail shows the race once. Is that number the v of the single pair it would serve you next (honest — it's what clicking gets you), the max over its servable pairs (its potential), or a sum (its total remaining work, which would leave big races permanently at the top of the rail)? The mockup hand-authors one `urgency` number per entry and dodges the question. Now that urgency decides what you can see rather than how loudly it is drawn, a wrong answer here costs a member sight of a question rather than a shade of colour.

100. **Where c_p goes now.** A participant's judgment cost is one scalar dividing every v, so it never reorders their own list — and with the rail showing everything at once it can't truncate one either. It has to act on the *drawing*: since 2026-08-16 the rail already shows only as many questions as fit, most urgent first, so c_p's natural home is that cut — an expensive judge is shown fewer of them, and the rest stay in the count at the foot of the rail rather than being hidden. Undecided, but the mechanism it would act on now exists.

102. **Urgency is kind-blind.** Nothing distinguishes a copy-edit from a constitutional fight, so a typo near its floor can burn as bright as a structural race. Ed's north star ("most of a session should feel like approving typo fixes") argues for letting cheap items shine; the risk is a margin lit up by typos while a structural race sits pale. The `race-labeler` already stores copy-edit/substantive/structural but nothing consumes it (Q49) — the same decision arriving from the other end.

109. **Scale: the margin assumes a short rail.** Sixteen entries beside a twenty-page charter is a map you can scan. Two hundred would be a filing system, and document order would stop steering anyone toward decision margins at all — which is what §8.1 exists to do. V1 tunes to 5–10 members, so this is not urgent, but it is the assumption the whole design now rests on and it should be named before anyone builds for a bigger room.

111. **An open patch cannot keep its sites level and stay on screen** (found 2026-08-16, building 110). The decision card opens inline at the patch's first site and is tall, so the second and third sites move hundreds of pixels down the document — further than the visible band. Ed's 110 says needs-you entries stay on screen, so the sibling entries hold at the band edge and their wires run at an angle; only the topmost is level. Three ways out: (a) leave it — the spine still cables them and each wire still points at its own site, so the relationship is legible even when the geometry isn't; (b) let a patch's siblings leave the band while it is open, treating an open judgment as an exception to 110; (c) open the decision card somewhere that doesn't displace the other sites — a fixed panel rather than an inline insert, which is a much bigger change and cuts against the whole in-place design. I would take (a), but it is the first place where 108 and 110 genuinely pull against each other.

113. **Where read state lives, and when a decision goes quiet** (raised 2026-08-16, from Q112; part (c) settled by Q114 — an explicit acknowledgement is the only thing that clears an unread decision, so nothing marks itself read). Read/unread is the first per-member state on this surface — everything else in the rail is identical for everyone. Two things remain. (a) It has to be **stored somewhere**: a per-member marker in the event log is the obvious home, but an acknowledgement is not a judgment and probably should not be hash-chained with them; a side table keyed by member and candidate is more honest, at the cost of the log no longer being the whole story. (b) **Does an unread decision ever give up?** Ours pins forever, and now that only an explicit OK clears it, that matters more: a member away for two sessions returns to a rail of purple lines. Ed's read (2026-08-16) is that this is survivable because 115 protects the most urgent live card regardless — whatever else is crowded out, the thing that most wants them is on the screen. So the failure mode is a cluttered rail, not a lost one. Still worth deciding whether some relief is wanted — expire at a session boundary, cap how many pin at once and count the rest, or an *acknowledge all* — since the clutter is what the overflow count in 110 will be reporting.

122. **When do rankings become visible — at seal, or at close?** (raised 2026-08-16, from Q121.) A sealed race now shows its full field ranked, with each candidate's win probability against the current text. The warrant is good: Bradley–Terry already carries a strength per candidate, SPEC §8.3's no-standings rule governs *live* feeds (where knowing who is ahead makes judging strategic), and the `record-builder` publishes rankings at close regardless. But this is still rankings appearing **during** a session rather than after it, which is a small extension nobody has ruled on. Two things to weigh. (a) Sealed races are independent of live ones, so there is no direct leak — but seeing standings on settled questions normalises the idea that standings exist, and a member may start reading the sealed ones for camp structure. (b) It gives an author immediate, precise feedback on how their proposal did, which is arguably the humane thing and arguably a reason to redraft on scoreboard rather than on argument. If Ed wants rankings held until close, the card can show the outcome and the field without the numbers, and the numbers arrive with the record. Needs a SPEC §8.3 sentence either way.

124. **Teasers made the rail say more about fewer things** (raised 2026-08-16, from Q123). Quoting rationales grew a hot card from ~76px to ~117px and a two-teaser race to ~164px, so 110's fit cap now drops four or five of twelve entries on an 800px window instead of none. The overflow count reports them honestly, and this is the first time that rule has fired outside a contrived test — but it is a genuine trade and nobody has chosen a side. Options: (a) accept it — a margin that says something about eight clauses beats a list that names twelve; (b) clamp harder — one line on hot as well as warm, which roughly halves the growth; (c) show the teaser only on the entry nearest your reading position, so at most one or two are expanded at a time and the rest stay compact. (c) is the most interesting and the most fiddly.

125. **The open card duplicates its own teaser.** While a suggestion is open, its rationale appears twice on screen — clamped in the rail entry and in full on the card a few inches away. Harmless but slightly silly. The entry could drop its teaser while open, or the card could drop the rationale on the assumption you have just read it (clearly wrong — the card is the place it belongs). Probably (a), but it interacts with 124(c).

159. **The re-served race after a ground shift isn't modelled** (raised 2026-08-16, from Q156). When a race's ground shifts, the old judgment locks *and* fresh pairs are served — so the clause should carry two rail entries, the stranded 🔄 stub and a new needs-you card. The mockup shows only the stranded half (§ The Garden). Needs a decision: do they stack at the clause, does the stub collapse into the new card's history, or does it go straight to a filed dot?

### Marks for the other asks (raised 2026-08-16, Ed's question; awaiting his pick)

The deadlocked case showed that these marks say *what you are being asked to do*, not where in a lifecycle something sits. Settled 2026-08-16 (166–167, 173): a deadlocked race is ❌ *stuck*, in the open yellow rather than grey, and 💡 marks the propose actions it leads to — the rail states, the buttons act. Still open:
168. **Bridge wanted** — a deadlocked race where camps have been detected (SPEC §6.3) asks for something sharper than any new draft: one that spans two positions, which the composer briefs for. Own mark, or 💡 with a different briefing?

169. **Your candidate was dominated** (SPEC §8.4 already names it as a digest item). The ask is revise-or-withdraw, addressed to you as author.

170. **Your candidate needs rebasing** — engine-core carries `rebase-pending` and `displaced` (Q53). Distinct from 🔄, which is your *judgment* stranded; this is your *draft* stranded, and only you can move it.

171. **Does the rail carry authorship at all?** Every state built so far is you-as-judge. 169 and 170 are you-as-author, and so are the composer, the graveyard and the token ledger. Whether that belongs in this rail or on a separate surface is a bigger question than any symbol, and 169–170 wait on it. *Partly answered 2026-08-16 by building the composer: the rail does carry authorship, as the green `yours` state, and it works — a proposal of your own stands beside its clause like everything else and is force-kept on screen because withdrawing is always available (Ed, 240). So the placement question is settled, and 169–170 become questions about **marks** rather than about **place**: a dominated candidate and a rebase-pending one are both things that happen to a green entry, at a clause that already has your entry beside it. What is still open is whether they are their own glyphs inside green, or a line on the card.*

182. **A wide patch is now a walk, not a view** (raised 2026-08-16, from Q181). Splitting the patch card into one card per site pushes its places much further apart than the old stacked lanes did — three cards of ~450px between them — so `foldBetweenSites` (Q80) no longer brings the whole footprint onto one screen, which was its whole purpose. The stepper answers it for three sites. At six or ten it is less clear: the member never sees the shape of what they are approving, only a sequence of local views, and the only thing asserting "these are one change" is the wire spine and the footer text. Options if it bites: a "show all places" summary panel inside the first card (reintroducing the old view as an option rather than the default); a compact map of the footprint in each card's header; or a cap above which a patch reverts to the stacked form. Wants a fixture with a wider patch before deciding — the current one has three sites, which is comfortably inside the range where this works.

185. **Does "2 of 3 places" go too?** (raised 2026-08-16, from Q184.) Ed named three subtitles to remove — *copy edit*, *n proposals racing*, *one suggestion* — all of them category labels, and the kind chip with them. I kept the patch place count, on the reading that it is not a category but a *position*: it says which of a patch's clauses you are beside, which nothing else on the card does, and with the chip gone it is the only thing distinguishing three sibling entries from three unrelated ones. If the instruction was "clear the whole subtitle line", it is a one-line removal — but then a patch's siblings are told apart only by the wire spine when one is open, and by nothing at all when none is.

187. **Should a live card show the floor as a headcount too?** (raised 2026-08-16, from Q186.) The navbar now says *voices needed 5 of 14* and the sealed record says *5 of 14 weighed in · floor was 5*. A **live** card shows only the evidence-meter and its caption, and § Front-door Keys is the one entry whose caption mentions the floor at all ("three of the fourteen have weighed in — the floor is 5"). Making that consistent — every live card stating "n of 14 so far, floor 5" — would be honest, magnitude-only, and probably the single most reassuring number on the surface. The reason to hesitate is that a running judge-count is *nearly* directional: watching it climb tells you the race is being sampled, and combined with the meter it narrows what the posterior can be. Probably safe (SPEC §8.2 puts per-race judge counts in the record and the mockup already exposes one), but it should be a decision rather than a drift.

207. **Nothing now says which lane is the current text** (raised 2026-08-16, from Q206). With "Option A/B" gone (197) and the lane descriptions gone too, a decision card is two boxes of prose. Which one is the incumbent is carried by **position** — left, always — by the fact that the left lane matches the paragraph directly above the card, and by a footer clause saying that choosing the current text records a judgment for it. That is probably enough for a member who has judged a few, and it is the strongest symmetry the card has ever had. The risk is the first card someone ever sees, where the convention has not been learned yet. Cheapest mitigations if it bites: a one-line prompt above the lanes on a member's first few cards only; or a very quiet marker on the incumbent lane that is not a title. Wants a real reader before deciding.

208. **Skip now exists only on the 🔥 card** (raised 2026-08-16, from Q202 — Ed's call, with the reasoning that skip belongs where the surface *insists* and nowhere it merely offers). Two loose ends. (a) SPEC §3.1 and §8.3 describe skipping as available on any card, so either the spec gains a sentence saying it is a property of *insistent* serving rather than of cards, or the product is knowingly narrower than the mechanism. (b) The personal recirculation-with-decay that skip triggers now applies to at most one suggestion at a time, which may make the decay function (Q175) nearly irrelevant — worth checking in the sim before anyone spends effort calibrating it.

212. **Closeness is now only on the queue card** (raised 2026-08-16, from Q210). Removing the `evidence-meter` from decision cards leaves closeness-to-resolution — and a race's "you've judged 2 of the pairs you'll be asked for" — visible *only* in the rail. A member who opens a card by clicking the paragraph in the document, rather than its queue entry, sees neither. Three readings: it does not matter, because the rail entry is a few inches away and lit; it matters for the coverage line specifically, which is personal and has no other home; or the card should carry a single quiet line of both. The coverage half has been in this state since 95 without complaint, which is weak evidence for the first reading.

264. **Green now means three things** (raised 2026-08-16; widened the same day by the fix to 263). In a rail or a gutter it means *yours, and you can still act on it*; inside a card it means *decided* — the winning text in a sealed record, the tick that commits — and inside a **lane** it means *new wording*, which it has meant since 91 and now appears in the composer as well as on every decision card. They never meet, because 164 washes every decided state grey in the margin, so `--ok` is confined to card interiors. The margin pair are kept apart by 164, which washes every decided state grey in a rail, so the *decided* green never leaves a card interior — a genuine dependency between two decisions rather than a coincidence, and one that breaks the moment a decided state gains a colour in the rail. The two **inside** a card are kept apart only by scale and place: a whole block or a whole button for *decided*, a few words inline for *new wording*. One collision has already been headed off — the editing lane’s focus ring was green and is now blue, because a green box around green words was two meanings an inch apart. Worth a proper look at the palette pass; the likely answer is that *new wording* wants a hue of its own, since it is the only one of the three that is not about a state.

266. **One draft at a time** (raised 2026-08-16; Ed asked what the question was, 2026-08-17). **The question: should starting a second draft somewhere else be possible, and if not, should the surface say so?** There is one caret, so there is one composer: with a draft open, typing elsewhere extends or reuses it rather than starting another. That is almost certainly right — a second unfinished draft is a second thing to lose — and it is what keeps the state model to two flags on an ordinary suggestion. But nobody chose it; it fell out of the implementation. And the surface never says it: pressing ✏️ on a lane while a draft is open quietly extends the open draft instead of warning that you are about to.

282. **A rendered document and a source offset do not agree** (raised 2026-08-17, from 279's build; Ed asked for a recommendation the same day). The charter fixture is plain text, so the composer can take a caret offset straight from the DOM and use it against the clause's source. The moment the *document* renders markdown that stops being true: a DOM selection measures the rendered text, and `**bold**` is four characters there and eight in the source, so every offset the composer computes is wrong by two for each mark above the caret.

**Recommendation: render markdown in the document, and make the rendered→source mapping a property of the block rather than of the composer.** One function that takes a block element and a DOM position and returns a *source* offset, used by everything that asks the question — `offsetIn`, `startDraftFromTyping`, `startDraftFromRun`. The arithmetic already exists (`richToSource` / `sourceToRich`, written for the lane's view toggle); what matters is that exactly one place knows about the distinction, because the failure mode here is a two-character drift that nothing complains about and no test catches unless the test happens to put a mark above the caret.

Two other options, for the record. Keep the charter plain and let markdown live only inside candidates — cheapest, but an adopted candidate would then print its asterisks into the charter, so it only defers the problem to the first adoption. Or store the document rendered and treat markdown purely as import/export — which gives up "the characters you see are what is stored", and that was the whole reason 279 chose source-of-truth markdown in the first place.

**Do it before the charter carries any formatting**, not after. Until then every test passes and the bug does not exist; the day somebody bolds a word near the top of a long clause it exists everywhere at once, and it will look like a caret bug rather than an offset bug.

**Deferred past the alpha, deliberately** (Ed, 2026-08-27, answering this from the alpha-readiness plan's Track 3.3 — plan-queue **77**, backlog entry 77; the plan had said *fix before the day*). Option **(b)**: the charter stays plain text for the alpha and the rendering is done afterwards. Nothing changes before the day.

Three facts the deferral rests on, checked in the tree at `0d160ad` rather than assumed. **The charter renders plain text today** — `design/session.js:2994` and `:3009` emit `esc(line.x)`, so a clause carrying `**bold**` prints its asterisks — which means the bug is not merely unobserved but *unreachable*: `offsetIn` (`design/session.js:1875`) measures rendered text, and for a plain-text document that **is** the source. **The recommendation above is not a bug fix**, it is *render markdown in the document*, a visible change to what every reader of every charter sees; the arithmetic it needs already exists as `richToSource` / `sourceToRich` (`design/cards.js:342`, `:352`), but the change also moves `design/tools/card-copy.golden.json`, both frozen probes, `scripts/journey-walk.mjs`'s caret and propose steps, and what `design/STYLE.md` takes a clause to be. And **the alpha is short**: reaching the bug needs somebody to bold a word *and* that proposal to win, where the measured preset produces about one adoption per fifteen-minute session (`PRODUCTION.md`'s running log, 2026-08-27).

What the deferral costs, stated so nobody rediscovers it as a surprise: **if an alpha room does adopt a clause carrying a mark, that document's offsets are quietly wrong from then on** — in a throwaway document, which is the whole of why this is affordable. The two other options are on the record above and neither is rejected; (c), building only the block-to-source mapper without changing what renders, remains the cheap half of (a) if the rendering is wanted in a hurry.

288. **A flatter glyph set for the lifecycle marks**

    **Where it stands (docs pass, 2026-09-07):** partly overtaken — the lifecycle family is drawn rather than emoji (`DRAWN` in `design/session.js`; SURFACE §6), which is option (a) for those marks; the subject glyphs are still emoji, so the pack question survives only for them.

    (Ed, 2026-08-17, alongside making the gutter marks read as buttons: "I think changing to a flatter emoji pack and making them a bit larger would help"). The larger half is built, along with a resting ground and a press. The pack is a real question, because the design series is **single self-contained files with no network**, so there is no CDN to point at. Three ways: (a) inline the glyphs as hand-authored flat SVGs — total control, they would take the lifecycle hue as a fill, and it is the only option that actually looks designed rather than borrowed; it costs drawing nine symbols and they then have to work at 16px. (b) `font-variant-emoji: text`, which asks the OS for the *text* presentation of the same characters — one line of CSS, genuinely flatter, and it hands the glyph over to `color`; but the shapes are then whatever the platform has, and it would take colour away from the emoji that currently carry their own, which the contents rail depends on (260). (c) Embed an existing flat pack (Twemoji is CC-BY) as data URIs — flat and consistent, but it is somebody else's drawing style and a licence line in a mockup. Recommendation: (a), and only once the alphabet has stopped moving, since it makes every future change to a mark a drawing job.

293. **Revising your own proposal** (Ed, 2026-08-17): *people will often draft something, submit it, then immediately want to edit it again because they saw a mistake — we should think about how to make this action convenient.* Today the path is withdraw, then write again: two acts, and the candidate leaves the race in between. The obvious affordance is a **✏️ on your own proposal card** that reopens the composer seeded from your own wording and, on Propose, replaces the live candidate rather than adding one. It composes out of moves the spec already has (withdraw refunds in full §3.3a, propose costs one) so it is **net zero edits**, which is right — a typo fix should not be priced like a second opinion.

What it needs a ruling on is the evidence. A revision is a **new candidate**: any comparisons already cast against the old wording were cast against a text that no longer exists, so they are discarded exactly as a ground shift discards yours (§4.4). That is correct and it is also a real cost — revise a proposal that six people have already judged and you have thrown their work away, and they will be served the pair again. Three ways. (a) **Always allowed, evidence always resets** — simple, honest, and quietly expensive on a busy race. (b) **A grace window**: free revision until the first judgment lands on it, and after that it is withdraw-and-repropose with the cost visible. This matches what the request is actually about — *immediately*, because they saw a mistake — and it makes the common case free and the expensive case explicit. (c) Revision only as **surgery on the wording**, with evidence carried over where the change is below some edit-distance floor; cheapest for the room and the hardest to explain, since it makes the ranking depend on a diff threshold. Recommendation: **(b)**, with the card saying which side of the window it is on.

296. **The experience of writing a patch** (Ed, 2026-08-17). The cable half is done — a patch is now joined at its cards by a cable in the `chip-gutter`, leaving each `clause-tab` by its left side (see the glossary). What remains: `draft-site` says a second edit somewhere else turns your draft into a patch in the making, and the mockup renders that, but nobody has designed the *experience* — how you know you are now writing one judgment in several places, how you move between the sites while drafting, whether a site can be dropped, and what the single Propose is committing you to. Both are unstarted.

305. **Per-candidate advisory labels** (raised 2026-08-17). A six-word label per wording saying what *this* one does differently — *removal only after a broken sanction* · *neutral proposer* · *removal is not a sanction* · *both, plus a night's sleep* · *twice in a year* · *sanctions expire* · *two-thirds for removal* · *ask the visitor first*. It makes a field of eight scannable in one pass, and you read the full wording only for the ones you care about. Same family as `race-labeler` and the same discipline — advisory, outside the state machine, never in the event log, never gating anything — but it is a **new** model output: the labeler names *races*, and this names *candidates*. It is also the cheapest possible version of what a camp map does, since two candidates with near-identical labels are visibly the same camp without anything having to say so. Recommended first, because it is useful on any card with more than two wordings, not only a deadlocked one.

306. **The `camp-map`, finally built** (raised 2026-08-17). Group the field by camp, each under a heading saying what that camp will not give up — *removal must not be reachable in one step* / *the ladder is fine, the proposer must be neutral*. For the job the deadlock card exists for this is the most useful thing that could be on it: a bridge has to satisfy both, and until you can name them you are guessing. Two costs. It is **judgment-derived** — which wordings cluster is computed from how people judged — so unlike 305 it is not free of SPEC §3.5, which lumps camps in with standings as briefing-only. The defensible refinement is that **structure without direction is a magnitude**, which §8.3 already permits: saying *the room divides in two, and here is what each half wants* names no leader and no sizes. That is a real spec amendment and wants Ed rather than me. Second cost: it is the one thing here that could be wrong in a way the reader cannot check.

308. **The one overhang a fitted pile cannot remove** (raised 2026-08-17). `fitStacks()` shrinks a stack's peek until it reaches no further than the next mark in the gutter, which makes two anchors claiming one strip structurally impossible — measured across all 32 columns in the fixture, zero collisions. But a pile's floor is **one tab**, 30px, and there is nothing to shrink when a clause holds only one. A block shorter than 30px whose next mark is within 30px would still overlap, and headings are 17–19px. It cannot happen in this fixture and it is not hypothetical: `always-on-typing` covers headings, so a heading can carry a decision. Two ways. (a) A **layout pass that pushes down** — no mark may start above the previous one's bottom, so a mark steps out of the way rather than overlapping, which is precisely what the `needs-you-queue` already does around a pinned card. Costs the rule that a mark stands beside its own text, in exactly the cases where it cannot. (b) **Give short blocks a floor** in the gutter — a minimum block height wherever a mark sits. Deforms the charter to suit the margin, which is the wrong way round. Recommendation: **(a)**, and only when a case actually appears, since it is invisible today.

310. **The three per-type notes on a decision card** (raised 2026-08-17, housekeeping pass). A race card, a patch card and a salience diagonal each carry one grey sentence saying what that kind of card does: *neither of these has to win — the clause above stands unless the leader clears the bar*; *one judgment for all 3 places — choosing here chooses everywhere*; *this ranks the questions, never the answers*. They are the same species as the three footnotes Ed cut on 2026-08-17 — the `yoursnote`, the revise line, the deadlock caveat — by the test he was applying: a sentence that appears on every card of its kind is a design note rather than information. Two things make these different, which is why they are a question rather than a deletion. They are per **type** rather than per card, so a member meets each of them a handful of times and not on every judgment. And each says something the card cannot say any other way: a race has no radio that keeps the clause, a patch's single ✓ commits in three places you cannot all see, and a diagonal is the one card that changes no text. Three ways. (a) **Keep them**, and treat "one line per card *type*" as a device the surface owns. (b) **Cut them**, and let the type declare itself — a race by having two candidates and no keep-radio, a patch by its ↑/↓ stepper and place count, a diagonal by its head being a question. (c) **Move them into the field label**, which already names the type (*Proposed · 2 rival proposals*), so the fact and its consequence are in one place. Recommendation: **(c)** for the race, where the label already says *rival*; (a) for the patch, whose note warns of a consequence rather than naming a shape. They were moved **above** the commit row in the same pass regardless of this, because the commit row is the card's bottom band everywhere else.

311. **Is a locked judgment card without a ground shift reachable?** (raised 2026-08-17, housekeeping pass). `reviseNote` has a branch for `s.locked` that says *this one is settled, so your judgment is on the record as it stands (SPEC §4.4)*, and `commitRowHtml` drops the ✓ for it. No fixture reaches it: the only locked card in the mockup is also ground-shifted, and `shifted` is tested first. So a whole card state exists in the code and has never been drawn. The doubt is whether it can exist at all — a race that settles becomes a **sealed record**, which is a different card, so it is not obvious what leaves a *judgment* card locked. Either it is a real intermediate state (settled but not yet sealed, or sealed while you had it open) and wants a fixture so it can be looked at, or it is not and the branch and the `locked` flag should go with it.

312. **A ↻ never has its re-served question beside it** (raised 2026-08-17, answering Ed's question about the ↻ lifecycle). SPEC §4.4 says a ground shift locks your old judgment *and* serves the pairs on the new ground fresh, and §6.2 gives a re-opened race a 1.5× routing boost so it comes back **sooner** than an ordinary question. So in the mechanism a ↻ is a receipt standing next to a live question — the locked fact about text that no longer exists, and the same race asking again. In the mockup it is a **terminus**: § The Garden carries exactly one entry, the ↻ itself, and there is no fresh 💡 anywhere near it. `shifted` is a fixture flag that nothing sets or clears at runtime, so the surface renders the state and never the transition into or out of it. Two consequences worth a ruling. **The pairing has never been looked at** — a member meeting a ground shift for the first time meets it as *your judgment is void* with nothing offered in exchange, when the actual experience should be *that one is void, here is the question again*, and whether those are two rail entries at one clause or one entry that carries both is a real design question nobody has asked. **And the mark may be wrong on its own terms**: ↻ was chosen to mean *comes back around, rebuilt* (glossary), which is a promise the surface currently never keeps. Cheap to fix in the fixture — a second entry at § The Garden, judged-fresh, would show the pair — and that is probably the first step, because it is hard to design the pairing without seeing it.

313. **Nothing acknowledges the work** (found 2026-08-17 running Ed's flame-to-flame workflow end to end: click 🔥, judge, click the next 🔥, repeat until the queue is empty). The mechanics are sound — eighteen judgments, every flame opening on the first click, the flame handing over every time, the clause never moving, the salience diagonal arriving when the queue emptied — and the whole run is **silent**. Two halves. **It ends with nothing said.** You answer the last question, the diagonal is served, you answer that, and the screen is simply a screen with nothing on it that wants you. Q291 recorded a third state — a panel saying the priorities are as clear as more answers would make them — and it went out with the *offer* when Ed changed the diagonal to *served, not offered*. That was right about the offer and took the completion state with it by accident: the two are different things, and one of them is the only moment the surface has to say *you are done here*. **And nothing accumulates.** The topbar's *confidence bar 74%* and *quorum 5 of 14* are static markup, never re-rendered; eighteen judgments move neither. In the mockup that is a fidelity gap rather than a bug, but it is the place a session's vital signs live, and a workflow made entirely of judging is precisely the one that never touches them — so the member with the highest contribution is the one shown the least evidence that contributing did anything. Worth deciding together, because the answer might be one thing: if the bar visibly moved as judgments landed, the end of the queue might need no panel at all. Related: the `room-pulse` already beats for your own actions, so the surface has a place that says *something happened* and no place that says *something changed*.

314. **Seeing which card other members have open** (Ed, 2026-08-17, and **shelved by him in the same breath** — *let's shelve this for now until we actually test with a group of real humans, otherwise this is quite speculative*). The idea: the surface shows who is judging what, right now. Recorded rather than acted on, with the two things that make it worth returning to and the one that makes it hard. It is the natural extension of the `room-pulse`, which already says *the room is moving* and deliberately says nothing else; presence is the next rung and the first one with content in it. And it is the only proposal so far that would tell you a live *question* is being worked on rather than that the document is. What makes it genuinely hard is that it collides with SPEC §3.5: knowing that eleven people are looking at one race is a fact about where the attention is, which is close to a standing, and knowing *who* is on a card is a step towards knowing how they judged it in a room of fourteen. Ed's instinct to wait for real humans is the right test — this is exactly the kind of thing that reads as either warm or surveilling depending on the room, and no amount of reasoning here settles which.

335. **Split `Constitution` into what the room agreed and what the engine is tuned to**

    **Where it stands (docs pass, 2026-09-07):** half done — SPEC's Appendix A is *Engine tuning* and §9.7.1 holds the settings (Q562); `packages/engine-core/src/types.ts` still declares one `Constitution` interface with `hotSetSize` beside the threshold (lines 16–19).

    (raised 2026-08-18, PLAN.md §2.2; expanded after Ed asked why it matters). engine-core has one object holding both — the threshold, the grant, the drip, the stake and the window on one side, and `hotSetSize`, `explorationEvery`, `boutGapMs`, `deadlockEpsilon`, `cooldownMs`, `redraftLimit` on the other. Three things go wrong while they share a name. The **record** published at the close would list a tuning constant beside the threshold as though the room had agreed it. The **motion** machinery routes on whether a setting is constitutional, and with one object there is nothing stopping somebody moving to amend `boutGapMs`. And the **ceremony** has no definable output: what the founding questions produce *is* the room’s half of this object, and it cannot produce half of a thing. The cost is a rename across engine-core and sim-harness today, against a data migration once real documents exist with saved settings. Recommendation: **now**. The only reason it is a question at all is that it touches a package that should not change without Ed’s word. **Status 2026-08-18:** the in-depth review Ed asked for is delivered as REVIEW-creation-session.md; its §7 is the definitive field-by-field mapping, and the split is one commit once he has read it (338’s answer decides one field’s home). *The document-level half landed 2026-08-22 (spec pass 1, Q562): Appendix A is now* Engine tuning *and holds only what nobody is asked; the settings are §9.7.1. The engine-core object is still one.*

346. **Sybil resistance at the front door** (Ed, 2026-08-18, parked: *if people can become members via a link, we have to be wary of sybil attacks — perhaps even accidental*). *Note, 2026-08-26 (entry 94): the open door is now 🤝 *yes* with 🪪 at ✒️ — an application admitted on submit — which is exactly where this bites; the same price at 🏛️ or ✏️ puts a room between the link and the membership.* Membership begins at first arrival (§9.6a) and the magic link **is** the credential, so the attack surface is the invitation itself. Ed’s own example is the subtlest and the most likely: **one person accidentally becoming two members on two devices** — possible only if membership is minted per *arrival* rather than per *invitation*, so the structural rule is that however many devices open a link, they land in one membership: a link must never create an identity, only unlock one. Three more vectors, one of them innocent: a **forwarded link** logs the bearer in *as the invitee* — two people quietly sharing one membership, or a colleague “helpfully” opening a link that was not theirs; a **double invitation** (one person at two addresses) makes one person two members, two wallets, two ceremony answers and two places in quorum; and deliberate padding by whoever controls invitations. Sharpened by §9.6a itself: a duplicate discovered **after the start** cannot be removed, because removal is a constitutional motion needing every member’s consent including the duplicate’s — so whatever the defence is, it has to work **before** judging opens. Candidate defences for later, cheapest first: (a) first-open **confirmation** (“you are Ida Fenwick — is that you?”), with later opens of the same link joining the same membership rather than a new one — note the tension with single-use links, which would lock the person’s second device out instead; (b) the convenor’s pre-start window is the human check — arrivals are visible, uninviting is free, and the start is the deadline for noticing; (c) a **merge** operation for discovered duplicates that is not an expulsion (one person’s two memberships becoming one re-rates nothing they did not do themselves) — needs care where both halves answered the ceremony differently. Deliberate sybil by an adversarial *convenor* is out of scope: they control the roster before the start anyway, and the defence against a bad-faith convenor is not joining their document.

349. **Crown residuals** (raised 2026-08-18, around §9.7’s 👑). Ed settled the shape — assent ends either route on any reserved setting, lapse is automatic assent, the 👑 follows any reservation, and (v0.52) the founder may delegate anything once proposing opens, which closed the old (b). Two edges remain. (a) **Whose clock?** A crowned convenor may be a clerk, who casts no judgments — does administering (inviting, changing ordinary settings) count as the activity that keeps a crown alive, or only member-acts? Recommendation: any authenticated act on the document resets the clock; the crown lapses on absence, not on abstinence from drafting. (c) **Does the crown cover removal during the ceremony’s blind collection?** A crowned convenor removing an answered member mid-ceremony is §9.6a’s ground-shift case already; nothing new needed — note and close.

350. **Setup-grammar residuals**

    **Where it stands (docs pass, 2026-09-07):** (a) is superseded — a delegated card waiting on the room leaves the rail whatever its kind (Q517; SURFACE §6 `wait`); (d) was closed by Ed the same day; (b) (c) (e) stand as built, unruled.

    (raised 2026-08-18, the pass that put setup tasks on the session-view lifecycle — Ed set the frame: *yellow when open, ⏳ while waiting for other input, grey when decided, they lose their custom emoji and just become ✔s*; these are the calls made inside it, each cheap to flip). (a) **⏳ entries stay in the rail**, their fill the watching UI — pile-only was tried on paper and fails concretely: mid-verification the rail would be empty with the mail modal closed and no handle left to reopen it. Gates still wait as pile tabs only, per their own rule. (b) **No 🔥 on setup**: the dependency chain already serves the founding one question at a time, and every setup question is mandatory — a flame chooses among optional judgments, which is a category this surface does not have. (c) **Owed decisions are news-green**: a member's un-acknowledged constitutional settings, a resolved blind question (for everyone who answered it, convenor included), and an opening gate all wear the changed wash with a drawn ✔ and pin until OK — superseding gate-cards' earlier *yellow with an OK*, because on the live surface an unacknowledged decision is green news, not a hot ask. (d) ~~All-✔ piles~~ closed by Ed same day: **a retired tab keeps its subject glyph** — the piles are a menu — so only the rail entry retires to ✔; the constitution block's lines stay buttons besides. (e) **The applicant wizard keeps its done tasks visible** as a checklist — an admitted departure from done-leaves-the-rail, defended by the four tasks being that surface's whole content.

384. **A creation-time holder choice for the title and link?** (raised 2026-08-19, unanswered in chat.) v0.52 has them *begin* reserved, with delegation available from the moment proposing opens — read off Ed's "the option to unreserve either or both as soon as proposals open". A radio at creation would let a founder start them delegated; probably noise (there is nobody to hold them for until the room exists), but cheap if wanted.

386. **No surface yet raises a reserve motion** (raised 2026-08-19, unanswered in chat; generalized by v0.52). The module carries the `reserve` payload on any held-able setting and the page renders such motions defensively, but no composer offers "return this to the founder's reserve" — it wants a home, probably a second option on a delegated setting's own composer, in the same sentence grammar the delegation itself now uses.

387. **Reserving the text** (Ed, 2026-08-19, kept out of scope *for now*): every change to the document needing the founder's assent to merge — against the spirit of the product, and also exactly how suggested amendments work in many text-editing products, which is why it is parked rather than dead. If it ever lands, it is a fourth thing the 👑 can hold, and the assent step already exists (§9.7).

388. **Individual delegation, foundership transfer, inheritance** (Ed, 2026-08-19, out of scope — *maybe when we make the sequel!*): founders delegating powers to individual members rather than the room; transferring the foundership deliberately or on lapse. The 👑 is a benevolent dictator for life, not a literal monarch — nobody inherits it when the founder lapses (v0.49's auto-assent is the whole story of an absent crown).

401. **(b only — (a) closed by Ed 2026-08-19, *perfect*, and built: SPEC v0.60; the founder-direct removal path closed 2026-08-26, built as ❌'s ✒️, exile at will and immediate — entry 94, v0.74.)** Under the 🥾 removal setting's **proposal** rung (*ordinary* until v0.74), may the subject judge their own removal race? Today nothing stops them: the module puts no per-race judge restriction, the mock counts every member's judgment, and the engine race (`remove:<id>`) serves them pairs like anybody. Arguments each way: their §3.3-style stake in the outcome is exactly the author's-own-preference case the spec already counts; against, a removal race with the subject judging is the one race whose electorate the constitutional rungs deliberately shape, and the ordinary rung silently un-shapes it.

406. **Subsection headings don't fold**

    **Where it stands (docs pass, 2026-09-07):** partly built — every heading folds, lvl3 subsections included since entry 96 (CLAUDE.md `sectoggle`); the lvl3s are still not in the TOC.

    (raised 2026-08-19, from the heading-click build). Clicking a heading's text folds it — prose headings, the constitution's sections, and the Constitution heading itself — but the lvl3 subsection headings ("Members" in Membership) carry no fold state: the membership body is hand-composed and the fold machinery is keyed per section. Worth folding too, or is one level of folding inside the constitution enough?

398. **The multi-candidate ordinary race card** (raised 2026-08-19, from the Q391 build; Q390 made rivals real). Rival values on one setting join one engine race, but the page still renders “the first running motion” as a two-way card (what stands vs the proposal). The engine serves rival pairs with the conditional prompt (“if this text changes, which change is better?”, Q48) and the served card is already in the view — what is missing is session-view card grammar for a setting race with three or more lanes, and the rule for which motion card a second rival renders into. Wants Ed's design pass, not a mechanical fix.

419. **Does the desktop focus order match the reading order?** (mobile plan, 2026-08-20). The phone fix for DOM order is `order:` on grid children precisely so the DOM stays canonical — but nobody has ever tabbed through the desktop surface, and if that order is wrong the phone inherits it. *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §4 (2026-08-23): no `order:` anywhere; the desktop pass tabs the page and asserts DOM order.*

420. **The narrow form of the margin index** (mobile plan, 2026-08-20): on one column, does a rail entry render immediately above its own clause in the document flow (recommended — "beside" is a claim about adjacency in document order, and a phone's only axis of adjacency is vertical), or do clauses carry marks only, with entries reachable from them? *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §4 (2026-08-23): the second — marks in the gutter, the pinned population in a bottom dock (Q657).*

421. **The fold triangle is invisible until hover** (mobile plan, 2026-08-20). On touch it must always be visible — should it then always be visible on desktop too, rather than the surface having two different answers? *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §4 (2026-08-23): visible under `(hover: none)`, hover-revealed elsewhere — one rule, two renderings (Q658).*

422. **Judging offline** (mobile plan, 2026-08-20): offline is read-only on the blindness argument — a judgment cast against a stale pair is a judgment on a question that no longer exists. Is a *queued* judgment ever acceptable if the client re-validates the pair on reconnect and discards silently when stale? *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §3.e (2026-08-23): no — the radio position survives and is restored if the same pair is still served; the member presses ✓.*

423. **Does `room-pulse` need a third state on a phone?** (mobile plan, 2026-08-20). It says *the room is moving*, and a backgrounded PWA cannot say anything at all — is silence distinguishable from stillness, and should it be? *Answered by Ed 2026-08-23, against the recommendation: **no pulse in the mobile app** — `design/MOBILE.md` §3.f; the offline word stays on the clock line.*

424. **Multi-block drafting on touch** (mobile plan, 2026-08-20). A selection spanning blocks is one candidate on desktop; the caret proxy (prose `contenteditable="false"` on touch, since mobile IMEs do not honour `preventDefault()` on `beforeinput`) has no obvious gesture for it. *Deferred by `design/MOBILE.md` (2026-08-23) to stage 6; §1.7 names the seam — the door is the lane's ✏️ and a long-press on a clause, never the caret.*

425. **Do lanes collapse or scroll on narrow** (mobile plan, 2026-08-20) when a card carries two candidates side by side? *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §4 (2026-08-23): closes — every candidate pair is already a stacked `.propblock`; asserted as no card overflow at 360px.*

426. **One PWA icon or many** (mobile plan, 2026-08-20) — the icon identifies the app, but a member may be in several documents. *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §3.b (2026-08-23): one icon art, a manifest per document, many installs (Q669).*

427. **`--border` contrast at phone brightness** (mobile plan, 2026-08-20) — several hairlines are below 3:1 and load-bearing (the incumbent's dashed edge in the sealed record, for one). *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §4 (2026-08-23): a `--border-strong` token for the load-bearing edges, users named in the selector, surface-wide (Q659).*

428. **Where do push subscriptions live** (mobile plan, 2026-08-20), given the log is append-only and a subscription is deletable per-device state — exactly the kind of thing the `people` table (Q436's schema) exists for? *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §3.d (2026-08-23): the `people` table is adopted but unbuilt — a `push_subscriptions` table now with a nullable `person_id`, both stores, never the log (Q673).*

429. **How big is the offline shell allowed to be** (mobile plan, 2026-08-20) before caching the surface needs its own budget? *Answered by Ed 2026-08-23, against the recommendation: **no budget** — `design/MOBILE.md` §3.c.*

463. **The close is undesigned, and by this session's own logic it needs 🍾's treatment** (raised 2026-08-20). Everything decided today rests on two rules — **a thing that happens should be a thing somebody does**, and **no power arrives without acknowledgment**. Both have an unexamined mirror at the other end of a document's life. A windowed document reaches its closing date; a perpetual one freezes when active plus holding falls below quorum. Neither moment has been designed at all, and both are larger than 🍾: the close is when the document becomes **final**, which is the only irreversible thing in the mechanism. **Ed, 2026-08-22 (spec pass 1, item 567): the communication of the closing ceremony has not been designed at all — put it in the backlog.** What a member is told at the close beyond the 🥂 card and the one mail (SURFACE.md E24) is this item's.

    What the symmetry asks:
    (a) **Is there a closing act, and whose?** 🍾 is the founder's, because before the start they are the only one who can act. At the close there is a whole room, so a founder-pressed ending would be the one unilateral power nobody granted them — which argues the close is either automatic (the clock) or a consensus act (🏛️). *Ending the document at all* is already constitutional, so the machinery exists.
    (b) **Do powers leave with an acknowledgment?** By 453's rule they arrived with one. Unspent ✏️s become meaningless at the close; a 🏛️ in flight has nowhere to land. Something should say so, and the wallet emptying is the obvious place — the inverse of the pencil-storm.
    (c) **What does the last hour look like?** A rising bar means late changes need near-unanimity, and a member who wants one last fix should be able to see that the window is closing. Nothing on the surface says how long is left.
    (d) **What happens to live races at the close?** They degrade to backlog, per the spec — but a member watching their own proposal die in a race that ran out of time is a real moment with no design.

464. **The record is undesigned, and it is what the machine produces** (raised 2026-08-20). `record-builder` has been in the glossary since the start — *final text plus the record: rankings, camps, graveyard, care map, minority map, backlog, audit log* — and no surface has ever been drawn for it. The per-decision `sealed record` card exists and is good, but that is one clause's history; the **document-level** artifact, the thing a convention hands to the people who were not there, does not exist as a design.

    It is worth raising now rather than later because two things this month bear directly on it. The **constitution-as-document-text** work means the finished artifact already has a natural shape — the rules, then the charter — and the record is the third part. And **the stranger's door** (456) is the first surface built for somebody outside the room, which is exactly the record's audience.

    (a) Is the record a **separate document**, or the same document read in a closed state — the redacted page's opposite, everything open?
    (b) Does it have its own URL, and is it the one people share?
    (c) How much of the graveyard and the minority map is public, given authorship is sealed by default and the record is where seals come off (§3.4)?

    **Answered 2026-08-29 (Ed): *ratify what is built — same document, same URL, read closed — and treat (c) as the only live question.*** So **(a) and (b) are settled by ratification and are spent**: the record is not a separate document and has no URL of its own; it is this document read in its closed state, which is what `closedBlocks` and `.doc.closedpage` already build — every clause grey, every tab filed, the `sealed-speaker` revealed, wallets and pulse gone, the document ending bracketed as constitution · charter · backlog with the signatures beneath. Nothing is owed for either half; the tree is the answer and this records that it was chosen rather than drifted into. **(c) is the only live question**, and it is the one that needs a mechanism rather than a page: how much of the graveyard and the minority map a **stranger** may read, given §3.4's seals come off at the close and §3.5a's judgments ladder now has a per-decision reveal rung coming (**996**). Those two interact — a document that reveals judgments as each decision settles has already published most of what (c) is about — so (c) should be answered **after** 996 is built, not before. **(a) and (b) spent; (c) open.**

465. **Notifications are the surface nobody has designed and everything depends on** (raised 2026-08-20). A convention runs over days or weeks and the participants are not sitting in the page. `router` has carried *notification digests* as a named part since the beginning, with no design and no copy. Every other decision this month assumes people come back — the drip accrues, the bar rises, lapse warns before it bites, a 👑 question waits, a motion needs everybody — and all of it is inert if the mail is wrong.

    (a) What is the default cadence, and is it a **setting** (in which case, whose — and is it constitutional by the test, since it changes who effectively participates)?
    (b) What does a digest contain, given the blind discipline? Counts and your own tasks are safe; anything about standings is not (§3.5).
    (c) Does a 🔥 — *the question you are being asked next* — ever justify an immediate mail, or is everything batched?
    (d) The lapse warning and the freeze alarm already have to be mails. They are the two that exist by necessity, so they are the place the vocabulary gets set.

    **Answered 2026-08-29 (Ed): *take the whole thing as one design pass with its own plan.*** So (a)–(d) are not four questions to be answered one at a time in the register — they are the agenda of a design pass, and the pass is what answers them. That is the same shape as **1009**'s extraction and **464**'s (c): a commissioning, not a resolution, and the item stays open until the pass has run. **What the plan inherits**, so it does not start from nothing: the mails that already exist are the founding invitation, the magic link, the §9.7a save notice and the close (`MAILS` on the page, `mail-fold` in `packages/server`), `design/STYLE.md` §6 already holds the mail copy rules, and **the lapse warning and the freeze alarm are the two the mechanism obliges** — (d) is therefore where the pass should start rather than where it ends. **Answered as a commission; the design pass is owed and unwritten.**

500. **The legal drafts' decisions.** (Stage 12, 2026-08-20; the drafts are `docs/legal/PRIVACY.md` and `TERMS.md`, placeholders marked, not in force.) The eight decisions that have to be made before either can be linked, from `docs/legal/README.md`: **(a)** who the legal entity is; **(b)** controller for everything, or controller for account data and processor for each document on the founder's behalf — the drafts assume the latter; **(c)** publish now with erasure done by hand, or wait for the `person_id` mechanism; **(d)** keep the operator-notification mail carrying the founder's address, or reduce it to title-and-URL; **(e)** retention for a closed document — a period, or each group setting its own as a rule; **(f)** whether the Terms promise a moderation process there is no mechanism for; **(g)** self-host the web font (deletes a third-party disclosure and a CSP exception); **(h)** whether member emails staying visible to other members is intended. The drafting agent's two independent findings ride (g) and (h): Google Fonts is currently the only third party beyond the three providers, and most members will assume their address is private. — **Parked by Ed, 2026-08-29: *park the whole item until go-live is actually scheduled.*** All eight, not a subset — the answers turn on facts nobody has (the entity, the launch date, who the first real members are), and eight guesses written into a document that is not in force is worse than eight blanks. **Parked, not spent**, the way **46** is: nothing here is decided and nothing is owed until there is a date. Two things ride along and should be re-read at that point rather than now, because both drifted while this sat: **(d)** is the same decision as **482**, which Ed has since answered — the operator mail keeps the founder's address — so (d) inherits that unless the legal reading overrides it; and **436**'s `person_id` is a stage 5/6 schema decision that (c) depends on and does not control. `PRODUCTION.md`'s stage-12 line carries the park.

517. **Does the same rule cover an *ordinary* delegated setting?**

    **Where it stands (docs pass, 2026-09-07):** answered (a), owed — both predicates still carry the `kind` test: `design/session-view.html:3825` and `design/setup.js:200`.

    (From Ed's ⏳ note, 2026-08-21, which said **constitutional cards only**.) Built as asked: a constitutional card waiting on the room keeps its subject glyph, greys, and leaves the rail. But ⏱️ the proposal rate and 🤖 AI proposals are **ordinary** and delegable too, and a member watching one of those can act on it no more than they can act on a constitutional one — the same *grey means nothing is being asked of you*. The asymmetry that would justify keeping ⏳ on them: an ordinary setting can be re-proposed by anybody at the threshold at any time, so a waiting one is arguably still *live* to you in a way a constitutional one is not. **(a)** Extend the rule to every delegated setting, ordinary included; **(b)** keep it constitutional-only as asked, and let ⏱️ and 🤖 keep their ⏳ entries. I lean (a) — the reason the rail entry was noise is the same in both cases — but it is your line, and (b) is what you asked for. — **Answered 2026-08-29 (Ed): *extend the rule to every delegated setting, ordinary included* — option (a).** So the predicate is *delegated and waiting on the room*, and the kind stops entering into it: what makes the rail entry noise is that nothing is being asked of **you**, which is equally true whichever route the setting takes. **Folded 2026-08-29 by the batch AD pre-flight** into SURFACE §6's `wait` row, which now says *a delegated card waiting on the room* where it said *a constitutional card*. **What is owed is two predicates and nothing else**: `design/session-view.html` `~L3469` (`.filter((c) => !(c.kind === 'constitutional' && stateOf(c, ctx) === 'wait'))` — the rail's own filter) and `design/setup.js` `~L190` (`if (st === 'wait' && c.kind === 'constitutional') return c.g;` — the tab's glyph), each losing its `kind` test, with the comment above each rewritten off *constitutional*. **Which cards this actually reaches**: the ordinary delegable settings are exactly `rate` (⏱️) and `machines` (🤖), and 🤖 has had no card since entry 251 — so ⏱️ is the only card a member will see change, plus a replayed document that delegated 🤖 before the retirement. **Answered, owed, not built.**
518. **Should the empty document be pre-filled with something to edit?**

    **Where it stands (docs pass, 2026-09-07):** answered (c), owed — the column's invitation is still `.prose.empty.writeready::before` (`design/session-view.html:120`), now keyed on the founder's empty column rather than 📄's turn, and no placeholder exists.

    (Ed, 2026-08-21, on Q513(c): *I am tempted to pre-fill it with something like "We the people…", to invite an edit.*) The appeal is real: an empty column is the hardest thing in any editor to start, and a first line somebody wants to change is the fastest way to teach that the text is editable at all. Against it, two things this surface has already decided. The column is where the **answer** goes, and *Untitled* left the title heading that same morning on exactly that ground — helper text standing where the answer goes reads as an answer somebody else gave. And a charter that opens *We the people* arrives with a voice and a tradition already in it, which is a strong thing to put in a room’s mouth before it has said anything. Three shapes: **(a)** leave it empty with the grey invitation that is there now (built); **(b)** pre-fill real prose as **document text**, which the founder edits or deletes; **(c)** pre-fill it as a **placeholder** — grey, not in the document, gone at the first keystroke — so the shape of a charter is suggested without anything being said on the room’s behalf. **(c)** is the one that gets the teaching without the ventriloquism, and my recommendation. — **Answered 2026-08-29 (Ed): *pre-fill as a grey placeholder — not document text, gone at the first keystroke* — option (c), as recommended.** So the empty column suggests the shape of a charter without the room having said anything: the words are `--muted`, they are drawn out of the text flow the way every other placeholder on this surface is, `proseText()` never sees them, and the first keystroke takes them away for good. **Where it lands, and the one thing that has to be settled with it**: the column's grey invitation today is `.prose.empty.writeready::before` (`design/session-view.html` `~L118`, *Start writing whenever you like…*) with `.prose.empty.pasteready::before` beside it (`~L123`), both keyed on `visible(card('text'))` at `~L5545–5547` — **and plan 129 (backlog 204) removes that key**, 📄's task being retired. So (c)'s placeholder must be keyed on the column being empty and writable rather than on 📄's turn, and whoever builds it should read plan 129's part 1 first; if 129 lands first there is no `writeready` left to hang it on. The empty document's **one empty clause** `L0` and its own `.doc p.editable.blank` placeholder (`design/system.css` `~L755`) are a **different** placeholder from this one and must not be conflated — that one says the block is empty, this one says what a charter looks like. Copy is owed through `design/STYLE.md` §8, and `card-copy.golden.json` re-freezes with it. **Answered, owed, not built.**

526. **A grant is acknowledged once, so a power restored later is never announced.**

    **Where it stands (docs pass, 2026-09-07):** built as (b) (`syncGrantAcks`); what is open is the test — the drop itself is not verified end to end (CLAUDE.md gotcha) — and the module still holds no grant-OK state.

    (From Q524's build.) `grant-pen` opens on `holdsPenAnywhere()` and its OK persists per document and seat, so a founder who relinquishes every pen and is later re-crowned by a carried `reserve` motion gets the pen back **silently** — no task, no acknowledgment, and the card's own *The membership returned the pen to you* sentence (now true, and read off `powerFrom`) never renders. Same shape for 🏛️ if a membership is ever restored. This is the acknowledgment rule's own logic pointing at a hole: *no power arrives without acknowledgment* should not quietly mean *no power arrives twice*. **(a)** A grant re-asks whenever the power arrives from a source it has not been acknowledged for — key the OK by `{grant, source}` rather than by grant alone, so the birth's pen and a motion's pen are two acknowledgments. **(b)** Re-ask whenever the power goes from not-held to held, however it got there. **(c)** Leave it: being re-crowned is a thing you voted on and watched happen, so it needs no receipt. I recommend (a) — it is the smallest change that keeps the rule true, and it is what makes the sentence Q524 built worth having. **Ed, 2026-08-21: (b)** — re-ask whenever the power goes from not-held to held, however it got there. Built as `syncGrantAcks`: an acknowledgment covers *this* holding, so losing the power drops its OK and regaining it asks again. **It watches a transition and never reads an absolute** — before the module hydrates every gate reports shut, so a rule of the form *gate closed, therefore drop the OK* would wipe acknowledgments on every page load and silently hand back powers already accepted; a drop needs the page to have seen the power held and then not held, in that order. Keyed by seat as well as by grant, since the dev dropdown changes who is sitting there and a seat change is not a transition. Verified: the OK survives a reload and a fresh navigation, and a founder→member→founder seat round-trip keeps the pen. **Not verified end to end: the drop itself** — driving a founder to zero pens needs every setting's pen laid down one at a time, and the harness managed 4 of 14 before it became more scaffolding than the check was worth. The rule is six lines and both adjacent hazards are proven; worth a real test when the reserve-motion path is next touched. Known gap, inherited: the module holds no grant-OK state, so a power relinquished in another browser is not seen here — this is a per-device approximation of a per-member fact.

631. **"When the session begins" is the only "session" a member can read.**

    **Where it stands (2026-09-07):** no member-visible string in `design/copy.js` says *session*; the sentence left the page in `8ada546` (2026-08-25, the Proposals run as one clause wearing four tabs, Q748–Q752), a rebuild rather than a ruling on the word. **Put to Ed as Q1218, he deferred it with a rule-to-be:** *We should return to this later; I think it should change depending whether the document has an ending or not. If it has an ending, then it's a session. If it has no ending, then it is a document.* So the word is not one vocabulary but a fact of ⏰: a windowed document is a *session*, a perpetual one a *document* — which would touch every string that names the thing (STYLE §1's *document / charter* row, the 🍾 and 🥂 bodies, the clock, the stranger's door) and is a copy pass of its own when he returns to it. Not built; not yet a rule in STYLE.

    The new Proposals preamble is Ed's own wording (2026-08-22), and I have built it verbatim — but every other member-visible string on the surface says **document**, including the sentence this one replaces (*The document has not begun.* / *The document began at…*), the 🍾 card's own body (*Only the Founder can begin the document*), the hold line (*The document cannot begin while…*) and the stranger's door. A scan of `session-view.html`, `session.js` and `cards.js` finds "session" in comments and file names only, never in copy. So the preamble now says *session* two paragraphs above a card that says *document* about the same act. **(a)** Keep *session* and leave the inconsistency — a convention **is** a session, and this is the one sentence about the room's life rather than about the text. **(b)** Keep *session* and sweep the rest of the surface to match, making it the vocabulary. **(c)** Say *When the document begins, members may propose changes to rules…*, which costs nothing and matches every neighbouring sentence. I recommend **(c)** unless *session* is deliberate; it is a one-word change either way, and STYLE.md's job is one vocabulary. Raised 2026-08-22.

901. **Three acts have no communication designed.**

    **Where it stands (docs pass, 2026-09-07):** built 2026-08-27 (SURFACE E31–E33); open inside it, unnumbered: whether the room is owed an OK for a departure, and what a member removed by a *carried* motion is told.

    (Same sweep.) Entry 94 added exile at will and resignation, and entry 97 added the refusal of an application verified before 🤝 shut — three acts with real audiences and no row in SURFACE's event matrix, which is meant to be complete. **Ed, 2026-08-26: add them, and put designing them on the backlog.** Added as **E31–E33**, each marked *undesigned and unbuilt* so `spec-check` counts them beside E28–E30; the design itself is plan-queue backlog **98**. What each audience is told — and above all whether an exiled member is told anything at all, and by what channel — is the design work this holds open. **Built 2026-08-27 (plan-queue 69):** E31–E33 designed and built — see SURFACE §2 and `design/DECISIONS.md` *Three acts nobody was told about*. Exile mails the member (the document's address, no login link) and their dead seat's door says why; resignation gets the door sentence and no mail; both leave a grey departure line under *Members*; the refused application gets Y25's sentence before and after the press, Submit dark. Two things stay open, unnumbered here: whether the room is owed an OK for a departure (the plan builds the sentence and not the OK), and what a member removed by a *carried* motion is told.

902. **A 💤 change names the members it returned.**

    **Where it stands (docs pass, 2026-09-07):** answered, owed — SURFACE Y26 still reads **Unbuilt**.

    (Same sweep, off entry 97.) Turning 💤 off, or lengthening it past somebody's quiet, returns lapsed members at once — a fact about people that today rides E5's change line as though it were only a fact about the rule. **Ed, 2026-08-26: agreed — the change to the lapse setting is the news, and it should include a list of the lapsed members that have now been made active.** Written as SURFACE **Y26**, marked unbuilt.

924. **Gate 1 merges at the paragraph, so two typo fixes in one paragraph become a rivalry**

    **Where it stands (docs pass, 2026-09-07):** answered, owed — `packages/engine-core/src/text/compose.ts` has no sentence-level merge.

    (raised 2026-08-27, from Ed's question *does git teach us anything about overlapping proposals?*; **Ed: good idea**). `blocksOf` makes one engine line one paragraph, so every footprint is a whole paragraph and two members editing different sentences of the same one fail the three-way merge (`compose.ts`) and escalate to a race the room must judge — the opposite of *most of a session should feel like approving typo fixes*. Git's lesson is that the granularity of conflict detection sets the collision rate, and that most conflicts are adjacent edits that a finer diff composes. Proposed: **Gate 1 composes at word or sentence level inside a paragraph** while the footprint stays paragraph-keyed for anchoring, so the page's *one block, one hunk* simplicity survives and only the merge gets finer. **Before deciding, the sim measures it**: rivalry rate at paragraph vs sentence granularity over the existing sweeps, since the whole case is that most paragraph collisions are accidental. SPEC §2.2 rule 1 is the sentence that changes. — **Answered 2026-08-29 (Ed): make the change now — merge at sentence level inside a paragraph.** So the measurement is not a precondition of the decision; it may still be worth running as evidence of how much the change bought, but nothing waits on it. Gate 1 composes at **sentence** granularity (not word — Ed named the sentence) inside a paragraph, while the footprint stays paragraph-keyed for anchoring, so `blocksOf`'s *one block, one hunk* survives and only `compose.ts` gets finer. **SPEC §2.2 rule 1 changes with the build, not ahead of it** — a spec sentence describing a granularity the engine does not have is the drift the spec pass exists to stop, and this is one commit's worth of both. **Answered, owed, not built.**

1015. **What is a member who is not the Founder told while a text adoption sits awaiting assent?**

    **Where it stands (docs pass, 2026-09-07):** answered (b), owed — no *awaiting assent* string exists in `design/copy.js`, and the SURFACE §2 row it needs is unwritten.

    *(raised by the R-056 build, 2026-08-29, backlog entry 159.)* Under 🛡️ on the Text a race that clears the bar now **parks**: the version does not move, no rival is rebased, and the candidate leaves every feed. What the room sees is the race disappearing from the rail and the text not changing — and nothing anywhere says why, or that anybody has been asked. The build adds nothing deliberately: the surface says nothing about it today, and inventing copy for it would be inventing a rule. The readings: **(a)** nothing — the room is told outcomes, and this is not one yet (SPEC §3.5, and the *no cleared-and-cooling state* rule in §4.2 is the same argument: saying *this has won and is waiting* is a standings reveal); **(b)** a mark on the clause and nothing more — *this clause is waiting on the Founder*, no wording, no author, no confidence; **(c)** the whole thing on the record when it resolves, and silence until then, which is what happens today by construction. I recommend **(a)** or **(c)** — they are the same behaviour, differing only in whether the silence is a rule or an accident, and (b) is the one that needs a ruling because it leaks *something cleared* in real time. What settles it: Ed. The founder's own card is built (SURFACE §9's *👑 question (the Text)* row); this is the other side of it. — **Answered 2026-08-29 (Ed): (b), and he wrote the card** — *It should appear as a ⏳ whose card says something like "Awaiting assent from the Founder 🛡️" [OK].* So the room **is** told, and told in the alphabet it already reads: the parked clause carries a ⏳ mark, its card says who is being waited on and asks nothing but to have been seen. Ed has overruled the standings-reveal objection (a) and (c) rested on — *this has won and is waiting* is news the room may have — so the build states it and does not re-argue it. **Owed, not built**, and it needs a SURFACE §2 event row of its own before it is: the audience, the channel, the close and the persistence of that ⏳ are exactly the columns nobody has filled in, and Ed's sentence gives the Ask and the Close (*[OK]*) but not the other three.

1016. **The seat-matrix row for a shielded Text is deferred to entry 158.** *(raised by the R-056 build, 2026-08-29.)* `npm run seat-matrix` needs a running server and a whole membership unanimously carrying a `reserve` motion on the Text to reach the parked state at all — a walk of its own. Entry 158's Begin-card power switches make the state reachable in one press, so the row belongs with that build. Until then the founder-facing text 👑 card has no end-to-end walk; its guard is `packages/constitution/test/text-powers.test.ts`'s *🛡️ on the Text parks the adoption (R-056)* block for the mechanism, and a DOM measurement of the card for the surface. — **Answered 2026-08-29 (Ed): fold it into whatever next touches the harness, rather than as its own job.** So the row is not a build of its own and no plan is owed for it; it is a **rider** on the next piece of work that opens `scripts/seat-matrix.mjs`, which is why this stays in the register rather than being spent — it is a standing instruction with nothing yet to attach to. Two other riders are now waiting on the same file and should ride with it: **Q930**'s missing `AUDIENCE` predicate for *every active member* (E10 and E11 both), and **Q919**'s fifteen findings, which are one bug.

1033. **What becomes of a *motion-backed* 👑 question standing when the convenor's seat is vacated?**

    **Where it stands (docs pass, 2026-09-07):** answered (a), owed — `crownSeatVacated` still skips motion-backed questions (`packages/constitution/src/session.ts:2439`, `if (… || !q.text) continue`).

    *(raised by the vacancy auto-pass build, 2026-08-29, plan 115.)* Ed ruled that a parked **text** adoption whose convenor has left auto-passes, the way a lapse does, and that is what is built: `crownSeatVacated` loops the pending questions and auto-passes the ones carrying `text`. The lapse loop it copies does **more** — it auto-passes motion-backed questions too, and calls `settleCarriedEffects` for each — and the build deliberately narrows away from that, because a carried invitation or removal **applying** without assent because the convenor left is a governance consequence nobody has ruled on. The asymmetry that decides it: a parked text adoption blocks every text adoption in the document for the rest of its life, and a standing motion-backed question blocks nothing at all. The readings: **(a)** auto-pass them too, matching the lapse loop exactly, on the ground that a seat nobody occupies cannot refuse anything and a motion the room carried should land; **(b)** leave them standing, and let the close resolve them the way §4.6 resolves a text question — the motion fails closed, having never been assented to. I recommend **(b)**, weakly: (a) applies acts nobody assented to, and its only argument is symmetry with a loop whose other half is about a crown that may still wake up. What settles it: Ed. Neither is implemented; the questions simply stand today. The wider version of the same question — every other crown power at a vacant seat, `crowned()`, `holdsAnythingReserved()`, `reservedTarget()` and the doors' pairs all still reading the seat as held — is gestured at here and is not claimed as a number of its own. The spend it was held for — whether 📨 widens past the founder where 🪪 stands at ✒️ and any member may invite — is not raised: E34 says *the founder's ✉️ row* and the build simply obeys it, `founderOnly` on `resend-invite`. — **Answered 2026-08-29 (Ed): (a)** — *Auto-pass them too, exactly as the lapse case does.* Against the recommendation, and the reason to record is Ed's own ground for (a) rather than mine for (b): a seat nobody occupies cannot refuse anything, and a motion the room carried should land. So `crownSeatVacated` stops narrowing away from the lapse loop and becomes it — every pending question auto-passes, motion-backed ones included, with `settleCarriedEffects` called for each, so a carried invitation or removal applies. **The consequence to build carefully**: this is the point at which a governance act lands with nobody having assented to it, so the record must say the seat was vacant, not that the convenor agreed. **Answered, owed, not built.** The wider version — every other crown power at a vacant seat, `crowned()`, `holdsAnythingReserved()`, `reservedTarget()` and the doors' pairs all still reading the seat as held — is still not claimed as a number of its own and is still not answered by this.

1205. **`seat-matrix` is red, and was before either of 2026-09-06's builds** — 56 findings and a `TypeError … reading 'closed'`, identical from a worktree at `b62fcbc`. CI's `walks` job does not run it, so nothing went red. Needs a look before it is trusted again; until then a claim that a seat change is safe cannot lean on it.

1229. **SPEC §2.6 (item 53).** (Raised by the documents pass of 2026-09-07, QUESTIONS.md log §6 item 20.) Neither `displaced` nor `rebase-pending` is a state in SPEC §2.6 while `packages/engine-core/src/types.ts:135–144` carries both. A spec amendment, so a question by the brief's rule 3, not an edit.

1230. **SPEC §8.3 (item 97).** (Raised by the documents pass of 2026-09-07, QUESTIONS.md log §6 item 21.) *the rival card's prompt states the conditional framing plainly* (SPEC.md line 172) describes a prompt Ed removed in 95. The one-clause amendment wants his sign-off.

1231. **E20's quoted sentence has no source string.** (Raised by the documents pass of 2026-09-07, SURFACE.md log §6 item 1.) E20's Channel cell reads *an acknowledgement task on their own answer card, marked ↻ — "one more member has arrived; your answer stands" (Q567, Q572)*. `grep -ra "one more member\|your answer stands\|has arrived" design/copy.js design/session-view.html design/setup.js design/session.js` finds nothing but a comment and 🍾's `one-voice` sentence (`session-view.html:10837`). Readings: (a) E20 is unbuilt and the row should say so in its Channel cell the way E28–E30 do (the checker then reports it as unbuilt); (b) it is built under other words and the row should quote them — I could not find which; (c) leave it. I left the row.

1232. **Q1183's *Set to … / Set by the founder …* card "retires", but the string is live.** (Raised by the documents pass of 2026-09-07, SURFACE.md log §6 item 2.) `design/session-view.html:5941` (the band's `lockline`), `:9192` (`hydrateFromModule`, the stranger's) and `design/setup.js:632` (the `Set to` statline) all still draw *Set by the founder when the document was made.*, and `card-copy.golden.json` holds *Set toSet by the founder when the document was made.* once. The §9 row for the stranger's settled card says the card is the read-only settled card (Q1183, Ed 2026-09-05); I dropped only the *retires* narrative. Readings: (a) the lockline survives on the **watching** card (its row says *the lockline and the value line only*) and the golden's `Set to` line is that card's — nothing to do; (b) the stranger's card still draws the old statline at 9192 and Q1183 is not fully built — a code finding for a `pq after` plan; (c) both. Someone with a running server should open a stranger's tab and look.

1233. **`text-unconfirmed` (Removed 4).** (Raised by the documents pass of 2026-09-07, SURFACE.md log §6 item 3.) F19 listed it as a `readiness()` reason; the type has five and the string is nowhere. Is a text-unconfirmed hold planned to return (Q1080 made ✒️ the only save and 🍾 confirms whatever stands, R-081), or was F19 simply stale? I removed it; revert if planned.

1234. **§7.2's "two frozen instruments" (Removed 6).** (Raised by the documents pass of 2026-09-07, SURFACE.md log §6 item 4.) The golden no longer freezes any gesture-naming 🏛️ string, and `spec-check.mjs:481–487`'s own comment still says it does. Readings: (a) the sentence now in SURFACE — the bold word and the 🏛️ tooltip, two edits — is the whole rule, and the checker comment should be corrected in a code commit; (b) the tooltip should be frozen in the golden again (a `card-audit` copy walk that captures `title` attributes), restoring the copy-freeze half; (c) the tooltip is not worth freezing.

1235. **`.switch` CSS is orphaned.** (Raised by the documents pass of 2026-09-07, SURFACE.md log §6 item 5.) CP1 says the switch control retired with lanepick (Q1103 (b), Q1181); `design/system.css:1288–1301` still styles `.switch .track / .thumb / .lab / .mixed`, and nothing in `design/*.js` or `session-view.html` emits `class="switch`. A code finding, not a document one: delete the block, or keep it if the `?gesture=`-style comparison pages still use it.

1236. **Y27's Ruling cell now carries a pointer** (Raised by the documents pass of 2026-09-07, SURFACE.md log §6 item 6.) (*Ed, 2026-09-01; post-mortem in DECISIONS.md, a gate never withholds from the seat that set it*). Every other Y row's Ruling is a bare date or Q. Is a DECISIONS pointer allowed in the Ruling column, or should it be a bare *Ed, 2026-09-01* with the post-mortem findable only through CLAUDE.md's gotcha? (Ed ruled on Q1005 that cells stay self-contained and carry no `→ why:` flag; this is a plain sentence, not a flag, but it is the same instinct.)

1237. **F5, F19, F21 and K31 remain very long** (Raised by the documents pass of 2026-09-07, SURFACE.md log §6 item 7.) (1,900–2,900 characters each) and the checker caps nothing in SURFACE.md. They are Ed-ruled throughout, so this pass trimmed only their history. Readings: (a) leave them — a founding rule is allowed to be long; (b) a later pass moves their card-level detail (F5's power-table description, K31's edit-mode geometry) into §9's 🍾 and 📝 rows, which already state most of it, leaving the rule a paragraph; (c) split F21 into subsection-per-status rows in the band table (§8), which now exists to hold them.

1238. **§5's survival.** (Raised by the documents pass of 2026-09-07, SURFACE.md log §6 item 8.) Ed's Q1004 (2026-08-28) said *§5 survives with its one bullet*; it now has two (the head of the document, and the 👑 pointer bullet). If the ruling meant exactly one, the 👑 bullet's Q1015 fact wants a home — E12's Audience cell (*the founder*) already implies it — and the bullet can go.

1239. **The `L` lifecycle table and the setup-alphabet and socket-state tables have no marker** (Raised by the documents pass of 2026-09-07, SURFACE.md log §6 item 9.) and are not parsed; the seat matrix reads `events` and `holds` only. Nothing asserts L1–L9 against the page. Worth a marker and a check, or are they illustration?

1240. **`design/spec-pass/` holds five files where its own rule says folded passes are deleted.** (Raised by the documents pass of 2026-09-07, CLAUDE.md log §6 item 1.) `pass-4.md/.html` (the card-pattern census, folded 2026-08-31 — Q1096–Q1102), `pass-5.md/.html` (SURFACE §8.1 as a table; its own header says *deleted when the pass folds*, and the QUESTIONS pass reports Q1122–1128 built), `card-audit.html` (STYLE log Q2 already asks), `claude-md-audit.md` (Q943, applied by entry 201) and `surface-md-extraction.md` (Q944, applied by entry 191). Readings: (a) delete all five — the rule as written; (b) keep the two proposals and the census as an archive and amend the rule to say so; (c) delete only `pass-N.*`. The table's row keeps the rule as written and names the two proposals.

1241. **The double-fire child gotcha is unguarded on its own text** (Raised by the documents pass of 2026-09-07, CLAUDE.md log §6 item 2.) (*A completed hold clicks for you…*, ~1,100 chars, `penHoldFired`). Its parent family cites `npm run journey`, but no assertion of *one commit per hold* is named, and DECISIONS holds no post-mortem for it (`grep -c penHoldFired design/DECISIONS.md` = 0). If `journey` does assert it, the paragraph should reduce and the post-mortem move; if not, it stays whole, as now. Someone who knows `journey-walk.mjs` should say which.

1242. **The 🎩 gotcha** (Raised by the documents pass of 2026-09-07, CLAUDE.md log §6 item 3.) (*🎩 is settled once the document begins, and `settled()` must say so*, ~900 chars) names no guard, though `npm run journey` and `npm run ladder` both walk a reloaded live document past 🍾. Same question: is it guarded in practice? If so it reduces.

1243. **`founding-ceremony` is one name for two things** (Raised by the documents pass of 2026-09-07, CLAUDE.md log §6 item 4.): a retired surface (→ `session-view.html`, in the table) and a live `[concept]` for the blind collection (SPEC §9.0a), while *Names* rules *"ceremony" never appears on the surface*. Readings: (a) leave — the concept is engine vocabulary and the retired row says *(the surface)*; (b) rename the concept (`blind-founding`, say) so the retired name is retired outright.

1244. **The seat matrix is stated twice** (Raised by the documents pass of 2026-09-07, CLAUDE.md log §6 item 5.): the `seat-matrix.mjs` entry and the *Conventions* bullet both carry the payload-is-gitignored rule and the exit-3 rule, and DECISIONS (entry 201) records that stating it twice once produced a wrong fact. I left both — the entry as the name, the convention as Ed's rule (*a question for Ed, not a predicate to invent*). Readings: (a) leave; (b) the entry drops both sentences and points at the convention.

1245. **The two tables grew the file by 310 bytes.** (Raised by the documents pass of 2026-09-07, CLAUDE.md log §6 item 6.) Ed asked for tabulation where it earns it; a table of 13 four-cell rows is denser to write than 13 bullets and costs pipes. If the prose read better, `5e8a41a` reverts cleanly on those two hunks alone; the corrected claims would need re-applying to the prose.

1246. **The evidence figures have lost their only home.** (Raised by the documents pass of 2026-09-07, README.md log §6 item 1.) The old README stated results — scripted welfare ratios 0.96–1.00 on both scenarios; the 2026-08 sweep robust at 0.94–0.99 with hot set 3 becoming the spec default; two engine bugs (router slot starvation, replay divergence) found by simulation — that appear in no file in the tree (grep in Removed 3). A stranger evaluating the project would want them, but a README claim with no source goes stale silently, which is the failure this pass is for. Readings: (a) leave them out, as committed; (b) re-home them in `packages/sim-harness/README.md` under *Calibration sweep* with the run date and the CSV that produced them, and let the README point there; (c) restore the bullets to the README with a *measured 2026-08* stamp. I recommend (b) if the CSVs in `packages/sim-harness/runs/` (git-ignored) still exist to source them from.

1247. **Q1204 names the wrong package in the task brief, and did not reproduce.** (Raised by the documents pass of 2026-09-07, README.md log §6 item 2.) The task described the known timeout as *one engine-core test*; QUESTIONS.md's Q1204 (`QUESTIONS.md:484`) places it at `packages/constitution/test/ranking/ceiling.test.ts`. On this run (`npm test`, 2026-09-07, exit 0) every workspace was green and the ceiling test passed, so the README carries the plain measured totals with no caveat. If Ed wants the flake noted for a stranger, it would go on the `npm test` line; I left it off, since a note about a test that passes on the machine that runs it is noise to anyone else.

1248. **The server's running test count fell from 99 to 86 between the old README and now.** (Raised by the documents pass of 2026-09-07, README.md log §6 item 3.) `packages/server` reports *86 passed | 14 skipped (100)*; the old README said *99 tests, 14 more against Postgres* (i.e. 113). `git log --oneline -- packages/server/test` would say which commit removed thirteen tests; I did not chase it, because the README states what is measured, but if a test file was dropped by accident rather than folded, that is worth a look.

1249. **`npx playwright install chromium` is stated as the headless tools' need without a root script for it.** (Raised by the documents pass of 2026-09-07, README.md log §6 item 4.) CI runs `npx playwright install --with-deps chromium` (`.github/workflows/ci.yml:164, 254`); nothing in `package.json` does. A `postinstall` or a `playwright:install` script would let the README say `npm run …` instead. A convenience, not a defect.

1250. **Should the README carry the CI/deploy paragraph at all?** (Raised by the documents pass of 2026-09-07, README.md log §6 item 5.) *A push to `main` is a deploy* is a fact about this repository that a contributor opening a PR needs (there is no gate behind a merge), which is why it stayed; but it is also operator material that `docs/OPERATING.md` §3 owns. Readings: (a) keep the paragraph, as committed; (b) reduce to one sentence and a pointer.

1251. **Was stage 9's "one real end-to-end send, to a non-Resend address" ever done?** (Raised by the documents pass of 2026-09-07, PRODUCTION.md log §6 item 1.) The stage's own text said *until that happens, "mail works" is a claim about DNS records rather than about mail*. The running log records Ed verifying the domain and clearing the sandbox sender (23:15) and nothing after. Members have since been invited to real documents through docs.vote (Ed's live walks of 2026-08-21/22 were on the host), which is strong circumstantial evidence, but no line records a friend's inbox receiving a link and the link working once. I marked the stage done with the send noted as unrecorded. Readings: (a) it happened — say so in the row with a date; (b) it has not — it belongs on the go-live checklist, where *test mail to Gmail/Outlook/iCloud lands in the inbox* already stands, and the row is right as is.

1252. **Is stage 10's security review #2 satisfied?** (Raised by the documents pass of 2026-09-07, PRODUCTION.md log §6 item 2.) The plan's review #2 was *after deployment … a different class of defect (headers, exposed paths, error leakage) than a source review can*. What ran were two source reviews (`c8a732d` over stages 6/7/11; the stranger's door on 2026-08-21) plus `verify-deploy`'s live checks of headers, exposed paths and error leakage on every deploy. I marked it done on that basis and named stage 19's targeted review (Ed, 2026-08-26) as what is still owed. If Ed meant an external or fresh-eyes review of the live host, the row should read open.

1254. **Stage 15's gate is *somebody else can operate it*, and nobody else has tried.** (Raised by the documents pass of 2026-09-07, PRODUCTION.md log §6 item 4.) Two passes have made the map and runbooks internally consistent (this one re-verifies every command — see `operating.log.md`). Marked done; if the gate is meant literally, it is open until an operator who is not Ed or a session follows a runbook cold.

1255. **Stage 13 has no plan document.** (Raised by the documents pass of 2026-09-07, PRODUCTION.md log §6 item 5.) Every other open stage has criteria in this file or its own document (MOBILE.md); accessibility has one word in a table row. When it is scheduled it wants the MOBILE.md shape (precedence, cite-don't-restate, per-stage acceptance with file:line) — not something this pass can write.

1256. **Three code comments point at content that moved** (Raised by the documents pass of 2026-09-07, PRODUCTION.md log §6 item 6.): `scripts/test-pg.mjs:15` (*the pinned local container from PRODUCTION.md* — now `docs/OPERATING.md` §7), `packages/sim-harness/src/soak.ts:215` (*see PRODUCTION.md's running log* — the numbers are PRODUCTION.md § Measurements), `render.yaml:48` (*the cutover is two variables (PRODUCTION.md)* — OPERATING §7). Comments are exempt from the pass and I left them; each is a one-line re-aim if wanted. The two runbook pointers of the same kind are fixed in item 2's commit.

1257. **Three code comments still send a reader to PRODUCTION.md for text that moved** (Raised by the documents pass of 2026-09-07, docs/OPERATING.md log §6 item 1.) — `scripts/test-pg.mjs:15` (the container), `render.yaml:48` (the two-variable cutover), `packages/sim-harness/src/soak.ts:215` (the running log). The pass leaves code comments alone; each is a one-line re-aim to `docs/OPERATING.md` §7 / PRODUCTION.md § Measurements if wanted.

1258. **OPERATING §1 states the managed Postgres is version 17 in frankfurt** (Raised by the documents pass of 2026-09-07, docs/OPERATING.md log §6 item 2.) and §2 that `DATABASE_URL` is the internal connection string — dashboard facts, verifiable only there. They match decision 493 and the lifted running log (23:15: Ed did 2). Left as they stand; nothing in the tree can contradict them.

1259. **`deploy-health-shutdown.md`'s *Configuration reference for this stage* table** (Raised by the documents pass of 2026-09-07, docs/OPERATING.md log §6 item 3.) duplicates three rows of OPERATING §2 (`DRAFT_STORE`, `DATABASE_URL`, `RENDER_GIT_COMMIT`/`DRAFT_BUILD_SHA`). A runbook that can be followed without the map has a case; so does one source. Left; if Ed prefers one source, the table reduces to *the variables: OPERATING §2*.

1260. **`constitution/NOTES.md` is a chronological ledger of calls, and a third of it is now struck.** (Raised by the documents pass of 2026-09-07, the package NOTES log §6 item 1.) Seven of thirty bullets carry `~~…~~` (the two from 2026-08-18 and the five from this pass). The file's purpose — *recorded as they are made, so Ed can flip any of them cheaply* — is served by the ledger shape, but a reader now wades through superseded calls to find live ones. Readings: (a) leave — the strikethroughs are the record and DECISIONS holds the reasoning; (b) move the struck bullets to a *Superseded* section at the foot; (c) move them to DECISIONS verbatim and keep one line each. I left (a), it being the file's own convention.

1261. **`server/NOTES.md`'s header still says *(Q368)* and its shape is the 2026-08-19 slice.** (Raised by the documents pass of 2026-09-07, the package NOTES log §6 item 2.) It is accurate after this pass, but it reads as a birth note with amendments rather than a description of the host as it stands (the CI/deploy facts, the outbox, the phase ladder, the dev host are in CLAUDE.md, OPERATING and PRODUCTION, not here). A rewrite in OPERATING's precedence-and-cite shape would be a separate, larger job; not done.

1262. **`engine-core/NOTES.md`'s *Dominated (SPEC §6.2, projected per Q11)*** (Raised by the documents pass of 2026-09-07, the package NOTES log §6 item 3.) and its *Router v1 (SPEC §8, simplified)* section describe the first build's router (deterministic slot pattern, c_p median split). The router has since moved (Q1178 *the unheard slots*, `f345167`; §8.3a's diagonal; hot set 3). I corrected nothing there because I could not verify each sentence against `session.ts`'s current routing in the time of this pass; someone who knows the router should read that section against `feed()`.

1263. **The sim-harness README's `--model claude-sonnet-5` example and the judge's `claude-fable-5`** (Raised by the documents pass of 2026-09-07, the package NOTES log §6 item 4.) are the model ids in `src/` (`grep -o 'claude-[a-z0-9-]+'`); whether both are current ids for the accounts in use is not a tree question.

1264. **§3.a's stage pointer for an asset pipeline.** (Raised by the documents pass of 2026-09-07, design/MOBILE.md log §7 item 1.) The text said hashed filenames arrive *with one — PRODUCTION stage 8*. Stage 8 was the surface merge and is done; its plan text listed *caching* among the halvings the merge enabled, but the caching stage itself is 14. I re-aimed to 14. If Ed meant stage 8's *one asset set* as the pipeline's precondition (already met), the pointer could equally read *none is scheduled; §3.a's rewrite is the substitute* — one word either way.

1265. **`holdWallet` is a name the tree no longer has** (Raised by the documents pass of 2026-09-07, design/MOBILE.md log §7 item 2.), but CLAUDE.md's Gotchas still use it (*`holdWallet` asks `mayPen()`*, *the ghost must never become a removal*, *the spend-preview … `holdWallet`*). `spec-check` checks `[symbol]` kinds in the glossary, not names in gotchas, so nothing reddens. MOBILE now says *the wallet family (`penHold`)*; the three CLAUDE.md gotchas are outside this item and were left — for the CLAUDE.md agent or a later pass.

1266. **The survey's counts** (Raised by the documents pass of 2026-09-07, design/MOBILE.md log §7 item 3.) (*~66 native `title=` tooltips*, *~25 `:hover` rules*, *43 cards / 42 steps*) are as of 2026-08-22 and were not re-measured; they are survey data, not pointers, and stage 0 re-measures them by design.

1267. **§7's table is the 2026-08-18 mapping and Q335 has moved under it.** (Raised by the documents pass of 2026-09-07, REVIEW-creation-session.md log §6 item 1.) Two of its three tables are now wrong in detail (the *missing from the engine* table names settings the constitution package has held since 2026-08-19; the field table names cards by retired glyphs), and its recommendation (*implement the split as its own commit once Ed has read this — it is still a rename today*) predates the constitution package, which took the room-agreed half out of engine-core's reach without splitting the engine's own object. The brief said keep §7 whole, so I did and added the state note. Readings for Ed: (a) leave until Q335 lands (as committed); (b) rewrite §7 as the current mapping — engine-core `Constitution` fields × where each is decided today (catalogue setting / operator env / config field) — which is most of Q335's remaining work and would make this file that work's plan; (c) close Q335 as *done by the constitution package* if the one-bag engine object is acceptable, and delete the file.

1268. **The footer names surfaces that are redirects** (Raised by the documents pass of 2026-09-07, REVIEW-creation-session.md log §6 item 2.) (`design/document-creation.html`, `design/founding-ceremony.html`). Provenance, left as is.

1269. **The report's engine observations name a gap that may since have closed** (Raised by the documents pass of 2026-09-07, REPORT-deferred-evidence.md log §7 item 1.): *`ParticipantApi` has no composer-opening call — the runner invokes `Session.openComposer` directly … when the product builds the composer, the participant-api will need that surface.* The composer exists (SURFACE §9.2, K13–K26). Whether `participant-api.ts` grew that call is a code question outside this report's scope; if it did, nothing in the report needs to change (it is dated), but the gotcha it names is worth a look by whoever next touches `participant-api.ts`.

1270. **The `runs/` CSVs are dated 2026-08-19, not 2026-08-14** (Raised by the documents pass of 2026-09-07, REPORT-deferred-evidence.md log §7 item 2.) — somebody re-ran the studies five days later and the report's *same seeds, same numbers, byte for byte* claim is exactly what makes that fine. Noted only so the mismatch does not read as an error.

1273. **Should the pair be listed in CLAUDE.md's Documents table?** (Raised by the documents pass of 2026-09-07, the card-reviews log, item 1.) Every other archive a session might need is there (`design/DECISIONS.md`, `design/SPEC-REASONING.md`, `design/spec-pass/`); the card reviews are the record of the rulings behind SURFACE §9.3's CP-rules and of Q1149–1195, and CLAUDE.md's `memory` note says the card-sheet notes also live in an artifact database (`draft-card-sheet-notes-in-db.md`). Readings: (a) leave — DECISIONS points at them and that is the archive's job; (b) one row: *`design/card-review-YYYY-MM-DD.md` — Ed's notes from a card review, sorted into questions; archives, never edited; the rulings are folded into SURFACE §9.3 and DECISIONS*. I lean (b) for the same reason `design/spec-pass/` has a row: a session that meets a `card-review-*` file should know without opening it that it is an archive.

1276. **Q949, the cap clamp (S7, line 21).** (Raised by the documents pass of 2026-09-07, SPEC.md log *Questions* item 3.) §7 says the clamp on a cap cut is *not yet built — see Q949* and the fact is still true (`tokens.ts:39`), but no item 949 exists in QUESTIONS.md or DECISIONS.md. Readings: (a) it was folded silently — re-file it as an open item with a fresh number; (b) it was decided against — then §7's sentence about clamping (v0.80) is itself the stale part and R-083 should say so; (c) leave the pointer at R-083's aside.

1277. **§12 Display (S9, line 54).** (Raised by the documents pass of 2026-09-07, SPEC.md log *Questions* item 4.) §12's five sentences describe rendering (*toggleable overlays*, *the care map renders as document heat*) that SURFACE.md now governs and does not do (SURFACE: *the prose carries no highlight*). Readings: (a) §12 reduces to the one mechanism rule it holds — every public view is magnitude-only (§3.5) — and points at SURFACE; (b) keep, as the engine's rendering intent independent of the current surface.

1278. **Q43 (H24, line 27).** (Raised by the documents pass of 2026-09-07, SPEC.md log *Questions* item 5.) §8.2 cites *(Q43, Ed 2026-08-14)* for *silence is never imputed*; Q43 appears in no file in the tree. The ruling is real and load-bearing (R-088 rests on it). Does Ed remember its substance well enough for a one-paragraph R, or should the R simply quote §8.2's sentence and date?

1279. **The membership R (H36, lines 47 and 51).** (Raised by the documents pass of 2026-09-07, SPEC.md log *Questions* item 6.) Two `→ why:` pointers name *entry 94* (plan-queue), which DECISIONS.md:1664 holds as *The membership system, settled*. Readings: (a) a new R-0nn lifting that DECISIONS section into SPEC-REASONING; (b) widen R-024 to cover it; (c) keep `entry 94` — the checker allows it. (a) is what §9's lead promises.

## Backlog (provisioned, build later)

| # | Title | Raised | State | Pointers |
|---|---|---|---|---|
| 1209 | Every settings title is an ask — imperative or question, case by case; the noun titles go | 2026-09-07 | answered 2026-09-07, owed — a copy pass over the 22 titles, two labels each (Q331 (b)) | STYLE T1–T2; `design/copy.js` CARDS; the copy and founding goldens; Q331 |
| 1202 | ⏳ means *waiting for other people to vote*; a race you can still act on keeps its action mark, dealt or not | 2026-09-06 | answered 2026-09-07, owed — the page still lights on the hand | SURFACE E13, L4, §6; SPEC §8.3b; `itemsFromView`'s `dealt`; `bestPairFor` |
| 996 | A per-decision reveal rung for 👁️ | 2026-08-28 | answered, owed — the catalogue still reads `never · after` | SPEC §3.5a, §9.7.1 |
| 1012 | A removed invitee who was never here | 2026-08-28 | answered, owed | `session.ts` departures |
| 315 | The ranking model as a document-creation setting | 2026-08-17 | parked note (Ed) | SPEC §3.3, §8.3 |
| 42 | Spectator commentary view | 2026-08-14 | provisioned | `spectator-api` |
| 55 | Participant-api composer surface | 2026-08-14 | provisioned | SPEC §3.3 |
| 56 | Orphan refunds credit departed ledgers | 2026-08-14 | provisioned | — |
| 244 | A flow entry can overlap a pinned one | 2026-08-16 | open — cosmetic | `freeFor` (session.js) |
| 248 | The ⌈E/3⌉ has never been calibrated | 2026-08-16 | open — wants a sweep | SPEC §8.2 |
| 252 | A perpetual document loses the reset | 2026-08-16 | open | SPEC §9.4, §4.5 |

1209. **Every settings title is an ask — imperative or question, case by case; the noun titles go**

    **Answered by Ed, 2026-09-07**, on a question that had only asked about T2's examples: *It should be examples. But I think we should change the titles! e.g. "Quorum" => "Choose the Quorum"*; and on the scope: *review all titles and use whatever you think is most natural case by case; an imperative or a question; it should be clear the user has a choice to make, and what the nature of that choice is.* STYLE T2 says so since the same day. **Owed, as a plan:** a review of the 22 settings-card titles in `design/copy.js`'s CARDS block — today thirteen nouns (*Title, Link, Your Email, Admissions, Applications, Your Name, Your Picture, Text, Proposal Pass Threshold, Quorum, Anonymous Proposals, Proposal Rate, Visibility*), seven questions, two acts — each re-titled as an imperative or a question by whichever reads most naturally, presented to Ed as a table (old · new · why) before it lands; then `copy-freeze`, `founding-golden --update` and `qa:freeze`, and every walk that asserts a title (`applicants-walk`'s labels, `journey`, the probes). **Q331 is answered the same day, (b): two labels on one card** — the ask while the card wants you, the noun once it is settled, which shows being the card's state, the tab and the rail following the card (STYLE T2). So the plan writes two labels per setting (the ask, and the noun the pile shows once settled — for most of the thirteen nouns the settled label is what stands today), states the switch in SURFACE §9's card rows and §6's rail, and accepts a tab that renames itself at the settle; the outstanding-side asks are the review Ed asked for.

1202. **⏳ means *waiting for other people to vote*; a race you can still act on keeps its action mark, dealt or not**

    **Answered by Ed, 2026-09-07**, with neither of the two readings offered: *⏳ should mean "waiting for other people to vote". If there are things you can do, it should show the symbol of that action, even if it's not urgent. My reasoning: there are two ways of engaging with a doc; resolve recommended tasks by urgency, and work on a particular section. For the latter, you don't want actions you can do to be hidden from you.* SURFACE E13, L4 and §6's ⏳ row say so since the same day. **Owed:** the page lights a race's entry on the served hand (`itemsFromView`'s `dealt`, session-view.html), so in a busy room an entry files ⏳ while the engine could still ask you a pair on the race. The build: the view carries, per race, whether anything on it can still be asked of the viewer — the same test SPEC §8.3b's deadlock disclosure already makes through `bestPairFor` (`session.ts`, the *nothing left to ask you* loop) — and the entry lights on that rather than on the hand; and since every lit entry must open a card, the view also carries that best pair for each askable race the hand does not hold, which is cheaper and blinder than a pair-on-demand route. The ⏳ card stays the ledger (Q1201). Guard: `npm run journey`'s deck step, extended to a race whose pair is askable but undealt.

996. **A per-decision reveal rung for 👁️**

    **Where it stands (docs pass, 2026-09-07):** answered, owed — `packages/constitution/src/catalogue.ts:228` still reads `rungs: ['never', 'after']`.

    (Ed, 2026-08-28 — *file a backlog entry for the per-decision rung*). The 👁️ judgments ladder has two rungs, *never revealed* and *revealed at the end of the document* (§3.5a, **994**). Ed's note asked for a third: names shown once the decision they contributed to closes, decision by decision, rather than at the end of everything. It is a SPEC §3.5a change with record semantics inside it — what the sealed record shows, and when — and the batch-O plan deliberately did not take it. What has to be settled before it can be built: whether a per-decision reveal names judges on the sealed record only or in the live view of a settled race; what it does to a race that resolves and is later re-opened by a ground shift; and whether the rung is elective per member or the room's alone, since every other rung on the ladder is the room's. — **Answered 2026-08-29 (Ed): build it, and show the names in the live document too as each decision settles.** That answers the first of the three open sub-questions outright — **not the sealed record only**: a settled race shows its judges in the live view, as it settles, not merely in the record at the close. The other two are still open and a plan must settle them before it builds: what a **re-opened** race does with names already shown (a ground shift re-opens a settled race, and a reveal cannot be taken back), and whether the rung is **elective per member or the room's alone** (every other rung on the ladder is the room's, and per-member election would make one document's names half-shown). SPEC §3.5a is the section, and the ladder gains a third rung between *never revealed* and *revealed at the end of the document*. **Answered, owed, not built** — and note it is the reveal-cannot-be-taken-back half that makes this dearer than a rung label. — **The other two are answered 2026-08-29 (Ed): *a re-opened race hides its names again until it re-settles; the rung stays the room's.*** So a reveal **is** taken back, and the rule is stated positively: names show while a race stands **settled** and are withdrawn the moment a ground shift re-opens it, returning when it settles again — the reveal is a property of the settled state rather than an event that fires once. And the rung is **the room's alone**, not elective per member, which keeps every rung on the 👁️ ladder the same kind of thing and rules out a document whose names are half-shown. **All three sub-questions are now answered and nothing is left to decide** — what remains is entirely a build, and it is one plan's: SPEC §3.5a's judgments sentence gains the third rung, **and §9.7.1's `judgments` row must move with it** (its ladder cell reads `never · after` today and `spec-check` asserts that table against `packages/constitution/src/catalogue.ts`, so the spec sentence, the table row and the catalogue values are one commit or a red build). The pre-flight of 2026-08-29 deliberately did **not** fold the rung into SPEC for that reason. **Answered, owed, not built.**

1012. **A removed invitee who was never here**

    **Where it stands (docs pass, 2026-09-07):** answered, owed — `packages/constitution/src/session.ts:567` still keeps an invitee who never arrived out of the departure fold.

    (Ed, 2026-08-28, on the batch-O review's one left-alone finding — *keep the mail and also record the departure*). The `member-removed` mail arm in `packages/server/src/server.ts` has no arrival check, so somebody who never turned up and is then removed via `remove` is told *You are no longer a member of …*; the session's own departure fold deliberately excludes people who never arrived, so the register says nothing about the same act. Ed keeps the mail and fixes the other half: **the departure fold records a removal whether or not the person ever arrived.** Unreachable from the page today — invitees are routed to `uninvite` — so nothing a member sees is wrong yet, and the mail's own guard (the `uninvite` precedent) is deliberately **not** added.

315. **The ranking model as a document-creation setting: approval · Bradley–Terry · Crowd-BT** (Ed, 2026-08-17, parked as a note for later). *There are situations where each is appropriate and I think much of our machinery still applies for each.* Crowd-BT is Chen, Bennett, Collins-Thompson & Horvitz, *Pairwise Ranking Aggregation in a Crowdsourced Setting*, WSDM 2013 (10.1145/2433396.2433420) — Bradley–Terry with a **per-judge reliability parameter** estimated jointly with the item scores, plus an active-learning scheme that chooses which pair to serve *and whom to serve it to*. Details want checking against the paper before anything is built on them.

Ed is right that most of the machinery is indifferent to the choice. The `patch-engine`, `overlap-gates` and `dedup-gate` are text machinery and never touch the model. The `adoption-threshold` and its ramp are stated as P(challenger beats incumbent), and all three produce something that plays that role. Quorum, adoption floors, the record, the gazette, and the whole of `session-view` are unaffected — a card asks for a judgment either way.

What genuinely changes is worth listing, because it is more than the estimator. **Active sampling largely disappears under approval**: everybody can rate everything, so the `router` stops *selecting* and only *orders*, which would make the queue dramatically shorter and the `evidence-meter` mean something different. **Deadlock (§8.3) is a Bradley–Terry construct** — *close relative to the marginal value of further sampling* — and under approval it becomes two candidates with similar approval rates, which is detectable but is not the same claim. **Indifference** has no separate existence under approval; approving both *is* indifference. And the **`bounty score`** rests on resolvable disagreement, which is model-specific.

Two things worth having written down before this is picked up.

**The choice is constitutional, not statistical.** Crowd-BT's reliability parameter says a judge is *often wrong*, which presupposes there is a right answer to be wrong about. This project's §3.3 says the opposite in as many words — *preference is not measurement*. If Hollis prefers the shorter clause and thirteen people prefer the longer one, Hollis is not unreliable; Hollis disagrees. Down-weighting a judge is **disenfranchising them by an algorithm nobody in the room can audit**, which is a serious thing to do to a member of a constitutional convention. But there are documents where it is exactly right — a technical standard, a specification being cleaned up, any domain where some readings are simply mistakes — and those are the same documents where a large roster contains people of very different expertise. So the real question a convenor is answering is **does this document treat disagreement as difference or as error**, which is the kind of question that belongs at `document-creation` or the `founding-ceremony` beside quorum and the bar, and not in a settings menu. Under the §9.0a logic it is arguably the roster's to settle rather than the convenor's: being told your judgments count for less is a thing done *to* you.

**And the sophisticated model is probably wrong at v1 scale.** Crowd-BT spends data on extra per-judge parameters, and CLAUDE.md tunes v1 for rosters of 5–10 where data-efficiency beats throughput; on fourteen judges it may be strictly worse than plain Bradley–Terry, which is itself chosen over approval *because* it extracts more per judgment. Crowd-BT's home is hundreds of annotators. Which puts its threshold at roughly the roster size where you stop being able to know everyone — the same point at which a room stops being able to vouch for each other's good faith. Those two thresholds coinciding is not a coincidence, and it is the strongest argument that this is a real setting rather than a knob: **the model should change when the room changes kind.**

One reversal to expect if approval is built: it produces the **more legible record**. The `sealed record`'s best sentence — *these two beat what we had and those three did not* — is currently a Bradley–Terry construction in which every number is a probability against the incumbent. Approval counts are less informative and far easier for a lay reader to trust. The more sophisticated model makes the harder-to-explain record, which matters for a document whose legitimacy rests on people believing the record.

42. **Spectator commentary view** (Ed, 2026-08-14): an optional commentating view for people watching a convention — like the sim's commentator, but consuming **public data only**: gazette, live candidate texts/rationales, bounty board, document state. Never individual judgments, never standings, authorship only per the session's visibility setting. Provision: expose a `spectator-api` in engine-core (a strictly-public projection, sibling of the participant-api) and have every spectator surface — commentary LLM included — consume only that, so privacy holds by construction rather than by prompt discipline. Note the sim's own commentator is deliberately different: it is omniscient (sees temperaments and hidden agendas) because it narrates a fiction for the experimenter; the product spectator box narrates real people and gets none of that.

55. **Participant-api composer surface** (sim evidence 2026-08-14): `ParticipantApi` has no composer-opening call — the sim runner invokes `Session.openComposer` directly (same precedent as its dedup co-sign path). The product composer needs that surface, including the §3.3 forfeit semantics, before the UI can exist.

56. **Orphan refunds credit departed ledgers** (sim evidence 2026-08-14): when an author-departed candidate is adopted, its refund credits the removed member's ledger — harmless (tokens are worthless at close and E excludes them), but the UI must not render it as an invitation to act.

244. **A flow entry can overlap a pinned one** (found 2026-08-16 while verifying 222; pre-existing, not caused by it). Flow entries are supposed to step around the pinned pile rather than hide under it (112), and `freeFor` re-checks every candidate position against the pinned blocks — yet one case still renders overlapping. Reproduce: open `quick-cellar` from the top of the document and `quick-probation` (a judged line) lands inside `quick-attendance`. The same shape occurs on the previous build with a different pair (`diag-quorum-keys` over `quick-notice`), so it predates the claim-absolute change. Not chased further because it is bounded and cosmetic, but the likely suspects are a height measured before a re-render or the `prev + QGAP` adjustment running against stale blocks.

248. **The ⌈E/3⌉ has never been calibrated** (raised 2026-08-16; since 2026-09-07 also carrying 251's worry, by Ed's Q1221: at the standard rate a wallet sits near its cap most of the time, so a full wallet tells a member nothing — the same sweep should say what rate and cap make the wallet move). It arrived with spec v0.5 in the initial commit and has never been the subject of a study, unlike its neighbours — the hot set went 6 → 3 on a sweep, the economy was checked at rosters 5–14, the ramp was argued out. §8.2 justifies it as "statistical sufficiency", which is the wrong account: sufficiency depends on effect size and noise, not on how many people are in the room, and the adoption threshold is already doing the confidence work. What the floor actually does is **legitimacy** — the claim that enough of the room looked for the outcome to be theirs. That is a political quantity, and political quantities are argued rather than derived, which is why a third feels arbitrary: it is. The one relevant piece of evidence is finding 10 of the deferred-evidence pass, where F dropping to 2 produced adoptions that were still fine *because they had to clear a ~0.9 posterior bar*. A sweep of F against welfare and against how much ends up parked as backlog would settle it. Now partly superseded by §9.0a — where the roster sets its own quorum, the default matters less, but a default is still needed.

252. **A perpetual document loses the reset** (raised 2026-08-16, from §9.0). §9.4 and §4.5 lean on it: next session the threshold resets, the backlog re-enters stake-waived, and everything becomes contestable again — "entrenchment is session-scoped". With no sessions, an adopted clause stands until something clears the same fixed bar, and there is no periodic moment inviting the room to reconsider. This is the one place where perpetual is worse rather than merely different. Candidates for a replacement: a scheduled thaw; a rule that a clause untouched for long enough re-enters the backlog; or accepting that a perpetual document *should* entrench, since that is what a standing charter is for.

## Spent numbers

**The next free number is 1280** — claim by writing the block here, then commit it alone. **A released number is dead** (Ed, 2026-09-07, Q1223): claimed and unused, it is never reused, so a number always dates its item — **1017** (claimed with 1015–1016 for backlog entry 159), **1022** (with 1020–1021 for entry 160), **1031** (with 1030 for entry 173) stay unspent for ever; 1109–1112's reuse was the one exception, not a precedent.

Every other claim block and built-narrative, and the map of where everything below 260 went, left this file verbatim on 2026-09-07 for `design/DECISIONS.md` § *QUESTIONS.md, the closed items and the spent numbers, lifted 2026-09-07 (the docs pass)*. What stays below is every claim block that still names an open number.

**1206–1279 are the questions of the documents pass of 2026-09-07**, claimed by the session that ran the pass after Ed had gone to bed, one item per question the fourteen logs left in their *Questions* sections, in log order: **1206–1209** STYLE.md (log §6 items 1–4); **1210–1230** QUESTIONS.md (log §6 items 1–21); **1231–1239** SURFACE.md (log §6 items 1–9); **1240–1245** CLAUDE.md (log §6 items 1–6); **1246–1250** README.md (log §6 items 1–5); **1251–1256** PRODUCTION.md (log §6 items 1–6); **1257–1259** docs/OPERATING.md (log §6 items 1–3); **1260–1263** the package NOTES files (log §6 items 1–4); **1264–1266** design/MOBILE.md (log §7 items 1–3); **1267–1268** REVIEW-creation-session.md (log §6 items 1–2); **1269–1270** REPORT-deferred-evidence.md (log §7 items 1–2); **1271–1272** design/spec-pass/ (log §6 items 1–2); **1273** the card reviews (log item 1); **1274–1279** SPEC.md (log *Questions* items 1–6). All seventy-four stand under *Open*, each body the log's question verbatim.

**1202–1205 are the pair-deck build's open findings (2026-09-07)**, claimed in the session that ran the two builds of 2026-09-06 (quorum-is-the-floor, pair-deck) after Ed had said *push* without answering them: **1202** what ⏳ means under the deck (dealt now vs held); **1203** where a revised pair sits in the ledger; **1204** the slow ceiling test on Ed's machine; **1205** `seat-matrix` red before either build. All four stand under *Open*.

**1200–1201 are the pair deck and the judged-pairs ledger (Ed, 2026-09-06)**, claimed by the plan that builds them (`~/.claude/plans/pair-deck.md`). Ed: *The reason I wanted to use Bradley-Terry is for when there are many alternatives to the same clause, we have a pairwise system. Come to think of it, I haven't been served multiple pairs from one clause yet.* He had not been, and the cause was the page: `itemsFromView` kept the first served card on a race and filed the race as ⏳ on any standing vote, so every member was capped at one comparison per race for the race's life, whatever the engine held for them. **1200** the fix he chose from three shapes (a stack of ⏳ tabs, a switcher on the card, the deck): **the card is a deck** — one entry per clause, lit while the router holds an unjudged pair for you on the race; judging closes the card and the next press opens the next pair in the router's order; ⏳ only when none is left; no new control. And on what the entry shows while the deck is not empty: *It should show you the pair that you would vote on next* — the teaser is the front pair's rationale(s), never a count. **1201** Ed: *Is there a way I can go back to see pairs that I've already judged?* There was not, live — the server sent *you have voted on this race* as a flag and never which pair or which way — and he chose to **fold the ledger into the deck plan**: the ⏳ card is the member's own ledger of every pair they judged on the race, each verdict marked and revisable, so the earlier plan's accepted cost (an earlier pair out of reach while later pairs are dealt) never ships.

    **Built 2026-09-06 (`pair-deck`), both spent, none released.** **1200**: `itemsFromView` reads the deck — every served edge card on the race, the front one the card, the state *needs* while any is dealt and *deciding* only once none is and a vote stands — and `session.js` keys its provisional judgment state by pair (`pairKeyOf`), so the entry re-lights with the next pair and the receipt never sticks to one you have not judged; the two captions move to `copy.js` unchanged. The engine deals one best pair per race per hand and excludes the pairs judged, so the deck reads across views: judge, and the command's own refresh deals the next — two incumbent pairs and then the rival pair, gate or no gate (`bestPairFor`'s second scan), which is how a race card is now reached on a live document. **1201**: the view carries `myJudgments` per race (ids and verdict, oldest first, superseded ones gone); the ⏳ card lists them as blocks with the chosen side marked, a press making that pair the card's active pair with the verdict pre-selected, and ✓ sending the revision on the same pair (§4.4). Guards: the two-challenger server test and `npm run journey`'s deck and ledger steps. SURFACE E13, L4, M18, §9's ledger row; SPEC unchanged.

**1196–1199 are *quorum is the adoption floor; the freeze and sign-out go* (Ed, 2026-09-06)**, claimed in chat before the plan is written (`~/.claude/plans/quorum-is-the-floor.md`). Ed opened by proposing to remove 👥 altogether — *I have realised that I misunderstood what Quorum does. I don't think we need it; the functionality I want is covered by Lapse and Leave, and we won't have "Sign Out"* — and, told that the quorum is also the per-race adoption floor (SPEC §4.2's Q), chose **keep 👥 as the floor, remove the freeze**. **1196** the ruling itself: §9.5 loses sign-out (holding / abstaining) and the freeze; the electorate of a founding question and of a 🏛️ motion is E; the quorum base is gone, E being the only base; the ways out of the base are resignation, removal and lapse. **1197** the topbar's *quorum n of E* reading — it already prints the floor (`cs.v.floor`), and Q860 kept it as a sentence; asked whether it stays now that quorum has only the one meaning. **1198** `/pairwise` said 👥 *is about the membership being present, never about how many votes one proposal has* — flatly false against §4.2, and the likely source of the misreading; a finding, fixed unless vetoed. **1199** whether the 👥 clause sentence (*Quorum is n of the membership.*) should itself say the floor, or leave that to the meaning line beneath it, which already does and loses only its freeze clause.

    **Built 2026-09-06 (`quorum-is-the-floor`), all four spent, none released.** **1196** is SPEC v0.99: §9.5 is one paragraph, *Presence* — plain silence is nothing, E is the only base, the ways out of E are resignation, removal and lapse, judgments cast keep counting, and a count-form quorum above E holds every race at the floor and nothing else (R-088; R-032, R-038, R-049 and R-077 marked superseded); the mechanism loses `signOut`, `maybeFreezeOrThaw`, `quorumBase` and `mustReturn`, `canJudge()` is *constituted*, the view loses `frozen`, the server loses the `sign-out` command and the ladder its sign-out rung, and `signed-out` / `frozen` / `thawed` stay in the event union as legacy no-ops (`legacy-events.test.ts`). **1197** built as resolved in the plan — the topbar's *quorum n of E* stays, since `cs.v.floor` *is* the floor and Q860 made it a sentence about a decision. **1198** fixed: `/pairwise`'s parenthesis now says the quorum is *the floor under all of this: the number of members who must have voted on a change before it can pass … It never decides a proposal; it only says how many people the document must have heard from first.* **1199** built as resolved in the plan — the clause sentence stays and the meaning line beneath it carries *what it does*, every branch now ending in the floor (STYLE audit item 83, `design/DECISIONS.md`). SURFACE E23 is struck; CLAUDE.md retires `sign-out` and `freeze`; the reasoning is `design/DECISIONS.md`'s section of the day.

**1181–1195 are Ed's card review round 3 (2026-09-05)**, claimed by the session sorting his 44 notes (`design/card-review-2026-09-05.md`) for the questions the sort raises, asked one at a time in chat, and for the readings built unless vetoed. **1181** 🍾's power table redesign (Ed asked for a proposal); **1182** the consent card's shape under the option-block grammar (settled 👤 with a constitutional motion in flight); **1183** what a stranger's tab opens on a settled setting; **1184** whether the release-batch news card stays; **1185** the delegation sentences' tense and whether *both alternatives* reaches every binary setting; **1186** the rejected record's shape; **1187** ✉️'s multi-address box at a member's price; **1188** provenance wording where the founder chose the standing rule. Readings: **1189** the *goes to the Founder* note goes everywhere; **1190** a close-only OK on every 🗑️-only card; **1191** *Any members* → *Any member*; **1192** the method note at `--t-ui` in the blue box; **1193** `.pick.off` greys the radio alone; **1194** the login card's send is a 📧 glyph commit; **1195** the build's residuals, filed open above. **All fifteen spent** — 1181–1188 answered by Ed in chat the same evening and built (`design/card-review-2026-09-05.md`, Answered), 1189–1194 built as read, 1195 open.

**900–904 are the entry-94/97 documentation sweep** (2026-08-26), a consistency pass over the session-loaded set after the membership rebuild, answered by Ed the same day. **900–902 are open above**, each with his answer; **903 is spent** — the rename, built 2026-08-27 — and **904 is spent** — 🤝's `proposed` and `open` rungs went with entry 94's four-rung `joinPolicy` and the module folds every legacy value to `{apply}` on load, so the two label maps that still speak them cannot fire; Ed's answer was to mark them dead where they sit and remove them in one deliberate sweep rather than piecemeal, which is what the code now says. **905–909 are released unspent.**

Twelve findings were fixed with them rather than asked, `spec-check` having been green throughout — which is the lesson of the pass and is worth keeping: **the checker asserts the tables and the prose around them is unguarded**, so a rebuild that appends beside what it contradicts passes it. The worst of them, SURFACE **F19**, had recorded entry 94's move of the invitation box as an *instruction to substitute* — its headline saying every 🪪 below now reads ✉️, its body still saying 🪪 — and four other sites had copied the unsubstituted form. The rest: SURFACE Y24's subject and gate; E10 claiming invitations and removals for the constitutional route alone, where their route is now 🪪's and 🥾's price; CLAUDE.md's *two kinds of decision* opening by listing the membership as constitutional and closing by calling it a fact; its 🤝 entry describing the pre-entry-94 split in every clause; `applicants` reading its policy off 🤝; `founderDirect` claiming the register's pair still lives in the applications value; `door-cards` filed under `constitution-section` and pointing at the page-key table; SPEC §9.6's *join policy*; and README at v0.73 and 501 tests against a v0.76 spec and 531. Commits `032f00b`, `c2e4dca`, `17bfbed`, `37daf20`.

**924 and 925 are the git question** (2026-08-27): 924 open, sub-paragraph Gate 1 with a sim measurement first; 925 parked, the dependent-candidate, version-chain and patch-id ideas kept together.

**918–923 are the seat matrix** (plan-queue **38**, backlog entry 127), claimed by the batch-N pre-flight on 2026-08-26 **before the build, not by it**, so a parked branch cannot take them twice. The plan asked for a block of five for the *no rule* rows its first green-path run reports — events its step table triggers whose SURFACE §2 Audience cell it cannot honestly reduce to a predicate (E9's *the actor; every member* and E17/E18's conditional cells being the likely shape) — and one spare stands beside them for whatever the first run turns up that is neither a finding nor a *no rule*. Each question quotes the cell verbatim, names the step and the seats, and offers the reading the builder would have taken. Unspent numbers are released. **Folded 2026-08-27 by the first full run** (plan-queue **41**, entry 139, against build `7eceef8`): **918** spent on E9 — the one genuine *no rule* row, once per hat, now carried as `filed: 'Q918'` so the harness is green over it; **919** spent on the run's one member-hat finding, 💤 unrehydratable and every card below it withheld from members; **920** on its one clerk-hat finding, `view.convenor.isMember` stale for a clerk so 🍾 waits on a voice they do not hold; **921–923 released unspent**. E17/E18's conditional cells were never reported — the step table raises no text race. The two last lines, verbatim: `seat-matrix: findings=15 noRule=0 shape=0 errors=0 refused=0 unstood=0 exit=1` (member) and `seat-matrix: findings=25 noRule=0 shape=0 errors=0 refused=0 unstood=4 exit=1` (clerk) — red on 919 and 920 and on nothing else; the run's JSON is gitignored by design, so a later session re-runs `npm run seat-matrix -- $DRAFT_BASE_URL --hat=<hat> --out=<file>` for its baseline rather than looking for one. Note that **924–925 are claimed twice in this section** — below, for the copy freeze (plan 39), and above, for the git question, which spent them (items 924 and 925 exist) — so plan 39's claim is void; which block gives way is not this fold's call.

**924–925 are the copy freeze** (plan-queue **39**, backlog entry 128), claimed at the same moment. The plan names **no** identifiers of its own and may well spend neither: the pair is provisioned for what freezing 278 cards of copy exposes that normalisation cannot honestly hide — a string that moves between two clean runs for a reason nobody has named, and the two known errors it freezes as facts (*seat:applicant offered no cards*, *closed: no backlog (U:) records on the page*) becoming questions rather than records. Unspent numbers are released.

**993–1012 are twenty decisions Ed gave on 2026-08-27 and 2026-08-28**, claimed and folded by the same pre-flight. Nineteen of them were **never in the open register**: each was asked from a plan while it was being written or from a batch's run report, and answered before anything here carried a number, so they are recorded as decided and deliberately not added above. The twentieth is **Q70**, which had a number already and stays above until its build lands.

**996 is the per-decision reveal rung**, filed above in the Backlog. Ed's 👁️ note had two halves; the first is built and the second — names shown as each decision closes, decision by decision, rather than at the end of everything — names nothing today. Ed: **file a backlog entry for the per-decision rung**. Done, under this number. **Spent.**

**1009–1010 are the two document refactors**, both filed above in the Backlog. plan-queue measures three of draft's documents at the end of every run against budgets Ed set: after batch Q `CLAUDE.md` stood at 99,290 of 100,000, `SPEC.md` at **81,091 against 80,000** and growing 1,000–2,000 a run, and `SURFACE.md` at **77,151 against 70,000** — the append-only surface inventory, where every batch adds rows and nothing takes any out unless somebody decides to. The tool never gates on this and never files the entry itself, because what to take out of a document is a judgement about what the document is for. Ed: **file both refactors as backlog entries, SURFACE first**. **1009** is SURFACE.md, **1010** is SPEC.md. Every session reads `CLAUDE.md` whole and `spec-check` reads `SPEC.md` end to end, so these are a cost per session as well as a readability question — and note that 1003 and 1006 both cut into the same two files from other directions, so whoever writes 1009 should read them first. **Spent.**

**1012 is the removed invitee who was never here**, filed above in the Backlog. The batch-O plan *Three acts nobody is told about* added a mail for a convenor's removal — *You are no longer a member of …* — and the mail arm in `packages/server/src/server.ts` has no arrival check, while the session's own departure fold deliberately excludes people who never arrived; so an invitee who never turned up and is then removed via `remove` is told they are no longer a member of something they were never in, and the register's departure lines say nothing about the same act. The batch review left it alone as a behaviour choice. Ed: **keep the mail and also record the departure** — so the guard is not added and the *other* half is fixed instead: the departure fold records a removal whether or not the person ever arrived. Today it is unreachable from the page (invitees are routed to `uninvite`), so nothing a member sees is wrong yet, which is why it is filed rather than hot. **Spent, less the build.**

**1013–1025 are claimed by the batch-U pre-flight** (2026-08-29), one block per plan, before any of the five is built. Every one of the five plans says *this run has no pre-flight, so claim your own numbers at claim time* — **that sentence is now false**, and a session that claims again takes numbers a sibling in the same batch has already been given. The blocks below are the claim; no build in this batch re-reads this tail for a number of its own. `R-nnn` keys are reserved here too, in this file rather than in `design/SPEC-REASONING.md`, because that file's tail is read by four of the five sessions in the same run and `R-055` was the highest at `ebb1882`. **Whatever a plan does not spend is released when it folds.**

**1015–1017 are *🛡️ on the Text holds an adoption rather than reverting one*** (backlog entry 159, plan 102). **1015** — what a member who is *not* the Founder is told while a text adoption sits awaiting assent; the surface says nothing about it today and the build deliberately adds nothing, so this is filed open rather than answered. **1016** — spare, for whatever the build defers (its *not in scope* list names the seat-matrix row and the author's own ✏️ line as the likely two). **1017** — spare. **`R-056` is reserved for this plan's ruling**: Ed, 2026-08-27, *a shielded Text parks an adoption, it does not revert one*, with adopt-then-unwind as the rejected alternative and the one-at-a-time rule as its consequence. — **Built 2026-08-29.** **1015** is filed open above (what the room is told while an adoption waits; the build adds nothing). **1016** is filed above too: the seat-matrix row is deferred to entry 158, which makes the state reachable in one press. **1017 is released, unspent.** **`R-056` is written** and SPEC is at v0.85 (§9.7 rule 8's second sentence, §4.2's batch rule, README followed); SURFACE §9 gained the *👑 question (the Text)* row. **Spent.**

**1033 and `R-060` are *A park with no convenor auto-passes, and a vacant seat holds no shield*** (plan 115, closing the batch review's deferred finding F7 on plan 102's range `b57a1a68..bb335de0`). Claimed at the tail at build time: the batch claim block above (1026–1032) covers plans 107–114 only, and this plan is not in it. **`R-060` is reserved for Ed's ruling of 2026-08-29** — *a park with no convenor auto-passes, the way a lapse does* — with the plan's own forward-looking half beside it (a vacated seat holds no shield, so the *next* race adopts by itself rather than parking against an empty seat). **1033** — the residual the plan names and deliberately does not implement: what becomes of a **motion-backed** 👑 question standing when the seat is vacated; filed open below.

