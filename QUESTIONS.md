# Open and deferred items

Resolved items are folded into SPEC.md and removed — this file holds only what's still open. Numbers are from the continuous project-wide sequence; never renumber or reuse.

## Open

The index, in the section's own order (nothing is pinned — Ed, 2026-09-07, Q1228). *State* is what the tree said on 2026-09-07: **open** (nobody has ruled), **waiting on Ed** (readings offered, no ruling), **partly answered**, **answered, owed** (Ed ruled, the build has not landed), **parked** (Ed's word), **provisioned**. A state ending *(log Qn)* names a question the docs pass filed about the item itself.

| # | Title | Raised | State | Pointers |
|---|---|---|---|---|
| 1326 | The host under two hundred bots: the knee at 115, and three levers | 2026-09-11 | Ed's moon room (200 bots, a bot founder): the starter fell over at 115–120 bots acting every 5–30 s; built the same day — judgments indexed by ground and candidate (the per-race scans 23% → 7% of samples), V8 told the container's size, and the slim view (a poll stating seq · `tv` · `rk` is answered without the projection, text and records it holds); the fold's uncached `races()` reads left for another day; **re-run 2026-09-11 19:00 on the deployed build** (`docs.vote/d/moon`): the knee moved to about 140 bots, views holding under a quarter of a second through it while judgments passed ten seconds — the command path is what saturates now, which is the unbuilt lever; the instance never restarted; PRODUCTION.md § *The second moon room* | `view`; `engine-core`; PRODUCTION.md § Measurements; Q1324, Q1332 |
| 925 | Three more things git knows about overlapping patches | 2026-08-27 | parked | SPEC §2.2, §2.4; Q924 |
| 109 | Scale: the margin assumes a short rail | 2026-08-16 | waiting on Ed's own read (the walk, 2026-09-14: *look at a moon-shaped room and rule from what you saw* — `docs.vote/d/moon2`, ~156 members, is idle; the alternatives on the table were naming a ceiling of ~30 live questions, or committing to a screen-only margin) | `needs-you-queue` |
| 170 | Your candidate needs rebasing | 2026-08-16 | waiting on Ed — a mark | Q53 |
| 282 | A rendered document and a source offset do not agree | 2026-08-17 | deferred past the alpha (Ed, 2026-08-27) | `richToSource` (cards.js) |
| 288 | A flatter glyph set for the lifecycle marks | 2026-08-17 | partly answered — the lifecycle family is drawn; the subject glyphs are emoji | `DRAWN`; SURFACE §6 |
| 311 | Is a locked judgment card without a ground shift reachable? | 2026-08-17 | ruled by Ed 2026-09-14 (the walk): **delete the branch, the flag and the sentence** — `reviseNote`'s `s.locked` arm, `lockedTail`, the card-level `locked` flag (`lockedOf` in session.js:160 and cards.js:766, session.js:887 and :3508) and the fixture's `quick-garden` `locked: true`; the closed-document and judge-gate locks stay; owed, to land once the Q170 and Q288 branches merge since both edit the same files | `reviseNote` |
| 314 | Seeing which card other members have open | 2026-08-17 | parked — shelved by Ed until real humans | SPEC §3.5 |
| 346 | Sybil resistance at the front door | 2026-08-18 | parked | SPEC §9.6a, §9.7½ |
| 386 | No surface yet raises a reserve motion | 2026-08-19 | open — unbuilt; the page only displays one | `motionDisplay` |
| 387 | Reserving the text | 2026-08-19 | parked | SPEC §9.7 |
| 388 | Individual delegation, foundership transfer, inheritance | 2026-08-19 | parked — out of scope (Ed) | — |
| 398 | The multi-candidate ordinary race card | 2026-08-19 | waiting on Ed's design pass | Q390 |
| 419 | Does the desktop focus order match the reading order? | 2026-08-20 | answered 2026-08-23, waiting on the mobile build | `design/MOBILE.md` §4 |
| 420 | The narrow form of the margin index | 2026-08-20 | answered, waiting on the mobile build | `design/MOBILE.md` §4; Q657 |
| 421 | The fold triangle is invisible until hover | 2026-08-20 | answered, waiting on the mobile build | `design/MOBILE.md` §4; Q658 |
| 422 | Judging offline | 2026-08-20 | answered, waiting on the mobile build | `design/MOBILE.md` §3.e |
| 423 | Does `room-pulse` need a third state on a phone? | 2026-08-20 | answered (no pulse), waiting on the mobile build | `design/MOBILE.md` §3.f |
| 424 | Multi-block drafting on touch | 2026-08-20 | deferred to MOBILE stage 6 | `design/MOBILE.md` §1.7 |
| 425 | Do lanes collapse or scroll on narrow | 2026-08-20 | answered, waiting on the mobile build | `design/MOBILE.md` §4 |
| 426 | One PWA icon or many | 2026-08-20 | answered, waiting on the mobile build | `design/MOBILE.md` §3.b; Q669 |
| 427 | `--border` contrast at phone brightness | 2026-08-20 | answered, waiting on the mobile build | `design/MOBILE.md` §4; Q659 |
| 428 | Where do push subscriptions live | 2026-08-20 | answered, waiting on the mobile build | `design/MOBILE.md` §3.d; Q673 |
| 429 | How big is the offline shell allowed to be | 2026-08-20 | answered (no budget), waiting on the mobile build | `design/MOBILE.md` §3.c |
| 463 | The close is undesigned, and it needs 🍾's treatment | 2026-08-20 | backlog (Ed, spec pass 1 item 567) | SURFACE E24 |
| 464 | The record is undesigned, and it is what the machine produces | 2026-08-20 | partly answered — (a) (b) ratified; (c) open, after Q996 | `closedBlocks`; SPEC §3.4 |
| 465 | Notifications are the surface nobody has designed | 2026-08-20 | answered as a commission — the design pass is owed | STYLE §6; `mail-fold` |
| 500 | The legal drafts' decisions | 2026-08-20 | parked — until go-live is scheduled (Ed, 2026-08-29) | `docs/legal/` |
| 631 | *Session* or *document*: Ed's rule-to-be is that it depends on whether the document has an ending | 2026-08-22 | deferred by Ed 2026-09-07 (Q1218): *return to this later* | `design/copy.js`; STYLE §1; ⏰ |
| 901 | Three acts have no communication designed | 2026-08-26 | built; two unnumbered residuals | SURFACE E31–E33 |
| 1205 | `seat-matrix` is red | 2026-09-06 | diagnosed 2026-09-07: 56 → 21 (walk) → 11 (Q919 (b), Q920 built); what stands is 1281, 1282 and a gate-news cluster still unread | `scripts/seat-matrix.mjs`; 1280–1282 |

1294. **📝 wants the markdown toggle** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; the text card in edit mode). *When in 📝 editing mode, the text area should have a "see markdown" control (like with proposal composers).* The `lane-controls` (SURFACE K22–K23: **B**, *I*, and the `[]` toggle) are drawn on the editing card only; 📝's column has none. One reading: the same three controls at the same place on the text card in edit mode, the `[]` toggle switching the whole column between rich and markdown as it does a draft lane. Pointers: `edit-mode`; `lane-controls`; SURFACE K13, K22–K23, K31.

1295. **📝's pump should be the birth's** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; the text card). *When clicking on the text area, the "pump" that 📝 does should be like the pump that the title card does during the birth when you click in the document area (raising up and down twice rapidly).* The `dead-click-nudge` (SURFACE F17) is the birth's gesture; whatever 📝 does on a click into the read-mode column today is a different motion. Make them one motion, and one implementation. Pointers: `dead-click-nudge`; `edit-mode`; SURFACE F17, K13.

1296. **The founder's ✒️ submits the text** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; the text card, founder). *As the founder, when I am editing the text, I should be able to click on ✒️ to submit it.* Which ✒️, and what it does today, to be established before anything moves: the riding tab's strip carries ✒️ 🛡️ in edit mode (K31), and the proposal row carries the commit; one of them is not acting as the founder's decree (SPEC §9.7 rule 8, `amendment-news`), or acts only on a hold where Ed expects a click. Pointers: `riding-tab`; `proposal-row`; `founderDirect`; SURFACE K31, §9.1; SPEC §9.7 rule 8.

1297. **The member's foot-of-window ✏️ submits the proposal** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; the text card, member). *As a member, when I am editing the text having clicked 📝 and a proposal card appears, the 📝 area ✏️ button at the bottom of the screen should submit that proposal.* In edit mode the proposal row is stuck to the foot of the window (K13); when typing opens the editing card, that card carries its own commit, and the foot row's ✏️ apparently does not submit the draft the card holds. Two controls for one act, or a dead one; establish which before fixing. Pointers: `edit-mode`; `editing-card`; `proposal-row`; SURFACE K13, K17–K18.

1298. **A green ✔️ entry opens nothing** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; the rail, an adopted clause). *For "The treasurer reports the balance at every meeting." clause I can see a green ✔️ queue card, when I click it no card opens in the central column (I think it should show a change acknowledgement).* A decided mark (SURFACE §6) whose entry opens no card in the charter column — a `sealed record` is what an adopted race's entry opens, and the acknowledgement is its OK. Reproduce on `test1`, then find which kind of item it is and why its open is a no-op. Pointers: `sealed record`; `lifecycle mark`; SURFACE §6; `itemsFromView`.

1299. **A constitutional setting's own tab stays on top** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; the tab-stack). *For constitutional settings, the tab for the setting itself should always be on top.* The pile order is `STACK_ORDER` (SURFACE M11–M13); on a 🏛️ setting with a motion or a question standing beside it, the setting's own tab is not front. A rule for `STACK_ORDER` or a special case — say which in the fold. Pointers: `tab-stack`; `STACK_ORDER`; SURFACE M11–M13, §8.

1300. **The founder-member's own answer says so** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; the answer card). *As a founder-member, when I'm answering my own blind questions, there should be a blue box on the decision card that says something like "This is your answer to this question as a member".* §9.0b's separate question card, born when the founder delegates; nothing on it today says which hat is answering. New copy (STYLE), a note block in `--primary` — the one accent, which is what *blue* is on this surface — and only where the viewer is both. Pointers: `answer-bodies`; SPEC §9.0b; SURFACE §9; `viewerIsClerk`.

1301. **🌡️ prints a percentage where three rungs were ruled** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; the 🌡️ card). *For 🌡️ I can see "For a proposal ✏️ to pass, 79% of the members that voted on it must prefer it to the alternatives.", but we decided to have three rungs with no numbers.* Drift against a ruling: find it (DECISIONS / SPEC-REASONING, the threshold's rung ruling), state what the card should say at each rung, and whether `meaningOf`'s sentence (T39) or the rung labels are the thing carrying the number. Pointers: `meaningOf` (`packages/constitution/src/meaning.ts`); STYLE T39; SURFACE §9's 🌡️ row; Q1290.

1302. **Backspace at a clause's start joins it to the previous** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; edit mode). *When editing text 📝, I should be able to backspace at the start of a clause to join it to the previous clause.* A `draft-site` is a run of adjacent blocks replaced by one text (K14–K16), so a join is a two-block site; the caret at offset 0 with Backspace has to extend the site upward instead of doing nothing. Enter at a clause edge already makes a `gap-site` — this is its inverse. Pointers: `draft-site`; `gap-site`; `edit-mode`; SURFACE K14–K16, K31.

1303. **A proposal's entry with no heading takes the document's title** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; the rail). *For a queue card for a proposal, if there is no heading for it to get a title, use the title of the document.* The `race-labeler`'s nearest-heading fallback has nothing to fall to on a heading-less document; the title is the outermost heading. Pointers: `race-labeler`; `needs-you-queue`; Q49.

1304. **Markdown does not render in edit mode** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; edit mode). *While editing text 📝, markdown formatting is not rendered. e.g. if I type "# this is a heading" the # disappears, "this is a heading" appears in the table of contents, but it is not rendered as a heading.* The block is keyed as a heading (`blocksOf`'s `# ` prefix) — the TOC proves it — and the rich view strips the marker without applying the heading style. The `htmlToMd` / rich round trip (K22–K23) at the block level rather than inline. Pointers: `blocksOf`; `lane-controls`; `edit-mode`; SURFACE K22–K23, K31.

1305. **The wash saturates but does not fill** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; the evidence meter). *I am seeing the saturation of queue cards increase, but I don't see the wash filling from left to right.* The `evidence-meter` is specified as a fill whose leading edge is the bar; what Ed sees is an opacity rising. Measure first (the fill's width against `closeness`, the gradient stops), then say whether the drawing or the spec is the thing to change. Pointers: `evidence-meter`; SURFACE §6; Q836.

1306. **🗑️ on one site of a patch discards that site alone** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; edit mode, a patch). *When I am in edit mode and am making a patch (edit in two places), the 🗑️ for the edit in one place should only discard that edit, not the whole patch.* Today the proposal row's 🗑️ is *discard all* (`row-discard`, `T.row.discardAll`) and each editing card's 🗑️ likewise drops the draft; a site needs its own discard, the patch surviving with one site fewer and becoming a plain candidate at one. Pointers: `draft-site`; `proposal-row`; `editing-card`; SURFACE K14–K18, §9.1.

1307. **Markdown bullets do not render in the text** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; the charter column). *in the text, markdown bullets are not rendering ("- a bullet")* — visible in `design/bug-shots-2026-09-10/missing decision card.png` under OUR STANCE. The column prints every block through `esc(line.x)` and `blocksOf` knows headings only; bullets are block markdown like headings, so this is 1294/1304's family: does the column render markdown, and which of it. Pointers: `blocksOf`; `mdToHtml`; SURFACE K22; Q1294, Q1304.

1308. **A race whose candidates span two blocks draws a strange card** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; a race card). `design/bug-shots-2026-09-10/strange card.png`, Ed's own title: a race on the first clause of AI Usage Policy whose head shows one block (*We believe … mass data*) while every candidate's text runs on into the next (*… The treasurer reports the balance at every meeting.*), and beneath the two *Prefer this* lanes a second pair under *Your vote on this clause* with its own radios and Indifferent — two pairs on one card, or a ledger drawn under a live pair. The bots' `--heat` proposals make the treasurer sentence; reproduce on a local bot room before reading further. Pointers: `proposal-block`; `pair-deck`; `keysOfSpan`; `itemsFromView`; SURFACE §9.

1309. **dev.docs.vote loses every document on a restart, not only a deploy** (raised 2026-09-10, Ed's first bot room on dev.docs.vote (`/d/test1`), 2026-09-10; the dev host). Ed's room died under him mid-test — *I refreshed the page and got {"error":"no such document"}* — with the dev host reporting 22 minutes of uptime and no deploy since 2026-09-09 16:16: Render restarted the service and the file store on its ephemeral disk went with it. `docs/OPERATING.md` §9 says *every deploy wipes it*; a restart does too, at Render's discretion, so a test room can vanish at any moment. Readings: (a) accept — a dev room is disposable, the note says so; (b) a persistent disk on the dev service; (c) the dev service on Postgres like production, one database per host. Recommend (c): the store seam already exists and the cutover is two variables. Pointers: `dev-host`; `Persistence`; `docs/OPERATING.md` §9.

1310. **Bots on docs.vote: mail to `*@bots.docs.vote` is caught by the host, never sent** (raised 2026-09-10, from Ed's answer to 1309; the mailer and `room-bots`). Ed: *we can have bot users in prod — we're still in alpha*, and on the door: *why don't we catch all emails to *@bots.docs.vote instead, since they could never be confused for a real email address* — confirmed as the server-side catch. Today `room-bots` seats a bot by reading `GET /api/dev/outbox` for invitations to `*.bot@` addresses and following the links; production has no outbox route, the Resend key is set, and a link cannot be forged outside the host (random token, stored hashed, one use), so a bot invited on docs.vote is a bounce. Ruling: any address at the subdomain `bots.docs.vote` is a bot's — the mailer files its mail in a bot outbox on the host instead of handing it to Resend, in the dev branch and the production branch alike; the outbox is served by an operator-keyed route (`DRAFT_BOT_KEY`, set in the dashboard) that ships in the production artifact; `room-bots` reads it with `--key` and drops the `.bot@` convention for the domain. A leaked key lets a stranger act as the bots in bot rooms and nothing more. Rejected: minting links by hand on the host (a shell trip per re-login); a real catch-all mailbox over IMAP (a mailbox to run, a client to write). Pointers: `mail-fold`; `room-bots`; `dev-outbox`; `docs/OPERATING.md` §9; PRODUCTION.md's security list.
1311. **Two gap sites in one patch draw one anchor** (raised 2026-09-10 by the Q1306 build, as a decision it left open; ruled by Ed 2026-09-10 16:04, *fix it now*). A gap site's bookkeeping — which gap (`gapKey`) and which block it follows (`insertAfterKey`) — lives on the draft rather than on the site, as `startDraft` left it, so a patch holding two gap sites draws one `insert-anchor` and the second gap has nowhere to stand; `dropDraftSite` re-derives the one anchor from whichever gap site survives. Ruling: the bookkeeping moves onto each site, every gap site draws its own anchor and its own editing card, and journey asserts a two-gap patch. Pointers: `gap-site`; `draft-site`; `proposal-row`; SURFACE K31; Q261, Q1306.
1313. **The founder's column before 🍾 has no B · I · `[]` strip** (raised by Ed 2026-09-11, founding his second document on docs.vote: *I am pasting the starting text, but I don't see a [] see markdown option in the top right of the box*; ruled 14:08 *same strip as after 🍾*). Before 🍾 the column is `#prose`, a different editor from the charter's lanes: a typed `# ` or `- ` becomes a block with the marker taken out (`relabel`, `textDivs`), inline marks stay raw characters, and no strip is drawn — the Q1294 (a) build read the column as *the characters already* and (b) followed it. Ruling: the same strip at the top right, `[]` flipping the founder's column between rendered and source, B and I wrapping the selection. Rejected: source only (what Ed met); pure source with `# `/`- ` no longer consumed. Pointers: `edit-mode`; `lane-controls`; SURFACE K22, K31.
1314. **The founder's pre-🍾 ✒️ does not submit a pasted starting text** (raised by Ed 2026-09-11 14:10, same document: *I also can't submit it by clicking ✒️*). The row's ✒️ is enabled by `proseDirty()` on every `input` (`syncProseRow`), the paste handler inserts through `execCommand('insertHTML')`, and the press calls `cs.confirmStartingText`, whose refusal is an unhandled rejection the page never shows. Ed, 14:06: *✒️ stayed dark and would not press* — the enable path, not a swallowed refusal; the Q1313 build reproduces a synthetic paste headless and checks whether the `input` listener that lights the row is still attached to the `#prose` in the DOM after the renders that follow a paste. Pointers: `proposal-row`; SURFACE K31; Q821, Q1080.
1315. **Before 🍾, a click outside 📝's card does not leave edit mode** (raised by Ed 2026-09-11 14:04, founding his second document: *as a founder before begin, clicking outside of 📝 in edit mode doesn't close it (clicking outside of cards should close them)*). SURFACE K31 lists two ways out of edit mode — 📝 again, or another card opening — and the outside-click rule that closes every other card (`closedByClick`) does not reach the text card, before or after 🍾. Leaving keeps the draft, so an outside click leaving would lose nothing. Ruled 14:07: **both eras** — the same structural outside test every other card uses, leaving through 📝's own path so the draft survives. Rejected: before 🍾 only (the text card behaving differently across 🍾); leave it. Pointers: `edit-mode`; `riding-tab`; SURFACE K31, C1–C5.
1316. **Your own row last in Members, the list above it** (raised by Ed 2026-09-11 14:05, founding his second document: *As a founder-member, why does the members list appear under my name in the Members section, instead of above?*; ruled 14:08 *your row last, the list above*). Built as *you are at the top* (Ed, 2026-08-21): the Members list opens with the reader's own row wearing ✋🖼️📧 and the *you* chip, everybody else beneath. Ruling: the membership listed first, the reader's own row and its tabs at the foot of *Members*. Rejected: keep (a row reading as a heading over thirty others); the reader's row in its arrival place in the list. The you-row's tabs share the paragraph's gutter with the 🪪 pile, and the 42px clearance and the `:has()` placeholder rules are written for the row at the head — the build re-measures both (`card-audit`, P2/P7). Pointers: `constitution-section`; SURFACE §8; CLAUDE.md Gotchas *Your own row clears the paragraph's own tab*.
1317. **A reason on a founder's first decision, not only on a change** (raised by Ed 2026-09-11 14:09, founding his second document: *Only the Lapse control gives me the opportunity to write a rationale. I think all settings should give this option after they are initially chosen*; closed 14:11). The founder's card draws *Why are you changing this?* only where the setting already holds a value (`isChange`, Q530: a first decision has no *from*); 💤 Lapse held one from the shape, so it read as a change. Offered: a reason lane on the first choice too (Q530's exemption gone, *Why this?*); as today; a shape's value not counting as a change. Ed: **reason only on a change, as today** — nothing changes. Pointers: `motion-controls`; `shape`; SURFACE K12, C11; Q530.
1318. **A delegated 🤝 cannot be taken back by choosing a value before 🍾** (raised by Ed 2026-09-11 14:15, founding his second document, screenshot *I should not be able to select middle choice (I think it should be greyed out)*; asked whether the values should grey, he answered *Actually I should be able to choose a value to take it back with ✒️, but I wasn't able to do that here*). The design stands (pre-🍾 picking a value is the take-back, `commitSetting` reclaiming and setting in one act; 🍾's hold sentence points there). The defect: the `[data-set]` radio handler writes the chosen value and clears the holder flag only for 👥 (`S.quorumBy = 'founder'`); on every setting whose value key and delegate key differ — 🤝 `policyBy`, 📍 `slugBy`, ✋ `titleBy`, 🌡️ `barBy`, ⏱️ `rateBy` — the flag stays `roster`, `wantsDelegate` stays true, and ✒️ delegates again instead of reclaiming. Where the two are one key (⏰ 👤 👁️ 🌍 🥾 💤) the value overwrites `roster` and the take-back works. Fix: a value press on the open card clears its delegate flag whatever the key; guard: `founding-walk --delegate=applications` extended to take it back. Pointers: `motion-controls`; `answer-bodies`; SURFACE §8, F6; Q771, Q826–Q830.
1319. **A constitutional motion's rail entry has no fill** (raised by Ed 2026-09-11 14:22, his bot room on document 1: *Constitutional setting proposals don't seem to have the wash progress bar*). `railEntry` in setup.js draws `--fill` as *how far the room has got on anything that is theirs* — `c.in / E` where `ctx.isRoom(c)` — and `isRoom` in session-view.html is true only for a **delegated question** (`delegated(c.key)`); a 🏛️ motion in flight on a settled setting (`motionOn(c)`) is the room's too, but reads `isRoom` false and takes the founder's `100%`, so the entry stands fully washed from the moment the motion is raised. `resolveCounts` already counts answers per setting off the module. Fix: a setting with an assembly motion in flight is the room's for the fill, `c.in` the motion's answers over `motionElectorate()`; an ordinary-route (raced) motion has no closeness on the page at all — the view filters `settingId` races out of `clauses` — and that stays a dev seam, noted. Pointers: `evidence-meter`; `motion`; `setup-queue`; `railEntry`; SURFACE §6, M-rules; Q1176.
1320. **A settled motion's ✔ record tops its setting's pile** (raised by Ed 2026-09-11 14:23, document 1, screenshot *setting itself with setting icon should sit at the top of setting tab stacks*: beside *New members may only join by invitation.* the pile shows a green ✔ on top and 🤝 beneath). Since Q942 each constitutional paragraph's pile files its settled motions **behind the rule**; setup.js's `stackOrder` ranks the host card first only where a `host` is passed, and the band's two `pileHtml` call sites (`data-para` paragraphs, `h.pile`) pass none, so the pile sorts by `RANK[stateOf]` alone and a `done` record outranks the setting's own tab. Fix: the paragraph's own setting is the pile's host, first in the closed pile and first in the open card's strip; the records file behind it in their own order. Pointers: `tab-stack`; `setup-band`; `card-morph`; SURFACE M11–M13, §8; Q942; guard `card-audit` P7.
1321. **💤's sentence says *After 0 days* for a spell under a day** (raised by Ed 2026-09-11 14:27, document 1, screenshot: *After 0 days, inactive members lapse and automatically abstain from votes.* on the 💤 composer's standing block; *it's not actually 0 days — you should intelligently show days or hours or minutes*). A motion set the spell below a day and the clause sentence rounds `afterMs` to whole days. Fix: the spell in the largest unit that divides it — days, else hours, else minutes — wherever the lapse value is worded (the clause map's lapse sentence, `meaningOf('lapse')`, the record, the warning mails), one helper, STYLE T39. Pointers: `lapse`; `meaningOf`; STYLE T39; SPEC §9.5a.
1322. **docs.vote crash-looped under a 31-bot room and quarantined document 1 at the next boot** (2026-09-11). Ed founded document 1 on docs.vote, invited 31 bots, began at about 14:00; `room-bots --min 5s --max 30s` ran from 13:57. From 14:21:12 the bots' log holds 502s at about sixty a minute interleaved with successes (a burst of fourteen queued actions landed at 14:20:59; a 🏛️ motion on 💤 had just been accepted); `/healthz` flapped 502/200 with the bots stopped at 14:25; at 14:29 it read `store pg, documents 2, documentsSkipped 0, errors total 0, uptime 160` and `/api/d/1/view` answered *no such document*. The server exits on any unhandled rejection or uncaught exception (main.ts), Render restarts it, and `loadAll` (store.ts) quarantines a document whose replay throws — loudly in the log, silently in `/healthz`, which counts only pre-people-shape skips. The rows are intact in Postgres; what is lost is the replay. Two findings and one question: (1) the Render log's *unhandled rejection — exiting* and *failed to load — quarantined* lines name the cause — Ed to paste them; (2) `/healthz` must count quarantined documents; (3) Ed: *we should be able to handle far more than 31 users acting every 30 seconds* — the local reproduction (`found-31.mjs` + `room-bots` + `monitor-8510.mjs`) measures view latency, RSS and CPU under the same load. Pointers: `server`; `replay`; `Persistence`; `engine-host`; `docs/OPERATING.md` §5; PRODUCTION.md.
1324. **The host saturates under a 31-member room: views 500–1000 ms, commands 2–13 s** (raised by Ed 2026-09-11 14:28: *we should be able to handle far more than 31 users acting every 30 seconds*). The Render log of 13:23–13:26 UTC, document 1 at about a hundred lines with thirty-one members and a dozen live races: every `GET /api/d/1/view` 500–1000 ms, every `POST /api/d/1/cmd` 2–13 s, bursts of queued commands landing together, then Render's proxy answering 502 and restarting the instance with SIGTERM. The server is one thread and the view is recomputed per request — `engine.races()` refits Bradley–Terry per race, `nextCards`, `askOn` per race per seat. Building: profile the view and command paths on a local thirty-one-bot room (`found-31.mjs`, `room-bots`, `monitor-8510.mjs`), memoise what does not change between engine sequence numbers, and state the operating envelope in PRODUCTION.md § *Measurements*. Pointers: `view`; `engine-core`; `ranking-model`; `router`; `soak-harness`; `alpha-preset`.
1326. **The host under two hundred bots: the knee at 115, and three levers** (raised by Ed 2026-09-11 16:20: *We should do a stress test with lots of active bots*; he chose 200 bots and a bot founder, the moon room `docs.vote/d/tranquillity-colony-mtx504ym`). Bots at 5–30 s, one invitation every 12 s: flat to seventy bots (view 65 ms, judge 60 ms), commands past Ed's 500 ms bar at ninety, at 115–120 healthz 8 s and a judgment 18 s, 502 from 17:23:53; the instance came back with every document intact (the Q1322 fix's first live test) and the drip stalled on its own retry. Ed: *what is the limiting factor … across all documents or isolated?* — one thread shared by every document; only write order is per document. Local profile at fifty bots and eighty races: `buildUsableComparisons` 23% of samples (every judgment walked per race per state version, and again uncached in every fold), the command path 30%; the live heap 60 MB under an RSS of 345 MB, an OOM on a 512 MB instance. Built (Ed 2026-09-11 17:50: *do any straightforward optimisations now*): edge judgments indexed by ground and by candidate as they land (7% and 16% after); `--max-old-space-size=384` in render.yaml; and — Ed's *do it now* — the slim view: a poll stating its document seq, text version (`tv`) and records' key (`rk`) is answered without the projection, the text and the sealed records it already holds, `slim` naming them and the page merging at the poll boundary. Not built: the fold's uncached `races()` reads (a version bump per mutation inside `apply`), and more than one process. Pointers: `view`; `engine-core`; `ranking-model`; PRODUCTION.md § *Measurements*; Q1324.
1325. **After 🍾, an unset founder-held setting hides every later section from everybody** (raised by Ed 2026-09-11 17:00 on the moon stress room, `docs.vote/d/tranquillity-colony-mtx504ym`: *I can't seem to see the doc settings below "Proposed for Removal"*). The bot founder never set 🥾, and `visible(c)` requires `orderReady(c)` — every card above it in `ORDER` settled — for every reader in every era, so with 🥾 unset every card from 💡 down (⏱️ ⏰ 🌡️ 👥 👤 👁️) is invisible, `secLive` drops the three sections that hold them, and a stranger and a member alike see a constitution that ends at *Proposed for removal*. The founding order is the founder's pacing rule before the start; after 🍾 it hides settled rules from the members they bind and contradicts Q510 (an undecided rule says who is deciding it). Recommended: `orderReady` gates only until `constituted()`; after the start every paragraph stands, the unset one reading as undecided. Pointers: `setup-queue`; `visible`; `orderReady`; SURFACE §8; Q510.
1327. **✋ and 🖼️ draw an uncommitted draft as the standing value on any render** (raised by Ed 2026-09-11 18:57 in the moon room, `docs.vote/d/moon`: *strange behaviour observed when choosing your name/picture and a new member joins*; asked which, Ed: *draft jumped to the head block, picker reset*). `S.myname` and `S.mypic` are defined onto the viewer's roster row (`session-view.html:1841`), and `nameBody` / `pictureBody` read the same row for the status-quo *keep* block (`setup.js:741`, `:843`), so the moment a name is typed or a face picked the row holds it, and the next full render — any movement in the room, every few seconds under the bots — draws the draft at the head as though it stood, empties the composer, drops the caret, and resets the picker's tone selector (picker state, never stored). In a quiet room no render lands between typing and the commit, which is why nothing caught it. Fix: the unsent value on its own row field (`nDraft` / `picDraft`) read only by the composer, the standing value feeding *keep*; the composer's text and the picker's selection preserved across a render, as the charter's draft already is (closing is not discarding). Pointers: `identity-cards`; `pictureBody`; `FACE_TONES`; `emoji-data.js`; SURFACE K26; Q645, Q1165. **Built 2026-09-11** on branch `q1327-q1331-page-a`: `S.mynameDraft` / `S.mypicDraft` on the row beside the standing pair, written by the input handler, the emoji pick and the upload; `nameBody` / `pictureBody` take a `draft` option (the applicant passes none and reads one value); `commitIdentity` moves draft to standing in either era, sends where there is a session, and puts the draft back on a refusal; `renderKeep` / `renderRestore` re-apply the caret and the grid's scroll after any render that leaves the same card open. The tone selector was already module state in setup.js and never reset — what Ed saw as *picker reset* was the grid rebuilt at scroll 0 with the picked glyph gone to the *keep* block. Guard: `npm run journey`'s *draft* step (the member seat types, the founder moves the room, the poll renders). SURFACE K26 amended; reasoning in `design/DECISIONS.md`.
1328. **No task is served until its main action can be taken: races withheld until ⚖️ is acknowledged** (raised by Ed 2026-09-11 19:00 in the moon room: *as a joining member, I see open proposals etc before I have accepted my ✏️ and 🏛️*). SURFACE C9 as written withholds *a question you may not answer* and *an offer you cannot take*: the charter's ingest filter (`withheld`, `session.js:5623`) drops only entries in the *needs* state, and a race being weighed by others stands in the rail and the margin as grey information until the ⚖️ OK turns it into a question. Put to Ed as keep / hide; **ruled 2026-09-11 19:20**: *I shouldn't be served a task until I can do its main action, so until I accept ⚖️ I shouldn't be given races.* So C9's test becomes *can you take the entry's main action* — a race entry of any state, the margin mark and the gutter tab with it, waits behind the ⚖️ OK; ✏️ likewise gates what it is the action of. To build, with SURFACE C9 and E8 amended and the journey's second seat asserting the rail before and after the OK. Pointers: `needs-you-queue`; `lifecycle mark`; `withheld`; `acked`; SURFACE C9, E4, E8; Q639. **Built 2026-09-11 (63b6019):** `withheld` (session.js) drops every entry a race made — the live race in both card shapes, the fixture's patch, the diagonal and the sealed record — while ⚖️ is unacknowledged; a park (E36), your own draft and the Founder's 👑 stay; measured on a ladder seat 8 → 5 tabs before the OK and 5 → 15 after it; C9 restated, E8 and E13 say what the charter holds before the OK; journey's *⚖️ waits* and *⚖️ OK*. Two findings for Ed: the arriving entries pop rather than grow, `setupRail()` scoping `birthPass` to the band by design and `layoutQueue` laying charter entries from measured heights; and `seat-matrix`'s E13 cell (*whoever the router serves*) still has no `AUDIENCE` predicate, as filed. Reasoning in `design/DECISIONS.md`.
1329. **Legacy value formats go: there are no old documents** (raised 2026-09-11 19:10, found by a probe seat in the moon room). The founding script (mine, last session) set 🤝 with `{ holder: 'members', apply: true }`; `validate` accepts `holder` and `joinPolicy` as legacy keys and `foldLegacy` (`packages/constitution/src/session.ts:1221`) reads `holder: 'members'` as laying down both powers on 🤝 **and on the ✉️ door** — so the room holds a setting that is members-held, founder-settled and collecting nothing, a state no page path can reach, and the page shows every member the blind `ans-applications` card with a 🏛️ commit that the module refuses as *not collecting*. Same in this afternoon's room. Put to Ed as refuse-at-the-command / leave; **ruled 2026-09-11 19:22**: *we are still in alpha — please get rid of old formats, there are no old documents.* So the legacy keys, `foldLegacy` and every other pre-entry-94 fold leave the module, `validate` refuses the keys, and the tests that pin the legacy readings go with them; a log carrying them is quarantined like a pre-schema-2 one. Pointers: `constitution`; `applications-card`; `replay`; `values.ts`; SPEC §9.7½; Q1318. **Built 2026-09-11** (branch `q1329-legacy`): `ApplicationsValue` is `{ apply: boolean }` and `validate` refuses any other key naming it; `foldLegacy` (🤝's `holder` and `joinPolicy`, 🥾's `rung`), `foldLegacyIds` (`membership` → `admission`) and the `signed-out` · `frozen` · `thawed` no-ops are deleted, `readValue` gating every value the fold reads — on replay as on a command — so a log carrying any of them throws and `DocStore.loadAll` quarantines it, counted under `documentsQuarantined`; `SCHEMA_VERSION` stays 2, because the shape this build writes did not change and a bump would skip every valid v2 document beside the two bad ones. The golden walk sent `holder: 'members'` itself and is re-frozen (one event's bytes, 🤝 now founder-held in the state); `room-walk`, `founding-evidence` and the page's `mayApply` sent or read the old keys and no longer do. **The two stress rooms on docs.vote (`/d/moon`, `/d/tranquillity-colony-mtx504ym`) were founded with the `holder` key and will be quarantined at the first boot after this deploys.** Reasoning: `design/DECISIONS.md`, *Q1329*.
1330. **A refused command prints an error on screen and lands in an error log** (raised 2026-09-11 19:15: Ed's 🏛️ press on the dead 🤝 card sent an `answer`, the module refused it, and the card stayed yellow with nothing said — SURFACE Y25's one-sentence refusal covers invitations and motions only, and `api.cmd`'s other callers drop the answer). Put to Ed as extend-Y25 / leave; **ruled 2026-09-11 19:24**: *we don't expect users to encounter refusals — refusals should print an error on screen with a debug message, and create some kind of error log we can debug.* So every refused command, whatever sent it, prints on the page with the module's own sentence and the command's name and arguments as the debug line; the server writes each refusal to an error log beside the request log (`errors.request` in `/healthz` already counts them, nothing keeps them), with the document, the seat, the command and the reason. Q1323's yellow 💤 entry is the same report from a 502'd host. Pointers: `api.cmd`; SURFACE Y25; `server`; `docs/OPERATING.md`; Q1323. **Built 2026-09-11 (5efa7fe):** every refusal passes through `refusalNoted` in `api.cmd` — the sentence under the card open when the command left (Y25's mechanism, on every card kind through the page's `cardHtml` wrapper; *That could not be set* for a set, *That was refused* otherwise) and always a stagehand's line at the foot of the window (`#errline`: the sentence, the command, its arguments cut at 200 characters, the document, the seat, the time; a click dismisses); a wire that answers with a status and no sentence, or not at all, is refused with the status for its reason. The host appends one line per refused command and failed request to `errors.jsonl` in the data dir under either store (`packages/server/src/error-log.ts`; seat as the member id, never the address; arguments capped at 2,000), served by `GET /api/dev/errors` under the DEV label and printed by `draft-tools errors <dataDir> [n]`; `docs/OPERATING.md` §11; Y25 generalised. Not done: no `refused` count on `/healthz`, and the wire's direct 401/404/409 answers outside the cmd route are not logged. Reasoning in `design/DECISIONS.md`.
1331. **The ordinary motion card takes the constitutional card's grammar** (raised by Ed 2026-09-11 18:59, screenshot `bugs/many things not to pattern.png`: ⏱️ under an ordinary motion — *One every 4 hours* as a bare head with *Prefer this*, the blue *A proposed change… reserved… 👑 question* explainer, an *As proposed* eyebrow over *a new proposal every 8 hours*, *0 of 38 have voted on it*). That is SURFACE §9's *ordinary motion* row exactly, which the file marks as the dev-seam shape; the constitutional row beside it was brought to the two-block grammar at the card review (Q1182) and this one was not. Put to Ed as all / all-but-the-explainer / the count only; **ruled 2026-09-11 19:26: all of it** — block one the standing rule in full clause text with *Keep this* (CP2), block two the proposed rule in the same sentence shape with the proposer's sealed rationale, no eyebrow, no explainer, no count (judgments are blind, so it always read zero); Indifferent as the textless block (CP4), ✓ and 🗑️ as they are. SURFACE's row rewritten to say so, card-audit opening the card. Pointers: `motion`; `option-block`; `meaningOf`; SURFACE §9 (ordinary motion), §9.3 CP2, CP4; Q1182, Q613. **Built 2026-09-11** on branch `q1327-q1331-page-a` (the commit after 1371a5d): one helper, `motionBlocks`, draws the consent card and the ordinary card alike — block one `decisionLine(c)` (the paragraph's own sentence) with *Keep this*, block two `sentenceFor(k, value)` with the proposer's `speakerHtml` and *Prefer this*, then the textless block — *Abstain* on a consent, *Indifferent* on a judgment — the values being the route's own (`no · yes · abstain` / `stands · proposed · either`, so `judgeRaceCard` and the dev seam read what they always did); `headFor` returns no head for a motion card on either route and `noTitleHead` covers 📍's; `motionBody`, the `unlocks` explainer, the *As proposed* `proposalHtml` and the count are deleted from setup.js with the `reserved` predicate that fed the explainer. SURFACE §9's row rewritten (no *dev seam* mark — the live card is the row); card-audit's settled and seat walks seed a running ordinary motion on ⏱️ (`m5`, beside the settled `m2`) and its CP2 vocabulary sanctions *Keep this / Kept / Abstain* (SURFACE CP2, T48), which had been reported on the consent card since Q1182. Found on the way and fixed on the same line: the stranger's tab on a setting under a motion in flight opened an empty card (👤 and 🌍 had read so since Q1183; the seeded ⏱️ made it three) — `headFor`'s motion branch is guarded by `atTheDoor()`, so the door shows the standing rule read-only as Q1183 says. Reasoning in `design/DECISIONS.md`. **One caller missed** (found 2026-09-12 by CI's `walks` job, red since the merge): the admit card at 🪪 *assembly* still called the retired `consentBlocks` and threw on open — the only card outside both probes that draws the consent blocks, and `applicants-walk --price=assembly` is the only walk that opens it. It calls `motionBlocks(…, 'constitutional')` now.
1332. **Under load a queued command is refused *timestamps must be non-decreasing*** (found 2026-09-11 in the second moon room, `docs.vote/d/moon`, between 19:20 and 19:25 with 140–160 bots acting: eight of the bots' judgments refused with that sentence, `room-bots`'s log). A command's `t` is stamped when the request is received; past the knee it waits seconds in its document's write chain, and the minute `tick()` — or any command received later and folded sooner on another chain the document shares — stamps a later *now* on the log first, so the queued command's time is in the past when it folds and the log refuses it (session.ts: *timestamps must be non-decreasing*). The `Pen` already carries the rule that a time never goes backwards for the ladder's writes. Two readings: stamp the command at the fold rather than at receipt (the log then records when the document changed, not when the member acted, which is the honest time anyway); or let the fold take `max(t, lastT)` for a command whose stamp fell behind, as `Pen`'s ceiling does. Recommended: stamp at the fold. **Ruled by Ed 2026-09-11 19:32: stamp at the fold** — the log records when the document changed, not when the member pressed. **Built 2026-09-11:** `foldTime(doc, nowMs)` in `packages/server/src/engine-host.ts` — the clock at the fold, never earlier than the last event of either log — read by `tOf`; the request routes take the fresh clock, the tick and the commit keep their own stated time; `packages/server/test/fold-time.test.ts`. Pointers: `server`; `engine-host`; `foldTime`; SPEC §11; PRODUCTION.md § *The second moon room*; Q1326.
1333. **A sealed record is pinned to the line index it had at adoption, not to its clause** (found by Ed 2026-09-11, about 20:00, in the moon room, `bugs/queue card that does not open decision card.png`: a ✔ *Food, today 19:17* whose tab sat beside the FOOD heading and whose click opened nothing). The page keys a record by `L<start>` from its footprint at the version it adopted (`itemsFromView`), and the document has moved since: r:c215 adopted the rota paragraph at line 41 of version 55, and line 41 of version 71 is *## Food*. Nine lines of the room carry two records each; the heading branch found the first and compared the pressed id against it, so the second opened nothing — that half is fixed on main (d9f397f, the open one is looked up by id, as Q1298 did for paragraphs). The misplacement is open: a record should follow the lines that descend from the text it adopted — the server translating each record's footprint from `version + 1` to the current version by line alignment (`documentAt` holds every version) and serving the current span, the record standing on the replacement where its lines were changed again and on the gap where they were deleted. Pointers: `sealed record`; `blocksOf`; `itemsFromView`; `patch-engine`; `record-builder`; SURFACE §6. *Built 2026-09-11 (branch `q1333-records`): the host walks the log's version steps rather than aligning versions — `packages/server/src/record-spans.ts`, memoised per state as `host:recordSpans` — and serves `at` on every record; the page keys the entry, tab and card by it, a deleted clause's record taking the gap's own site (`G<n>`) while unread; SURFACE M1; DECISIONS 2026-09-11.*
1334. **A held-open gap is the height of a tab** (ruled by Ed 2026-09-11, about 20:05: *gaps for proposed insertions should be the same vertical height as a tab*). The `insert-anchor` a gap site holds open, and the gap a live insertion race stands in, take the `clause-tab`'s 30px so the margin reads as one alphabet. To build; card-audit measures it. Pointers: `gap-site`; `insert-anchor`; `clause-tab`; SURFACE K31, M12. **Built 2026-09-11** on branch `q1334-q1336-page-c`: measured first — the fixture's `insert-quiet` anchor and every held-open gap on a live room of the moon's shape stood 51.19px tall (`min-height: 3.2rem` plus 6px of padding each side, content-box), the 30px tab 2.39px in from the top (`.chipcol`'s `0.15em`, a clause's line-box nudge); the anchor is `box-sizing: border-box; min-height: 30px` with no vertical padding now, and its `.chipcol` sits at `top: 0`, so the wash and the mark are one 30px object — on a draft's gap site and a live insertion's anchor alike, the two being one element. Measured after: 30 × 616 on both pages, the tab at 0, and P2's horizontal travel on `insert-quiet` still 0. Guard: card-audit's **P9** (`closedGeo` reads the anchor's height and the tab's offset in it before the card opens; red above half a pixel from 30 and 0). SURFACE K31 states the height, M19 points at it; session-probe re-frozen (`qa:freeze`) for the 21px every block below a gap rises. Reasoning in `design/DECISIONS.md`.
1335. **After 🍾, a floating 📝 opens edit mode from the bottom right** (ruled by Ed 2026-09-11, about 20:10: *after begin, there should be an additional way to open 📝 edit mode on a floating action button in the bottom right, in the same place and size as the floating ✏️ appears in edit mode*). The riding tab stays; the button is a second door, drawn only once the document has begun, in the proposal row's ✏️'s own place and size, and it becomes that ✏️ when edit mode opens. To build. Pointers: `edit-mode`; `riding-tab`; `proposal-row`; SURFACE K13, K31, §9's 📝 row. **Built 2026-09-11** on branch `q1334-q1336-page-c`: the proposal-row's ✏️ was measured first — a 52×40 `.btn.glyphbtn` at the window's foot, its right edge a gutter inside the column's, the same box at scroll 0 and at the scroll's end — and the door is drawn as **the same row** rather than a control placed at a number: `#editdoor`, a `display: contents` mount at the foot of `.doc` after a `#runway` block, holding a `.race-mid.commitrow.proposalrow` whose one button wears 📝 and the riding tab's tooltip (*Text — press to write*), so the containing width, the row's padding and the button's class are the row's own and the two boxes coincide by construction (measured equal to the hundredth). In read mode after 🍾 the runway moves from `.doc::after` to `#runway` so the door rides to the page's end exactly as the row does in edit mode; nothing else moves. Drawn by `renderEditDoor` (session-view.html) beside `renderRideTab` under the riding tab's own gate (`canEditText()`: a member who may propose, not the stranger, the applicant or a closed document — C9) and only in read mode, so entering edit mode replaces it with the row in one render; its click is `toggleEditMode`. Guards: card-audit's **D1** on the charter walk — the door's box before entering, the row's ✏️'s after, the door's again after leaving, equal within half a pixel, the glyph at B6's 21.6px; journey's *door* step on the live path, which found the row caged: drawn inside `#charter`, a sticky row cannot rise above the charter's top, so on a founder's page with the band open (the charter 1,843px down) the door was at the foot and the ✏️ appeared 580px below the window — pre-existing, first visible beside a control that promised to become it; `#charter` is `display: contents` now, as `#proserow` and `#editdoor` are, and the three boxes agree at [1053.88, 1052, 52, 40] on that page. Both probes read identical (the session-probe measures cards, rail, anchors and chips, none of which the row is); the references re-frozen to the bytes all the same. Reasoning in `design/DECISIONS.md`.
1336. **A blank column stands above the editing card in edit mode** (found by Ed 2026-09-11, about 20:10, `bugs/blank gap appears when I am making a text edit proposal.png`: on the moon room, editing the Founding clause, the lifted column above the card is empty for some 1,400px, the strip's B · I · [] at its top right, the clauses above the card gone). To reproduce on a document of the moon room's size and fix. Pointers: `edit-mode`; `editing-card`; `draft-site`; `lane-controls`; SURFACE K13–K16. **Built 2026-09-11** on branch `q1334-q1336-page-c`: reproduced on a dev-server room of the moon's shape (the moon charter, 40 bots, 73 open races and 50 records after four minutes of `room-bots`) with the caret in the 36th block — the editing card stood 1,116px under the last clause above it, all 35 blocks above present and with height. **The cause, measured:** `.doc.editing.begun #charter > .prose` carries `padding-bottom: 95vh` — the runway that keeps the card's foot off the page — and an open card splits the column into a `.prose` before it and one after, so the segment *before* the card carried a viewport of runway too (1,045px at a 1,100px window); the clauses were a screen up, and the sticky strip and riding tab at the top of the blank made it read as the column. Not a section fold, not the swallow logic, not the records: the same blank stands on any document with a block above the site, the fixture included, and every walk had typed into the first clause. One rule in `system.css`: the segment another `.prose` follows takes no runway. Guard: journey's *column* step, which types into the last clause, counts the blocks above the card against the document, asserts each has height, the card within 80px of the last, only the final segment padded, and drops the draft through the row's 🗑️. Reasoning in `design/DECISIONS.md`.
1337. **The quorum floor counts movers on the race, not judges of the winner** (raised by Ed 2026-09-11, about 20:05: *the setting is currently that 50% of the membership must vote before something can pass, but I see accepted proposals that have 16/168 votes, is this really correct*). Reproduced locally in the same evening: a room of fifteen, quorum 60% so a floor of nine, and forty-four adoptions in two minutes with as few as **one** judgment on the winner — the engine log shows candidate c2 adopted at p 0.88 with one comparison touching it. Both are the mechanism as written: SPEC §4.2 carries a challenger when its win probability clears the bar and *≥ F distinct participants have moved on the race*, and a race holding many rivals on one clause reaches F while its leader has been judged by almost nobody; the leader's confidence comes from its own few comparisons under the fit's prior (Q840: one member tops out at 79%). Two defects beside the rule: the record's *judges* line undercounts (`judgedBy` is keyed by the field's candidate ids, so the judges of withdrawn rivals drop out — 3 shown where the race held dozens), and the moon room's card said 50% where the setting is 60. Put to Ed as keep the race-wide floor / count the floor on the winner / both; **ruled by Ed 2026-09-11 20:22: count judges of the winner** — F distinct members must have judged the winning candidate, which is what *must vote before something can pass* says; the record's count becomes the same number; SPEC §4.2 amends with Ed's sign-off (this ruling), version bumped, the reason filed as an R-entry. *Built* 2026-09-11 (8362130): `RaceView.leaderJudges` — distinct participants with a usable comparison touching the leader, its author's derived preference one of them and a rival's author none — is what `clearsBarAndFloor` tests against F, what the meter's floor half reads and what the live card and an adopted record serve as `judges`; `leaderMeasured` is R-063's room-has-spoken clause drawn at the winner; SPEC v0.116 (§3.3, §4.2, §8.2, Appendix A), R-102; the surface half in DECISIONS. Pointers: `adoption-threshold`; `ranking-model`; `sealed record`; SPEC §4.2, §8.2; Q840; PRODUCTION.md § *The second moon room*.
1338. **Races arriving after ⚖️'s OK pop onto the rail rather than arriving** (raised 2026-09-11 in the Q1328 build). `setupRail` scopes `birthPass` — *what is born arrives, it does not appear* (SURFACE F14) — to the constitution band, and the charter's rail entries are outside it. Before Q1328 the gap never showed, a race being served the moment it existed; now a dozen can land in one render at the ⚖️ OK, and they pop. Put to Ed as extend / leave; **ruled 2026-09-11 22:25: extend the birth-pass to the charter rail.** Pointers: `birth-pass`; `needs-you-queue`; SURFACE F14; Q1328. **Built 2026-09-11** on branch `q1338-q1339`: `setupRail` is deleted and the call site hands `birthPass` the rail itself (`design/session-view.html`), so a charter entry is keyed by its item id exactly as a setup task is by its page key and grows into place over the same 240 ms. Q1328's stacking worry was measured and does not hold — `layoutQueue` positions the pile from the newborn's full height before the pass shrinks it, and every `.qitem` is absolutely positioned, so on the session fixture a newborn stood at 36.22px of 40 and opacity 0.39 at 120 ms with every other entry's top unchanged to the hundredth. Guards: both probes IDENTICAL, `npm run journey`'s *⚖️ OK* step (the arrival this is about) green, card-audit and spec-check unchanged. Reasoning in `design/DECISIONS.md`.
1339. **The closed fixture holds no undecided race** (raised 2026-09-11 by `card-audit`'s closed walk: *no backlog (U:) records on the page — nothing measured for the backlog*, pre-existing). The closed page ends bracketed — constitution · charter · the backlog as ranked document text · the signatures — and the fixture `?fixture=session&closed=1` carries nothing undecided, so the backlog section has never been measured. **Ruled by Ed 2026-09-11 22:27: add an undecided race to the closed fixture.** Pointers: `closedBlocks`; `card-audit`; `design/fixture-session.js`; SURFACE §9's 🥂 row. **Built 2026-09-11** on branch `q1338-q1339`, and **the premise was the instrument's, not the fixture's**: `closeFixture` has always taken the first two open races (`race-purse`, `race-quorum` — two wordings and a rationale each, six judges, undecided at 0.58 against a bar of 0.74) into `FIX.CLOSED`, the closed page draws both as backlog paragraphs, and the charter walk *already measured* them — `rec:fx-u0` and `rec:fx-u1` stand at the end of every closed payload. What lied was the audit's own extra block: it read the paragraphs' marks and filtered them for `U:`, which is the **document block** key, where a mark carries the **record** id `rec:<raceId>`, so it matched nothing and announced *no backlog records on the page* on every run. No fixture change was needed and none was made; the block now asks the closed page for the marks its `U:` paragraphs carry and asserts the walk measured every one — red as *the backlog is drawn but unmeasured* if it ever stops (verified by blinding the measured set, which named both records). Guard: `npm run card-audit`'s closed walk, now error-free at both window sizes. Reasoning in `design/DECISIONS.md`.
1340. **E13's audience cell cannot be asserted** (raised 2026-09-11 from the seat matrix's standing no-rule exit on the member hat; Q1244's convention: a cell with no rule is a question for Ed). SURFACE §2 E13 — *a text race wants a judgment* — reads *whoever the router serves*, written when the page filed what the feed dealt; since Q1202 the entry shows while anything on the race can still be asked of the member, dealt or by `ask`, which is a seat-side fact, and the cell is older than the rule in its own Close column. Put to Ed in plain terms (*when somebody proposes a change to a clause, who should see the judge-this entry?*); **ruled 2026-09-11 22:34: every member who could still judge it, except the author** (the room of one, Q835, the standing exception). The cell rewritten to that sentence, an `AUDIENCE` predicate (member, not the actor, `ask` or a dealt pair in their view) added, `judge-text`'s row keyed. **Built 2026-09-11:** the predicate reads the seat's own snapshot, which now rides fifth into every audience predicate — *could still judge it* is the module's `askable` on the race's clause row in that seat's view, so a member who has spent every pair on the race is outside the audience from then on (their entry ⏳); the author is the seat of the step the event names as `author` (`propose-text`), not the judging step's own; the key is the race id the early seat judged, learned by the step and resolved once after it runs (a `key` function on the event), so a label, the payload and the assertion agree. Run on the member hat against a fresh dev server: E13 asserted with no finding; the run's other lines — four E4 gate findings, one E10, the E11 no-rule and the stranger's snapshot fault — are the baseline's, the pre-change script reproducing them on the same server. Reasoning in `design/DECISIONS.md`. Pointers: `seat-matrix.mjs`; `pair-deck`; SURFACE §2 E13; Q1202, Q835.
1344. **Setting motions are served before the member has accepted ⚖️ or 🏛️** (raised by Ed 2026-09-11 23:44 on `docs.vote/d/moon2`, `bugs/seeing proposals before I've accepted any powers.png`: a member's rail holding the unacknowledged 💡 ⚖️ 🏛️ grant cards and, beneath them, four *Under motion* entries, the ⏰ one open with *Prefer this* and a live ✓). Q1328 (Ed, 19:20 the same day: *I shouldn't be served a task until I can do its main action*) was built for what a text race makes — `withheld` in session.js — and never reached the band: `liveMotionRec` gated on `motionsLive()`, which is `canJudge()`, the document's permission, not `mayJudge()`, the member's. A finding against a ruling already made, so fixed rather than asked: `motionWaitsOnMe` withholds a *running* motion from a member who lacks the grant its main action needs — ⚖️ (`mayJudge`) for an ordinary motion, 🏛️ (`mayVoice`) for a constitutional one — so its card, its *Under motion* entry and its tab wait together; a motion awaiting the crown is the Founder's own question and is not withheld, and a non-member's view is unchanged. SURFACE E10 and E11's channel cells say so beside C9. Guard: journey's ⚖️ step now has the founder move ⏱️ before the guest's OK and asserts the entry absent before and present after; and since 2026-09-12 `applicants-walk` asserts the same shape on the 🪪 *assembly* admit motion — absent from the founder's rail before the 🏛️ OK, present after — pressing that OK at every price before it reads the rail. Pointers: `motion`; `liveMotionRec`; `acked`; SURFACE C9, E10, E11; Q1328.

1345. **A deploy splits a live document across two instances, and the losing one never recovers** (raised 2026-09-12 12:20 after Ed's demo on `docs.vote/d/notanotherpizza-agenda`: *there were errors and the invited account wasn't able to log in, and there was an error submitting the starting text*). Three pushes between 11:27 and 11:34 were three deploys. Render boots the new instance beside the old and moves traffic over; a browser's keep-alive connection stays pinned to the old instance while a fresh connection (the invited member's login) reaches the new one — so for some minutes two processes served one document with two memories, each appending to the same Postgres log. `appendChain`'s advisory lock serialises them and the primary key on (document, seq) rejects the second writer (23505; first at 11:41:17, the minute `verify-deploy`'s fetch of the host failed). By design the loser's request is refused, but nothing reloads it: its `persisted` cursor is behind the database for ever, every later write on that instance fails — the tick 32 times in 32 minutes — and the acts it took in memory (🍾 at 11:45:56, the text) were never saved. `/healthz` counted it (`errors.request` 23, `errors.tick` 32, `last.kind` 23505) and nothing else did. **Proposed:** (a) on a 23505 the store treats the document as *foreign-written* — reloads it from the database (a per-document `reload(id)` beside `loadAll`), replaces the in-memory session, marks the request refused with *the document moved under you, try again* rather than 500 — the loser recovers at the cost of the one command; (b) `verify-deploy` refuses to pass while `/healthz` reports two builds answering, i.e. polls until the old instance is gone, so CI's own check does not race the overlap; (c) the operating rule, written into OPERATING §3: **no deploy while a room is live** — a deploy is a restart and a restart is a split for as long as the overlap lasts; today's were mine, during the demo, and the cost was not stated when Ed said *push when built*. Salvage of this document: a restart reloads it from the database at the old instance's last write; what the new instance took in memory is gone. **Ruled 2026-09-12 (Ed):** *I think what we should do is explicitly pause documents while a deploy is happening — have a modal pop up and say "this document is paused while we do some quick database maintenance" with a progress bar that guesses how long the downtime will be.* **Built the same afternoon:** `POST /api/admin/pause` and `/resume` under `DRAFT_BOT_KEY` (404 unset, 401 wrong); paused, `commit` persists nothing, the cmd route answers 503 with the pause, the tick returns, and every view answer — the short one and the door included — carries `paused: {at, expectedMs, elapsedMs}`; the page (`noteHost`, `renderHost`) draws the modal with the bar filling over `expectedMs` (six minutes, measured today) and holding at 96%, a paused command draws no refusal, and when a new `x-build` answers after a pause the page reloads itself; a pause lifts itself after fifteen minutes. CI pauses before it fires the hook, bearing the `DRAFT_BOT_KEY` repository secret — **which Ed must add**; without it the deploy runs unpaused and says so. Proposal (a), the losing instance reloading itself, is not built: with the pause there is no losing instance. (b), `verify-deploy` waiting for the overlap to end, is not built either — the pause covers the room; CI's fetch failing mid-overlap stays a red verify, which is the truth. (c) is in OPERATING §3. Guards: `packages/server/test/pause.test.ts`; `verify-deploy`'s *the pause is closed to a stranger*. Pointers: `announced-pause`; `Persistence`; `replay`; `server`; OPERATING §3, §11; Q1322 (the same cursor, the other race).

1346. **A document that cannot save must say so** (Ed, 2026-09-12: *is there a way we can mark broken documents so that people won't mistake them for documents that aren't broken?*). Today a document whose persist fails serves its view as if nothing were wrong: reads succeed from memory, every write is refused with *something went wrong*, and the member concludes the product is flaky. **Proposed:** the store marks a document `stalled` at the first persist failure that is not transient (a 23505, a chain break), `view()` carries `stalled: true` with the time, the page draws one sentence in the alpha flag's place on every seat — *This document cannot save changes at the moment; nothing you do here will be kept* — and disables the commit rows; `/healthz` lists `documentsStalled`; a successful persist clears it. The stranger's door says the same. **Ruled 2026-09-12 (Ed):** *let's do this in the cheapest way, just a red flag on the page.* **Built:** `LoadedDoc.stalled` is set in `commit` on a 23505 and cleared by a save that lands; `stalled: true` rides every view answer including the short one and the door; `documentsStalled` in `/healthz`; the page draws `.stalledflag` beside the alpha flag — *This document cannot save changes at the moment. Nothing you do here will be kept.* — in the surface's one red, and removes it when the flag clears. The commit rows are **not** darkened (the cheap version). Guard: `pause.test.ts`'s red-flag case. Pointers: `stalled`; `view`; `alpha-flag`; `stranger’s door`; SURFACE §2; Q1345.

1347. **A surface-only change should not restart the host** (Ed, 2026-09-12: *I would have thought that only deploys to the engine would break a document, not just changes to the surface (which presumably a mobile design would be?)*). It is not the change that breaks a document but the restart: the one process serves `design/` off its own disk and holds every document in memory, so a CSS edit ships the same way as an engine edit — a new instance, an overlap, a split (Q1345). **Proposed, in order of cost:** (a) serve `design/` from a place a deploy does not replace — a Render static site or a bucket in front of the same paths — so a surface change is a file copy and the host is not touched; the probes already prove the page has no build step; (b) failing that, a `surface` deploy lane in CI that copies `design/` onto the running instance without a restart (Render offers no such thing on the starter plan; a small `POST /api/admin/reload-surface` behind a key would); (c) at minimum, Q1345 (a) so that a restart costs one command and not the document. **Costed 2026-09-12** at Ed's *sounds good, what does this cost us?*: **(a) a static host** — a Render static site is free and a bucket is pennies, but the page must keep the host's origin for its cookies and its relative `/api/` calls, so either the static site becomes docs.vote and the API moves to api.docs.vote with CORS and `SameSite=None` cookies (a day or two, and a real change to the security posture the stage-3 review settled), or a CDN sits in front of docs.vote caching `design/*` only — which does not stop the restart, and so does not answer the question. **(b) a surface reload on the running host** — half a day and nothing to pay: `designDir` becomes a writable copy the host serves from, `POST /api/admin/surface` under `DRAFT_BOT_KEY` takes the `design/` tree of a named commit (fetched from GitHub by the host, or uploaded by CI) and swaps it in, and CI grows a lane that, when a push touches `design/**` alone, calls the reload instead of the hook. The probes already prove the page has no build step, which is what makes the swap safe. The cost that is not money: a page and a server can be out of step for one push, so a commit that changes both must go the hook's way, and the lane's test for *surface only* is a path filter, which is a rule that can be got wrong. **Recommended: (b)**, with (a) as a PRODUCTION.md stage if docs.vote ever needs a CDN anyway. **Ruled 2026-09-12 (Ed):** *5 seems good* — (b). **Built the same afternoon:** `packages/server/src/surface.ts` reads a gzipped ustar tar of `design/<file>` entries (a 512-byte-block reader, no dependency; only names matching `SURFACE_NAME`, the page files at the top of the tree, and `session-view.html` must be among them) and writes them beside the data; `POST /api/admin/surface?sha=` under `DRAFT_BOT_KEY` (404 unset, 401 wrong, 400 on a bad sha or archive, 413 past 8 MB) installs the set and moves `designDir` and the `x-build` sha together, so every open page reloads itself and `/healthz` shows `surface`. CI's deploy step asks the compare API what the push touched and takes one of three lanes: documents only — nothing deployed; `design/` only — the served files packed with `tar --format=ustar` and uploaded, `x-build` checked, `verify-deploy` run, and the full deploy as the fallback if the host will not take it; anything else — the paused full deploy. Guards: `surface.test.ts` (the reader against the writer, the refusals, the upload served at every asset route with the new sha in `x-build` and `/healthz`); `verify-deploy` asks the route as a stranger. Not done: a surface that is served across a *restart* (the directory is on the ephemeral disk and the artifact's own `design/` serves after a deploy, which is the same bytes by then). Pointers: `surface-reload`; `server`; `dev-host`; `announced-pause`; OPERATING §3; PRODUCTION.md.
1312. **Rival wordings a member never sees** (raised 2026-09-10 by Ed's bot room — *I didn't see any races at all*, confirmed as *only one-candidate cards*; closed by Ed 2026-09-10 16:05, *keep the deal, say nothing*). Reproduced on a twelve-bot room at default heat: the engine forms rival races within a minute (51 of 64 proposals on contested clauses, the biggest race holding 7 wordings, 5 rival races open at once), and the founder's rail deals one wording against the clause at each press, because SPEC §8.3's rival gate (R-062) serves incumbent pairs first and a rival pair only once a member's incumbent pairs on the race are spent or a challenger has opened the gate. Not a defect: a challenger-against-clause judgment is the displacement evidence adoption needs. Offered and declined: a line on the quick card naming how many wordings race on its clause; dealing rival pairs sooner; listing the field on the card. Pointers: `pair-deck`; `quick card`; SPEC §8.3; SPEC-REASONING R-062.

925. **Three more things git knows about overlapping patches, parked** (raised 2026-08-27 with 924; **Ed: park for now** — each wants design work on how it is communicated before it is worth a mechanism). **(a) Dependent candidates.** `propose-edit` (K24) starts a draft from a candidate's wording, so C can be built on unadopted A; if A loses, C's rebase onto the incumbent fails over a decision made elsewhere. *Presupposes* is a different relation from *composes* (§2.2's lattice), and git's stacked patches say: declare it at submission, adopt in order, and let C fall with A or return to its author with A's fate stated. Verify what the engine does today first. **(b) A candidate's version chain.** §2.4's blanket *evidence resets* does not distinguish a clean rebase (no words changed) from an author's revision; Mercurial's obsolescence markers give a change an identity across rewrites, each recording what it superseded and why, so the rule can be *rebase keeps, revision resets*. With it, git's `rerere`: an author's confirmation against new text is remembered and reused when the same span moves again, instead of the leader failing identically every batch. **(c) Two cheap ones**, no spec change: a base-independent content hash of the normalised hunk (`patch-id`) before the dedup gate asks the LLM, and an interdiff — what moved under you, not the whole new paragraph — on the confirm-against-new-text card.

### Queue ordering — what survives Ed's 104–108

Ed replaced ranked ordering with a **margin index** (2026-08-16): entries stand beside their own clauses in document order, sealed items are dots, a patch appears once per site. Urgency was carried by colour strength and card size; since 2026-08-16 the card sizes are equal and urgency instead decides **which** entries fit on the screen, so the number now gates visibility rather than compression — which makes 98 below considerably more load-bearing than it was. That closed 99 (document position doesn't move when others judge), 101 (sealed stay, as dots — the rail is a map, and a map shows settled ground) and 103 (there are no serving *slots* left to explain; everything is shown at once). Three remain, all about the urgency number itself rather than about order.
109. **Scale: the margin assumes a short rail.** Sixteen entries beside a twenty-page charter is a map you can scan. Two hundred would be a filing system, and document order would stop steering anyone toward decision margins at all — which is what §8.1 exists to do. V1 tunes to 5–10 members, so this is not urgent, but it is the assumption the whole design now rests on and it should be named before anyone builds for a bigger room.

### Marks for the other asks (raised 2026-08-16, Ed's question; awaiting his pick)

The deadlocked case showed that these marks say *what you are being asked to do*, not where in a lifecycle something sits. Settled 2026-08-16 (166–167, 173): a deadlocked race is ❌ *stuck*, in the open yellow rather than grey, and 💡 marks the propose actions it leads to — the rail states, the buttons act. Still open:
170. **Your candidate needs rebasing** — engine-core carries `rebase-pending` and `displaced` (Q53). Distinct from 🔄, which is your *judgment* stranded; this is your *draft* stranded, and only you can move it.

282. **A rendered document and a source offset do not agree** (raised 2026-08-17, from 279's build; Ed asked for a recommendation the same day). The charter fixture is plain text, so the composer can take a caret offset straight from the DOM and use it against the clause's source. The moment the *document* renders markdown that stops being true: a DOM selection measures the rendered text, and `**bold**` is four characters there and eight in the source, so every offset the composer computes is wrong by two for each mark above the caret.

**Recommendation: render markdown in the document, and make the rendered→source mapping a property of the block rather than of the composer.** One function that takes a block element and a DOM position and returns a *source* offset, used by everything that asks the question — `offsetIn`, `startDraftFromTyping`, `startDraftFromRun`. The arithmetic already exists (`richToSource` / `sourceToRich`, written for the lane's view toggle); what matters is that exactly one place knows about the distinction, because the failure mode here is a two-character drift that nothing complains about and no test catches unless the test happens to put a mark above the caret.

Two other options, for the record. Keep the charter plain and let markdown live only inside candidates — cheapest, but an adopted candidate would then print its asterisks into the charter, so it only defers the problem to the first adoption. Or store the document rendered and treat markdown purely as import/export — which gives up "the characters you see are what is stored", and that was the whole reason 279 chose source-of-truth markdown in the first place.

**Do it before the charter carries any formatting**, not after. Until then every test passes and the bug does not exist; the day somebody bolds a word near the top of a long clause it exists everywhere at once, and it will look like a caret bug rather than an offset bug.

**Deferred past the alpha, deliberately** (Ed, 2026-08-27, answering this from the alpha-readiness plan's Track 3.3 — plan-queue **77**, backlog entry 77; the plan had said *fix before the day*). Option **(b)**: the charter stays plain text for the alpha and the rendering is done afterwards. Nothing changes before the day.

Three facts the deferral rests on, checked in the tree at `0d160ad` rather than assumed. **The charter renders plain text today** — `design/session.js:2994` and `:3009` emit `esc(line.x)`, so a clause carrying `**bold**` prints its asterisks — which means the bug is not merely unobserved but *unreachable*: `offsetIn` (`design/session.js:1875`) measures rendered text, and for a plain-text document that **is** the source. **The recommendation above is not a bug fix**, it is *render markdown in the document*, a visible change to what every reader of every charter sees; the arithmetic it needs already exists as `richToSource` / `sourceToRich` (`design/cards.js:342`, `:352`), but the change also moves `design/tools/card-copy.golden.json`, both frozen probes, `scripts/journey-walk.mjs`'s caret and propose steps, and what `design/STYLE.md` takes a clause to be. And **the alpha is short**: reaching the bug needs somebody to bold a word *and* that proposal to win, where the measured preset produces about one adoption per fifteen-minute session (`PRODUCTION.md`'s running log, 2026-08-27).

What the deferral costs, stated so nobody rediscovers it as a surprise: **if an alpha room does adopt a clause carrying a mark, that document's offsets are quietly wrong from then on** — in a throwaway document, which is the whole of why this is affordable. The two other options are on the record above and neither is rejected; (c), building only the block-to-source mapper without changing what renders, remains the cheap half of (a) if the rendering is wanted in a hurry.

288. **A flatter glyph set for the lifecycle marks**

    **Where it stands (docs pass, 2026-09-07):** partly overtaken — the lifecycle family is drawn rather than emoji (`DRAWN` in `design/session.js`; SURFACE §6), which is option (a) for those marks; the subject glyphs are still emoji, so the pack question survives only for them.

    (Ed, 2026-08-17, alongside making the gutter marks read as buttons: "I think changing to a flatter emoji pack and making them a bit larger would help"). The larger half is built, along with a resting ground and a press. The pack is a real question, because the design series is **single self-contained files with no network**, so there is no CDN to point at. Three ways: (a) inline the glyphs as hand-authored flat SVGs — total control, they would take the lifecycle hue as a fill, and it is the only option that actually looks designed rather than borrowed; it costs drawing nine symbols and they then have to work at 16px. (b) `font-variant-emoji: text`, which asks the OS for the *text* presentation of the same characters — one line of CSS, genuinely flatter, and it hands the glyph over to `color`; but the shapes are then whatever the platform has, and it would take colour away from the emoji that currently carry their own, which the contents rail depends on (260). (c) Embed an existing flat pack (Twemoji is CC-BY) as data URIs — flat and consistent, but it is somebody else's drawing style and a licence line in a mockup. Recommendation: (a), and only once the alphabet has stopped moving, since it makes every future change to a mark a drawing job.

What it needs a ruling on is the evidence. A revision is a **new candidate**: any comparisons already cast against the old wording were cast against a text that no longer exists, so they are discarded exactly as a ground shift discards yours (§4.4). That is correct and it is also a real cost — revise a proposal that six people have already judged and you have thrown their work away, and they will be served the pair again. Three ways. (a) **Always allowed, evidence always resets** — simple, honest, and quietly expensive on a busy race. (b) **A grace window**: free revision until the first judgment lands on it, and after that it is withdraw-and-repropose with the cost visible. This matches what the request is actually about — *immediately*, because they saw a mistake — and it makes the common case free and the expensive case explicit. (c) Revision only as **surgery on the wording**, with evidence carried over where the change is below some edit-distance floor; cheapest for the room and the hardest to explain, since it makes the ranking depend on a diff threshold. Recommendation: **(b)**, with the card saying which side of the window it is on.

311. **Is a locked judgment card without a ground shift reachable?** (raised 2026-08-17, housekeeping pass). `reviseNote` has a branch for `s.locked` that says *this one is settled, so your judgment is on the record as it stands (SPEC §4.4)*, and `commitRowHtml` drops the ✓ for it. No fixture reaches it: the only locked card in the mockup is also ground-shifted, and `shifted` is tested first. So a whole card state exists in the code and has never been drawn. The doubt is whether it can exist at all — a race that settles becomes a **sealed record**, which is a different card, so it is not obvious what leaves a *judgment* card locked. Either it is a real intermediate state (settled but not yet sealed, or sealed while you had it open) and wants a fixture so it can be looked at, or it is not and the branch and the `locked` flag should go with it.

314. **Seeing which card other members have open** (Ed, 2026-08-17, and **shelved by him in the same breath** — *let's shelve this for now until we actually test with a group of real humans, otherwise this is quite speculative*). The idea: the surface shows who is judging what, right now. Recorded rather than acted on, with the two things that make it worth returning to and the one that makes it hard. It is the natural extension of the `room-pulse`, which already says *the room is moving* and deliberately says nothing else; presence is the next rung and the first one with content in it. And it is the only proposal so far that would tell you a live *question* is being worked on rather than that the document is. What makes it genuinely hard is that it collides with SPEC §3.5: knowing that eleven people are looking at one race is a fact about where the attention is, which is close to a standing, and knowing *who* is on a card is a step towards knowing how they judged it in a room of fourteen. Ed's instinct to wait for real humans is the right test — this is exactly the kind of thing that reads as either warm or surveilling depending on the room, and no amount of reasoning here settles which.

346. **Sybil resistance at the front door** (Ed, 2026-08-18, parked: *if people can become members via a link, we have to be wary of sybil attacks — perhaps even accidental*). *Note, 2026-08-26 (entry 94): the open door is now 🤝 *yes* with 🪪 at ✒️ — an application admitted on submit — which is exactly where this bites; the same price at 🏛️ or ✏️ puts a room between the link and the membership.* Membership begins at first arrival (§9.6a) and the magic link **is** the credential, so the attack surface is the invitation itself. Ed’s own example is the subtlest and the most likely: **one person accidentally becoming two members on two devices** — possible only if membership is minted per *arrival* rather than per *invitation*, so the structural rule is that however many devices open a link, they land in one membership: a link must never create an identity, only unlock one. Three more vectors, one of them innocent: a **forwarded link** logs the bearer in *as the invitee* — two people quietly sharing one membership, or a colleague “helpfully” opening a link that was not theirs; a **double invitation** (one person at two addresses) makes one person two members, two wallets, two ceremony answers and two places in quorum; and deliberate padding by whoever controls invitations. Sharpened by §9.6a itself: a duplicate discovered **after the start** cannot be removed, because removal is a constitutional motion needing every member’s consent including the duplicate’s — so whatever the defence is, it has to work **before** judging opens. Candidate defences for later, cheapest first: (a) first-open **confirmation** (“you are Ida Fenwick — is that you?”), with later opens of the same link joining the same membership rather than a new one — note the tension with single-use links, which would lock the person’s second device out instead; (b) the convenor’s pre-start window is the human check — arrivals are visible, uninviting is free, and the start is the deadline for noticing; (c) a **merge** operation for discovered duplicates that is not an expulsion (one person’s two memberships becoming one re-rates nothing they did not do themselves) — needs care where both halves answered the ceremony differently. Deliberate sybil by an adversarial *convenor* is out of scope: they control the roster before the start anyway, and the defence against a bad-faith convenor is not joining their document.

386. **No surface yet raises a reserve motion** (raised 2026-08-19, unanswered in chat; generalized by v0.52). The module carries the `reserve` payload on any held-able setting and the page renders such motions defensively, but no composer offers "return this to the founder's reserve" — it wants a home, probably a second option on a delegated setting's own composer, in the same sentence grammar the delegation itself now uses.

387. **Reserving the text** (Ed, 2026-08-19, kept out of scope *for now*): every change to the document needing the founder's assent to merge — against the spirit of the product, and also exactly how suggested amendments work in many text-editing products, which is why it is parked rather than dead. If it ever lands, it is a fourth thing the 👑 can hold, and the assent step already exists (§9.7).

388. **Individual delegation, foundership transfer, inheritance** (Ed, 2026-08-19, out of scope — *maybe when we make the sequel!*): founders delegating powers to individual members rather than the room; transferring the foundership deliberately or on lapse. The 👑 is a benevolent dictator for life, not a literal monarch — nobody inherits it when the founder lapses (v0.49's auto-assent is the whole story of an absent crown).

398. **The multi-candidate ordinary race card** (raised 2026-08-19, from the Q391 build; Q390 made rivals real). Rival values on one setting join one engine race, but the page still renders “the first running motion” as a two-way card (what stands vs the proposal). The engine serves rival pairs with the conditional prompt (“if this text changes, which change is better?”, Q48) and the served card is already in the view — what is missing is session-view card grammar for a setting race with three or more lanes, and the rule for which motion card a second rival renders into. Wants Ed's design pass, not a mechanical fix.

419. **Does the desktop focus order match the reading order?** (mobile plan, 2026-08-20). The phone fix for DOM order is `order:` on grid children precisely so the DOM stays canonical — but nobody has ever tabbed through the desktop surface, and if that order is wrong the phone inherits it. *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §4 (2026-08-23): no `order:` anywhere; the desktop pass tabs the page and asserts DOM order.*

420. **The narrow form of the margin index** (mobile plan, 2026-08-20): on one column, does a rail entry render immediately above its own clause in the document flow (recommended — "beside" is a claim about adjacency in document order, and a phone's only axis of adjacency is vertical), or do clauses carry marks only, with entries reachable from them? *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §4 (2026-08-23): the second — marks in the gutter, the pinned population in a bottom dock (Q657).*

421. **The fold triangle is invisible until hover** (mobile plan, 2026-08-20). On touch it must always be visible — should it then always be visible on desktop too, rather than the surface having two different answers? *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §4 (2026-08-23): visible under `(hover: none)`, hover-revealed elsewhere — one rule, two renderings (Q658).*

422. **Judging offline** (mobile plan, 2026-08-20): offline is read-only on the blindness argument — a judgment cast against a stale pair is a judgment on a question that no longer exists. Is a *queued* judgment ever acceptable if the client re-validates the pair on reconnect and discards silently when stale? *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §3.e (2026-08-23): no — the radio position survives and is restored if the same pair is still served; the member presses ✓.*

423. **Does `room-pulse` need a third state on a phone?** (mobile plan, 2026-08-20). It says *the room is moving*, and a backgrounded PWA cannot say anything at all — is silence distinguishable from stillness, and should it be? *Answered by Ed 2026-08-23, against the recommendation: **no pulse in the mobile app** — `design/MOBILE.md` §3.f; the offline word stays on the clock line.*

424. **Multi-block drafting on touch** (mobile plan, 2026-08-20). A selection spanning blocks is one candidate on desktop; the caret proxy (prose `contenteditable="false"` on touch, since mobile IMEs do not honour `preventDefault()` on `beforeinput`) has no obvious gesture for it. *Deferred by `design/MOBILE.md` (2026-08-23) to stage 6; §1.7 names the seam — the door is the lane's ✏️ and a long-press on a clause, never the caret.*

425. **Do lanes collapse or scroll on narrow** (mobile plan, 2026-08-20) when a card carries two candidates side by side? *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §4 (2026-08-23): closes — every candidate pair is already a stacked `.propblock`; asserted as no card overflow at 360px.*

426. **One PWA icon or many** (mobile plan, 2026-08-20) — the icon identifies the app, but a member may be in several documents. *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §3.b (2026-08-23): one icon art, a manifest per document, many installs (Q669).*

427. **`--border` contrast at phone brightness** (mobile plan, 2026-08-20) — several hairlines are below 3:1 and load-bearing (the incumbent's dashed edge in the sealed record, for one). *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §4 (2026-08-23): a `--border-strong` token for the load-bearing edges, users named in the selector, surface-wide (Q659).*

428. **Where do push subscriptions live** (mobile plan, 2026-08-20), given the log is append-only and a subscription is deletable per-device state — exactly the kind of thing the `people` table (Q436's schema) exists for? *Answered by Ed 2026-08-23 as recommended in `design/MOBILE.md` §3.d (2026-08-23): the `people` table is adopted but unbuilt — a `push_subscriptions` table now with a nullable `person_id`, both stores, never the log (Q673).*

429. **How big is the offline shell allowed to be** (mobile plan, 2026-08-20) before caching the surface needs its own budget? *Answered by Ed 2026-08-23, against the recommendation: **no budget** — `design/MOBILE.md` §3.c.*

463. **The close is undesigned, and by this session's own logic it needs 🍾's treatment** (raised 2026-08-20). Everything decided today rests on two rules — **a thing that happens should be a thing somebody does**, and **no power arrives without acknowledgment**. Both have an unexamined mirror at the other end of a document's life. A windowed document reaches its closing date; a perpetual one freezes when active plus holding falls below quorum. Neither moment has been designed at all, and both are larger than 🍾: the close is when the document becomes **final**, which is the only irreversible thing in the mechanism. **Ed, 2026-08-22 (spec pass 1, item 567): the communication of the closing ceremony has not been designed at all — put it in the backlog.** What a member is told at the close beyond the 🥂 card and the one mail (SURFACE.md E24) is this item's.

    What the symmetry asks:
    (a) **Is there a closing act, and whose?** 🍾 is the founder's, because before the start they are the only one who can act. At the close there is a whole room, so a founder-pressed ending would be the one unilateral power nobody granted them — which argues the close is either automatic (the clock) or a consensus act (🏛️). *Ending the document at all* is already constitutional, so the machinery exists.
    (b) **Do powers leave with an acknowledgment?** By 453's rule they arrived with one. Unspent ✏️s become meaningless at the close; a 🏛️ in flight has nowhere to land. Something should say so, and the wallet emptying is the obvious place — the inverse of the pencil-storm.
    (c) **What does the last hour look like?** A rising bar means late changes need near-unanimity, and a member who wants one last fix should be able to see that the window is closing. Nothing on the surface says how long is left.
    (d) **What happens to live races at the close?** They degrade to backlog, per the spec — but a member watching their own proposal die in a race that ran out of time is a real moment with no design.

464. **The record is undesigned, and it is what the machine produces** (raised 2026-08-20). `record-builder` has been in the glossary since the start — *final text plus the record: rankings, camps, graveyard, care map, minority map, backlog, audit log* — and no surface has ever been drawn for it. The per-decision `sealed record` card exists and is good, but that is one clause's history; the **document-level** artifact, the thing a convention hands to the people who were not there, does not exist as a design.

    It is worth raising now rather than later because two things this month bear directly on it. The **constitution-as-document-text** work means the finished artifact already has a natural shape — the rules, then the charter — and the record is the third part. And **the stranger's door** (456) is the first surface built for somebody outside the room, which is exactly the record's audience.

    (a) Is the record a **separate document**, or the same document read in a closed state — the redacted page's opposite, everything open?
    (b) Does it have its own URL, and is it the one people share?
    (c) How much of the graveyard and the minority map is public, given authorship is sealed by default and the record is where seals come off (§3.4)?

    **Answered 2026-08-29 (Ed): *ratify what is built — same document, same URL, read closed — and treat (c) as the only live question.*** So **(a) and (b) are settled by ratification and are spent**: the record is not a separate document and has no URL of its own; it is this document read in its closed state, which is what `closedBlocks` and `.doc.closedpage` already build — every clause grey, every tab filed, the `sealed-speaker` revealed, wallets and pulse gone, the document ending bracketed as constitution · charter · backlog with the signatures beneath. Nothing is owed for either half; the tree is the answer and this records that it was chosen rather than drifted into. **(c) is the only live question**, and it is the one that needs a mechanism rather than a page: how much of the graveyard and the minority map a **stranger** may read, given §3.4's seals come off at the close and §3.5a's judgments ladder now has a per-decision reveal rung coming (**996**). Those two interact — a document that reveals judgments as each decision settles has already published most of what (c) is about — so (c) should be answered **after** 996 is built, not before. **(a) and (b) spent; (c) open.**

465. **Notifications are the surface nobody has designed and everything depends on** (raised 2026-08-20). A convention runs over days or weeks and the participants are not sitting in the page. `router` has carried *notification digests* as a named part since the beginning, with no design and no copy. Every other decision this month assumes people come back — the drip accrues, the bar rises, lapse warns before it bites, a 👑 question waits, a motion needs everybody — and all of it is inert if the mail is wrong.

    (a) What is the default cadence, and is it a **setting** (in which case, whose — and is it constitutional by the test, since it changes who effectively participates)?
    (b) What does a digest contain, given the blind discipline? Counts and your own tasks are safe; anything about standings is not (§3.5).
    (c) Does a 🔥 — *the question you are being asked next* — ever justify an immediate mail, or is everything batched?
    (d) The lapse warning and the freeze alarm already have to be mails. They are the two that exist by necessity, so they are the place the vocabulary gets set.

    **Answered 2026-08-29 (Ed): *take the whole thing as one design pass with its own plan.*** So (a)–(d) are not four questions to be answered one at a time in the register — they are the agenda of a design pass, and the pass is what answers them. That is the same shape as **1009**'s extraction and **464**'s (c): a commissioning, not a resolution, and the item stays open until the pass has run. **What the plan inherits**, so it does not start from nothing: the mails that already exist are the founding invitation, the magic link, the §9.7a save notice and the close (`MAILS` on the page, `mail-fold` in `packages/server`), `design/STYLE.md` §6 already holds the mail copy rules, and **the lapse warning and the freeze alarm are the two the mechanism obliges** — (d) is therefore where the pass should start rather than where it ends. **Answered as a commission; the design pass is owed and unwritten.**

500. **The legal drafts' decisions.** (Stage 12, 2026-08-20; the drafts are `docs/legal/PRIVACY.md` and `TERMS.md`, placeholders marked, not in force.) The eight decisions that have to be made before either can be linked, from `docs/legal/README.md`: **(a)** who the legal entity is; **(b)** controller for everything, or controller for account data and processor for each document on the founder's behalf — the drafts assume the latter; **(c)** publish now with erasure done by hand, or wait for the `person_id` mechanism; **(d)** keep the operator-notification mail carrying the founder's address, or reduce it to title-and-URL; **(e)** retention for a closed document — a period, or each group setting its own as a rule; **(f)** whether the Terms promise a moderation process there is no mechanism for; **(g)** self-host the web font (deletes a third-party disclosure and a CSP exception); **(h)** whether member emails staying visible to other members is intended. The drafting agent's two independent findings ride (g) and (h): Google Fonts is currently the only third party beyond the three providers, and most members will assume their address is private. — **Parked by Ed, 2026-08-29: *park the whole item until go-live is actually scheduled.*** All eight, not a subset — the answers turn on facts nobody has (the entity, the launch date, who the first real members are), and eight guesses written into a document that is not in force is worse than eight blanks. **Parked, not spent**, the way **46** is: nothing here is decided and nothing is owed until there is a date. Two things ride along and should be re-read at that point rather than now, because both drifted while this sat: **(d)** is the same decision as **482**, which Ed has since answered — the operator mail keeps the founder's address — so (d) inherits that unless the legal reading overrides it; and **436**'s `person_id` is a stage 5/6 schema decision that (c) depends on and does not control. `PRODUCTION.md`'s stage-12 line carries the park.

631. **"When the session begins" is the only "session" a member can read.**

    **Where it stands (2026-09-07):** no member-visible string in `design/copy.js` says *session*; the sentence left the page in `8ada546` (2026-08-25, the Proposals run as one clause wearing four tabs, Q748–Q752), a rebuild rather than a ruling on the word. **Put to Ed as Q1218, he deferred it with a rule-to-be:** *We should return to this later; I think it should change depending whether the document has an ending or not. If it has an ending, then it's a session. If it has no ending, then it is a document.* So the word is not one vocabulary but a fact of ⏰: a windowed document is a *session*, a perpetual one a *document* — which would touch every string that names the thing (STYLE §1's *document / charter* row, the 🍾 and 🥂 bodies, the clock, the stranger's door) and is a copy pass of its own when he returns to it. Not built; not yet a rule in STYLE.

    The new Proposals preamble is Ed's own wording (2026-08-22), and I have built it verbatim — but every other member-visible string on the surface says **document**, including the sentence this one replaces (*The document has not begun.* / *The document began at…*), the 🍾 card's own body (*Only the Founder can begin the document*), the hold line (*The document cannot begin while…*) and the stranger's door. A scan of `session-view.html`, `session.js` and `cards.js` finds "session" in comments and file names only, never in copy. So the preamble now says *session* two paragraphs above a card that says *document* about the same act. **(a)** Keep *session* and leave the inconsistency — a convention **is** a session, and this is the one sentence about the room's life rather than about the text. **(b)** Keep *session* and sweep the rest of the surface to match, making it the vocabulary. **(c)** Say *When the document begins, members may propose changes to rules…*, which costs nothing and matches every neighbouring sentence. I recommend **(c)** unless *session* is deliberate; it is a one-word change either way, and STYLE.md's job is one vocabulary. Raised 2026-08-22.

901. **Three acts have no communication designed.**

    **Where it stands (docs pass, 2026-09-07):** built 2026-08-27 (SURFACE E31–E33); open inside it, unnumbered: whether the room is owed an OK for a departure, and what a member removed by a *carried* motion is told.

    (Same sweep.) Entry 94 added exile at will and resignation, and entry 97 added the refusal of an application verified before 🤝 shut — three acts with real audiences and no row in SURFACE's event matrix, which is meant to be complete. **Ed, 2026-08-26: add them, and put designing them on the backlog.** Added as **E31–E33**, each marked *undesigned and unbuilt* so `spec-check` counts them beside E28–E30; the design itself is plan-queue backlog **98**. What each audience is told — and above all whether an exiled member is told anything at all, and by what channel — is the design work this holds open. **Built 2026-08-27 (plan-queue 69):** E31–E33 designed and built — see SURFACE §2 and `design/DECISIONS.md` *Three acts nobody was told about*. Exile mails the member (the document's address, no login link) and their dead seat's door says why; resignation gets the door sentence and no mail; both leave a grey departure line under *Members*; the refused application gets Y25's sentence before and after the press, Submit dark. Two things stay open, unnumbered here: whether the room is owed an OK for a departure (the plan builds the sentence and not the OK), and what a member removed by a *carried* motion is told.

    **Where it stands (docs pass, 2026-09-07):** answered, owed — `packages/engine-core/src/text/compose.ts` has no sentence-level merge.

    (raised 2026-08-27, from Ed's question *does git teach us anything about overlapping proposals?*; **Ed: good idea**). `blocksOf` makes one engine line one paragraph, so every footprint is a whole paragraph and two members editing different sentences of the same one fail the three-way merge (`compose.ts`) and escalate to a race the room must judge — the opposite of *most of a session should feel like approving typo fixes*. Git's lesson is that the granularity of conflict detection sets the collision rate, and that most conflicts are adjacent edits that a finer diff composes. Proposed: **Gate 1 composes at word or sentence level inside a paragraph** while the footprint stays paragraph-keyed for anchoring, so the page's *one block, one hunk* simplicity survives and only the merge gets finer. **Before deciding, the sim measures it**: rivalry rate at paragraph vs sentence granularity over the existing sweeps, since the whole case is that most paragraph collisions are accidental. SPEC §2.2 rule 1 is the sentence that changes. — **Answered 2026-08-29 (Ed): make the change now — merge at sentence level inside a paragraph.** So the measurement is not a precondition of the decision; it may still be worth running as evidence of how much the change bought, but nothing waits on it. Gate 1 composes at **sentence** granularity (not word — Ed named the sentence) inside a paragraph, while the footprint stays paragraph-keyed for anchoring, so `blocksOf`'s *one block, one hunk* survives and only `compose.ts` gets finer. **SPEC §2.2 rule 1 changes with the build, not ahead of it** — a spec sentence describing a granularity the engine does not have is the drift the spec pass exists to stop, and this is one commit's worth of both. **Answered, owed, not built.**

1205. **`seat-matrix` is red, and was before either of 2026-09-06's builds** — 56 findings and a `TypeError … reading 'closed'`, identical from a worktree at `b62fcbc`. CI's `walks` job does not run it, so nothing went red. Needs a look before it is trusted again; until then a claim that a seat change is safe cannot lean on it.

    **Where it stands (2026-09-07, after the diagnosis and two builds).** The 56 were four things, none a stale selector: the founder's gate cards no longer exist (Ed, 2026-09-01) so two steps drove a control the surface stopped offering; Q919 (b) was unbuilt (the whole member-hat cluster); Q920 was unbuilt (the whole clerk-hat cascade); and the applicant's live page throws on load, which the walk had been dropping in silence. The walk fixes (`7a6dd61`) took it to 21; building Q919 (b) and Q920 the same morning took it to **11**, on a fresh server: the applicant page (**1281**, two seats and one error), a lapsed member offered a motion answer (**1282**, E10), and **an E4 cluster still unread** — the 💡/⚖️ gate news not carried by the `early` and `lapsed` seats on either hat, and carried *as a tab* by a clerk founder who is outside the audience. That cluster is the next look; the gate news is not a setting, so Q919 (b)'s poll fix does not reach it. Four `noRule` cells stand (E11, E13, E22, and E11's audience — Q930). **Re-run 2026-09-12 14:00 on a fresh server at 8a4df49: 11 findings, 3 no-rule, errors none, refused none, unstood none** — the same E4 cluster (💡 ⚖️ not carried by the `early` and `lapsed` seats on either hat, eight cells), E10 on three seats (1282), and the no-rule cells E22 ×2 and E11's audience; the applicant's page error (1281) is gone, and the stranger's `gates` error the 2026-09-11 handover mentioned did not occur here or on `docs.vote/d/moon2` as a stranger.

## Backlog (provisioned, build later)

| # | Title | Raised | State | Pointers |
|---|---|---|---|---|
| 1350 | Edit mode on a phone is hidden, not gated: a paired keyboard still enters it | 2026-09-12 | backlog by Ed 2026-09-12 (*leave it for now — see if we can make it work nicely in the future*): the doors are `display: none` below 900px and the prose takes no caret, but a physical key still calls `setEditMode` / `typeAt`; the mobile composer stage (MOBILE.md §1.7's lane door) is where it gets a real answer | `edit-mode`; `design/system.css` (the narrow block hiding `#ridetab`); MOBILE.md §1.7 |
| 1343 | `REPORT-deferred-evidence.md` is pinned to spec v0.12 and its numbers moved under Q1337 | 2026-09-11 | backlog by Ed 2026-09-11 22:38 (*leave it, file as backlog*): `npm run evidence -w @draft/sim-harness` re-runs it deterministically; the report wants a rewrite against v0.116 when it is next read for a decision | `sim-harness`; `packages/sim-harness/REPORT-deferred-evidence.md`; Q1337 |
| 1287 | The erase route on the running server: row and memory in one act | 2026-09-08 | answered (a), owed **at go-live** — the operator route, not before; (b) closed, `[redacted]` (STYLE T49) | `people`; `draft-tools erase`; `docs/OPERATING.md` §5; PRODUCTION.md stage 12 |
| 996 | A per-decision reveal rung for 👁️ | 2026-08-28 | answered, owed — the catalogue still reads `never · after` | SPEC §3.5a, §9.7.1 |
| 315 | The ranking model as a document-creation setting | 2026-08-17 | parked note (Ed) | SPEC §3.3, §8.3 |
| 42 | Spectator commentary view | 2026-08-14 | provisioned | `spectator-api` |
| 55 | Participant-api composer surface | 2026-08-14 | provisioned | SPEC §3.3 |
| 56 | Orphan refunds credit departed ledgers | 2026-08-14 | provisioned | — |
| 244 | A flow entry can overlap a pinned one | 2026-08-16 | open — cosmetic | `freeFor` (session.js) |
| 248 | The ⌈E/3⌉ has never been calibrated | 2026-08-16 | open — wants a sweep | SPEC §8.2 |
| 252 | A perpetual document loses the reset | 2026-08-16 | open | SPEC §9.4, §4.5 |
| 26 | Evidence-clock adoption threshold | 2026-08-13 | backlog by Ed 2026-09-14 (the walk's fifth question: *move it to the Backlog as a sim study*) — wall clock stays; a sim-harness A/B of the evidence clock and the hybrid, beside Q248's sweep and Q1343's rewrite, when a real room gives a reason | SPEC §4; Q22 |
| 94 | A rationale is machine-drafted and human-edited | 2026-08-16 | backlog by Ed 2026-09-14 (the walk's findings batch): ruled 2026-08-16 — the *why* lane arrives with a mechanical first sentence the author rewrites before submitting — and never built; the index's *(b) built; (a) (c) (d) open* was Q52's lettering copied by mistake; today the lane is empty with `whyPlaceholder` | SPEC §2.6, §3.4 |
| 924 | Gate 1 merges at the paragraph | 2026-08-27 | backlog by Ed 2026-09-14 (the walk's second findings batch): ruled 2026-08-29 — merge at sentence level inside a paragraph, the paragraph staying the unit everything anchors to, and *make the change now* — never built; `text/compose.ts` is still line-level and SPEC §2.2 rule 1 changes with the build, not ahead of it | SPEC §2.2; `text/compose.ts` |
| 308 | The one overhang a fitted pile cannot remove | 2026-08-17 | backlog by Ed 2026-09-14 (the walk: *wait for a real case, then fix it as a push-down*) — no mark may start above the previous one's bottom, so a tab on a 17px heading steps down rather than overlapping the next; reachable since headings carry proposals (K31) and records follow their descendants (M1, Q1333); nobody has hit it | `fitStacks` |

1287. **The erase route on the running server** (raised 2026-09-08 as (a) of the people split's two residuals; ruled by Ed the same day — (b), the word for an erased person, closed as `[redacted]`, STYLE T49, DECISIONS.md)

    **What exists:** `draft-tools erase <store> <docId> <personId>` deletes the person's row in the store, and every hash holds because the log never carried the name; but a running server holds a document's rows in memory from the moment it loads them and reads them on every view until it reloads, so the operator's road is *erase, then restart* (`docs/OPERATING.md` §5). **Ruled (Ed, 2026-09-08):** the operator route — `POST /api/admin/erase` behind a secret the tool carries, so the row leaves memory and store in one act — **built when go-live is scheduled and not before**; the restart stays the documented procedure until then, being honest while the store holds only alpha documents. Rejected: writing the restart into the privacy policy's manual-process sentence (the weakest promise), and the store watching its own rows (a polling loop and a delay on a promise that should be immediate). **The build, when it comes:** the route on the server, the secret in the environment beside the mailer's, `draft-tools erase` calling it when given a URL rather than a store, a server test with an erasure between two views and no restart, OPERATING.md §5 rewritten, PRIVACY.md's placeholder losing its restart clause. Go-live's own gate is PRODUCTION.md stage 12 and Q500.

996. **A per-decision reveal rung for 👁️**

    **Where it stands (docs pass, 2026-09-07):** answered, owed — `packages/constitution/src/catalogue.ts:228` still reads `rungs: ['never', 'after']`.

    (Ed, 2026-08-28 — *file a backlog entry for the per-decision rung*). The 👁️ judgments ladder has two rungs, *never revealed* and *revealed at the end of the document* (§3.5a, **994**). Ed's note asked for a third: names shown once the decision they contributed to closes, decision by decision, rather than at the end of everything. It is a SPEC §3.5a change with record semantics inside it — what the sealed record shows, and when — and the batch-O plan deliberately did not take it. What has to be settled before it can be built: whether a per-decision reveal names judges on the sealed record only or in the live view of a settled race; what it does to a race that resolves and is later re-opened by a ground shift; and whether the rung is elective per member or the room's alone, since every other rung on the ladder is the room's. — **Answered 2026-08-29 (Ed): build it, and show the names in the live document too as each decision settles.** That answers the first of the three open sub-questions outright — **not the sealed record only**: a settled race shows its judges in the live view, as it settles, not merely in the record at the close. The other two are still open and a plan must settle them before it builds: what a **re-opened** race does with names already shown (a ground shift re-opens a settled race, and a reveal cannot be taken back), and whether the rung is **elective per member or the room's alone** (every other rung on the ladder is the room's, and per-member election would make one document's names half-shown). SPEC §3.5a is the section, and the ladder gains a third rung between *never revealed* and *revealed at the end of the document*. **Answered, owed, not built** — and note it is the reveal-cannot-be-taken-back half that makes this dearer than a rung label. — **The other two are answered 2026-08-29 (Ed): *a re-opened race hides its names again until it re-settles; the rung stays the room's.*** So a reveal **is** taken back, and the rule is stated positively: names show while a race stands **settled** and are withdrawn the moment a ground shift re-opens it, returning when it settles again — the reveal is a property of the settled state rather than an event that fires once. And the rung is **the room's alone**, not elective per member, which keeps every rung on the 👁️ ladder the same kind of thing and rules out a document whose names are half-shown. **All three sub-questions are now answered and nothing is left to decide** — what remains is entirely a build, and it is one plan's: SPEC §3.5a's judgments sentence gains the third rung, **and §9.7.1's `judgments` row must move with it** (its ladder cell reads `never · after` today and `spec-check` asserts that table against `packages/constitution/src/catalogue.ts`, so the spec sentence, the table row and the catalogue values are one commit or a red build). The pre-flight of 2026-08-29 deliberately did **not** fold the rung into SPEC for that reason. **Answered, owed, not built.**

315. **The ranking model as a document-creation setting: approval · Bradley–Terry · Crowd-BT** (Ed, 2026-08-17, parked as a note for later). *There are situations where each is appropriate and I think much of our machinery still applies for each.* Crowd-BT is Chen, Bennett, Collins-Thompson & Horvitz, *Pairwise Ranking Aggregation in a Crowdsourced Setting*, WSDM 2013 (10.1145/2433396.2433420) — Bradley–Terry with a **per-judge reliability parameter** estimated jointly with the item scores, plus an active-learning scheme that chooses which pair to serve *and whom to serve it to*. Details want checking against the paper before anything is built on them.

Ed is right that most of the machinery is indifferent to the choice. The `patch-engine`, `overlap-gates` and `dedup-gate` are text machinery and never touch the model. The `adoption-threshold` and its ramp are stated as P(challenger beats incumbent), and all three produce something that plays that role. Quorum, adoption floors, the record, the gazette, and the whole of `session-view` are unaffected — a card asks for a judgment either way.

What genuinely changes is worth listing, because it is more than the estimator. **Active sampling largely disappears under approval**: everybody can rate everything, so the `router` stops *selecting* and only *orders*, which would make the queue dramatically shorter and the `evidence-meter` mean something different. **Deadlock (§8.3) is a Bradley–Terry construct** — *close relative to the marginal value of further sampling* — and under approval it becomes two candidates with similar approval rates, which is detectable but is not the same claim. **Indifference** has no separate existence under approval; approving both *is* indifference. And the **`bounty score`** rests on resolvable disagreement, which is model-specific.

Two things worth having written down before this is picked up.

**The choice is constitutional, not statistical.** Crowd-BT's reliability parameter says a judge is *often wrong*, which presupposes there is a right answer to be wrong about. This project's §3.3 says the opposite in as many words — *preference is not measurement*. If Hollis prefers the shorter clause and thirteen people prefer the longer one, Hollis is not unreliable; Hollis disagrees. Down-weighting a judge is **disenfranchising them by an algorithm nobody in the room can audit**, which is a serious thing to do to a member of a constitutional convention. But there are documents where it is exactly right — a technical standard, a specification being cleaned up, any domain where some readings are simply mistakes — and those are the same documents where a large roster contains people of very different expertise. So the real question a convenor is answering is **does this document treat disagreement as difference or as error**, which is the kind of question that belongs at `document-creation` or the `founding-ceremony` beside quorum and the bar, and not in a settings menu. Under the §9.0a logic it is arguably the roster's to settle rather than the convenor's: being told your judgments count for less is a thing done *to* you.

**And the sophisticated model is probably wrong at v1 scale.** Crowd-BT spends data on extra per-judge parameters, and CLAUDE.md tunes v1 for rosters of 5–10 where data-efficiency beats throughput; on fourteen judges it may be strictly worse than plain Bradley–Terry, which is itself chosen over approval *because* it extracts more per judgment. Crowd-BT's home is hundreds of annotators. Which puts its threshold at roughly the roster size where you stop being able to know everyone — the same point at which a room stops being able to vouch for each other's good faith. Those two thresholds coinciding is not a coincidence, and it is the strongest argument that this is a real setting rather than a knob: **the model should change when the room changes kind.**

One reversal to expect if approval is built: it produces the **more legible record**. The `sealed record`'s best sentence — *these two beat what we had and those three did not* — is currently a Bradley–Terry construction in which every number is a probability against the incumbent. Approval counts are less informative and far easier for a lay reader to trust. The more sophisticated model makes the harder-to-explain record, which matters for a document whose legitimacy rests on people believing the record.

42. **Spectator commentary view** (Ed, 2026-08-14): an optional commentating view for people watching a convention — like the sim's commentator, but consuming **public data only**: gazette, live candidate texts/rationales, bounty board, document state. Never individual judgments, never standings, authorship only per the session's visibility setting. Provision: expose a `spectator-api` in engine-core (a strictly-public projection, sibling of the participant-api) and have every spectator surface — commentary LLM included — consume only that, so privacy holds by construction rather than by prompt discipline. Note the sim's own commentator is deliberately different: it is omniscient (sees temperaments and hidden agendas) because it narrates a fiction for the experimenter; the product spectator box narrates real people and gets none of that.

55. **Participant-api composer surface** (sim evidence 2026-08-14): `ParticipantApi` has no composer-opening call — the sim runner invokes `Session.openComposer` directly (same precedent as its dedup co-sign path). The product composer needs that surface, including the §3.3 forfeit semantics, before the UI can exist.

56. **Orphan refunds credit departed ledgers** (sim evidence 2026-08-14): when an author-departed candidate is adopted, its refund credits the removed member's ledger — harmless (tokens are worthless at close and E excludes them), but the UI must not render it as an invitation to act.

244. **A flow entry can overlap a pinned one** (found 2026-08-16 while verifying 222; pre-existing, not caused by it). Flow entries are supposed to step around the pinned pile rather than hide under it (112), and `freeFor` re-checks every candidate position against the pinned blocks — yet one case still renders overlapping. Reproduce: open `quick-cellar` from the top of the document and `quick-probation` (a judged line) lands inside `quick-attendance`. The same shape occurs on the previous build with a different pair (`diag-quorum-keys` over `quick-notice`), so it predates the claim-absolute change. Not chased further because it is bounded and cosmetic, but the likely suspects are a height measured before a re-render or the `prev + QGAP` adjustment running against stale blocks.

248. **The ⌈E/3⌉ has never been calibrated** (raised 2026-08-16; since 2026-09-07 also carrying 251's worry, by Ed's Q1221: at the standard rate a wallet sits near its cap most of the time, so a full wallet tells a member nothing — the same sweep should say what rate and cap make the wallet move). It arrived with spec v0.5 in the initial commit and has never been the subject of a study, unlike its neighbours — the hot set went 6 → 3 on a sweep, the economy was checked at rosters 5–14, the ramp was argued out. §8.2 justifies it as "statistical sufficiency", which is the wrong account: sufficiency depends on effect size and noise, not on how many people are in the room, and the adoption threshold is already doing the confidence work. What the floor actually does is **legitimacy** — the claim that enough of the room looked for the outcome to be theirs. That is a political quantity, and political quantities are argued rather than derived, which is why a third feels arbitrary: it is. The one relevant piece of evidence is finding 10 of the deferred-evidence pass, where F dropping to 2 produced adoptions that were still fine *because they had to clear a ~0.9 posterior bar*. A sweep of F against welfare and against how much ends up parked as backlog would settle it. Now partly superseded by §9.0a — where the roster sets its own quorum, the default matters less, but a default is still needed.

252. **A perpetual document loses the reset** (raised 2026-08-16, from §9.0). §9.4 and §4.5 lean on it: next session the threshold resets, the backlog re-enters stake-waived, and everything becomes contestable again — "entrenchment is session-scoped". With no sessions, an adopted clause stands until something clears the same fixed bar, and there is no periodic moment inviting the room to reconsider. This is the one place where perpetual is worse rather than merely different. Candidates for a replacement: a scheduled thaw; a rule that a clause untouched for long enough re-enters the backlog; or accepting that a perpetual document *should* entrench, since that is what a standing charter is for.

26. **Evidence-clock adoption threshold.** Wall clock chosen for v1 (Q22, options 23–25, 2026-08-13): simpler, knowable, one clock shared with the token drip. Revisit the evidence-clock (or a hybrid max-of-both) ramp as a sim A/B once everything else works.

94. **A rationale is machine-drafted and human-edited** (opened 2026-08-16; settled by Ed 2026-08-16). The composer generates a first draft describing the change in mechanical terms — what it drops, what it adds — and the author edits it into their own words before submitting. This is the option I had been most wary of, and Ed's addition of the edit step is what fixes it: the words are the author's by the time anyone reads them, so nothing machine-written ever appears on a blind card under a human's name. It also recovers what the removed ledger was for, because a mechanical draft names the losses by construction and an author has to actively delete them.

924. **Gate 1 merges at the paragraph, so two typo fixes in one paragraph become a rivalry**

308. **The one overhang a fitted pile cannot remove** (raised 2026-08-17). `fitStacks()` shrinks a stack's peek until it reaches no further than the next mark in the gutter, which makes two anchors claiming one strip structurally impossible — measured across all 32 columns in the fixture, zero collisions. But a pile's floor is **one tab**, 30px, and there is nothing to shrink when a clause holds only one. A block shorter than 30px whose next mark is within 30px would still overlap, and headings are 17–19px. It cannot happen in this fixture and it is not hypothetical: `always-on-typing` covers headings, so a heading can carry a decision. Two ways. (a) A **layout pass that pushes down** — no mark may start above the previous one's bottom, so a mark steps out of the way rather than overlapping, which is precisely what the `needs-you-queue` already does around a pinned card. Costs the rule that a mark stands beside its own text, in exactly the cases where it cannot. (b) **Give short blocks a floor** in the gutter — a minimum block height wherever a mark sits. Deforms the charter to suit the margin, which is the wrong way round. Recommendation: **(a)**, and only when a case actually appears, since it is invisible today.

## Spent numbers

**The next free number is 1353** — claim by writing the block here, then commit it alone.

**1350–1351 are the mobile pass of 2026-09-12** (claimed 2026-09-12 17:40, Ed: *let's do a quick mobile pass*): 1350 the keystroke door into edit mode on a phone, left open and backlogged; 1351 Ed's four rulings — the tab meets the card, the drawer holds only what asks you, the topbar takes two rows, 🪶 stands with the powers.

**1349 is the page's two 404s on every load** (claimed 2026-09-12 14:58, Ed: *have the page load errors as a question*).

**1348 is the Q1323 re-try's finding** (claimed 2026-09-12 13:55): several motions running on one setting, the page offering only the first.

**1338–1343 are the evening's handover questions** (claimed 2026-09-11 22:40, put to Ed one at a time as multiple choice after the push of 0981374): 1338 races after ⚖️'s OK pop onto the rail (ruled: birth-pass extends to the charter rail) · 1339 the closed fixture holds no undecided race (ruled: add one) · 1340 E13's audience cell (ruled: every member who could still judge it, except the author) · 1341 the login door against venue wifi (open) · 1342 a pasted list shows only the last refusal (open) · 1343 the evidence report pinned to v0.12 (backlog). Asked and answered the same sitting without a number: Q1337's author-as-one-judge stands (*keep*); the quarantined documents stay (Q1322); Q1323 is Ed's own re-try on the next moon room; Q1177's CI question waits on a broader look at what CI runs; the Resend quota goes unfiled.

**1293 is the doubled standing rule on the founder's settled card** (claimed 2026-09-09, Ed's report in chat: the status quo text twice, both radios pressed). Verified on 🌍 and ⏱️; the cause is `composerOn`'s one-seat subtraction handing the founder the founding ladder under Q1167 (a)'s rule head. Three readings offered; Ed chose (a) the same day — the founder's ladder omits what stands by value as the composer does, nothing pressed on open, the commit dark until the card differs — and it is built (`ladderView`, `standingFields`, `rungOpt`; SURFACE §9's *setting (founder, pen)* row amended), with two finds on the way: the head block followed the founder's pick, and a post-start pen set read *Chosen by the membership*. Verbatim in DECISIONS.md. **Spent.**

**1289–1292 are Ed's QA of 2026-09-08 (evening)**, four reports in chat, claimed together and built the same night: **1289** ⏰'s picker — *I click the datetime picker button, choose date and time, and then there's no "OK" button, and when I click outside of it the datetime I picked is not submitted*; **1290** 🌡️'s two sentences — *the text on the decision card doesn't match the text put on the document; they should match, and it should be the text on the decision card*; **1291** the emoji picker — *very slow to open* and *the emoji in the picker are too small; the size that they appear on the right of the topbar is good*; **1292** the text card — *I can see the bottom edge of this card - it should "go off the bottom of the screen"*. Each verbatim in DECISIONS.md with what was found. **Spent.**

**1288 is the one-character address** (claimed 2026-09-08, asked and answered in chat the same day): Ed could not found a document at `docs.vote/d/1`. The link grammar the server and the page share (`SLUG_OK`, Q460's build of 2026-08-21) demanded three characters or more — a floor no ruling ever set, a session's own call while building 📍, recorded nowhere but the regex and its refusal line. Ed: **we should allow one character addresses.** The floor goes to one on both sides; the refusal sentence, the page's one member-readable literal outside `copy.js`, moves into it on the way. Verbatim in DECISIONS.md. **Spent.**

**1287 is the people split's two residuals** (claimed 2026-09-08, building half (1) of decision 1253): (a) erasure while the service runs — `draft-tools erase` deletes the row in the store, and a running server holds its rows in memory until it reloads, so the operator's road today is *stop, erase, start* (or erase, then restart); whether a hot path is wanted (an operator route, a store watch) or the restart is the honest procedure for an alpha; (b) the surface word for an erased person — `[withdrawn]` is PRODUCTION.md stage 12's own word and stands where a name would, in the register, the departures, the signatures, a named author and the door; whether it passes STYLE (brackets, lower case, a state rather than a name) or wants another. Filed under *Open*. Answered by Ed 2026-09-08: (a) the operator route, built when go-live is scheduled and not before — the restart is the procedure until then; the item moves to *Backlog*; (b) `[redacted]` — the brackets stay, the word changes, since an erasure strikes the record rather than reporting a departure; STYLE T49. Both verbatim in DECISIONS.md.

**1286 is the findings item of Q1239's three checkers** (claimed 2026-09-08, before the checkers were built): every place SURFACE §2's L table, §6's setup alphabet and §7.1's socket table disagree with the code on the first run, one lettered sub-item per disagreement, for Ed to say which is right. Filed under *Open*; the checkers stay red on it until he rules. Answered by Ed 2026-09-08, (a)–(e), and folded the same day — the rulings are in DECISIONS.md.

**1285 is the lapse-warning lead** Ed raised on 2026-09-08 for the backlog: the warning mail a proportionate absolute time before the lapse rather than at three quarters of the spell. Answered by Ed the same evening — *only three: a week, a day, and an hour — and you get all three*: three warnings before the lapse, each that fits inside the spell, each naming its lead; built as `WARN_LEADS` and `warningDue` (R-097, SPEC v0.111). DECISIONS.md.

**1284 is the E21 audience question** the seat-matrix raised when Q1282 was built (2026-09-07): whether a lapsed member carries the admit card — answered by Ed on 2026-09-08 (*every active member*; DECISIONS.md).

**1283 is the router re-read's aside** (the Q1262 diagnosis, 2026-09-07): `feed()` drops deadlocked races from serving entirely where SPEC §8.3b says a deadlocked race is served as an ordinary race until it has nothing left to ask *you*.

**1280–1282 are the three product defects the seat-matrix re-read of 2026-09-07 found** (Q1205's diagnosis, and the stranger render for Q1232): **1280** the stranger's settled Admissions card has no rule block; **1281** the applicant's live page throws on load; **1282** a lapsed member is offered a constitutional answer the module refuses. **A released number is dead** (Ed, 2026-09-07, Q1223): claimed and unused, it is never reused, so a number always dates its item — **1017** (claimed with 1015–1016 for backlog entry 159), **1022** (with 1020–1021 for entry 160), **1031** (with 1030 for entry 173) stay unspent for ever; 1109–1112's reuse was the one exception, not a precedent.

Every other claim block and built-narrative, and the map of where everything below 260 went, left this file verbatim on 2026-09-07 for `design/DECISIONS.md` § *QUESTIONS.md, the closed items and the spent numbers, lifted 2026-09-07 (the docs pass)*. What stays below is every claim block that still names an open number.

**1206–1279 are the questions of the documents pass of 2026-09-07**, claimed by the session that ran the pass after Ed had gone to bed, one item per question the fourteen logs left in their *Questions* sections, in log order: **1206–1209** STYLE.md (log §6 items 1–4); **1210–1230** QUESTIONS.md (log §6 items 1–21); **1231–1239** SURFACE.md (log §6 items 1–9); **1240–1245** CLAUDE.md (log §6 items 1–6); **1246–1250** README.md (log §6 items 1–5); **1251–1256** PRODUCTION.md (log §6 items 1–6); **1257–1259** docs/OPERATING.md (log §6 items 1–3); **1260–1263** the package NOTES files (log §6 items 1–4); **1264–1266** design/MOBILE.md (log §7 items 1–3); **1267–1268** REVIEW-creation-session.md (log §6 items 1–2); **1269–1270** REPORT-deferred-evidence.md (log §7 items 1–2); **1271–1272** design/spec-pass/ (log §6 items 1–2); **1273** the card reviews (log item 1); **1274–1279** SPEC.md (log *Questions* items 1–6). All seventy-four stand under *Open*, each body the log's question verbatim. — **1237 built 2026-09-08, spent**: Ed's (b) of 2026-09-07 — F5, F19, F21 and K31 each left as its ruling and a pointer; the power table's blocks to §9's 🍾 row, the edit-mode card and the riding tab to §9's 📝 row, the five statuses to a new table under §8's band table, your own card's opening to §9's identity row, the remedy card's lead to §9's ✉️ row, the reasons to `design/DECISIONS.md` § *SURFACE's four long rules trimmed to their rulings*, which lists every move as a before/after.

**1202–1205 are the pair-deck build's open findings (2026-09-07)**, claimed in the session that ran the two builds of 2026-09-06 (quorum-is-the-floor, pair-deck) after Ed had said *push* without answering them: **1202** what ⏳ means under the deck (dealt now vs held); **1203** where a revised pair sits in the ledger; **1204** the slow ceiling test on Ed's machine; **1205** `seat-matrix` red before either build. All four stand under *Open*. — **1202 built 2026-09-08, spent**: Ed's ruling of 2026-09-07 (*⏳ should mean "waiting for other people to vote"*) is the engine's `askOn` — the pair a race can still ask you, dealt or not, one test with the feed's — the view's `askable` / `ask` per clause row, and the page lighting the entry on that rather than on the hand; guards `askable.test.ts`, the server's Q1202 test and `npm run journey`'s *askable* case; the reasoning is `design/DECISIONS.md`.

**1200–1201 are the pair deck and the judged-pairs ledger (Ed, 2026-09-06)**, claimed by the plan that builds them (`~/.claude/plans/pair-deck.md`). Ed: *The reason I wanted to use Bradley-Terry is for when there are many alternatives to the same clause, we have a pairwise system. Come to think of it, I haven't been served multiple pairs from one clause yet.* He had not been, and the cause was the page: `itemsFromView` kept the first served card on a race and filed the race as ⏳ on any standing vote, so every member was capped at one comparison per race for the race's life, whatever the engine held for them. **1200** the fix he chose from three shapes (a stack of ⏳ tabs, a switcher on the card, the deck): **the card is a deck** — one entry per clause, lit while the router holds an unjudged pair for you on the race; judging closes the card and the next press opens the next pair in the router's order; ⏳ only when none is left; no new control. And on what the entry shows while the deck is not empty: *It should show you the pair that you would vote on next* — the teaser is the front pair's rationale(s), never a count. **1201** Ed: *Is there a way I can go back to see pairs that I've already judged?* There was not, live — the server sent *you have voted on this race* as a flag and never which pair or which way — and he chose to **fold the ledger into the deck plan**: the ⏳ card is the member's own ledger of every pair they judged on the race, each verdict marked and revisable, so the earlier plan's accepted cost (an earlier pair out of reach while later pairs are dealt) never ships.

    **Built 2026-09-06 (`pair-deck`), both spent, none released.** **1200**: `itemsFromView` reads the deck — every served edge card on the race, the front one the card, the state *needs* while any is dealt and *deciding* only once none is and a vote stands — and `session.js` keys its provisional judgment state by pair (`pairKeyOf`), so the entry re-lights with the next pair and the receipt never sticks to one you have not judged; the two captions move to `copy.js` unchanged. The engine deals one best pair per race per hand and excludes the pairs judged, so the deck reads across views: judge, and the command's own refresh deals the next — two incumbent pairs and then the rival pair, gate or no gate (`bestPairFor`'s second scan), which is how a race card is now reached on a live document. **1201**: the view carries `myJudgments` per race (ids and verdict, oldest first, superseded ones gone); the ⏳ card lists them as blocks with the chosen side marked, a press making that pair the card's active pair with the verdict pre-selected, and ✓ sending the revision on the same pair (§4.4). Guards: the two-challenger server test and `npm run journey`'s deck and ledger steps. SURFACE E13, L4, M18, §9's ledger row; SPEC unchanged.

**1196–1199 are *quorum is the adoption floor; the freeze and sign-out go* (Ed, 2026-09-06)**, claimed in chat before the plan is written (`~/.claude/plans/quorum-is-the-floor.md`). Ed opened by proposing to remove 👥 altogether — *I have realised that I misunderstood what Quorum does. I don't think we need it; the functionality I want is covered by Lapse and Leave, and we won't have "Sign Out"* — and, told that the quorum is also the per-race adoption floor (SPEC §4.2's Q), chose **keep 👥 as the floor, remove the freeze**. **1196** the ruling itself: §9.5 loses sign-out (holding / abstaining) and the freeze; the electorate of a founding question and of a 🏛️ motion is E; the quorum base is gone, E being the only base; the ways out of the base are resignation, removal and lapse. **1197** the topbar's *quorum n of E* reading — it already prints the floor (`cs.v.floor`), and Q860 kept it as a sentence; asked whether it stays now that quorum has only the one meaning. **1198** `/pairwise` said 👥 *is about the membership being present, never about how many votes one proposal has* — flatly false against §4.2, and the likely source of the misreading; a finding, fixed unless vetoed. **1199** whether the 👥 clause sentence (*Quorum is n of the membership.*) should itself say the floor, or leave that to the meaning line beneath it, which already does and loses only its freeze clause.

    **Built 2026-09-06 (`quorum-is-the-floor`), all four spent, none released.** **1196** is SPEC v0.99: §9.5 is one paragraph, *Presence* — plain silence is nothing, E is the only base, the ways out of E are resignation, removal and lapse, judgments cast keep counting, and a count-form quorum above E holds every race at the floor and nothing else (R-088; R-032, R-038, R-049 and R-077 marked superseded); the mechanism loses `signOut`, `maybeFreezeOrThaw`, `quorumBase` and `mustReturn`, `canJudge()` is *constituted*, the view loses `frozen`, the server loses the `sign-out` command and the ladder its sign-out rung, and `signed-out` / `frozen` / `thawed` stay in the event union as legacy no-ops (`legacy-events.test.ts`). **1197** built as resolved in the plan — the topbar's *quorum n of E* stays, since `cs.v.floor` *is* the floor and Q860 made it a sentence about a decision. **1198** fixed: `/pairwise`'s parenthesis now says the quorum is *the floor under all of this: the number of members who must have voted on a change before it can pass … It never decides a proposal; it only says how many people the document must have heard from first.* **1199** built as resolved in the plan — the clause sentence stays and the meaning line beneath it carries *what it does*, every branch now ending in the floor (STYLE audit item 83, `design/DECISIONS.md`). SURFACE E23 is struck; CLAUDE.md retires `sign-out` and `freeze`; the reasoning is `design/DECISIONS.md`'s section of the day.

**1181–1195 are Ed's card review round 3 (2026-09-05)**, claimed by the session sorting his 44 notes (`design/card-review-2026-09-05.md`) for the questions the sort raises, asked one at a time in chat, and for the readings built unless vetoed. **1181** 🍾's power table redesign (Ed asked for a proposal); **1182** the consent card's shape under the option-block grammar (settled 👤 with a constitutional motion in flight); **1183** what a stranger's tab opens on a settled setting; **1184** whether the release-batch news card stays; **1185** the delegation sentences' tense and whether *both alternatives* reaches every binary setting; **1186** the rejected record's shape; **1187** ✉️'s multi-address box at a member's price; **1188** provenance wording where the founder chose the standing rule. Readings: **1189** the *goes to the Founder* note goes everywhere; **1190** a close-only OK on every 🗑️-only card; **1191** *Any members* → *Any member*; **1192** the method note at `--t-ui` in the blue box; **1193** `.pick.off` greys the radio alone; **1194** the login card's send is a 📧 glyph commit; **1195** the build's residuals, filed open above. **All fifteen spent** — 1181–1188 answered by Ed in chat the same evening and built (`design/card-review-2026-09-05.md`, Answered), 1189–1194 built as read, 1195 open.

**900–904 are the entry-94/97 documentation sweep** (2026-08-26), a consistency pass over the session-loaded set after the membership rebuild, answered by Ed the same day. **900–902 are open above**, each with his answer; **903 is spent** — the rename, built 2026-08-27 — and **904 is spent** — 🤝's `proposed` and `open` rungs went with entry 94's four-rung `joinPolicy` and the module folds every legacy value to `{apply}` on load, so the two label maps that still speak them cannot fire; Ed's answer was to mark them dead where they sit and remove them in one deliberate sweep rather than piecemeal, which is what the code now says. **905–909 are released unspent.**

Twelve findings were fixed with them rather than asked, `spec-check` having been green throughout — which is the lesson of the pass and is worth keeping: **the checker asserts the tables and the prose around them is unguarded**, so a rebuild that appends beside what it contradicts passes it. The worst of them, SURFACE **F19**, had recorded entry 94's move of the invitation box as an *instruction to substitute* — its headline saying every 🪪 below now reads ✉️, its body still saying 🪪 — and four other sites had copied the unsubstituted form. The rest: SURFACE Y24's subject and gate; E10 claiming invitations and removals for the constitutional route alone, where their route is now 🪪's and 🥾's price; CLAUDE.md's *two kinds of decision* opening by listing the membership as constitutional and closing by calling it a fact; its 🤝 entry describing the pre-entry-94 split in every clause; `applicants` reading its policy off 🤝; `founderDirect` claiming the register's pair still lives in the applications value; `door-cards` filed under `constitution-section` and pointing at the page-key table; SPEC §9.6's *join policy*; and README at v0.73 and 501 tests against a v0.76 spec and 531. Commits `032f00b`, `c2e4dca`, `17bfbed`, `37daf20`.

**924 and 925 are the git question** (2026-08-27): 924 open, sub-paragraph Gate 1 with a sim measurement first; 925 parked, the dependent-candidate, version-chain and patch-id ideas kept together.

**918–923 are the seat matrix** (plan-queue **38**, backlog entry 127), claimed by the batch-N pre-flight on 2026-08-26 **before the build, not by it**, so a parked branch cannot take them twice. The plan asked for a block of five for the *no rule* rows its first green-path run reports — events its step table triggers whose SURFACE §2 Audience cell it cannot honestly reduce to a predicate (E9's *the actor; every member* and E17/E18's conditional cells being the likely shape) — and one spare stands beside them for whatever the first run turns up that is neither a finding nor a *no rule*. Each question quotes the cell verbatim, names the step and the seats, and offers the reading the builder would have taken. Unspent numbers are released. **Folded 2026-08-27 by the first full run** (plan-queue **41**, entry 139, against build `7eceef8`): **918** spent on E9 — the one genuine *no rule* row, once per hat, now carried as `filed: 'Q918'` so the harness is green over it; **919** spent on the run's one member-hat finding, 💤 unrehydratable and every card below it withheld from members; **920** on its one clerk-hat finding, `view.convenor.isMember` stale for a clerk so 🍾 waits on a voice they do not hold; **921–923 released unspent**. E17/E18's conditional cells were never reported — the step table raises no text race. The two last lines, verbatim: `seat-matrix: findings=15 noRule=0 shape=0 errors=0 refused=0 unstood=0 exit=1` (member) and `seat-matrix: findings=25 noRule=0 shape=0 errors=0 refused=0 unstood=4 exit=1` (clerk) — red on 919 and 920 and on nothing else; the run's JSON is gitignored by design, so a later session re-runs `npm run seat-matrix -- $DRAFT_BASE_URL --hat=<hat> --out=<file>` for its baseline rather than looking for one. Note that **924–925 are claimed twice in this section** — below, for the copy freeze (plan 39), and above, for the git question, which spent them (items 924 and 925 exist) — so plan 39's claim is void; which block gives way is not this fold's call.

**924–925 are the copy freeze** (plan-queue **39**, backlog entry 128), claimed at the same moment. The plan names **no** identifiers of its own and may well spend neither: the pair is provisioned for what freezing 278 cards of copy exposes that normalisation cannot honestly hide — a string that moves between two clean runs for a reason nobody has named, and the two known errors it freezes as facts (*seat:applicant offered no cards*, *closed: no backlog (U:) records on the page*) becoming questions rather than records. Unspent numbers are released.

**993–1012 are twenty decisions Ed gave on 2026-08-27 and 2026-08-28**, claimed and folded by the same pre-flight. Nineteen of them were **never in the open register**: each was asked from a plan while it was being written or from a batch's run report, and answered before anything here carried a number, so they are recorded as decided and deliberately not added above. The twentieth is **Q70**, which had a number already and stays above until its build lands.

**996 is the per-decision reveal rung**, filed above in the Backlog. Ed's 👁️ note had two halves; the first is built and the second — names shown as each decision closes, decision by decision, rather than at the end of everything — names nothing today. Ed: **file a backlog entry for the per-decision rung**. Done, under this number. **Spent.**

**1009–1010 are the two document refactors**, both filed above in the Backlog. plan-queue measures three of draft's documents at the end of every run against budgets Ed set: after batch Q `CLAUDE.md` stood at 99,290 of 100,000, `SPEC.md` at **81,091 against 80,000** and growing 1,000–2,000 a run, and `SURFACE.md` at **77,151 against 70,000** — the append-only surface inventory, where every batch adds rows and nothing takes any out unless somebody decides to. The tool never gates on this and never files the entry itself, because what to take out of a document is a judgement about what the document is for. Ed: **file both refactors as backlog entries, SURFACE first**. **1009** is SURFACE.md, **1010** is SPEC.md. Every session reads `CLAUDE.md` whole and `spec-check` reads `SPEC.md` end to end, so these are a cost per session as well as a readability question — and note that 1003 and 1006 both cut into the same two files from other directions, so whoever writes 1009 should read them first. **Spent.**

**1012 is the removed invitee who was never here**, filed above in the Backlog. The batch-O plan *Three acts nobody is told about* added a mail for a convenor's removal — *You are no longer a member of …* — and the mail arm in `packages/server/src/server.ts` has no arrival check, while the session's own departure fold deliberately excludes people who never arrived; so an invitee who never turned up and is then removed via `remove` is told they are no longer a member of something they were never in, and the register's departure lines say nothing about the same act. The batch review left it alone as a behaviour choice. Ed: **keep the mail and also record the departure** — so the guard is not added and the *other* half is fixed instead: the departure fold records a removal whether or not the person ever arrived. Today it is unreachable from the page (invitees are routed to `uninvite`), so nothing a member sees is wrong yet, which is why it is filed rather than hot. **Spent, less the build.**

**1013–1025 are claimed by the batch-U pre-flight** (2026-08-29), one block per plan, before any of the five is built. Every one of the five plans says *this run has no pre-flight, so claim your own numbers at claim time* — **that sentence is now false**, and a session that claims again takes numbers a sibling in the same batch has already been given. The blocks below are the claim; no build in this batch re-reads this tail for a number of its own. `R-nnn` keys are reserved here too, in this file rather than in `design/SPEC-REASONING.md`, because that file's tail is read by four of the five sessions in the same run and `R-055` was the highest at `ebb1882`. **Whatever a plan does not spend is released when it folds.**

**1015–1017 are *🛡️ on the Text holds an adoption rather than reverting one*** (backlog entry 159, plan 102). **1015** — what a member who is *not* the Founder is told while a text adoption sits awaiting assent; the surface says nothing about it today and the build deliberately adds nothing, so this is filed open rather than answered. **1016** — spare, for whatever the build defers (its *not in scope* list names the seat-matrix row and the author's own ✏️ line as the likely two). **1017** — spare. **`R-056` is reserved for this plan's ruling**: Ed, 2026-08-27, *a shielded Text parks an adoption, it does not revert one*, with adopt-then-unwind as the rejected alternative and the one-at-a-time rule as its consequence. — **Built 2026-08-29.** **1015** is filed open above (what the room is told while an adoption waits; the build adds nothing). **1016** is filed above too: the seat-matrix row is deferred to entry 158, which makes the state reachable in one press. **1017 is released, unspent.** **`R-056` is written** and SPEC is at v0.85 (§9.7 rule 8's second sentence, §4.2's batch rule, README followed); SURFACE §9 gained the *👑 question (the Text)* row. **Spent.**

**1033 and `R-060` are *A park with no convenor auto-passes, and a vacant seat holds no shield*** (plan 115, closing the batch review's deferred finding F7 on plan 102's range `b57a1a68..bb335de0`). Claimed at the tail at build time: the batch claim block above (1026–1032) covers plans 107–114 only, and this plan is not in it. **`R-060` is reserved for Ed's ruling of 2026-08-29** — *a park with no convenor auto-passes, the way a lapse does* — with the plan's own forward-looking half beside it (a vacated seat holds no shield, so the *next* race adopts by itself rather than parking against an empty seat). **1033** — the residual the plan names and deliberately does not implement: what becomes of a **motion-backed** 👑 question standing when the seat is vacated; filed open below.

